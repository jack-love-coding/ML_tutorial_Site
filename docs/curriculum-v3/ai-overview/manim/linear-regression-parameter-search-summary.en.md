# Linear-regression parameter search — English summary

This silent Chinese video connects one input–output dataset to a complete regression error calculation. It plots the `clear-trend` observations, introduces the candidate line `ŷ=wx+b`, and separates the visual roles of slope `w` and intercept `b`. For `w=6,b=47`, it follows the first sample through prediction 53, residual `52−53=−1`, and squared error 1, then combines all five squared errors into `MSE=0.6`.

The video next synchronizes seven committed parameter candidates with their lines and MSE values. Among that finite list, `w=6.5,b=46` has the lowest displayed MSE, 0.15. The ending explicitly calls this visible enumeration a teaching simplification and hands off to gradient descent; it does not claim a global continuous optimum.
