# AI Foundations B 编发布验证

**验证日期：** 2026-08-29  
**发布范围：** `07-ml-experiment-design` 至 `14-tabular-pipeline`

## 发布结论

B 编八个单元已形成独立、双语且连续的学习闭环，状态统一切换为 `published`。课程首页现在把 A、B 两编共 14 个单元作为默认学习路径；C、D 两编仍为 `planned`，只展示阶段目标，不生成可点击的未完成页面。

单元 09 只通过 canonical curriculum resource reference 接入 Phase 29/30 的逻辑回归与分类决策课程、冻结结果和双语 Notebook，没有复制实验数据或改写原课程正文。全部旧路由与既有进度存储保持不变。

## 内容门禁

每个发布单元均包含：

- 双语核心问题、学习目标、前置知识、教学重点和参考学时；
- 概念解释、代码或数值连接、实践任务、误区反馈和三选一 checkpoint；
- 至少三项成果验收标准，以及前后单元连续导航；
- 可解析的课程资源引用；涉及下载的资产均使用 GitHub Pages base-safe 路径。

八个单元分别覆盖实验设计、线性回归、逻辑回归与阈值、KNN/NB/SVM、决策树、Bagging/随机森林/Extra Trees、Boosting 四库比较，以及端到端表格 Pipeline。

## 验证结果

| 门禁 | 结果 |
| --- | --- |
| 课程架构、路由、进度和首页定向测试 | 16/16 通过 |
| 真实浏览器矩阵 | 48/48 通过（8 单元 × 2 语言 × 3 视口） |
| 键盘勾选与刷新后进度持久化 | 通过 |
| 未发布单元 15 返回 C 编阶段状态 | 通过 |
| 全量测试 | 1142 项：1114 通过、28 跳过、0 失败 |
| 标准生产构建 | 通过（仅保留既有 large-chunk 提示） |
| GitHub Pages 构建 | 通过（仅保留既有 large-chunk 提示） |
| 安全审计 | 0 vulnerabilities |

全量测试首次运行时，两个 Math-to-Code 文件级测试出现瞬时失败；相同文件单独复跑 23/23 通过，随后完整测试在未修改相关代码的情况下 1142/1142 非跳过项全部通过。该现象按既有并发测试抖动记录，不作为 B 编代码回归处理。

## 兼容性结论

- `/spine`、`/learn/*`、Math Lab、Data Lab 和旧章节深链未删除或改写。
- `ml-atlas:course-progress:v1` 继续只保存课程步骤与自检状态；旧 V1 数据源和 Learning Progress V2 不变。
- 内容始终可访问，准备度只提供提示，不形成硬锁。
- 首页“继续学习”可在 A→B 的 14 个已发布单元中连续选择。
