---
phase: 29-logistic-regression-rebuild
plan: "02"
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - scripts/logistic-regression/phase29_analysis.py
  - scripts/logistic-regression/build-phase-29-assets.py
  - src/modules/logistic-regression/assets.ts
  - public/logistic-regression/phase-29/
  - tests/logistic-regression-assets.test.ts
  - tests/logistic-regression-parity.test.ts
  - tests/logistic-regression-calibration.test.ts
autonomous: true
requirements: [LOGR-02, LOGR-03, LOGR-04]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A clean, bilingual executed Notebook and one atomic public package reproduce the fixed Banknote split, train-only standardization, selected rows, deterministic scratch trace, aligned scikit-learn parity, L2 comparison, calibration summaries, and nonlinear diagnostics."
    - "Scratch and scikit-learn are compared first under the same unregularized objective and explicit preprocessing/intercept/solver/stopping contract; L2 appears only as a second, separately labeled objective."
    - "Phase 29 displays training and validation information only, while one hash-bound CSV/JSON handoff freezes all row predictions for Phase 30 without exposing test labels or test results through Phase 29 lesson loaders or downloads."
    - "Every runtime JSON payload is contract-versioned, scene-bound, source-cell-bound, hash-checked, finite, immutable after parsing, and fetched through a GitHub Pages-safe path with abort support."
  artifacts:
    - path: "scripts/logistic-regression/phase29_analysis.py"
      provides: "Pure deterministic Banknote analysis, parity, calibration, row selection, and diagnostic functions"
    - path: "scripts/logistic-regression/build-phase-29-assets.py"
      provides: "Pinned clean-kernel Notebook builder and atomic asset publisher with read-only drift check"
    - path: "public/logistic-regression/phase-29/manifest.json"
      provides: "Complete package inventory, numerical configuration, source cells, hashes, visibility boundaries, and environment provenance"
    - path: "public/logistic-regression/phase-29/frozen-predictions.csv"
      provides: "Phase 29/30 row-level prediction handoff"
    - path: "src/modules/logistic-regression/assets.ts"
      provides: "Pages-safe validated interaction loader"
      exports: ["parseLogisticInteractionAsset", "loadLogisticInteractionAsset", "loadLogisticManifest"]
  key_links:
    - from: "scripts/logistic-regression/build-phase-29-assets.py"
      to: "public/datasets/numerical-methods/banknote-authentication.csv"
      via: "strict source hash/schema/split verification before analysis"
      pattern: "banknote-authentication"
    - from: "public/logistic-regression/phase-29/manifest.json"
      to: "public/logistic-regression/phase-29/interactions/*.json"
      via: "SHA-256 inventory plus sourceCellId and contractVersion"
      pattern: "sha256|sourceCellId|contractVersion"
    - from: "src/modules/logistic-regression/assets.ts"
      to: "public/logistic-regression/phase-29/interactions/*.json"
      via: "withPublicBase fetch, HTTP/identity/hash/finite validation, AbortSignal"
      pattern: "withPublicBase"
  prohibitions:
    - statement: "Phase 29 learner-facing content and loaders must not disclose test labels, test metrics, test calibration bins, or test-driven model/threshold choices."
      status: unresolved
---

<objective>
Create the single reproducible numerical authority for Phase 29 and publish it as one hash-bound, Pages-safe asset transaction.

Purpose: Make Notebook code, page outputs, labs, figures, and later animations replay the same executed Banknote results while sealing the Phase 30 evaluation boundary.
Output: Pure Python analysis, two executed Notebooks, six interaction payloads, figures, training/parity/calibration outputs, a frozen prediction handoff, manifest, and a strict TypeScript loader.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/29-logistic-regression-rebuild/29-CONTEXT.md
@.planning/phases/29-logistic-regression-rebuild/29-RESEARCH.md
@.planning/phases/29-logistic-regression-rebuild/29-PATTERNS.md
@.planning/phases/29-logistic-regression-rebuild/29-VALIDATION.md
@.planning/phases/29-logistic-regression-rebuild/29-01-SUMMARY.md
@public/datasets/numerical-methods/banknote-authentication.csv
@public/datasets/numerical-methods/banknote-authentication-manifest.json
@public/datasets/numerical-methods/banknote-authentication-data-dictionary.json
@public/notebooks/numerical-methods/requirements.txt
@public/notebooks/numerical-methods/banknote-logistic-optimization.zh-CN.ipynb
@scripts/linear-regression/build-phase-27a-assets.py
@src/simulations/linearRegressionInteraction.ts

<interfaces>
Plan 29-01 supplies `LogisticInteractionAsset`, `LogisticAssetManifest`, `LogisticPredictionHandoff`, the six `LogisticObservationSceneId` values, and pure TypeScript parity helpers.

The source data contract is `BANKNOTE_CONTRACT_VERSION`, `BANKNOTE_FEATURES = ['variance','skewness','curtosis','entropy']`, split counts `960/206/206`, stable row IDs, and train-only population standardization (`ddof=0`).
</interfaces>
</context>

<flagged_assumptions>
- **EDGE-29-04 / LOGR-03 unclassified:** The unregularized scratch/library coefficient and probability tolerances must be chosen from reproducible executed convergence, persisted in the manifest, and then enforced; an executor may not pick a tolerance that merely hides an objective/configuration mismatch.
- **EDGE-29-05 / LOGR-04 boundary:** Start the controlled calibration candidates at positive logit temperatures `0.65`, `1.0`, and `1.75`; publish them only if ordering and all `p >= 0.5` predictions are identical. If the fixed data invalidate the teaching contrast, choose another positive pair from executed results and record the selection rule and values in the manifest.
- **EDGE-29-06 / LOGR-04 precision:** Use ten equal-width probability bins with lower-inclusive/upper-exclusive intervals except the final upper-inclusive bin; omit empty bins, calculate weighted ECE from full precision, and round only in the renderer.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive the deterministic scratch, parity, calibration, and capacity analysis</name>
  <files>scripts/logistic-regression/phase29_analysis.py, tests/logistic-regression-parity.test.ts, tests/logistic-regression-calibration.test.ts</files>
  <read_first>
    - `public/datasets/numerical-methods/banknote-authentication-manifest.json` for exact source hashes, feature order, split counts, and train statistics.
    - `src/modules/math-lab/utils/banknoteLogistic.ts` for the existing objective, parameter ordering, L2 convention, and stopping semantics.
    - `tests/numerical-methods-batch-4.test.ts` parity and finite-difference groups for historical anchors that must not be silently redefined.
    - `29-RESEARCH.md` pitfalls 2 through 5 for parity, underflow, calibration, and test isolation.
  </read_first>
  <behavior>
    - Source loading rejects any hash, header, row count, row ID, split, feature order, missingness, target, or train-statistics drift before fitting.
    - The selected canonical/comparison rows are deterministic, named by mathematical behavior, and use class 0/class 1 wording only.
    - Scratch training records finite iteration-0 and accepted states, explicit stopping reason/config, feature order, intercept convention, and validation selection without reading test labels or metrics.
    - The no-penalty sklearn run has explicit solver, penalty, intercept, tolerance, maximum iterations, and matching preprocessing/objective; coefficient and probability tolerances are derived from the converged run and persisted.
    - The L2 run declares its changed objective and excludes the intercept from the scratch penalty; it is not presented as a failed parity check.
    - Temperature candidates preserve validation ranking and fixed-threshold predictions; calibration bins omit empty intervals and XOR/circles are tagged synthetic and isolated.
  </behavior>
  <action>Write the parity and calibration tests first, then implement `load_banknote_source`, `select_teaching_rows`, `build_one_row_trace`, `build_batch_trace`, `finite_difference_sweep`, `train_scratch_logistic`, `compare_unregularized_sklearn`, `compare_l2_objective`, `build_temperature_calibration`, `build_reliability_bins`, `build_xor_diagnostic`, and `build_circle_diagnostic` as pure Python functions. Use the existing pinned NumPy/pandas/scikit-learn environment and no new dependency per D-01 through D-07, D-10, D-11, D-20, and D-25. Fit preprocessing and models only on the training partition; use validation for stopping/comparison and never test for selection per D-02 and D-22. Return strict finite Python structures suitable for Notebook cells and JSON publication; do not perform file publication in this analysis module.</action>
  <acceptance_criteria>
    - Repeated analysis runs return identical row IDs, parameters, traces, parity arrays, calibration bins, and synthetic diagnostic points.
    - Every analytic parameter gradient matches a centered finite difference across the selected step sweep.
    - The aligned no-penalty comparison satisfies persisted coefficient/probability tolerances, while L2 is labeled as a changed objective.
    - Original/sharpened/softened validation modes keep identical ordering and 0.5 labels but produce measurably different full-precision calibration summaries.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/logistic-regression-parity.test.ts tests/logistic-regression-calibration.test.ts</automated>
  </verify>
  <done>The pure executed analysis can reproduce every complex result without browser fitting or test-set selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build and atomically publish the bilingual Phase 29 asset package</name>
  <files>scripts/logistic-regression/build-phase-29-assets.py, public/logistic-regression/phase-29/, tests/logistic-regression-assets.test.ts</files>
  <read_first>
    - `scripts/linear-regression/build-phase-27a-assets.py` for `TemporaryDirectory`, complete-tree verification, atomic rename/rollback, and read-only drift conventions.
    - `public/notebooks/numerical-methods/requirements.txt` and the existing audited wheel-cache invocation used by prior numerical phases.
    - `29-CONTEXT.md` D-04, D-05, D-16, D-20, D-21, and D-22 for package content and visibility boundaries.
  </read_first>
  <behavior>
    - Chinese and English Notebooks share identical ordered code cells and normalized numerical outputs while prose is localized; both execute from clean, network-blocked kernels using the existing pinned environment.
    - The complete package contains six scene JSON files, bilingual Notebooks, finite CSV/JSON outputs, deterministic local figures, frozen predictions, and one manifest with every byte/hash/source-cell/config relationship.
    - Publication builds in a temporary complete tree, validates it, swaps the directory atomically, and restores exact prior bytes/modes after every injected pre/mid/post publication failure.
    - `--check` regenerates externally, compares every file/hash/value, and leaves tracked repository bytes, sizes, mtimes, and public assets unchanged.
    - Phase 29 presentation payloads and download registry contain train/validation outputs only; frozen test rows remain reserved for Phase 30 and are never used by interaction JSON or selection summaries.
  </behavior>
  <action>Create the build script and failing asset-contract tests before generation. Compose Notebook cells from `phase29_analysis.py`, bind every visible output/figure/interaction to stable cell IDs, use Matplotlib `Agg`, serialize strict JSON with non-finite values forbidden, and publish the full directory as one transaction. Generate `manifest.json`; `interactions/{linear-score,sigmoid-probability,threshold-decisions,log-loss,regularization,linear-limits}.json`; `outputs/{training-trace.csv,parity.json,calibration.json,gradient-check.json}`; five local PNG figures; two executed Notebooks; and `frozen-predictions.{csv,json}` per D-16 and D-21. The handoff includes stable row ID, split, label, logit, probability, feature/model contract versions, and model/config hashes, but Phase 29 interaction payloads and final downloads do not surface test records per D-22. Record exact environment versions, source dataset hashes, feature order, standardization, solver/penalty/stopping, tolerances, row-selection rules, calibration transforms, figure hashes, and Notebook output hashes in the manifest.</action>
  <acceptance_criteria>
    - A clean build produces a complete deterministic package and a second read-only check reports no drift.
    - Notebook code/output parity, figure source-cell bindings, CSV/JSON recomputation, and every SHA-256 entry pass.
    - Failure injection proves complete rollback for an absent target and a seeded existing target.
    - No Phase 29 learner payload or download link exposes test labels, test metrics, test bins, or threshold-selection results.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/logistic-regression/build-phase-29-assets.py --check && npm test -- tests/logistic-regression-assets.test.ts tests/logistic-regression-parity.test.ts tests/logistic-regression-calibration.test.ts</automated>
  </verify>
  <done>The exact complete public package is reproducible, atomic, hash-bound, bilingual, and safe for Phase 29/30 reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add a fail-closed Pages-safe runtime asset boundary</name>
  <files>src/modules/logistic-regression/assets.ts, tests/logistic-regression-assets.test.ts</files>
  <read_first>
    - `src/simulations/linearRegressionInteraction.ts` for finite-tree validation, scene identity checks, fetch injection, and `AbortSignal` handling.
    - `src/utils/publicPath.ts` for the only approved public-base resolution path.
    - `src/modules/logistic-regression/types.ts` for manifest and discriminated scene payload contracts.
  </read_first>
  <behavior>
    - Parsers reject unknown contract versions, wrong scene IDs, missing source cell/hash, extra/missing discriminant fields, non-finite leaves, invalid row/split/target values, and manifest hash mismatch.
    - Loaders use `withPublicBase`, require an OK response, pass `AbortSignal`, distinguish aborted/HTTP/schema/integrity failures, and never fetch a remote arbitrary URL.
    - Returned assets are detached and recursively frozen so a scene cannot mutate the publication authority.
    - Learner loaders expose only the six interaction files and manifest-approved training/validation outputs, never the reserved test section of the handoff.
  </behavior>
  <action>Implement strict `parseLogisticManifest` and `parseLogisticInteractionAsset` unknown-to-typed parsers plus injectable async `loadLogisticManifest` and `loadLogisticInteraction` loaders. Validate the declared inventory and SHA-256 before handing data to Vue; compute or verify hashes using the project's browser-compatible existing pattern rather than adding a dependency. Resolve only registry-owned local paths through `withPublicBase`, abort stale calls, and return localized failure categories for the later shell. Keep CSV parsing, Notebook execution, sklearn fitting, and test-handoff access outside the learner loader per D-16, D-21, and D-22.</action>
  <acceptance_criteria>
    - Root and `/ML_tutorial_Site/` base-path fetches resolve correctly and honor cancellation.
    - Corrupted identity, hash, schema, or finite-tree fixtures fail before any scene receives them.
    - Successful parsed assets are deeply immutable and preserve their discriminated scene subtype.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/logistic-regression-assets.test.ts</automated>
  </verify>
  <done>Vue scenes have one auditable, abortable, fail-closed boundary to the published numerical authority.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/logistic-regression/phase29_analysis.py`: deterministic analysis API.
- `scripts/logistic-regression/build-phase-29-assets.py`: clean-kernel build, atomic publish, rollback injection, and read-only drift CLI.
- `public/logistic-regression/phase-29/manifest.json`: complete package manifest.
- `public/logistic-regression/phase-29/interactions/linear-score.json`, `sigmoid-probability.json`, `threshold-decisions.json`, `log-loss.json`, `regularization.json`, and `linear-limits.json`: chapter-specific payloads.
- `public/logistic-regression/phase-29/outputs/training-trace.csv`, `parity.json`, `calibration.json`, and `gradient-check.json`: replayable numeric outputs.
- `public/logistic-regression/phase-29/figures/banknote-projection.png`, `training-trace.png`, `parity-probabilities.png`, `calibration-reliability.png`, and `linear-boundary-limits.png`: Notebook-derived figures.
- `public/logistic-regression/phase-29/banknote-logistic-regression.zh-CN.ipynb` and `banknote-logistic-regression.en.ipynb`: executed bilingual notebooks.
- `public/logistic-regression/phase-29/frozen-predictions.csv` and `frozen-predictions.json`: sealed Phase 30 handoff.
- `src/modules/logistic-regression/assets.ts`: `parseLogisticManifest`, `parseLogisticInteractionAsset`, `loadLogisticManifest`, and `loadLogisticInteraction`.
- `tests/logistic-regression-assets.test.ts`, `tests/logistic-regression-parity.test.ts`, and `tests/logistic-regression-calibration.test.ts`: asset, parity, and calibration contracts.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local CSV/manifest → Python analysis | Committed static bytes become model inputs and executable Notebook outputs. |
| Generated temporary tree → public package | Many related files must become visible as one consistent generation. |
| Static JSON response → browser scene | Network-delivered public bytes are untrusted until identity/hash/schema/finite validation. |
| Frozen prediction handoff → Phase 29 learner UI | Test records exist for Phase 30 but are not authorized for current learner presentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-02-01 | Tampering | dataset and generated package | high | mitigate | Verify source and package SHA-256, schema, split, source-cell bindings, strict JSON, and complete inventory before atomic publication. |
| T-29-02-02 | Information Disclosure | frozen prediction handoff | high | mitigate | Separate reserved test records from Phase 29 interaction/load/download registries and add structural absence tests for all learner-facing outputs. |
| T-29-02-03 | Tampering | browser asset loader | high | mitigate | Require expected contract/scene/hash and finite leaves; reject non-OK/remote/unregistered responses; deep-freeze accepted data. |
| T-29-02-04 | Denial of Service | malformed asset trees | medium | mitigate | Bound arrays/bin counts/trace lengths and abort stale fetches before rendering work. |
| T-29-02-05 | Repudiation | scratch/sklearn comparison | medium | mitigate | Persist full preprocessing/objective/solver/stopping/tolerance/environment configuration and recompute coefficients/probabilities in tests. |
</threat_model>

<verification>
- Asset, parity, calibration, and math tests pass against published bytes.
- The builder's read-only `--check` and rollback probes pass without repository mutation.
- `npm run build:pages` resolves all registered assets under the repository base.
- No external API or ORM schema is introduced; the API-coverage and schema-push detectors are non-applicable.
</verification>

<success_criteria>
- LOGR-02/03/04 complex results have one executed, testable, hash-bound authority.
- D-01 through D-07, D-10, D-11, D-16, D-20 through D-22, and D-25 are implemented in the asset contract.
- The test partition cannot influence or appear in Phase 29 teaching even though the frozen handoff is ready for Phase 30.
</success_criteria>

<output>
Create `.planning/phases/29-logistic-regression-rebuild/29-02-SUMMARY.md` when done.
</output>
