---
phase: 29-logistic-regression-rebuild
plan: "06"
type: execute
wave: 5
depends_on: [29-02, 29-04, 29-05]
files_modified:
  - src/modules/logistic-regression/data/course.ts
  - src/components/LogisticRegressionPagedLesson.vue
  - src/data/logisticRegressionModule.ts
  - src/styles/modules/logistic-regression.css
  - docs/curriculum-v3/logistic-regression/sources.md
  - tests/logistic-regression-content.test.mjs
  - tests/logistic-regression-rendering.test.ts
  - tests/logistic-regression-cockpit.test.mjs
autonomous: true
requirements: [LOGR-01, LOGR-02, LOGR-03, LOGR-04]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All six unchanged deep links render one complete bilingual content-first chapter in the approved responsibility order, with code, executed output, assigned media, one dedicated lab, observation, misconception, and conclusion."
    - "References, attribution, licenses, reproducible downloads, the unchanged checkpoint, and the Phase 30 bridge appear only in `linear-limits`; the frozen test handoff is not offered as a Phase 29 learner download."
    - "The page uses the sanitized Markdown/KaTeX renderer, bilingual clipboard feedback, lazy labs/media, a single-column reading width, desktop/mobile TOC, and responsive controls without mounting the old shared cockpit."
    - "Existing module/route/chapter/progress/checkpoint/catalog identities and GitHub Pages base paths remain unchanged."
  artifacts:
    - path: "src/modules/logistic-regression/data/course.ts"
      provides: "Six complete bilingual chapters, final references, and authorized downloads"
    - path: "src/components/LogisticRegressionPagedLesson.vue"
      provides: "Complete content-first page composition with media, lazy lab, final resources, checkpoint, and pager"
    - path: "src/data/logisticRegressionModule.ts"
      provides: "Legacy-compatible module definition with rebuilt titles/responsibilities"
    - path: "docs/curriculum-v3/logistic-regression/sources.md"
      provides: "Auditable source, license, access-date, and usage-scope record"
  key_links:
    - from: "src/components/LogisticRegressionPagedLesson.vue"
      to: "src/modules/logistic-regression/labs/LogisticLessonLab.vue"
      via: "one current chapter scene after the prediction block"
      pattern: "LogisticLessonLab"
    - from: "src/components/LogisticRegressionPagedLesson.vue"
      to: "src/modules/logistic-regression/data/media.ts"
      via: "chapter media IDs resolve through ChapteredMediaPlayer"
      pattern: "ChapteredMediaPlayer"
    - from: "src/components/LogisticRegressionPagedLesson.vue"
      to: "src/data/logisticRegressionModule.ts"
      via: "preserved StorySection/checkpoint/progress props and route IDs"
      pattern: "moduleDefinition|section"
---

<objective>
Complete the six-chapter bilingual course and integrate the numerical assets, four media packages, six dedicated labs, final resources, checkpoint, and Phase 30 handoff into the preserved page.

Purpose: Turn the verified foundations into one coherent beginner teaching experience without migrating global lesson infrastructure or legacy identities.
Output: Final course copy/data, paged renderer, module metadata, responsive styles, source record, and content/rendering/compatibility tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-logistic-regression-rebuild/29-CONTEXT.md
@.planning/phases/29-logistic-regression-rebuild/29-RESEARCH.md
@.planning/phases/29-logistic-regression-rebuild/29-02-SUMMARY.md
@.planning/phases/29-logistic-regression-rebuild/29-04-SUMMARY.md
@.planning/phases/29-logistic-regression-rebuild/29-05-SUMMARY.md
@src/modules/logistic-regression/data/course.ts
@src/modules/logistic-regression/data/media.ts
@src/components/LogisticRegressionPagedLesson.vue
@src/components/ChapteredMediaPlayer.vue
@src/modules/logistic-regression/labs/LogisticLessonLab.vue
@src/data/logisticRegressionModule.ts
@tests/logistic-regression-cockpit.test.mjs

<interfaces>
`logisticCourseChapters` contains six ordered `LogisticCourseChapter` records keyed by the preserved `LogisticChapterId`; each block carries localized title/body and optional code/output/media/scene bindings. `logisticMediaRegistry` and `LogisticLessonLab` are ready consumers, not new page-owned implementations.

The page must retain its existing `AlgorithmModuleDefinition`/`StorySection` props, preset/config emits, progress behavior, route builder, `logistic-*` test IDs, and final `AlgorithmCheckpointQuiz` binding.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author all six complete bilingual teaching chapters</name>
  <files>src/modules/logistic-regression/data/course.ts, src/data/logisticRegressionModule.ts, docs/curriculum-v3/logistic-regression/sources.md, tests/logistic-regression-content.test.mjs, tests/logistic-regression-rendering.test.ts</files>
  <read_first>
    - `29-CONTEXT.md` D-01 through D-12 and D-19 through D-24 for the exact teaching/evaluation contract.
    - `public/logistic-regression/phase-29/manifest.json` and six interaction payloads for every visible numerical anchor and source-cell binding.
    - `src/modules/optimizer-comparison/data/course.ts` for fixed block order and final-only resource patterns.
    - `src/utils/markdownMath.ts` for supported delimiters and sanitizer behavior.
  </read_first>
  <behavior>
    - Every locale/chapter contains the fixed sequence: question, intuition/data observation, notation/math, copyable NumPy, locked output, prediction, assigned animation when present, interaction, observation result, misconception, conclusion.
    - Chapters implement exactly the D-08 responsibilities and use one real row before batch math; likelihood derives Bernoulli contribution through product, log-sum, NLL, and mean BCE.
    - Code, output, formulas, table captions, alt/read prompts, and lab labels use one variable/feature/parameter order and bind visible numbers to the published asset registry.
    - Safe rendering produces valid KaTeX in both locales with no raw delimiters, malformed escape remnants, unsafe HTML/event attributes, or unresolved Markdown code delimiters.
    - The final chapter alone owns deduplicated references, data attribution/license/access dates, authorized downloads, checkpoint, frozen-handoff explanation, and Phase 30 link.
  </behavior>
  <action>Fully populate `logisticCourseChapters`, `logisticCourseReferences`, and `logisticCourseDownloads` and update `logisticRegressionModule` bilingual titles/responsibilities against the Plan 29-00 content/rendering contracts while retaining its IDs and checkpoint per D-08, D-12, and D-23. Build all examples from the Phase 29 Banknote package per D-01 through D-06; keep XOR/circles explicitly synthetic per D-07. Explain stable sigmoid, odds/log-odds, complete likelihood-to-BCE derivation, row/vector gradients plus finite differences, deterministic training, two-stage parity/L2, calibration, and capacity limits per D-09 through D-11. Use `String.raw` for formula-bearing text and render through the existing safe chain. Use plain learner wording and final-only citations/downloads per D-24. Record UCI/scikit-learn/numerical-stability/calibration references, exact license, access date, and usage scope in `sources.md`; learner copy paraphrases rather than copying external text. Restrict the threshold handoff and calibration accuracy contrast to D-19/D-20, and explain that test inspection waits for Phase 30 per D-21/D-22. Refine only locale/fixture bindings required by the complete content data.</action>
  <acceptance_criteria>
    - Both locales have matching block/media/scene/code/output structure across all six IDs and every visible number resolves to an asset or pure engine calculation.
    - Runtime rendering produces sanitized valid KaTeX/Markdown for every narrative block and hostile renderer fixtures remain sanitized.
    - Intermediate chapters contain no source/download/checkpoint sections; final references are deduplicated and the reserved prediction handoff is not linked.
    - Module ID, six literal IDs/order, checkpoint identity, and catalog-facing definition remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/logistic-regression-content.test.mjs tests/logistic-regression-rendering.test.ts tests/logistic-regression-cockpit.test.mjs</automated>
  </verify>
  <done>The complete bilingual course contract teaches LOGR-01 through LOGR-04 without citation clutter, semantic invention, or Phase 30 scope leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose the final paged lesson with media, dedicated lab, resources, and checkpoint</name>
  <files>src/components/LogisticRegressionPagedLesson.vue, tests/logistic-regression-cockpit.test.mjs, tests/logistic-regression-content.test.mjs</files>
  <read_first>
    - `src/components/LogisticRegressionPagedLesson.vue` existing prop/emit/route/progress/test-ID surface.
    - `src/modules/optimizer-comparison/OptimizerPagedLesson.vue` for safe blocks, clipboard errors, current-only lab, final resources, and final checkpoint composition.
    - `src/components/ChapteredMediaPlayer.vue` and `src/modules/logistic-regression/labs/LogisticLessonLab.vue` ready interfaces.
  </read_first>
  <behavior>
    - The current route selects exactly one typed chapter and renders blocks in their declared order; only current-chapter lab/media chunks load.
    - Copy button is keyboard-operable and reports localized success, unavailable clipboard, and write failure without hiding the code.
    - Prediction precedes the matching lab; observation result follows it; no old generic results/cockpit panel duplicates the experience.
    - Final resources, unchanged algorithm checkpoint, and Phase 30 bridge render only for `linear-limits`; previous/next pager and mobile/desktop TOCs keep all six deep links.
  </behavior>
  <action>Replace the remaining mixed legacy layout in the paged component with a fixed single-column block renderer over `logisticCourseChapters`. Mount assigned entries from `logisticMediaRegistry` through `ChapteredMediaPlayer` and one `LogisticLessonLab` keyed by current chapter. Remove primary mounting of the old shared cockpit, inline video markup, generic metric/results duplication, and chapter-local source panels while leaving the legacy component file intact per D-13 and D-23. Keep existing route/progress props and emits, TOC/pager IDs, chapter resolution, and final checkpoint. Resolve every local download through `withPublicBase`; do not link the reserved prediction handoff. Reset clipboard state on chapter change and preserve safe Markdown/KaTeX for all narrative/output explanations.</action>
  <acceptance_criteria>
    - Each deep link shows one chapter, assigned media, one assigned lab, correct TOC state, and previous/next navigation.
    - Old cockpit and generic results are not mounted by the rebuilt course; the compatibility component remains present.
    - Only `linear-limits` displays resources, checkpoint, and the classification-decision bridge.
    - Clipboard success/failure and media/lab loading failures remain understandable in both locales.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/logistic-regression-cockpit.test.mjs tests/logistic-regression-content.test.mjs tests/logistic-regression-media.test.mjs tests/logistic-regression-labs.test.mjs</automated>
  </verify>
  <done>The preserved page now composes the complete Phase 29 teaching loop without the retired shared cockpit experience.</done>
</task>

<task type="auto">
  <name>Task 3: Finish responsive single-column, accessibility, and public-base styling contracts</name>
  <files>src/styles/modules/logistic-regression.css, tests/logistic-regression-cockpit.test.mjs, tests/logistic-regression-rendering.test.ts</files>
  <read_first>
    - `src/styles/modules/optimizer-comparison.css` and current logistic styles for existing tokens, breakpoints, focus states, and reading widths.
    - Six completed scene templates for actual control/table/SVG class names and mobile structure.
    - `AGENTS.md` mobile, contrast, non-color, reduced-motion, and public-resource rules.
  </read_first>
  <action>Style a single reading column around 960–1040px, full-width lab after explanation, desktop sidebar only at the established wide breakpoint, and a collapsible top TOC below it. Give code, formulas, SVGs, controls, details, tables, media, and pager explicit overflow/focus/touch behavior; switch scene diagrams to their vertical topology at mobile widths. Use existing design tokens and module CSS only. Add reduced-motion transitions, visible focus rings, semantic status/line/shape support, and 390px wrapping rules per D-15, D-18, and D-23. Keep assets lazy/current-chapter only and avoid changing global CourseShell or unrelated module styles.</action>
  <acceptance_criteria>
    - Source/layout tests prove no body/lab two-column squeeze, no eager six-scene imports, and no fixed-width overflow-prone controls.
    - Desktop/tablet/mobile TOC behavior, focus visibility, reduced motion, table fallback, and code/media overflow are explicit.
    - Page, lab, media, figure, and download paths remain correct under root and GitHub Pages bases.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/logistic-regression-cockpit.test.mjs tests/logistic-regression-rendering.test.ts && npm run build && npm run build:pages</automated>
  </verify>
  <done>The integrated course is readable, keyboard-accessible, reduced-motion complete, and base-safe across the declared responsive widths.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/modules/logistic-regression/data/course.ts`: final `logisticCourseChapters`, `logisticCourseReferences`, and authorized `logisticCourseDownloads`.
- `src/components/LogisticRegressionPagedLesson.vue`: rebuilt single-column composition.
- `src/data/logisticRegressionModule.ts`: preserved `logisticRegressionModule` identity and updated six-chapter metadata.
- `src/styles/modules/logistic-regression.css`: responsive lesson/lab/media rules.
- `docs/curriculum-v3/logistic-regression/sources.md` attribution/license/reference record.
- `tests/logistic-regression-content.test.mjs`, `tests/logistic-regression-rendering.test.ts`, and `tests/logistic-regression-cockpit.test.mjs`: content, rendering, and compatibility suites.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Typed bilingual copy → Markdown/KaTeX DOM | Formula, code, link, and table content reaches learners. |
| Route/progress state → current chapter composition | Existing IDs determine rendered chapter and stored progress. |
| Public asset path → browser download/media | Local registry values become request URLs under two build bases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-06-01 | Tampering | Markdown/KaTeX content | high | mitigate | Render exclusively through the existing sanitizer wrapper and test hostile HTML, URL schemes, TeX delimiters, and both locales. |
| T-29-06-02 | Spoofing | route/chapter identity | medium | mitigate | Assert literal six-ID route order, invalid-ID fallback, active TOC, checkpoint binding, and progress keys against existing authorities. |
| T-29-06-03 | Information Disclosure | resources/downloads | high | mitigate | Whitelist final-only public downloads and reject any learner link or loaded payload that exposes reserved test records/results. |
| T-29-06-04 | Denial of Service | eager labs/media and overflow | medium | mitigate | Load only the current lazy lab/media, constrain responsive SVG/code/table overflow, and retain lightweight static fallbacks. |
| T-29-06-05 | Repudiation | public references/provenance | low | accept | Static source/license records and manifest hashes provide an auditable trail; there is no user-generated publication action. |
</threat_model>

<verification>
- Content, rendering, cockpit/route, labs, media, asset, parity, and calibration tests pass together.
- Standard and Pages builds pass with no missing public resource or eager lab/media regression.
- `npm run security:audit` remains green without dependency changes.
</verification>

<success_criteria>
- D-08, D-12, D-17 through D-24 are visible in the final page composition.
- All four LOGR requirements form one coherent bilingual six-chapter teaching flow.
- Legacy URLs, Progress stores, checkpoint, catalog identity, sanitizer, copy behavior, and Pages paths remain compatible.
</success_criteria>

<output>
Create `.planning/phases/29-logistic-regression-rebuild/29-06-SUMMARY.md` when done.
</output>
