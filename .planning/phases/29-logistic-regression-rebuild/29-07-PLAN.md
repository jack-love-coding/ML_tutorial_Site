---
phase: 29-logistic-regression-rebuild
plan: "07"
type: execute
wave: 6
depends_on: [29-06]
files_modified:
  - scripts/qa/logisticRegressionBrowserMatrix.js
  - tests/logistic-regression-release.test.mjs
  - .planning/phases/29-logistic-regression-rebuild/29-VALIDATION.md
autonomous: true
requirements: [LOGR-01, LOGR-02, LOGR-03, LOGR-04]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All six Chinese chapters and the declared bilingual desktop/tablet/mobile key-chapter matrix work in a real browser with correct content order, interactions, fallbacks, checkpoint placement, and no horizontal overflow."
    - "Asset drift, media drift, numerical parity, safe rendering, legacy routes/progress/checkpoint, standard build, Pages build, and security audit pass together on the same commit."
    - "The final release contains no Phase 30 metric/threshold workflow or learner-visible test result, and no unrelated user or generated files are modified."
  artifacts:
    - path: "scripts/qa/logisticRegressionBrowserMatrix.js"
      provides: "Deterministic real-browser locale/viewport/interaction/failure matrix"
    - path: "tests/logistic-regression-release.test.mjs"
      provides: "Cross-contract release and scope gate"
    - path: ".planning/phases/29-logistic-regression-rebuild/29-VALIDATION.md"
      provides: "Completed Nyquist validation record"
  key_links:
    - from: "scripts/qa/logisticRegressionBrowserMatrix.js"
      to: "src/components/LogisticRegressionPagedLesson.vue"
      via: "stable data-testid and semantic table/output probes on real routes"
      pattern: "logistic-current-chapter|logistic-course-lab"
    - from: "tests/logistic-regression-release.test.mjs"
      to: "public/logistic-regression/phase-29/manifest.json"
      via: "recompute and cross-check page registry, asset/media hashes, route contracts, and Phase 30 boundary"
      pattern: "phase-29"
---

<objective>
Close Phase 29 with one fail-fast automated and real-browser release proof, then mark the validation strategy complete only after every gate passes.

Purpose: Detect cross-layer drift that unit tests cannot see and hand Phase 30 a frozen, trustworthy prediction contract without exposing its reserved evaluation early.
Output: Browser matrix, release test, completed validation record, and a verified PR-ready branch.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-logistic-regression-rebuild/29-CONTEXT.md
@.planning/phases/29-logistic-regression-rebuild/29-VALIDATION.md
@.planning/phases/29-logistic-regression-rebuild/29-06-SUMMARY.md
@scripts/qa/linearRegressionBrowserMatrix.js
@src/components/LogisticRegressionPagedLesson.vue
@public/logistic-regression/phase-29/manifest.json
@public/manim/logistic-regression/metadata.json

<interfaces>
The Playwright CLI wrapper is `${CODEX_HOME:-$HOME/.codex}/skills/playwright/scripts/playwright_cli.sh`. The matrix file exports an async `(page) => result` evaluator for `playwright-cli run-code --filename`, matching the repository's existing QA contract.

The expected matrix result is strict JSON with `cases`, `failures`, `results`, `interactions`, and `failureInjections`; the release command validates exact expected route/locale/viewport sets and semantic booleans rather than accepting a process exit alone.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the deterministic bilingual responsive browser matrix</name>
  <files>scripts/qa/logisticRegressionBrowserMatrix.js, tests/logistic-regression-release.test.mjs</files>
  <read_first>
    - `scripts/qa/linearRegressionBrowserMatrix.js` for session evaluator structure, request/console capture, semantic output probes, overlap/overflow checks, and failure injections.
    - `29-VALIDATION.md` manual-only matrix and all six final scene/page data-testid hooks.
    - Playwright CLI skill instructions for wrapper/session/open/run-code/close and artifact cleanup.
  </read_first>
  <behavior>
    - Visit all six Chinese deep links at 1200px, then `linear-score`, `threshold-decisions`, `log-loss`, and `linear-limits` in both locales at 1440px, 768px, and 390px; each record asserts current ID, TOC/pager, one lab, correct block order, no dead/empty local links, no horizontal overflow/interactive overlap, and no console/page error.
    - Exercise one meaningful control path for each of the six scenes, keyboard step/reset, code copy success/failure, mobile TOC, final-only resources/checkpoint, and the Phase 30 link.
    - Exercise reduced motion plus forced asset HTTP failure, asset corruption/hash failure, and MP4 failure; require the correct localized text/table/poster/transcript fallback and successful cleanup after route changes.
    - Inspect network requests for root/Pages-local assets, current-chapter lazy loading, absence of eager unrelated scene/media requests, and absence of any learner request that consumes reserved test records.
  </behavior>
  <action>Create the Playwright CLI evaluator against the Plan 29-00 release contract using the stable final page/scene hooks. Return exact structured records for the 30 route/viewport/locale cases, six semantic scene interactions, and a declared failure-injection matrix. Validate chapter/content order, one current lab, allowed media, safe formula/code rendering, clipboard feedback, keyboard/reset/reduced-motion behavior, responsive overflow/overlap, local requests, final-only resources/checkpoint, and no learner-visible reserved-test data per D-12, D-13, D-18, and D-19 through D-24. Use route interception for deterministic media/asset failures and restore it after each probe. Keep screenshots/traces in ignored `output/playwright/` only when diagnosing failure; the evaluator itself must leave no tracked artifact. Refine only expected structured fixture bindings; do not reduce the case or failure matrix.</action>
  <acceptance_criteria>
    - The release test locks the exact chapter/locale/viewport/interaction/failure sets and rejects missing, duplicate, or silently skipped records.
    - Every semantic scene action changes its declared readout and reset returns the deterministic initial state.
    - Reduced-motion, media-failure, asset-failure, and corruption states retain complete teaching fallbacks with no uncaught error.
    - All course requests are same-origin/base-safe and only current chapter interaction/media assets load.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/qa/logisticRegressionBrowserMatrix.js && npm test -- tests/logistic-regression-release.test.mjs tests/logistic-regression-cockpit.test.mjs tests/logistic-regression-labs.test.mjs</automated>
  </verify>
  <done>The browser matrix is machine-checkable and covers the visual/interactive states left manual-only in the initial validation draft.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete fail-fast release gate and seal validation</name>
  <files>.planning/phases/29-logistic-regression-rebuild/29-VALIDATION.md</files>
  <read_first>
    - Every Phase 29 summary and the final git diff/status.
    - `29-VALIDATION.md` test map and sign-off checklist.
    - `package.json` scripts plus asset/media `--check` help output.
    - The user-owned primary-checkout exclusions; work only in this Phase 29 worktree and never stage `.planning/research/`.
  </read_first>
  <action>Run one fail-fast gate on the final commit: Phase 29 math/assets/parity/calibration/content/rendering/labs/media/cockpit/release tests; read-only asset regeneration; read-only media verification; `npm run test:ci`; `npm run build`; `npm run build:pages`; `npm run security:audit`; then a strict Vite preview on an unused local port and a named Playwright CLI session that opens the course, runs `scripts/qa/logisticRegressionBrowserMatrix.js`, parses the returned JSON, and closes/cleans the session/server/temp files even on failure. Confirm the base redirect and all six IDs, Progress V1/V2 and checkpoint contracts, sanitizer/copy paths, exact current-chapter loading, and D-19 through D-22 evaluation boundary. Audit git status so only Phase 29-owned files are changed and the unrelated `.planning/research/` cache is absent from the commit. Only after every command passes, set `nyquist_compliant: true`, `wave_0_complete: true`, mark every test-map/sign-off row green, and record the executed commands/date in `29-VALIDATION.md`.</action>
  <acceptance_criteria>
    - Focused tests, full CI tests, both builds, security audit, asset drift, media check, and exact real-browser matrix pass on the same HEAD.
    - Preview/browser processes, ports, sessions, temporary output, and route interceptions are cleaned after success and injected failure.
    - Validation frontmatter/checklist changes only after the complete gate succeeds.
    - Git diff contains only Phase 29 plans/implementation/assets/docs/tests and does not include the unrelated cache or primary-checkout user files.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/logistic-regression/build-phase-29-assets.py --check && python3 scripts/manim/render_logistic_regression.py --check && npm run test:ci && npm run build && npm run build:pages && npm run security:audit</automated>
    <human-check>Review the final browser-matrix JSON and representative 1440px/390px screenshots for teaching hierarchy, touch comfort, poster readability, and bilingual pacing before merging the Phase 29 PR.</human-check>
  </verify>
  <done>Phase 29 is Nyquist-complete, reproducible, responsive, accessible, scope-clean, and ready for an isolated PR before Phase 30 begins.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/qa/logisticRegressionBrowserMatrix.js`: strict 30-case route matrix plus semantic interactions and failure injections.
- `tests/logistic-regression-release.test.mjs`: cross-contract release/scope assertions.
- Finalized `29-VALIDATION.md`: green automated/manual coverage record and command provenance.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Trace scores through sigmoid, likelihood, stable BCE, scratch gradients, library parity, calibration, and linear failure | 00–07 | COVERED |
| REQ | LOGR-01 | Score, sigmoid, log-odds, MLE, BCE | 00, 01, 03–07 | COVERED |
| REQ | LOGR-02 | Stable BCE, gradients, deterministic training, finite differences | 00–07 | COVERED |
| REQ | LOGR-03 | Explicit aligned scratch/sklearn and separate regularization | 00, 02, 05–07 | COVERED |
| REQ | LOGR-04 | Calibration and nonlinear linear-boundary limits | 00, 02, 05–07 | COVERED |
| RESEARCH | R-ARCH | Phase-local typed course, pure engine, lazy scenes, strict asset loader | 00–02, 05, 06 | COVERED |
| RESEARCH | R-ASSET | Executed static authority, atomic package, hashes, source cells, Pages paths | 00, 02, 07 | COVERED |
| RESEARCH | R-PARITY | Fixed scratch/sklearn configs and tolerances before separate L2 objective | 00, 02, 05, 06 | COVERED |
| RESEARCH | R-MEDIA | Four source-complete, ffprobe/hash-verified packages and shared player | 00, 03, 04, 06, 07 | COVERED |
| RESEARCH | R-SEC | V5 finite/range/schema validation and V6 SHA-256 integrity checks | 00–07 | COVERED |
| RESEARCH | R-INSTALL | No new package installation or external runtime API | 00–07 | COVERED |
| CONTEXT | D-01–D-07 | One fixed Banknote spine, honest labels, selected rows, calibration, isolated synthetic diagnostics | 00–02, 05, 06 | COVERED |
| CONTEXT | D-08–D-12 | Six preserved responsibilities, beginner derivation, gradients, two-stage parity, fixed chapter flow | 00, 01, 02, 03, 06 | COVERED |
| CONTEXT | D-13–D-16 | Six lazy dedicated labs, bounded controls, browser calculation/static replay boundary | 00, 02, 05, 06 | COVERED |
| CONTEXT | D-17–D-18 | Four production Manim packages and accessible user-controlled fallbacks | 00, 03, 04, 06, 07 | COVERED |
| CONTEXT | D-19–D-22 | Phase 30 threshold/metric/test boundary and frozen prediction handoff | 00, 02, 05–07 | COVERED |
| CONTEXT | D-23–D-25 | Compatibility, learner wording/final references, pure tested calculations | 00, 01, 03–07 | COVERED |
| CONTEXT | Deferred | Fit calibration, full EDA, free exploration, Phase 30 metrics, backend/browser Python/exercise bank | — | EXCLUDED BY DECISION |
| PROBE | P-01 | Unsupported target semantics prohibition | 01 | COVERED / FLAGGED-UNVERIFIED |
| PROBE | P-02 | Test-result disclosure prohibition | 02 | COVERED / FLAGGED-UNVERIFIED |
| PROBE | P-03 | Synthetic/real provenance prohibition | 05 | COVERED / FLAGGED-UNVERIFIED |

No source item is missing. The API-coverage detector returned `detected:false`; Phase 29 uses local authoring libraries and static files, not an external network API. The assumption-delta detector returned `detected:false`. No ORM/schema path is in scope, so no schema-push task applies. Package legitimacy is not triggered because no install task exists.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Built static site → real browser | Routing, asset requests, storage, media, and interaction behavior meet an actual browser/runtime. |
| Test/asset/media commands → release decision | A false-positive or skipped command could incorrectly mark the phase ready. |
| Phase worktree → Git commit | Unrelated cached/user files could enter the phase PR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-07-01 | Tampering | browser matrix result | high | mitigate | Parse strict JSON, require exact case/interaction/failure sets, reject duplicates/skips/errors, and retain per-case semantic booleans. |
| T-29-07-02 | Repudiation | release gate | medium | mitigate | Record exact commands and validation statuses only after a fail-fast same-HEAD run; no retry may hide a failure. |
| T-29-07-03 | Information Disclosure | browser network/DOM | high | mitigate | Inspect requests and learner DOM/download links for reserved test data and Phase 30-only outputs. |
| T-29-07-04 | Denial of Service | leaked preview/browser processes | medium | mitigate | Use strict port ownership and cleanup traps; assert session/process/port/temp absence after completion. |
| T-29-07-05 | Tampering | git scope | medium | mitigate | Whitelist Phase 29 paths, inspect staged/untracked files, and exclude the unrelated research cache and primary-checkout user changes. |
</threat_model>

<verification>
- The complete Task 2 gate passes on one HEAD and `29-VALIDATION.md` records the result.
- Requirement and decision coverage tools find LOGR-01..04 and D-01..D-25 in executable tasks/acceptance criteria.
- Real-browser matrix verifies responsive bilingual interaction plus failure/reduced-motion states.
</verification>

<success_criteria>
- All four roadmap success criteria and all four requirements are demonstrably satisfied.
- Every locked decision is covered; every deferred idea remains absent; every spec-less edge item is explicit and executable.
- All three retained phase-specific prohibitions are present under `must_haves.prohibitions` and remain descriptor-less flagged-unverified as required by the fallback protocol.
- The phase is ready for one isolated Phase 29 PR; Phase 30 can consume the frozen handoff without retraining.
</success_criteria>

<output>
Create `.planning/phases/29-logistic-regression-rebuild/29-07-SUMMARY.md` when done.
</output>
