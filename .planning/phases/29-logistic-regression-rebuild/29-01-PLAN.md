---
phase: 29-logistic-regression-rebuild
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/logistic-regression/types.ts
  - src/modules/logistic-regression/engine.ts
  - src/modules/logistic-regression/data/course.ts
  - src/components/LogisticRegressionPagedLesson.vue
  - tests/logistic-regression-math.test.ts
autonomous: true
requirements: [LOGR-01, LOGR-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A learner opening /learn/logistic-regression/linear-score can follow one audited Banknote row from four standardized features through z = w^T x + b to probability and log-odds without changing the route or chapter identity."
    - "Stable sigmoid, odds/log-odds, log-likelihood, logit-domain BCE, row and batch gradients, central differences, calibration transforms, and synthetic capacity helpers are pure, deterministic, finite-guarded TypeScript functions."
    - "The six preserved chapter IDs, module identity, checkpoint identity, Progress V1/V2 behavior, safe Markdown/KaTeX path, and code-copy behavior remain compatible."
  artifacts:
    - path: "src/modules/logistic-regression/types.ts"
      provides: "Phase-local chapter, block, scene, interaction, manifest, prediction-handoff, and media contracts"
      exports: ["LogisticChapterId", "LogisticCourseBlock", "LogisticCourseChapter", "LogisticObservationSceneId", "LogisticInteractionAsset", "LogisticAssetManifest"]
    - path: "src/modules/logistic-regression/engine.ts"
      provides: "Pure one-row-to-batch logistic mathematics and finite validation"
      exports: ["oneRowLogisticTerms", "logisticBatchTerms", "centralDifferenceGradient", "temperatureProbability", "calibrationBins", "xorContradiction", "circleBoundaryDiagnostics"]
    - path: "src/modules/logistic-regression/data/course.ts"
      provides: "Typed bilingual course registry and the complete linear-score tracer chapter"
      exports: ["logisticCourseChapters", "logisticCourseReferences", "logisticCourseDownloads"]
  key_links:
    - from: "src/components/LogisticRegressionPagedLesson.vue"
      to: "src/modules/logistic-regression/data/course.ts"
      via: "current legacy section ID resolves the phase-local chapter without changing route/progress keys"
      pattern: "logisticCourseChapters"
    - from: "src/modules/logistic-regression/engine.ts"
      to: "src/modules/math-lab/utils/banknoteLogistic.ts"
      via: "reuse/re-export stable sigmoid, softplus, and logit BCE instead of duplicating them"
      pattern: "stable(Sigmoid|BinaryCrossEntropy)|softplus"
  prohibitions:
    - statement: "Learner-facing Phase 29 material must not assign unsupported real-world meanings to class 0 or class 1."
      status: unresolved
---

<objective>
Prove the Phase 29 architecture with one production-quality `linear-score` teaching path, then establish the pure typed numerical contract all later assets, media, and labs consume.

Purpose: Catch notation, numerical-stability, safe-rendering, and compatibility errors before the expensive Notebook, Manim, and six-scene expansions begin.
Output: A complete first chapter path plus reusable phase-local types, pure logistic calculations, and focused tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-logistic-regression-rebuild/29-CONTEXT.md
@.planning/phases/29-logistic-regression-rebuild/29-RESEARCH.md
@.planning/phases/29-logistic-regression-rebuild/29-PATTERNS.md
@.planning/phases/29-logistic-regression-rebuild/29-VALIDATION.md
@src/data/logisticRegressionModule.ts
@src/components/LogisticRegressionPagedLesson.vue
@src/modules/math-lab/utils/banknoteDataset.ts
@src/modules/math-lab/utils/banknoteLogistic.ts
@src/utils/markdownMath.ts

<interfaces>
Existing authority: `BANKNOTE_FEATURES`, `BanknoteRow`, `BanknoteTarget`, `ParameterVector`, `FeatureVector`, `stableSigmoid(value)`, `softplus(value)`, `stableBinaryCrossEntropy(logit, target)`, and `lossAndGrad(features, targets, parameters, l2)`.

The paged component retains `defineProps<{ moduleDefinition: AlgorithmModuleDefinition; section: StorySection; snapshots: TrainingSnapshot[]; ... }>()`, its existing preset/config emits, and the `logistic-course-page`, `logistic-current-chapter`, sidebar, mobile-TOC, and pager test IDs.
</interfaces>
</context>

<flagged_assumptions>
- **EDGE-29-01 / LOGR-01 boundary:** Treat `z = 0` as `p = 0.5` and the default bridge as class 1; test `z = -1e-12`, `0`, and `+1e-12`, plus lab-domain endpoints and one rejected step outside them. This is an executable provisional boundary selected under the agent discretion in D-19.
- **EDGE-29-02 / LOGR-01 precision:** Keep full-precision values in engine/assets, round only at presentation, and test stable branches at logits `-1000`, `0`, and `1000`; exact display digits remain a presentation decision, not a numerical authority.
- **EDGE-29-03 / LOGR-02 unclassified:** Define the teaching objective as mean unregularized BCE, use `(p-y)x` and `p-y` for row gradients, and exclude the intercept from later L2; central differences cover every parameter before training consumes the gradient.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ship one audited Banknote row from typed content through the preserved linear-score route</name>
  <files>tests/logistic-regression-math.test.ts, src/modules/logistic-regression/types.ts, src/modules/logistic-regression/engine.ts, src/modules/logistic-regression/data/course.ts, src/components/LogisticRegressionPagedLesson.vue</files>
  <read_first>
    - `src/modules/math-lab/utils/banknoteDataset.ts` for feature order, row IDs, split vocabulary, and target type.
    - `src/modules/math-lab/utils/banknoteLogistic.ts` for stable scalar primitives and five-parameter convention.
    - `src/components/LogisticRegressionPagedLesson.vue` for compatibility props/emits/test IDs and current route composition.
    - `src/modules/optimizer-comparison/data/course.ts` and `src/modules/optimizer-comparison/OptimizerPagedLesson.vue` for the phase-local typed-block renderer pattern.
  </read_first>
  <behavior>
    - The canonical row trace reports four ordered feature contributions, intercept, logit, probability, odds/log-odds, one-row BCE, `(p-y)x`, and intercept gradient from one shared calculation.
    - At the score boundary, `z=-1e-12` maps below 0.5, `z=0` maps exactly to 0.5, and `z=+1e-12` maps above 0.5; the one-sample class bridge uses `p >= 0.5` per D-19.
    - Extreme finite logits keep sigmoid/BCE finite and preserve monotonicity; non-finite inputs and wrong feature/parameter widths throw `RangeError` before arithmetic.
    - `/learn/logistic-regression/linear-score` renders the fixed question-to-conclusion block order through `MarkdownMathContent`, keeps copy feedback, and leaves all other chapter routes on their current working presentation until expanded.
  </behavior>
  <action>Start with failing assertions, then define the complete phase-local discriminated contracts and pure `oneRowLogisticTerms` path. Reuse the existing stable primitives and Banknote feature order. Select the initial canonical training row and aligned parameter vector only from committed Banknote authorities, record its row ID and provenance, and assert the literal full-precision trace against those sources so copied values cannot drift. Implement the full `linear-score` chapter around the necessary data contract, one row, batch notation, design matrix, score contributions, and log-odds per D-01 through D-05, D-08, D-09, and D-12. Teach only class 0/class 1 per D-03. Make the page select the new typed chapter only for this route while preserving all compatibility props/emits/test IDs and the other five existing deep links per D-23. Formula-bearing copy uses `String.raw` and the existing sanitized renderer per D-24; no raw HTML path is added.</action>
  <acceptance_criteria>
    - The canonical row's contribution sum plus intercept equals its displayed logit within `1e-12`, and the displayed probability/BCE/gradient derive from that same trace.
    - A learner can open the unchanged `linear-score` deep link and see a complete bilingual single-column explanation with copyable NumPy code and locked output.
    - The route, current chapter test ID, pager, progress callbacks, and remaining five chapters remain operational.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/logistic-regression-math.test.ts tests/logistic-regression-cockpit.test.mjs</automated>
  </verify>
  <done>The production `linear-score` tracer works end to end and is backed by source-audited numerical assertions rather than hand-maintained display math.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the pure score-to-calibration numerical engine</name>
  <files>tests/logistic-regression-math.test.ts, src/modules/logistic-regression/types.ts, src/modules/logistic-regression/engine.ts</files>
  <read_first>
    - `29-CONTEXT.md` decisions D-05, D-06, D-07, D-09, D-10, D-11, D-16, D-19, and D-25.
    - `src/modules/math-lab/utils/banknoteLogistic.ts` for mean objective, L2/intercept convention, deterministic training, and finite guards.
    - `29-RESEARCH.md` sections “Common Pitfalls” and “Code Examples” for stable likelihood and calibration invariants.
  </read_first>
  <behavior>
    - Dataset likelihood exposes finite per-row Bernoulli contributions, an intentionally underflowing raw product status, and a finite additive log-likelihood/mean BCE without treating the raw product as the production objective.
    - Batch gradients equal `X.T @ (p-y) / n` and `mean(p-y)` and match central differences for all five parameters over the declared step sweep.
    - Positive temperature transforms preserve logit ordering and the default class labels; non-positive/non-finite temperature is rejected.
    - Calibration bins use deterministic interval semantics, omit empty bins, keep full precision internally, and compute a declared weighted calibration-error summary.
    - XOR contradiction and circle diagnostics return explicitly synthetic, finite geometry without importing Banknote fitting or evaluation state.
  </behavior>
  <action>Implement guarded `linearScore`, `sigmoidOddsTerms`, `bernoulliLogLikelihood`, `stableMeanBce`, `logisticGradient`, `centralDifferenceGradient`, `temperatureProbability`, `buildCalibrationBins`, `xorBoundaryDiagnostic`, and `circleBoundaryDiagnostic` exports. Keep computations outside Vue per D-25 and make functions consume readonly typed inputs. Use one canonical parameter order `[variance, skewness, curtosis, entropy, intercept]`; validate feature order, widths, targets, non-empty batches, range limits, and every finite leaf before returning. Keep entropy/KL, automatic differentiation, Phase 30 metric calculations, threshold sweeps, and model fitting outside this engine per D-09, D-10, and D-19. Preserve existing `banknoteLogistic.ts` behavior behind imports/adapters rather than changing the historical Phase 25 trainer.</action>
  <acceptance_criteria>
    - Scalar, vectorized, and finite-difference gradients agree under a single documented tolerance for every parameter.
    - Extreme-logit sigmoid and BCE cases remain finite; invalid widths, targets, temperatures, bins, NaN, and Infinity fail closed.
    - Calibration and synthetic diagnostics cannot mutate or masquerade as the Banknote training authority.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/logistic-regression-math.test.ts</automated>
  </verify>
  <done>One tested DOM-free engine owns all simple browser calculations required by the six Phase 29 chapters.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/modules/logistic-regression/types.ts`: `LogisticChapterId`, `LogisticCourseBlockKind`, `LogisticCourseBlock`, `LogisticCourseChapter`, six-scene discriminated unions, typed controls, asset/manifest/prediction/media contracts.
- `src/modules/logistic-regression/engine.ts`: `oneRowLogisticTerms`, `linearScore`, `sigmoidOddsTerms`, `bernoulliLogLikelihood`, `stableMeanBce`, `logisticGradient`, `centralDifferenceGradient`, `temperatureProbability`, `buildCalibrationBins`, `xorBoundaryDiagnostic`, and `circleBoundaryDiagnostic`.
- `src/modules/logistic-regression/data/course.ts`: chapter registry with the complete first chapter and stable six-ID ordering.
- `src/components/LogisticRegressionPagedLesson.vue`: preserved shell with the phase-local `linear-score` tracer path.
- `tests/logistic-regression-math.test.ts`: Wave 0 numerical contract for LOGR-01/02 and the spec-less edge assumptions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Typed course copy → sanitized DOM | Bilingual Markdown/TeX becomes rendered learner content. |
| Lab/test input → pure numerical engine | Numbers and arrays may contain invalid widths, targets, NaN, Infinity, or extreme finite values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-01-01 | Tampering | `LogisticRegressionPagedLesson.vue` rich text | high | mitigate | Route all narrative through `MarkdownMathContent`/`sanitize-html`; add rendered malicious-HTML and KaTeX assertions before later content expansion. |
| T-29-01-02 | Denial of Service | `engine.ts` numerical inputs | medium | mitigate | Reject non-finite values, wrong dimensions, invalid targets/ranges, and unsafe iteration counts before calculation; test extremes and last-valid behavior. |
| T-29-01-03 | Information Disclosure | learner class copy | medium | mitigate | Use only source-supported class 0/class 1 wording and scan bilingual visible content for unsupported semantics. |
| T-29-01-04 | Repudiation | canonical row/output provenance | low | accept | Full source-row and parameter provenance is asserted in tests; immutable static teaching data has no authenticated actor or audit-log requirement. |
</threat_model>

<verification>
- Focused numerical and existing compatibility tests pass after each task.
- `npm run build` proves the new contracts and page slice type-check without changing the public route interface.
- No package install, external API, ORM/database schema, backend, or secret is introduced.
</verification>

<success_criteria>
- LOGR-01 has a working first deep link that uses one real Banknote row and consistent score/probability/log-odds notation.
- LOGR-02 has a tested stable numerical core ready for executed assets and labs.
- D-01 through D-05, D-08 through D-12, D-19, and D-23 through D-25 are represented without pulling Phase 30 evaluation into scope.
</success_criteria>

<output>
Create `.planning/phases/29-logistic-regression-rebuild/29-01-SUMMARY.md` when done.
</output>
