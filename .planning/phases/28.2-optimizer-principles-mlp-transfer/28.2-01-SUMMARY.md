---
phase: 28.2
plan: 01
subsystem: optimizer numerical authority
tags: [optimizers, mlp, banknote, reproducibility, planning]
requires: []
provides: [typed-optimizer-engine, mlp-strategy-seam, optimizer-benchmark-assets]
affects: [optimizer-comparison, math-lab-optimizer-race, mlp-playground]
tech-stack:
  added: []
  patterns: [pure TypeScript state transitions, manifest-bound local assets]
key-files:
  created:
    - src/types/optimizer.ts
    - src/simulations/optimizers/index.ts
    - scripts/optimizer-comparison/build-assets.mjs
    - public/datasets/optimizer-comparison/benchmark-manifest.json
  modified:
    - src/modules/math-lab/utils/optimizers.ts
    - src/simulations/mlpPlayground.ts
    - src/types/ml.ts
decisions:
  - Keep the historical MLP SGD update path as the default and use the shared engine only for explicit optimizer selections.
  - Preserve the existing Banknote split and train-only standardization; final test display remains a Plan 03 responsibility.
metrics:
  duration: 0m
  completed_date: 2026-08-12
status: complete
---

# Phase 28.2 Plan 01: Optimizer Numerical Authority Summary

Reconciled Phase 28/28A/28B planning metadata and added a deterministic, typed optimizer authority with MLP compatibility plus hash-bound local benchmark assets.

## Completed Tasks

1. Reconciled Phase 28 TPRJ completion, Phase 28A GRAD traceability, Phase 28B OPT-01…OPT-06 registration, roadmap progress, approved context, and three-plan set.
2. Added pure SGD, PyTorch-compatible Momentum/RMSProp/Adam, L2/AdamW separation, schedules, finite guards, typed traces, and legacy Math Lab re-exports.
3. Added an explicit MLP optimizer seam that preserves the default SGD path; published deterministic circle `2→4→1 tanh` trajectory JSON/CSV, bilingual executed NumPy Notebook, and existing-Banknote manifest using only train-fitted standardization.

## Validation

- `node --test tests/optimizer-engine.test.ts tests/optimizer-assets.test.ts` — pass (7 tests).
- `npm run test:ci` — pass (1,013 passed; 28 skipped).
- `npm run build` — pass (existing Vite large-chunk warning).
- `npm run build:pages` — pass (existing Vite large-chunk warning).
- `node scripts/optimizer-comparison/build-assets.mjs --check` — pass.
- `npm run security:audit` — reports one high `nanoid <3.3.17` advisory. `npm ls nanoid` returns no declared package; this is pre-existing installed dependency state outside this plan's changed files, so no dependency mutation was made.

## Commits

- `f679b74` — planning reconciliation and Phase 28B registration.
- `6abc41a` — shared optimizer engine and Math Lab compatibility adapter.
- `bb64a8f` — MLP strategy seam and deterministic benchmark package.
- `b1072ba` — corrected the generator to execute the complete fixed `2→4→1 tanh` MLP trajectory.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected the initial benchmark generator's scalar projection**
- **Found during:** Task 3 validation
- **Fix:** Replaced it with deterministic forward/backprop updates for the complete fixed `2→4→1 tanh` circle MLP, then regenerated the hash-bound trajectory package.
- **Commit:** `b1072ba`

The existing security-audit advisory is outside this plan's changed dependency files and remains unresolved.

## Self-Check: PASSED

Required engine, strategy, script, assets, tests, and all three task commits exist on the Phase 28B worktree branch.
