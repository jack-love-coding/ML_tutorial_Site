---
phase: 28.2-optimizer-principles-mlp-transfer
verified: 2026-08-12T15:57:23Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps: []
---

# Phase 28.2: Optimizer Principles, State, and MLP Transfer Verification Report

**Phase Goal:** Rebuild `optimizer-comparison` from deterministic optimizer state machines through a controlled `2→4→1 tanh` MLP comparison and a leakage-safe Banknote transfer, while preserving existing course identity and Math Lab entry points.
**Verified:** 2026-08-12T15:57:23Z
**Status:** passed
**Re-verification:** Yes — media fallback gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | One pure engine implements finite-guarded SGD, Momentum, RMSProp, Adam, coupled L2, AdamW, and schedules. | ✓ VERIFIED | `src/simulations/optimizers/index.ts` is the sole update authority; `tests/optimizer-engine.test.ts` executes locked two-step recurrences, state semantics, decay separation, schedules, and invalid-input guards. |
| 2 | Math Lab Optimizer Race and default MLP Playground SGD retain numerical compatibility. | ✓ VERIFIED | Math Lab re-exports the engine and its paths match engine recurrences; the MLP test compares historical and explicit-SGD snapshots and exercises optimizer lifecycle boundaries. |
| 3 | The controlled `2→4→1 tanh` comparison publishes deterministic, hash-bound local results under matched and practical settings. | ✓ VERIFIED | The shared runner calls `stepOptimizer()` for every one of 328 rows; the clean-kernel `build-assets.mjs --check` replay, CSV/JSON agreement, manifest assertions, and hash checks passed. |
| 4 | Banknote fitting/selection use only train/validation and the fixed test partition is evaluated once after freeze. | ✓ VERIFIED | `computeBanknoteTransfer()` fits mean/scale on `grouped.train`, trains on train batches, records validation before one final-test metric; artifact/manifest drift tests and clean-kernel check passed. |
| 5 | Existing six lesson IDs, deep links, final checkpoint routes, Progress identity, and Math Lab entry point remain available. | ✓ VERIFIED | Typed course retains all six IDs; lazy shell is selected by `AlgorithmView`; checkpoint resolver creates chapter routes; routing tests cover course, Gradient Descent, MLP, Math Lab, and CNN handoffs. |
| 6 | Learners can follow training order and compare full, mini-batch, and stochastic SGD on deterministic shared cases (OPT-01). | ✓ VERIFIED | `TrainingLedgerScene` renders forward/loss/zero-grad/backward/step and `BatchNoiseScene` switches 960/64/1 samples; scene model and mounted-control tests passed. |
| 7 | Learners can trace distinct Momentum and RMSProp state on a common loss surface (OPT-02). | ✓ VERIFIED | `MomentumRmspropScene` renders velocity, square-average, effective step, and semantic table from shared engine traces; numerical/model tests passed. |
| 8 | Learners can inspect Adam raw/corrected moments and distinguish L2 from AdamW (OPT-03). | ✓ VERIFIED | `AdamDecayScene` displays `m`, `v`, `m̂`, `v̂`, `t`, and distinct L2/AdamW results; engine and scene tests execute the differences. |
| 9 | Learners can use constant, step-decay, and warmup-cosine schedules in optimizer-then-scheduler cadence (OPT-04). | ✓ VERIFIED | `ScheduleCadenceScene` drives `learningRateForStep()` and labels `optimizer.step() → scheduler.step()`; tests exercise all three schedules and selection/reset behavior. |
| 10 | Learners can compare four optimizers on the fixed MLP and interpret a separate real Banknote transfer without a universal-winner/test-selection claim (OPT-05, OPT-06). | ✓ VERIFIED | `CurveDiagnosisScene` fetches the published trajectory and Banknote artifact, renders matched/practical rows plus frozen-test labels, and falls back to a generated hash-bound snapshot; mounted tests cover data, controls, mode change, reduced motion, and fetch failure. |
| 11 | The four optimizer lesson media preserve the full teaching message when video cannot or should not play. | ✓ VERIFIED | `optimizerMediaRegistry` is a typed, metadata-hash-bound runtime mirror that supplies Pages-safe MP4/poster paths, localized markers, alt text, and complete published zh/en transcripts to `ChapteredMediaPlayer`; mounted regression covers normal, reduced-motion, and video-error states for SGD, Momentum, RMSProp, and Adam. |

**Score:** 11/11 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/simulations/optimizers/index.ts` | Pure optimizer/state/schedule authority | ✓ VERIFIED | 177 substantive lines; imported by benchmark runner, Banknote generator, scene models, Math Lab adapter, and MLP seam. |
| `src/modules/math-lab/utils/optimizers.ts` | Legacy Math Lab compatibility adapter | ✓ VERIFIED | Re-exports shared engine and uses it for all four historical race paths. |
| `scripts/optimizer-comparison/trajectory-runner.ts` | Controlled fixed-MLP execution | ✓ VERIFIED | Runs two comparisons × four optimizers × updates 0–40 exclusively through shared engine. |
| `scripts/optimizer-comparison/banknote-transfer.ts` | Frozen, train-only Banknote authoring calculation | ✓ VERIFIED | Uses local fixed CSV, train-derived standardization, engine AdamW updates, validation, then one final test record. |
| `public/datasets/optimizer-comparison/*` and notebook outputs | Hash-bound published data | ✓ VERIFIED | Manifest and generator check validate source, artifact, snapshot, CSV/JSON, and notebook execution. |
| `src/modules/optimizer-comparison/{data/course.ts,OptimizerPagedLesson.vue,labs/*}` | Six bilingual lessons and lazy scenes | ✓ VERIFIED | All six entries are lazy-loaded, substantive, route-selected, table/log backed, and mounted-control tested. |
| `src/components/ChapteredMediaPlayer.vue` | Accessible reusable player | ✓ VERIFIED | Supports base-safe video/poster, marker seeks, transcript disclosure, reduced motion, error fallback, and cleanup; used by MLP and Gradient Descent. |
| `src/modules/optimizer-comparison/data/media.ts` + `OptimizerPagedLesson.vue` media configuration | Wire packaged markers/transcripts to optimizer lessons | ✓ VERIFIED | The registry validates its metadata hash, asset/poster hashes, marker IDs/times, and transcript source hashes in `optimizer-course-media.test.mjs`; `v-bind="mediaConfig"` passes the complete typed contract to the player. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Optimizer runner / Banknote generator / lesson scene models | Shared engine | `createOptimizerState` + `stepOptimizer` imports | ✓ WIRED | No second scalar optimizer recurrence found in the phase execution paths. |
| `build-assets.mjs` | trajectories, Banknote artifact, manifest, fallback snapshot | deterministic generation + `--check` | ✓ WIRED | Clean-kernel check passed and rejects source/metric/parameter drift. |
| `AlgorithmView.vue` | `OptimizerPagedLesson.vue` | lazy `defineAsyncComponent`, optimizer branch | ✓ WIRED | Route tests show legacy course IDs render the dedicated shell. |
| `CurveDiagnosisScene.vue` | local trajectories and Banknote JSON | `fetch(withPublicBase(...))`, validated fallback | ✓ WIRED | Mounted fetch-success/failure test proves real-data and hash-bound fallback paths. |
| Optimizer course media config | `ChapteredMediaPlayer.vue` | `optimizerMediaRegistry` + `v-bind="mediaConfig"` | ✓ WIRED | All four published entries reach the player with base-safe MP4/poster paths, localized marker labels, alt/title copy, and hash-bound bilingual transcript content. |
| Gradient Descent, MLP, Math Lab, optimizer, CNN | course routes | explicit router links and checkpoint resolver | ✓ WIRED | Focused routing test passed. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `CurveDiagnosisScene.vue` | `trajectory`, `banknote` | local published JSON fetched with `withPublicBase`; generated snapshot on failure | Yes; numeric rows and frozen metrics are verified generator outputs | ✓ FLOWING |
| optimizer scene models | trace/state values | shared pure engine | Yes; rendered tables/logs derive from calls to the engine | ✓ FLOWING |
| optimizer course media | paths, marker labels, transcripts, hashes | typed registry mirrored from package metadata and published transcript documents | Yes; every runtime value is validated against package metadata and exact transcript bytes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Engine, course, route, scene lifecycle, reduced-motion/fetch fallback models | `node --test tests/optimizer-engine.test.ts tests/optimizer-assets.test.ts tests/optimizer-course.test.ts tests/optimizer-layout.test.mjs tests/optimizer-scene-models.test.ts tests/optimizer-scene-runtime.test.mjs tests/curriculumRoutingNavigation.test.ts` | 40 passed, 0 failed | ✓ PASS |
| Asset integrity and Manim package contract | `python3 scripts/manim/render_optimizer_comparison.py --check` | all asset/hash/ffprobe/marker/transcript/numeric-anchor checks passed | ✓ PASS |
| Deterministic assets and executed notebook | `OPTIMIZER_NOTEBOOK_KERNEL=ml-atlas-phase27a node scripts/optimizer-comparison/build-assets.mjs --check` | passed outside sandbox; sandbox only blocked Jupyter's required temporary port | ✓ PASS |
| Production and GitHub Pages output | `npm run build && npm run build:pages` | both passed; only existing large-chunk warnings | ✓ PASS |
| Optimizer lesson media transcript/marker fallback | `node --experimental-strip-types --test tests/optimizer-course-media.test.mjs` | 2 passed: validates all four package entries and mounts normal, reduced-motion, and video-error player states in both locales | ✓ PASS |
| Full CI suite | `npm run test:ci` | 1,043 passed, 0 failed, 28 skipped; run outside the sandbox so the existing Jupyter asset check can bind its local kernel port | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED — this phase declares no `probe-*.sh` scripts and is not a probe-based migration/tooling phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| OPT-01 | 28.2-01, 28.2-03 | Deterministic training loop and full/mini/stochastic SGD comparison | ✓ SATISFIED | `TrainingLedgerScene`, `BatchNoiseScene`, and numerical/mounted tests. |
| OPT-02 | 28.2-01, 28.2-02, 28.2-03 | Momentum/RMSProp state and distinct behavior | ✓ SATISFIED | Shared engine recurrences, dedicated scene, package assets, and tests. |
| OPT-03 | 28.2-01, 28.2-03 | Adam moments, L2, and decoupled AdamW | ✓ SATISFIED | Engine recurrence tests plus `AdamDecayScene` rendered model. |
| OPT-04 | 28.2-01, 28.2-02, 28.2-03 | Constant, step, warmup-cosine cadence | ✓ SATISFIED | `learningRateForStep`, scheduler scene, and tests. |
| OPT-05 | 28.2-01, 28.2-02, 28.2-03 | Four optimizers on fixed controlled MLP, no universal winner | ✓ SATISFIED | 328 hash-bound trajectory rows, matched/practical separation, published curve scene/copy. |
| OPT-06 | 28.2-01, 28.2-03 | Fixed-split, train-only Banknote transfer, one test evaluation after freeze | ✓ SATISFIED | Pure evaluator, drift validation, manifest, and rendered artifact table. |

All phase-mapped requirements are declared by at least one plan; no orphaned Phase 28.2 requirements were found. The Plan 02 media-fallback accessibility contract is now fully wired and tested.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
No blocking anti-patterns remain after the typed media-registry wiring.

No `TBD`, `FIXME`, or `XXX` debt markers were found in the phase implementation paths scanned.

### Human Verification Required After Gap Closure

### 1. Optimizer media reduced-motion fallback

**Test:** In an actual browser with `prefers-reduced-motion: reduce`, open each optimizer animation chapter.
**Expected:** Each starts with its poster and exposes an explicit play action, chapter markers, and its locale's complete transcript.
**Why human:** The automated mounted regression proves the component states and complete props; a browser check remains useful to confirm the platform media UI and visual layout.

### 2. Optimizer media failure fallback

**Test:** Block or fail one optimizer MP4 request on each of the SGD, Momentum/RMSProp, and Adam lesson pages.
**Expected:** The poster, complete bilingual transcript, and non-video marker/review affordance remain available; no teaching message is lost.
**Why human:** The mounted regression dispatches the media-error path, but a browser request failure remains the final platform-level smoke check.

### Re-verification Summary

The typed registry removes the observable gap: all four published optimizer packages now provide their Pages-safe paths, localized markers, alt/title copy, and exact zh/en transcript content to the shared player. The registry test detects metadata, asset/poster hash, marker, transcript source, and runtime-copy drift, while the mounted player regression confirms normal, reduced-motion, and video-error fallbacks. Focused tests, Manim integrity, production/Pages builds, and the full CI suite pass; the remaining browser checks are confirmation, not blockers.

---

_Verified: 2026-08-12T15:57:23Z_
_Verifier: the agent (gsd-verifier)_
