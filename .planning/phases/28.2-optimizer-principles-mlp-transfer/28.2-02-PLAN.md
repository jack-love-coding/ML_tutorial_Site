---
phase: 28.2
plan: 02
type: execute
wave: 2
depends_on: [28.2-01]
autonomous: true
requirements: [OPT-02, OPT-03, OPT-04, OPT-05]
files_modified:
  - src/components/ChapteredMediaPlayer.vue
  - src/components/NeuralGuidedLesson.vue
  - src/modules/gradient-descent/components/GradientLessonBlock.vue
  - src/types/ml.ts
  - scripts/manim/render_optimizer_comparison.py
  - scripts/manim/scenes/optimizer_comparison.py
  - public/manim/optimizer-comparison/
  - tests/chaptered-media-player.test.mjs
  - tests/optimizer-media-assets.test.ts
---

<objective>
Publish four short optimizer animations whose visible numbers come from the shared engine, and replace duplicated chaptered-video behavior with one accessible, base-safe media player.
</objective>

<tasks>

<task type="auto">
  <name>Build and publish the four optimizer Manim packages</name>
  <action>Follow the Math-To-Manim pipeline for SGD, Momentum, RMSProp, and Adam. Produce language-neutral 1920×1080 30fps scenes, 35–55 seconds each, using fixed non-color-only encodings and shared numerical anchors. Publish MP4, poster, source, knowledge tree, prompt, bilingual transcripts, markers, versions, and hashes with selective preview/publish/check support.</action>
  <verify>Run ffprobe, duration/resolution/fps, marker-bounds, transcript, source-contract, numeric-anchor, and hash-integrity checks.</verify>
</task>

<task type="auto">
  <name>Extract and migrate the shared ChapteredMediaPlayer</name>
  <action>Extract marker seeking, transcript disclosure, loading/error states, poster fallback, reduced-motion behavior, cleanup, and `withPublicBase` resolution from current MLP and Gradient Descent implementations. Migrate both without changing their lesson IDs, media contracts, or visible teaching behavior.</action>
  <verify>Run component structure tests plus MLP, Gradient Descent, base-path, keyboard, reduced-motion, cleanup, and fallback regression tests.</verify>
</task>

</tasks>

<verification>
Focused media tests, full test suite, standard build, Pages build, media asset check, and security audit pass.
</verification>

<success_criteria>
- Four complete and hash-verified animation packages exist and use the shared engine's anchors.
- MLP, Gradient Descent, and Optimizer media share one player with no duplicated chapter-navigation implementation.
- Video failure and reduced-motion modes preserve the full teaching message through poster, markers, and bilingual transcript.
</success_criteria>

