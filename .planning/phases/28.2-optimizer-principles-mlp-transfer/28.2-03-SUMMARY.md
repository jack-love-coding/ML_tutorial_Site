---
phase: 28.2
plan: 03
subsystem: optimizer course UI
tags: [vue, typescript, optimizer, bilingual, svg, accessibility]
requires:
  - phase: 28.2-01
    provides: deterministic optimizer engine and published comparison assets
  - phase: 28.2-02
    provides: optimizer animation media and reusable chapter player
provides:
  - six typed bilingual optimizer course chapters with chapter-specific interaction scenes
  - lazy optimizer lesson shell with safe math rendering, copyable code, and final-only completion resources
  - curriculum bridges from gradient descent, MLP backprop, and Math Lab into the optimizer course
affects: [optimizer-comparison, algorithm routing, curriculum navigation, cnn bridge]
tech-stack:
  added: []
  patterns: [typed paged course data, lazy dedicated scene models, semantic table and ordered-log fallbacks]
key-files:
  created:
    - src/modules/optimizer-comparison/data/course.ts
    - src/modules/optimizer-comparison/OptimizerPagedLesson.vue
    - src/modules/optimizer-comparison/labs/sceneModels.ts
  modified:
    - src/views/AlgorithmView.vue
    - src/data/optimizerComparisonModule.ts
    - src/styles/modules/optimizer-comparison.css
key-decisions:
  - "Keep the established optimizer module and route IDs while moving detailed bilingual teaching content into a dedicated typed course source."
  - "Use the Phase 28.2 shared engine, published trajectories, and a frozen Banknote evaluation artifact for interaction displays."
  - "Show references, downloads, and the checkpoint only on curve-diagnosis so intermediate chapters stay focused."
patterns-established:
  - "Optimizer chapters use one fixed question-to-conclusion block sequence, rendered through the safe MarkdownMathContent path."
  - "Each lazy chapter scene owns its specific engine/asset-backed state while always rendering the matching semantic table or ordered log."
requirements-completed: [OPT-01, OPT-02, OPT-03, OPT-04, OPT-05, OPT-06]
coverage:
  - id: D1
    description: "Six bilingual optimizer chapters preserve legacy IDs and the complete teaching sequence."
    requirement: OPT-01
    verification:
      - kind: unit
        ref: tests/optimizer-course.test.ts#optimizer course preserves six legacy IDs with complete bilingual teaching flow
        status: pass
    human_judgment: false
  - id: D2
    description: "Each chapter mounts a lazy, keyboard-operable optimizer interaction with SVG/table fallback."
    requirement: OPT-02
    verification:
      - kind: unit
        ref: tests/optimizer-layout.test.mjs
        status: pass
      - kind: unit
        ref: tests/optimizer-engine.test.ts
        status: pass
    human_judgment: false
  - id: D3
    description: "The course consumes the deterministic comparison data and all supplied optimizer media."
    requirement: OPT-03
    verification:
      - kind: unit
        ref: tests/optimizer-course.test.ts#the course consumes every supplied optimizer animation
        status: pass
      - kind: other
        ref: node scripts/optimizer-comparison/build-assets.mjs --check
        status: pass
    human_judgment: false
  - id: D4
    description: "Controlled-circle and Banknote transfer views present matched/practical context without a universal-winner or test-selection claim."
    requirement: OPT-04
    verification:
      - kind: integration
        ref: tests/optimizer-course.test.ts
        status: pass
      - kind: automated_ui
        ref: playwright deep-link snapshots at 1200px zh-CN and 1440px bilingual
        status: pass
    human_judgment: false
  - id: D5
    description: "Existing gradient descent, MLP, Math Lab, optimizer, and CNN paths remain linked through lazy route bridges."
    requirement: OPT-05
    verification:
      - kind: integration
        ref: tests/curriculumRoutingNavigation.test.ts
        status: pass
      - kind: other
        ref: npm run test:ci
        status: pass
    human_judgment: false
  - id: D6
    description: "Responsive course layout remains readable at desktop, tablet, and mobile widths."
    requirement: OPT-06
    verification:
      - kind: automated_ui
        ref: playwright deep-link snapshots at 1200/1440/768/390px; mobile scrollWidth check
        status: pass
    human_judgment: true
    rationale: "Reduced-motion and deliberate media-failure browser injection could not be executed because the local Playwright CLI rejected the evaluator syntax; component/static coverage passed, but a human/browser follow-up should exercise those two states."
duration: 20min
completed: 2026-08-12
status: complete
---

# Phase 28.2 Plan 03: Optimizer Principles MLP Transfer Summary

Six bilingual, paged optimizer lessons now connect update mechanics to controlled MLP comparison and a frozen Banknote transfer through lazy, accessible, engine-backed interaction scenes.

## Performance

- **Duration:** 20 min
- **Started:** 2026-08-12T14:23:45Z
- **Completed:** 2026-08-12T14:44:18Z
- **Tasks:** 3/3
- **Files modified:** 20

## Accomplishments

- Added six complete typed chapters with exact legacy IDs, safe bilingual Markdown/KaTeX, copyable NumPy examples, final-only sources/downloads/checkpoint, and all four supplied media animations.
- Added a lightweight dedicated optimizer shell plus six lazy chapter labs with finite guards, three-or-fewer main control groups, keyboard use that preserves native form controls, and matching semantic table/ordered-log views.
- Published a hash-bound Banknote artifact: fixed `960/206/206` split, train-only standardization, frozen AdamW configuration, validation loss/accuracy `0.216948/95.15%`, and one final test loss/accuracy `0.210550/97.57%`.
- Preserved existing URLs and progress compatibility while bridging Gradient Descent, MLP Backprop, Optimizer Race, the full optimizer course, and CNN.

## Task Commits

1. **Task 1: Author typed bilingual optimizer chapters** — `a52b2c9` (feat)
2. **Task 2: Build optimizer interaction scenes** — `55546d9` (feat)
3. **Task 3: Integrate routes and validate the course** — `eb8babd` (feat)
4. **Task 3 follow-up: Expose RMSProp course media** — `ab6a115` (fix)

## Files Created/Modified

- `src/modules/optimizer-comparison/data/course.ts` — typed bilingual chapter flow and final-only resource definitions.
- `src/modules/optimizer-comparison/OptimizerPagedLesson.vue` — dedicated paged shell, safe rendering, media player, and completion surfaces.
- `src/modules/optimizer-comparison/labs/sceneModels.ts` — deterministic shared-engine models for five state chapters.
- `src/modules/optimizer-comparison/labs/*Scene.vue` — six lazy, distinct chapter scenes and semantic fallbacks.
- `src/views/AlgorithmView.vue` — lazy optimizer course branch without duplicate generic checkpoint/results surfaces.
- `src/styles/modules/optimizer-comparison.css` — responsive, reduced-motion-safe course layout.
- `tests/optimizer-course.test.ts`, `tests/optimizer-layout.test.mjs`, `tests/curriculumRoutingNavigation.test.ts` — content, layout/accessibility, and bridge coverage.
- `tests/optimizer-scene-models.test.ts` — executable scene, Banknote binding, and both checkpoint-revisit route coverage.

## Decisions Made

- Kept the existing optimizer module and routes as compatibility anchors; the new course shell is targeted rather than a global renderer or homepage rewrite.
- Reused Phase 28.2 engine trajectories and Phase 28.2 media rather than recreating fitting/training calculations.
- Kept the graded checkpoint exclusively on the final diagnosis chapter; all earlier pages retain a focused learning flow.

## Validation

- Passed focused content, layout/accessibility, engine, and routing tests.
- Passed elevated `npm run test:ci`: 1,031 passed, 28 existing skips, 0 failures.
- Passed `npm run build` and `npm run build:pages` (existing large-chunk warnings only).
- Passed `node scripts/optimizer-comparison/build-assets.mjs --check` and `python3 scripts/manim/render_optimizer_comparison.py --check`.
- Browser deep-link inspection passed for all six zh-CN chapters at 1200px and key bilingual pages at 1440px, 768px, and 390px; the 390px `scrollWidth <= innerWidth` check passed. The browser session reported existing Vite development warnings and KaTeX font 403s from the shared external `node_modules` path; production builds emitted the font assets successfully.
- Browser reduced-motion and deliberate media-failure injection were not run: the available Playwright CLI rejected the evaluator syntax. Static/component coverage for those paths passed; this is retained as human-judgment follow-up above rather than claimed as browser evidence.
- `npm run security:audit` reports the pre-existing transitive high-severity `nanoid <3.3.17` advisory. This plan changed no dependencies.

### Review-03 corrective validation

- `node --test tests/optimizer-engine.test.ts tests/optimizer-assets.test.ts tests/optimizer-scene-models.test.ts tests/optimizer-course.test.ts tests/optimizer-layout.test.mjs tests/curriculumRoutingNavigation.test.ts` — 34 passed.
- Elevated `npm run test:ci` — 1,033 passed, 28 skipped; the only two failures are pre-existing planning-state assertions in this worktree's historical state file, corrected by the current tracking update.
- `npm run build`, `npm run build:pages`, `node scripts/optimizer-comparison/build-assets.mjs --check`, and `python3 scripts/manim/render_optimizer_comparison.py --check` passed.
- Browser check rendered the Banknote table at `/learn/optimizer-comparison/curve-diagnosis`, verified its first final-checkpoint revisit route to `/learn/optimizer-comparison/training-loop`, and retained native select focus. Forced data-failure and explicit reduced-motion browser emulation remain a manual follow-up; pure model/fallback coverage is present.

### Review-03 runtime fixes

- Added one lifecycle-safe shared playback controller and localized accessible controls to all six scenes, with bounded deterministic cadence and reduced-motion Step/Reset behavior.
- Recomputed the ledger from one explicit feature vector, target, and parameter vector; extracted the pure Banknote authoring evaluator; and made `--check` deep-compare its full canonical result.
- Generated a hash-bound typed fallback snapshot from the published trajectory/Banknote assets, retained values on failed fetch, and added mounted locale/playback/fallback plus mutation-drift regression coverage.
- Validation after the fixes: focused suite 38 passed; real-kernel asset check, Manim check, `npm run test:ci` 1,039 passed/28 skipped, `npm run build`, and `npm run build:pages` passed. `npm audit` retains the pre-existing transitive `nanoid <3.3.17` advisory. Direct browser fault/motion emulation remains a manual follow-up; mounted component tests exercise both states.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Build correctness] Added the missing RMSProp media configuration.**
- **Found during:** Task 3 validation
- **Issue:** The shell media mapping lacked an RMSProp entry, preventing the TypeScript build from accepting the intended player configuration.
- **Fix:** Added the RMSProp player configuration and verified all four supplied optimizer animations are represented in typed course data.
- **Files modified:** `OptimizerPagedLesson.vue`, `course.ts`, `optimizer-course.test.ts`
- **Verification:** focused course test and `npm run build`
- **Committed in:** `ab6a115`

**2. [Rule 1 - UI correctness] Removed the generic algorithm checkpoint/results surfaces from optimizer course pages.**
- **Found during:** Task 3 browser inspection
- **Issue:** The surrounding algorithm page rendered a generic checkpoint/results panel on intermediate optimizer chapters, violating the final-chapter-only checkpoint requirement.
- **Fix:** Excluded optimizer pages from those generic surfaces; the dedicated final chapter now owns the sole course checkpoint.
- **Files modified:** `src/views/AlgorithmView.vue`
- **Verification:** browser deep-link inspection and routing tests
- **Committed in:** `eb8babd`

**3. [Rule 3 - Blocking test alignment] Updated the legacy optimizer extension test to read the dedicated typed course source.**
- **Found during:** Task 3 validation
- **Issue:** The old test assumed detailed chapter text remained in the legacy module after the planned typed-course extraction.
- **Fix:** Preserved legacy registration while asserting detailed optimizer content in its new typed data file.
- **Files modified:** `tests/deep-learning-extension-modules.test.mjs`
- **Verification:** elevated `npm run test:ci`
- **Committed in:** `eb8babd`

**Total deviations:** 3 auto-fixed (2 Rule 1, 1 Rule 3).

## Known Stubs

None. The empty trajectory fallback is an intentional media/data-load failure guard and renders a readable fallback rather than placeholder UI.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

The optimizer course can be reached from the existing curriculum bridges and hands learners to CNN after the final diagnosis. A follow-up browser session may manually exercise reduced-motion and forced media-failure states if a compatible page evaluator is available.

## Self-Check: PASSED

- Confirmed the summary and all four implementation commits exist on `codex/phase-28b-course`.
