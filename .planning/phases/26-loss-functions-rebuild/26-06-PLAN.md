---
phase: 26-loss-functions-rebuild
plan: "06"
type: execute
wave: 5
depends_on:
  - 26-05
files_modified:
  - src/data/lossFunctionsAssets.ts
  - src/data/lossFunctionsModule.ts
  - tests/loss-functions-content.test.mjs
  - tests/loss-functions-compatibility.test.ts
  - tests/algorithm-progress.test.ts
autonomous: true
requirements:
  - LOSS-01
  - LOSS-02
  - LOSS-03
must_haves:
  truths:
    - "The module remains `loss-functions` at `/learn/loss-functions` with the six existing chapter IDs in their existing order plus final `gradient-verification` (D-01, D-02, D-22)."
    - "Every chapter has complete zh-CN/en teaching with a core question, concept explanation, formula, code/output connection, misconception feedback, and next-step handoff; teaching dominates selective non-blocking exercises (D-03, D-23)."
    - "MSE/MAE, BCE, and gradient chapters bind typed locked output IDs and local asset paths rather than copying Notebook numbers into prose (D-03, D-19, D-20)."
    - "Probability intuition precedes likelihood → negative log → MLE origin, while Softmax remains a concise accessible BCE bridge and no primary Phase 26 gradient/Notebook/checkpoint topic (D-04)."
    - "Existing checkpoint IDs/revisit links, Progress V1/V2 storage identities, safe Markdown/math rendering, and root/non-root deep links remain unchanged (D-22)."
  artifacts:
    - path: "src/data/lossFunctionsAssets.ts"
      provides: "Typed topic/chapter/output/download registry and runtime output validators"
      contains: "LossFunctionsOutputId"
    - path: "src/data/lossFunctionsModule.ts"
      provides: "Seven-chapter bilingual AlgorithmModuleDefinition"
      contains: "gradient-verification"
    - path: "tests/loss-functions-content.test.mjs"
      provides: "Bilingual teaching-loop, output-binding, formula/code, and exercise-density contract"
    - path: "tests/loss-functions-compatibility.test.ts"
      provides: "Slug, route, deep-link, checkpoint, Progress, safe-render, and base-path protection"
    - path: "tests/algorithm-progress.test.ts"
      provides: "Updated exact seven-chapter completion set without storage changes"
  key_links:
    - from: "src/data/lossFunctionsModule.ts"
      to: "src/data/lossFunctionsAssets.ts"
      via: "chapter content references typed code/output/asset IDs"
      pattern: "lossFunctionsAssets|outputId"
    - from: "src/data/lossFunctionsModule.ts"
      to: "src/data/algorithmCheckpoints.ts"
      via: "existing checkpoint registry remains bound to the same module slug"
      pattern: "algorithmCheckpointsBySlug\\['loss-functions'\\]"
    - from: "tests/loss-functions-compatibility.test.ts"
      to: "src/utils/algorithmProgress.ts"
      via: "tests lock V1 and V2 identities without migration"
      pattern: "ml-atlas:algorithm-progress:v1|ml-atlas:learning-progress:v2"
---

<objective>
Rebuild the typed bilingual lesson contract around the locked assets while preserving every public identity and stored-progress boundary.

Purpose: The data and mathematics become useful teaching only when the seven chapters form a coherent beginner-first loop whose prose, code, output IDs, labs, checkpoint, and next route agree.
Output: One typed asset registry, one complete seven-chapter module, and focused content/compatibility tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-loss-functions-rebuild/26-CONTEXT.md
@.planning/phases/26-loss-functions-rebuild/26-RESEARCH.md
@.planning/phases/26-loss-functions-rebuild/26-PATTERNS.md
@.planning/phases/26-loss-functions-rebuild/26-05-SUMMARY.md
@src/types/ml.ts
@src/data/lossFunctionsModule.ts
@src/data/algorithmCheckpoints.ts
@src/curriculum/routes.ts
@src/curriculum/routeManifest.ts
@src/utils/publicPath.ts
@src/utils/algorithmProgress.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock compatibility and create the typed output/download contract</name>
  <files>tests/loss-functions-compatibility.test.ts, tests/algorithm-progress.test.ts, tests/loss-functions-content.test.mjs, src/data/lossFunctionsAssets.ts, src/data/lossFunctionsModule.ts</files>
  <behavior>
    - Exact module slug and root route remain `loss-functions` and `/learn/loss-functions`.
    - Exact chapter sequence is why-loss, regression-losses, classification-losses, likelihood-intuition, negative-log, mle-bridge, gradient-verification; all seven canonical deep links resolve.
    - Checkpoint IDs remain loss-error-rule and loss-nll-scale with their existing revisit targets; Algorithm Progress V1 and Curriculum Progress V2/migration keys are byte-for-byte unchanged.
    - Typed registry lists two topics, four locale Notebooks, two datasets, manifests, attribution, requirements, environment, two summaries, and two plots using root-relative local paths.
    - Runtime validators reject missing/wrong contract versions, output IDs, row shapes, non-standard status values, and non-finite ordinary numeric fields.
    - Every root-relative path resolves correctly under `/` and `/ML_tutorial_Site/`.
  </behavior>
  <action>Write the compatibility/content tests first and confirm they fail only on the absent seventh chapter and asset registry. Then add `lossFunctionsAssets.ts`, append the complete bilingual `gradient-verification` chapter with an explicit embedded lab ID, update only the exact loss chapter set in `algorithm-progress.test.ts`, and return the tests to GREEN. Define local `DownloadableCourseAsset`, topic/chapter/output unions, locale-specific Notebook descriptors, chapter bindings, and pure JSON shape validators; do not extend global `StorySection` for one module and do not store Notebook-derived numbers in the descriptors. Per D-22, prove existing generic routing supports the new deep link without editing router/catalog/navigation/progress/checkpoint production files.</action>
  <verify>
    <automated>node --test --test-name-pattern="identity|chapter|asset|output|base|progress|checkpoint" tests/loss-functions-compatibility.test.ts tests/algorithm-progress.test.ts tests/loss-functions-content.test.mjs</automated>
  </verify>
  <done>The typed registry and seventh chapter exist; all identity, deep-link, checkpoint, progress, output-shape, and public-base contracts pass without a route/store/checkpoint implementation edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild the error, regression-loss, and BCE teaching chapters</name>
  <files>src/data/lossFunctionsModule.ts, tests/loss-functions-content.test.mjs</files>
  <behavior>
    - why-loss distinguishes per-example error/loss from aggregate training objective and traces one real row through y, yhat, residual, contribution, gradient, and mean.
    - regression-losses calculates MSE and MAE per row and in aggregate, contrasts a typical and long-duration real row, and explains MSE versus MAE gradient scale.
    - classification-losses introduces probability BCE, connects p=sigmoid(z), establishes stable logit BCE as canonical, and explains a confident wrong prediction with loss and logit-gradient scale.
    - Each chapter places its vectorized NumPy code/output binding beside the concept and uses the same y, yhat, residual, p, z, and n notation as the locked summaries and TypeScript math.
    - Softmax remains present only as a concise bridge from binary to multiclass cross-entropy.
  </behavior>
  <action>Add chapter-contract assertions first for D-01 through D-04, D-11/D-12, LOSS-01/02, bilingual parity, and typed output binding; confirm RED against the old content, then rewrite these three chapter objects and return the tests to GREEN. Keep formulas, variable explanations, code snippets, misconceptions, and next-step text complete in both locales. Bind page-visible real row tables, distributions, BCE contributions, and stability output by typed IDs from `lossFunctionsAssets.ts`; prose may state formulas and interpret loaded values but must not create a second freehand numeric authority. Include code-copy-ready vectorized snippets that match Notebook code hashes/identifiers under the content test. Keep controls/prompts selective and non-blocking per D-23.</action>
  <verify>
    <automated>node --test --test-name-pattern="why-loss|regression-losses|classification-losses|Softmax|bilingual" tests/loss-functions-content.test.mjs</automated>
  </verify>
  <done>The first three chapters provide detailed, bilingual, numerically bound teaching for fixed/aggregate losses, outliers, confident errors, and stable BCE without expanding Softmax or parameter training.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete the likelihood-origin bridge and seven-chapter teaching loop</name>
  <files>src/data/lossFunctionsModule.ts, tests/loss-functions-content.test.mjs, tests/loss-functions-compatibility.test.ts</files>
  <behavior>
    - likelihood-intuition begins only after practical loss use and explains probabilities as scores of observed data under a model.
    - negative-log explains products-to-sums, why unlikely observations become large positive losses, and how stable BCE is the Bernoulli negative log-likelihood.
    - mle-bridge connects maximizing likelihood to minimizing NLL and hands off by chain rule to later linear/logistic parameter courses without deriving dL/dw or training.
    - gradient-verification compares analytic and central-difference output/logit gradients, displays absolute/scaled-relative error and h sweep, and explains the MAE kink convention.
    - All seven chapters have zh-CN/en core question, explanation, formula, code/output connection, misconception feedback, and next-step copy; exercises remain fewer and shorter than teaching sections.
  </behavior>
  <action>Expand the content tests first to cover the final four chapters, deferred-scope exclusions, formula/variable parity, safe Markdown/math, and chapter-to-output/lab references; observe RED, then complete the chapter copy and return the suite to GREEN. Preserve the existing three probabilistic chapter IDs and embedded lab IDs exactly. Keep the final chapter focused on `dL/dyhat` and `dL/dz`, mean factors, central differences, tolerances, h behavior, and the nondifferentiable MAE row. End with the existing checkpoint and a clear Phase 27/29 chain-rule handoff; do not add full model fitting, parameter gradients, threshold decisions, or equal-depth multiclass treatment.</action>
  <verify>
    <automated>node --test tests/loss-functions-content.test.mjs tests/loss-functions-compatibility.test.ts tests/algorithm-progress.test.ts && npm run build</automated>
  </verify>
  <done>The complete seven-chapter bilingual teaching loop passes content, scope, safe-render, identity, deep-link, checkpoint, progress, and type/build checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated public JSON → typed lesson consumers | Locked files are local but still require runtime contract validation. |
| Typed course markdown → browser renderer | Bilingual formulas/code must remain on the sanitized shared path. |
| New chapter → routing/progress/checkpoint compatibility | An identity change would break saved links and local user history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-22 | Tampering | locked output consumption | medium | mitigate | Validate contract version, output IDs, shapes, statuses, and finite ordinary values before presentation. |
| T-26-23 | Elevation of Privilege | lesson markdown/code | medium | mitigate | Continue through MarkdownMathContent/markdownMath sanitizer; forbid raw executable HTML, handlers, scripts, and iframes in tests. |
| T-26-24 | Tampering | route/checkpoint/progress identity | high | mitigate | Lock slug, route, seven IDs, checkpoint IDs/revisit links, and all V1/V2 storage keys in focused tests; avoid production edits to authorities. |
| T-26-25 | Denial of Service | public asset paths | low | mitigate | Require local root-relative paths, base-safe resolution, and typed load-error fallbacks in the following UI plan. |
| T-26-SC | Tampering | dependencies | low | accept | This plan adds no package or external runtime service. |
</threat_model>

<verification>
- `node --test tests/loss-functions-content.test.mjs tests/loss-functions-compatibility.test.ts tests/algorithm-progress.test.ts`
- `npm run build`
- Confirm no production edit to router, catalog, navigation, checkpoints, or Progress implementations.
- Confirm every learner-facing localized object has both `zh-CN` and `en`.
</verification>

<success_criteria>
- The existing identity and six chapter IDs are preserved and one final chapter is reachable.
- All seven chapters form the locked beginner-first teaching sequence.
- Formula, code identifier, output ID, lab ID, and variable naming agree.
- Softmax remains support; full parameter gradients/training and threshold decisions remain deferred.
- Content stays detailed and exercises stay selective/non-blocking.
</success_criteria>

<output>
Create `.planning/phases/26-loss-functions-rebuild/26-06-SUMMARY.md` when done.
</output>
