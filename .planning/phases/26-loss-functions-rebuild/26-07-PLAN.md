---
phase: 26-loss-functions-rebuild
plan: "07"
type: execute
wave: 6
depends_on:
  - 26-06
files_modified:
  - src/components/LossGradientVerificationLab.vue
  - src/components/LossFunctionsDownloads.vue
  - src/components/LossFunctionsLessonLab.vue
  - src/components/LossFunctionsResults.vue
  - src/components/WhyLossLab.vue
  - src/components/RegressionLossLab.vue
  - src/components/ClassificationLossLab.vue
  - src/views/AlgorithmView.vue
  - src/styles/modules/loss-functions.css
  - src/styles/modules/loss-functions-visuals.css
  - scripts/qa/lossFunctionsBrowserMatrix.js
  - tests/loss-functions-labs.test.mjs
autonomous: true
requirements:
  - LOSS-01
  - LOSS-02
  - LOSS-03
must_haves:
  truths:
    - "Learners can follow one real row through target/prediction, per-example loss, output/logit gradient, and mean objective, then compare it with the full-data locked summary (D-03, D-08, D-09, D-20)."
    - "The regression lab makes outlier influence visible for both MSE and MAE, and the classification lab makes confident wrong BCE/gradient influence visible without exposing arbitrary extreme inputs (D-10, D-12, D-16)."
    - "The gradient lab presents MSE, smooth MAE, MAE kink, and BCE analytic/central-difference values, errors, tolerances, and h sweep using the pure TypeScript engine (D-13, D-14, D-24)."
    - "Existing six labs remain explicitly routed, the seventh lab has its own branch, and an unknown section cannot fall through to MleBridgeLab (D-01, D-02, D-21)."
    - "Selected locked tables/plots appear beside relevant chapters and one consolidated bilingual download area uses withPublicBase; no generated image or Manim is added because the labs/plots cover the identified teaching needs (D-17 through D-21)."
    - "The canonical route, seven deep links, checkpoint submission, Progress V1/V2 behavior, safe rendering, code copy, Pages base paths, reduced motion, desktop, and 390px layout all remain usable (D-22, D-23)."
  artifacts:
    - path: "src/components/LossGradientVerificationLab.vue"
      provides: "Bounded analytic-versus-central-difference interaction with explicit kink status"
    - path: "src/components/LossFunctionsLessonLab.vue"
      provides: "Explicit seven-chapter lab registry and typed locked-output loading"
    - path: "src/components/LossFunctionsResults.vue"
      provides: "Chapter-keyed locked row/table/plot/code output presentation"
    - path: "src/components/LossFunctionsDownloads.vue"
      provides: "Consolidated bilingual base-safe datasets, Notebooks, manifests, summaries, and plots"
    - path: "scripts/qa/lossFunctionsBrowserMatrix.js"
      provides: "Bilingual desktop/390px route, interaction, offline-asset, console, and overflow matrix"
    - path: "tests/loss-functions-labs.test.mjs"
      provides: "Lab registry, pure-math wiring, asset fallback, bounds, accessibility, and page-composition tests"
  key_links:
    - from: "src/components/LossFunctionsLessonLab.vue"
      to: "src/simulations/lossFunctionsMath.ts"
      via: "labs receive validated fixture data and call pure math results instead of reimplementing formulas"
      pattern: "lossFunctionsMath"
    - from: "src/components/LossFunctionsResults.vue"
      to: "src/data/lossFunctionsAssets.ts"
      via: "typed output IDs, validators, and local paths drive chapter results"
      pattern: "lossFunctionsAssets"
    - from: "src/components/LossFunctionsDownloads.vue"
      to: "src/utils/publicPath.ts"
      via: "every learner download resolves through withPublicBase"
      pattern: "withPublicBase"
    - from: "src/views/AlgorithmView.vue"
      to: "src/components/LossFunctionsLessonLab.vue"
      via: "existing lazy loss-functions page branch mounts the seven chapter labs without route changes"
      pattern: "LossFunctionsLessonLab"
---

<objective>
Integrate the rebuilt content, real outputs, pure math, existing interactions, gradient verification, and downloads into the current loss-functions page and close the phase with complete automated release evidence.

Purpose: The phase succeeds only when learners can read and manipulate the same quantities that the clean-kernel assets and deterministic tests certify, without route, checkpoint, progress, security, Pages, or mobile regressions.
Output: Updated primary labs, one new verification lab, chapter results/downloads, scoped styles, and a full bilingual release/browser matrix.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-loss-functions-rebuild/26-CONTEXT.md
@.planning/phases/26-loss-functions-rebuild/26-RESEARCH.md
@.planning/phases/26-loss-functions-rebuild/26-PATTERNS.md
@.planning/phases/26-loss-functions-rebuild/26-06-SUMMARY.md
@src/components/LossFunctionsLessonLab.vue
@src/components/LossFunctionsResults.vue
@src/modules/math-lab/components/CodeLab.vue
@src/modules/math-lab/labs/FiniteDifferenceLab.vue
@src/views/AlgorithmView.vue
@src/styles/modules/loss-functions.css
@src/styles/modules/loss-functions-visuals.css
@scripts/qa/mathToCodePilotBrowserMatrix.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire typed locked results, explicit lab routing, gradient checks, and downloads</name>
  <files>tests/loss-functions-labs.test.mjs, src/components/LossGradientVerificationLab.vue, src/components/LossFunctionsDownloads.vue, src/components/LossFunctionsLessonLab.vue, src/components/LossFunctionsResults.vue</files>
  <behavior>
    - The registry has explicit branches for all seven known chapter IDs, preserves the six current components, mounts LossGradientVerificationLab only for gradient-verification, and renders a clear unsupported state or nothing for unknown IDs.
    - Local summaries load via withPublicBase and the typed validators; loading/error states leave formulas and worked teaching visible rather than blanking the chapter.
    - Results select real rows, aggregate values, distribution plot, confident-error table, fixed stability table, and h-sweep output by typed chapter/output IDs.
    - Gradient controls select loss kind, representative element, and h from a bounded readable set; reset/current values/keyboard labels are present, and MAE zero residual displays nondifferentiable rather than pass.
    - Consolidated downloads include all four Notebooks, both datasets/manifests, attribution, requirements/environment, summaries, plots, and output manifest with localized labels and base-safe local links.
  </behavior>
  <action>Add the component/source contracts first and confirm RED, then implement the new components and refactor the existing registry/results to return GREEN. Use `src/data/lossFunctionsAssets.ts` for paths/types/validators and `src/simulations/lossFunctionsMath.ts` for all calculations. The registry may fetch only the small locked summary needed by the active chapter and must abort stale requests on section change/unmount; it must not load the full 590-feature SECOM table in the browser. Reuse the existing CodeLab/code-copy presentation for interleaved NumPy code/output. Render fixed statuses using text and shape/labels in addition to color. Keep likelihood/NLL/MLE labs intact and replace the current catch-all with explicit branches. Do not add image/Manim media: per D-21, the reproducible plots and labs already cover the concrete and continuous processes.</action>
  <verify>
    <automated>node --test --test-name-pattern="registry|gradient|result|download|fallback|copy" tests/loss-functions-labs.test.mjs tests/loss-functions-content.test.mjs tests/loss-functions-notebook-assets.test.ts</automated>
  </verify>
  <done>All seven chapters route explicitly; locked outputs, code copy, gradient/kink states, local fallbacks, and consolidated downloads are wired through typed/base-safe contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild the primary real-row loss interactions and responsive visuals</name>
  <files>tests/loss-functions-labs.test.mjs, src/components/WhyLossLab.vue, src/components/RegressionLossLab.vue, src/components/ClassificationLossLab.vue, src/styles/modules/loss-functions.css, src/styles/modules/loss-functions-visuals.css</files>
  <behavior>
    - WhyLossLab steps through a selected real row's target/prediction, residual or logit, per-example contribution, gradient contribution, and mean objective.
    - RegressionLossLab offers representative real rows including typical/zero-residual/long-duration cases and compares MSE/MAE scale plus gradients without changing multiple variables invisibly.
    - ClassificationLossLab distinguishes label, logit, probability, per-row BCE, and gradient; a confident wrong real row is named only when the locked output supplies one, otherwise a clearly labeled teaching logit is separate.
    - Learner controls remain bounded, display current value, expose reset, reject NaN/Infinity through the pure engine, and never expose ±1000 as free input.
    - Tables/charts remain readable at 390px, do not encode status by color alone, and preserve all teaching information under reduced motion.
  </behavior>
  <action>Write the lab behavior/accessibility/source tests first and confirm RED, then update only the three labs that own fixed loss/outlier/confident-error calculations and return the tests to GREEN. Consume validated representative rows from the locked summaries passed by the registry, use the pure TypeScript engine for recalculation, and keep the existing likelihood, negative-log, and MLE interactions available as conceptual labs. Make the real-versus-synthetic boundary explicit in both locales. Add scoped module CSS for scroll/stack behavior, text-plus-shape statuses, visible focus, and reduced-motion fallback; do not introduce global CSS or a UI framework. Keep exercises subordinate to the detailed teaching surface per D-23.</action>
  <verify>
    <automated>node --test tests/loss-functions-labs.test.mjs tests/loss-functions-math.test.ts && npm run build</automated>
  </verify>
  <done>The three primary labs use honest real rows and pure math, expose the required outlier/confident-error mechanisms, remain bounded/accessibly labeled, and pass focused tests plus the production build.</done>
</task>

<task type="auto">
  <name>Task 3: Complete page composition and run the full release/browser matrix</name>
  <files>src/views/AlgorithmView.vue, scripts/qa/lossFunctionsBrowserMatrix.js, tests/loss-functions-labs.test.mjs, src/styles/modules/loss-functions.css, src/styles/modules/loss-functions-visuals.css</files>
  <action>Keep the existing lazy `isLossFunctionsPage` branch and StoryScroller/checkpoint composition. Mount chapter-keyed results adjacent to the concepts they support and render `LossFunctionsDownloads` once at the end; do not move data/generator/math logic into AlgorithmView or edit the router, module catalog, checkpoint registry, or Progress implementations. Add source tests for placement and one committed Playwright-CLI matrix modeled on the existing Math-to-Code probe. The matrix must cover zh-CN/en at desktop and 390x844, root route plus all seven deep links, chapter order, code-copy button, bounded lab/reset interactions, MSE/MAE outlier comparison, confident-error BCE, MAE kink, h change, fixed ±1000 table, checkpoint presence, download links, reduced motion, no overflow/overlap/dead fragments/console errors, and no course-asset request outside the local origin. Run `npm run build` first, serve that root-base dist, and complete the root URL matrix before any Pages build can replace dist. Only after closing the root preview run `npm run build:pages`, then use focused source/asset tests and static dist inspection to verify `/ML_tutorial_Site/` base links. Finish with offline generator check, focused suites, full tests, security audit, and git diff/scope review; known Vite large-chunk warnings may remain but new errors may not.</action>
  <verify>
    <automated>
set -euo pipefail &&
python3 scripts/loss-functions/build-phase-26-assets.py --check --offline &&
node --test tests/loss-functions-*.test.* tests/algorithm-progress.test.ts &&
npm test &&
npm run build &&
export CODEX_HOME="${CODEX_HOME:-$HOME/.codex}" &&
PWCLI="$CODEX_HOME/skills/playwright/scripts/playwright_cli.sh" &&
export PWCLI &&
export PLAYWRIGHT_CLI_SESSION=phase-26-loss-functions &&
test -x "$PWCLI" &&
(npm run preview -- --host 127.0.0.1 --port 4173 >/tmp/phase-26-root-preview.log 2>&1 & PREVIEW_PID=$!; trap 'kill "$PREVIEW_PID" 2>/dev/null || true' EXIT; "$PWCLI" open 'http://127.0.0.1:4173/learn/loss-functions' --headed && "$PWCLI" run-code --filename scripts/qa/lossFunctionsBrowserMatrix.js) &&
npm run build:pages &&
node --test --test-name-pattern="base|Pages|asset" tests/loss-functions-notebook-assets.test.ts tests/loss-functions-compatibility.test.ts &&
npm run security:audit &&
git diff --check
    </automated>
  </verify>
  <done>Focused/full/offline/build/Pages/security gates and the bilingual desktop/390px browser matrix pass; all assets remain local/base-safe and the route/checkpoint/progress identities remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public JSON/assets → Vue page | Local static files can still be missing, stale, malformed, or served under a non-root base. |
| Learner controls → pure engine | Values must remain bounded and validated even when events are manipulated. |
| Course markdown/code → DOM/clipboard | Rendering and copy behavior must not bypass the existing sanitizer or introduce executable content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-26 | Tampering | locked result loading | medium | mitigate | Validate JSON contracts and hashes, abort stale requests, and show a readable local fallback on rejection. |
| T-26-27 | Denial of Service | interactive controls | medium | mitigate | Use bounded ranges/enums, finite guards, reset, and no browser load of full SECOM features or arbitrary extreme inputs. |
| T-26-28 | Elevation of Privilege | markdown/code/output rendering | medium | mitigate | Reuse sanitized MarkdownMathContent and text CodeLab; add no raw HTML, script, event handler, or iframe. |
| T-26-29 | Denial of Service | GitHub Pages asset paths | medium | mitigate | Resolve every path with withPublicBase; test root and non-root bases plus build:pages and local-only browser requests. |
| T-26-30 | Information Disclosure | browser data surface | low | mitigate | Load only privacy-minimized summary rows; never fetch raw LaDe cache or full SECOM feature matrix in the page. |
| T-26-SC | Tampering | frontend supply chain | low | accept | No new dependency is installed; the standard security audit remains a blocking phase gate. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID/range | Feature or constraint | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 26 | Fixed errors → verified MSE/MAE/stable BCE/gradients/objective intuition | 26-02, 26-04, 26-05, 26-06, 26-07 | COVERED |
| REQ | LOSS-01 | Fixed per-example and aggregate MSE/MAE/stable BCE | 26-02, 26-04, 26-05, 26-06, 26-07 | COVERED |
| REQ | LOSS-02 | Outlier and confident-wrong loss/gradient explanation | 26-02, 26-04, 26-05, 26-06, 26-07 | COVERED |
| REQ | LOSS-03 | Vectorized NumPy plus finite-difference verification | 26-02, 26-03, 26-04, 26-05, 26-06, 26-07 | COVERED |
| CONTEXT | D-01–D-04 | Seven-chapter spine, interleaved code/output, concise Softmax | 26-06, 26-07 | COVERED |
| CONTEXT | D-05–D-10 | New real LaDe/SECOM cases, rows/full outputs, synthetic extremes | 26-01, 26-04, 26-05, 26-07 | COVERED |
| CONTEXT | D-11–D-16 | Stable BCE, output gradients, h sweep, MAE kink, bounded probes | 26-02, 26-04, 26-05, 26-06, 26-07 | COVERED |
| CONTEXT | D-17–D-21 | Four Notebooks, locale parity, chapter results/downloads, need-driven visuals | 26-03, 26-04, 26-05, 26-06, 26-07 | COVERED |
| CONTEXT | D-22–D-24 | Identity/progress/render/base/mobile compatibility, selective exercises, pure math | 26-01, 26-02, 26-05, 26-06, 26-07 | COVERED |
| RESEARCH | Data contract | License gate, privacy removal, exact hashes, SECOM 590/591 | 26-01, 26-04, 26-05 | COVERED |
| RESEARCH | Numerical contract | Strict JSON, logaddexp/softplus, mean factors, fixed probes, tolerances | 26-02, 26-04, 26-05 | COVERED |
| RESEARCH | Reproducibility | Shared code, four kernels, parity, atomic publish, offline check | 26-03, 26-04, 26-05 | COVERED |
| RESEARCH | Integration/security | Typed outputs, existing labs, sanitized rendering, base paths, compatibility | 26-06, 26-07 | COVERED |

Deferred full parameter gradients/training, equal-depth multiclass Softmax, arbitrary numeric input, browser Python, backend assessment, and checkpoint persistence are intentionally excluded.
</source_audit>

<verification>
- `python3 scripts/loss-functions/build-phase-26-assets.py --check --offline`
- `node --test tests/loss-functions-*.test.* tests/algorithm-progress.test.ts`
- `npm test`
- `npm run build`
- `npm run build:pages`
- `npm run security:audit`
- Playwright-CLI matrix: zh-CN/en × desktop/390px × root/seven deep links, with local-only assets, reduced motion, zero console errors, and zero overflow.
- `git diff --check` plus scope audit confirming no route/checkpoint/progress authority was changed.
</verification>

<success_criteria>
- Roadmap success criterion 1: lesson, NumPy, locked output, and primary lab values agree for fixed and aggregate losses.
- Roadmap success criterion 2: learners can explain outlier MSE/MAE and confident-wrong BCE/gradient behavior from visible real examples.
- Roadmap success criterion 3: vectorized stable BCE remains finite at extremes and deterministic finite differences pass at smooth points with honest MAE kink handling.
- Roadmap success criterion 4: route, seven deep links, checkpoint, Progress V1/V2, focused tests, local assets, and Pages paths remain valid.
- Full tests, standard build, Pages build, security audit, offline asset check, and bilingual desktop/390px browser acceptance pass.
</success_criteria>

<output>
Create `.planning/phases/26-loss-functions-rebuild/26-07-SUMMARY.md` when done.
</output>
