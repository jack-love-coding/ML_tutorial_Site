---
phase: 26-loss-functions-rebuild
plan: "04"
type: execute
wave: 3
depends_on:
  - 26-03
files_modified:
  - scripts/loss-functions/build-phase-26-assets.py
  - tests/loss-functions-dataset-contract.test.ts
  - tests/loss-functions-notebook-assets.test.ts
  - .cache/loss-functions/phase-26-staging/**
autonomous: true
requirements:
  - LOSS-01
  - LOSS-02
  - LOSS-03
must_haves:
  truths:
    - "The ignored staging package contains complete LaDe-D Jilin and SECOM transformations with provenance, privacy, schema, checksum, target/label, and representative-row contracts (D-05 through D-09)."
    - "The ignored staging package contains all four separately executed Notebooks, two locked summaries, two plots, attribution, environment records, and one complete candidate manifest (D-17 through D-20)."
    - "Regression candidates expose real per-row/aggregate MSE/MAE and output gradients; manufacturing candidates expose real OOF stable BCE/logit gradients, the fixed extreme table, and h sweeps (D-10 through D-15)."
    - "All candidates pass internal dataset, locale, code, output, plot, strict-JSON, and TypeScript-parity checks while nothing under public/datasets/loss-functions or public/notebooks/loss-functions/generated-output groups is written."
  artifacts:
    - path: ".cache/loss-functions/phase-26-staging/datasets/loss-functions/"
      provides: "Ignored complete privacy-minimized LaDe/SECOM dataset candidate group"
    - path: ".cache/loss-functions/phase-26-staging/notebooks/loss-functions/"
      provides: "Ignored four-Notebook, JSON, plot, environment, and manifest candidate group"
    - path: "scripts/loss-functions/build-phase-26-assets.py"
      provides: "Complete transformation/execution/candidate validation implementation"
      contains: "--verify-candidates"
    - path: "tests/loss-functions-notebook-assets.test.ts"
      provides: "Candidate numerical, locale, kernel, output, and parity tests"
  key_links:
    - from: "scripts/loss-functions/build-phase-26-assets.py"
      to: ".cache/loss-functions/phase-26-staging/"
      via: "complete candidate preparation writes only to the ignored staging root"
      pattern: "staging"
    - from: "tests/loss-functions-notebook-assets.test.ts"
      to: "src/simulations/lossFunctionsMath.ts"
      via: "candidate summaries are independently recomputed with the TypeScript authority"
      pattern: "lossFunctionsMath"
---

<objective>
Build and fully validate the complete real-data/Notebook/output package in ignored staging without publishing any subset.

Purpose: Numerical, privacy, locale, and clean-kernel failures must be found before learner-facing public bytes exist.
Output: One complete, validated staging package containing both datasets, all four executed Notebooks, locked JSON/plots, attribution, environment, and manifest candidates.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-loss-functions-rebuild/26-CONTEXT.md
@.planning/phases/26-loss-functions-rebuild/26-RESEARCH.md
@.planning/phases/26-loss-functions-rebuild/26-PATTERNS.md
@.planning/phases/26-loss-functions-rebuild/26-03-SUMMARY.md
@src/simulations/lossFunctionsMath.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transform the full LaDe and SECOM sources into validated candidates</name>
  <files>scripts/loss-functions/build-phase-26-assets.py, tests/loss-functions-dataset-contract.test.ts, tests/loss-functions-notebook-assets.test.ts, .cache/loss-functions/phase-26-staging/datasets/loss-functions/**</files>
  <behavior>
    - LaDe candidate uses all 31,415 rows, derives finite delivery duration with rollover handling, selects stable course IDs and representative zero/typical/long-duration rows, and excludes courier/coordinate/precise stop fields.
    - LaDe uses the complete-file median 175-minute fixed prediction only as the reference arithmetic baseline and publishes per-row/aggregate MSE/MAE and output-gradient inputs.
    - SECOM candidate uses all 1,567 rows, preserves missing values, maps -1 to 0 and 1 to 1, records declared 591 versus observed 590 fields, and never pads/truncates/imputes the canonical CSV.
    - SECOM OOF predictions use the exact deterministic five-fold train-fold-only preprocessing/logistic baseline from research and publish scores as auxiliary inputs, not parameter training.
    - Both candidate manifests include source/generator/transformation/data hashes, licenses, schemas, units, row counts, target/label meaning, representative IDs, and field-removal/missing-value policies.
  </behavior>
  <action>Add full-transformation and failure-injection assertions first, confirm RED, then implement both complete transformations into the ignored staging dataset group and return them to GREEN. Use only the Plan 26-01 verified source cache and approved LaDe decision. Select SECOM confident-error rows from deterministic clean execution and contribution ranking; if none meets the locked confidence threshold, emit a separately labeled teaching logit fallback and never claim it is a real row. Fail on hash/license/row/schema/label/privacy drift. Do not write or replace any public file.</action>
  <verify>
    <automated>python3 scripts/loss-functions/build-phase-26-assets.py --prepare-dataset-candidates --source-cache .cache/loss-functions/phase-26-sources --staging-root .cache/loss-functions/phase-26-staging && node --test --test-name-pattern="LaDe|SECOM|privacy|590|591|representative|OOF" tests/loss-functions-dataset-contract.test.ts tests/loss-functions-notebook-assets.test.ts</automated>
  </verify>
  <done>The ignored staging dataset group contains both complete verified transformations and manifests; public generated-asset groups remain untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute all four candidate Notebooks and validate locked outputs</name>
  <files>scripts/loss-functions/build-phase-26-assets.py, tests/loss-functions-notebook-assets.test.ts, .cache/loss-functions/phase-26-staging/notebooks/loss-functions/**</files>
  <behavior>
    - All four Notebooks execute independently in fresh kernels with execution count starting at 1, allow_errors false, no error output, and no runtime network code.
    - Delivery locales share exact code/cell IDs/output hashes and publish real rows, MSE/MAE objectives/gradients, distributions, and high-contribution cases.
    - Manufacturing locales share exact code/cell IDs/output hashes and publish OOF BCE contributions/gradients, fixed naive/clipped/stable probes, MSE/MAE/BCE h sweeps, and honest MAE kink status.
    - Strict JSON encodes non-finite demonstrations with null plus status, never bare NaN/Infinity; plots have deterministic dimensions/metadata and non-color labels.
    - The candidate manifest covers every dataset, Notebook, summary, plot, environment, requirements, attribution, generator, code, output, and standalone-rerun expectation in one inventory.
  </behavior>
  <action>Add clean-kernel, locale-parity, output, fixed-probe, finite-difference, strict-JSON, plot, and complete-manifest assertions first; confirm RED, then execute all four candidate Notebooks and return them to GREEN. Use float64, `np.logaddexp`, per-element and mean gradients, explicit tolerances, and h=10^-1 through 10^-9. Candidate execution must fail as a unit if any locale, output, plot, or hash fails. Record no machine-specific temporary paths or kernelspec names. Keep every output in ignored staging; do not publish either language/topic early.</action>
  <verify>
    <automated>python3 scripts/loss-functions/build-phase-26-assets.py --prepare-candidates --source-cache .cache/loss-functions/phase-26-sources --staging-root .cache/loss-functions/phase-26-staging && python3 scripts/loss-functions/build-phase-26-assets.py --verify-candidates --staging-root .cache/loss-functions/phase-26-staging --offline && node --test --test-name-pattern="clean kernel|locale|BCE|gradient|finite difference|manifest|strict JSON|plot" tests/loss-functions-notebook-assets.test.ts tests/loss-functions-math.test.ts</automated>
  </verify>
  <done>One complete ignored candidate package passes data, four-kernel, numerical, locale, output, plot, manifest, and cross-runtime validation with zero public publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Verified raw cache → normalized candidates | Transformations must enforce privacy/schema contracts without silent repair. |
| Candidate data → executable Notebooks | Full-data predictions and teaching outputs must remain deterministic and local. |
| Candidate staging → public tree | This plan must never cross the publication boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-12 | Tampering | data transformations | high | mitigate | Recheck pinned source hashes, exact rows/schema/labels, stable IDs, generator hash, and candidate hashes. |
| T-26-13 | Information Disclosure | LaDe candidate | medium | mitigate | Enforce field allowlist and scan candidate CSV/manifest for courier and precise location fields. |
| T-26-14 | Tampering | locale/output parity | medium | mitigate | Shared code plus four independent executions and exact normalized code/output hashes. |
| T-26-15 | Repudiation | real versus teaching SECOM logit | medium | mitigate | Derive real rows by locked ranking and type/provenance-label any teaching fallback separately. |
| T-26-16 | Denial of Service | candidate generation | medium | mitigate | Bounded four-job execution, strict errors, complete-candidate cleanup, and no public writes. |
| T-26-SC | Tampering | isolated Python packages | high | mitigate | Use only Plan 26-03 verified exact pins and wheel hashes with pip no-index. |
</threat_model>

<verification>
- Full dataset-candidate preparation and focused source/privacy/schema tests.
- Four candidate Notebook executions and candidate-manifest verification.
- Cross-runtime loss/gradient parity.
- Confirm `git status --short` contains no staging files and public generated groups are unchanged.
</verification>

<success_criteria>
- Both complete real datasets and all four independently executed Notebooks exist in one ignored candidate package.
- LOSS-01/02/03 outputs are numerically and bilingually validated.
- LaDe privacy and SECOM 590/591 contracts remain fail closed.
- No public dataset, Notebook, JSON, or plot is created or modified.
</success_criteria>

<output>
Create `.planning/phases/26-loss-functions-rebuild/26-04-SUMMARY.md` when done.
</output>
