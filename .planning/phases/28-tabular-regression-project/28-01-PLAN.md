---
phase: 28-tabular-regression-project
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/tabular-regression/
  - public/datasets/tabular-regression/
  - public/notebooks/tabular-regression/
  - public/tabular-regression/
  - docs/curriculum-v3/tabular-regression/
  - tests/tabular-regression-assets.test.mjs
  - .planning/phases/28-tabular-regression-project/
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [TPRJ-01, TPRJ-02, TPRJ-03, TPRJ-04, TPRJ-05]
must_haves:
  truths:
    - "A frozen local California Housing package records provenance, hashes, schema, target meaning, and exact 60/20/20 membership without runtime network access."
    - "Executed bilingual Notebooks fit preprocessing on train only, select Ridge only on validation, and evaluate the frozen final model on test once."
    - "Every published metric, residual, failure case, and figure can be recomputed from the released CSV/JSON assets."
---

<objective>
Build and verify the deterministic California Housing data, Notebook, metric, residual, failure-case, and figure package that will be the sole numerical authority for the Phase 28 teaching page.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Acquire, freeze, document, and split California Housing</name>
  <read_first>
    - src/data/housingPriceProjectModule.ts
    - src/data/californiaHousingSubset.ts
    - scripts/linear-regression/build-phase-27a-assets.py
  </read_first>
  <action>Create an isolated acquisition/build pipeline that verifies the official scikit-learn archive, publishes a local CSV with stable row IDs, records provenance and license scope, and assigns exact deterministic train/validation/test membership.</action>
  <verify>Run the asset builder in build and check modes, then verify hashes, schema, counts, finite values, and disjoint membership.</verify>
  <acceptance_criteria>
    - The local dataset contains 20,640 rows, eight features, one target, and a stable row ID.
    - Split counts are exactly 12,384 / 4,128 / 4,128 and are reproducible from seed 42.
    - Runtime website and Notebook execution require no network access.
    - Source notes do not claim that the loader's BSD license is the dataset license.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Execute the leakage-safe baseline and controlled Ridge comparison</name>
  <read_first>
    - .planning/phases/28-tabular-regression-project/28-CONTEXT.md
    - public/notebooks/linear-regression/environment.json
    - public/notebooks/linear-regression/requirements.txt
  </read_first>
  <action>Generate and execute matching Chinese and English Notebooks that use train-only exploration and scaling, compare the fixed LinearRegression baseline with the declared Ridge alpha path on validation RMSE, refit the selected model on train+validation, and evaluate test once.</action>
  <verify>Recompute all metrics and parameters from the released dataset and predictions, and verify selection and refit boundaries.</verify>
  <acceptance_criteria>
    - The same eight features and StandardScaler boundary are used for baseline and Ridge.
    - Ridge alpha candidates are exactly 0.01, 0.1, 1, 10, and 100 with deterministic SVD fitting.
    - The simpler baseline is retained unless Ridge improves validation RMSE by at least 1%.
    - Test metrics and named failures are produced only after selection is frozen.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Publish figures, interaction payloads, manifests, and numerical tests</name>
  <read_first>
    - scripts/linear-regression/build-phase-27a-assets.py
    - tests/linear-regression-notebook-assets.test.ts
    - src/utils/publicPath.ts
  </read_first>
  <action>Publish local Matplotlib figures, chapter-scoped interaction JSON, summaries, coefficient paths, predictions, residuals, named failures, environment pins, and atomic manifests with source cell IDs and hashes.</action>
  <verify>Run focused asset tests, asset drift check, npm test, both production builds, and security audit.</verify>
  <acceptance_criteria>
    - Every figure and interaction payload has a source cell ID and valid SHA-256 entry.
    - All JSON numbers are finite and all displayed metrics are reproducible from released tables.
    - The asset builder supports atomic publication and check-only drift detection.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `python3 scripts/tabular-regression/build-assets.py --check`
- `node --test tests/tabular-regression-assets.test.mjs`
- `npm test`
- `npm run build`
- `npm run build:pages`
- `npm run security:audit`
</verification>

<success_criteria>
- All Phase 28 numerical claims have one local, deterministic, clean-kernel authority.
- Plan 01 can merge independently without changing the current housing page behavior.
- User-owned local files remain excluded.
</success_criteria>
