---
phase: 25-numerical-methods-batch-4-logistic-regression-optimization-a
plan: "10"
type: execute
wave: 5
depends_on: [25-06, 25-07, 25-08]
files_modified:
  - scripts/manim/numerical_methods_batch_4/common.py
  - scripts/manim/numerical_methods_batch_4/palette.py
  - scripts/manim/render_numerical_methods_batch_4.py
  - tests/numerical-methods-batch-4-manim.test.ts
autonomous: true
requirements: [P25-SC5]
must_haves:
  truths:
    - "One renderer validates all three D-27 six-role packages and D-28 output dependencies before rendering."
    - "Publication is transactional, preserves Batches 1–3 media, and `--check` is offline/write-free."
    - "Source tests require exact IDs, depth 3, bilingual labels, hashes, and non-motion fallbacks per D-29."
  artifacts:
    - path: "scripts/manim/render_numerical_methods_batch_4.py"
      provides: "Three-scene transactional renderer and integrity checker"
    - path: "tests/numerical-methods-batch-4-manim.test.ts"
      provides: "Source, renderer, metadata, media, and route-binding contract"
  key_links:
    - from: "scripts/manim/render_numerical_methods_batch_4.py"
      to: "scripts/manim/numerical_methods_batch_4/*_tree.json"
      via: "exact three-scene six-role manifest validation"
      pattern: "banknote-training-diagnostics"
---

<objective>Implement and source-verify the shared Batch 4 rendering pipeline without rendering binary media yet.

Purpose: Separate renderer logic and contract tests from the long three-video publication gate.
Output: Shared helpers, transactional renderer/checker, and green source-contract tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-PATTERNS.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-06-SUMMARY.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-07-SUMMARY.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-08-SUMMARY.md
@scripts/manim/render_numerical_methods_batch_3.py
</context>

## Artifacts this phase produces

- Files: `common.py`, `palette.py`, renderer, and media-contract test.
- Renderer CLI: ordinary full render and offline/write-free `--check`.
- Renderer manifest: exactly three scene IDs/classes/output dependencies and all six role paths.

<tasks>

<task type="auto">
  <name>Task 1: Add shared scene helpers and palette</name>
  <read_first>
    - `scripts/manim/numerical_methods_batch_3/common.py` — Chinese font, equation fitting, title/card/disclaimer helpers.
    - `scripts/manim/numerical_methods_batch_3/palette.py` — visual tokens and fallback conventions.
    - `scripts/manim/numerical_methods_batch_4/banknote_feature_scaling.py` — first concrete consumer.
    - `scripts/manim/numerical_methods_batch_4/banknote_fixed_vs_armijo.py` — second concrete consumer.
    - `scripts/manim/numerical_methods_batch_4/banknote_training_diagnostics.py` — third concrete consumer.
  </read_first>
  <files>scripts/manim/numerical_methods_batch_4/common.py, scripts/manim/numerical_methods_batch_4/palette.py</files>
  <action>Adapt the established Batch 3 shared helpers and palette for all three D-27 sources. Keep Chinese font fallback, Unicode equation fitting, title/card/disclaimer helpers, high contrast, text/shape status cues, and centralized colors. Do not introduce per-scene duplicated helpers or new dependencies.</action>
  <verify><automated>python3 -m py_compile scripts/manim/numerical_methods_batch_4/common.py scripts/manim/numerical_methods_batch_4/palette.py scripts/manim/numerical_methods_batch_4/banknote_feature_scaling.py scripts/manim/numerical_methods_batch_4/banknote_fixed_vs_armijo.py scripts/manim/numerical_methods_batch_4/banknote_training_diagnostics.py</automated></verify>
  <acceptance_criteria>
    - All five Python files compile, and each of the three scenes imports the shared Batch 4 helper/palette modules.
    - Chinese font fallback, equation fitting, high-contrast tokens, and text/shape status cues are centralized rather than duplicated per scene.
    - No new dependency or per-scene color/helper fork is introduced.
  </acceptance_criteria>
  <done>All three sources compile against one helper/palette contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the renderer and complete source-contract tests</name>
  <read_first>
    - `scripts/manim/render_numerical_methods_batch_3.py` — manifest, role validation, hash collection, render/probe, preserve-existing-media transaction, and `--check` patterns.
    - `tests/numerical-methods-batch-4-manim.test.ts` — Wave 0 scaffold to complete.
    - `public/notebooks/numerical-methods/batch-4-outputs/manifest.json` — upstream contract/hash authority.
  </read_first>
  <files>scripts/manim/render_numerical_methods_batch_4.py, tests/numerical-methods-batch-4-manim.test.ts</files>
  <behavior>
    - Exactly three canonical scene records require all six roles, depth 3, bilingual labels, and locked output IDs.
    - Renderer targets 1920×1080, 30fps, silent H.264 videos plus 1920×1080 posters and complete hashes.
    - Transaction copies existing Numerical Methods media before adding Batch 4 and restores prior state on any failure.
  </behavior>
  <action>Implement the full Batch 3 transaction pattern for the three exact D-27 scene IDs. Validate contract version, roles, tree depth, bilingual labels, output IDs/anchors, and every dependency before render. Prepare cache-disabled 1920×1080/30fps rendering, poster extraction, ffprobe validation, complete metadata hashes, copy-preserve of Batches 1–3, atomic replace, rollback, and offline/write-free `--check`. Complete source/document/renderer assertions now; binary assertions remain red until Plan 11 publishes media.</action>
  <verify><automated>python3 -m py_compile scripts/manim/render_numerical_methods_batch_4.py &amp;&amp; node --test --test-name-pattern='scene source|six-role|labels|Notebook anchors|renderer contract' tests/numerical-methods-batch-4-manim.test.ts</automated></verify>
  <acceptance_criteria>
    - The renderer declares exactly the three D-27 scene IDs and rejects missing roles, non-depth-3 trees, missing bilingual labels, or mismatched Notebook output anchors before rendering.
    - Renderer contract fixes output at silent H.264 1920×1080/30fps with 1920×1080 posters, complete hashes, atomic replace, rollback, and preservation of Batches 1–3.
    - Compilation and the filtered source/renderer tests pass; no MP4 or poster is rendered in this task, and `--check` is specified as offline and write-free.
  </acceptance_criteria>
  <done>The renderer declares exactly three packages, source-contract tests pass, and no media render occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scene packages/output manifests → renderer | Source and numerical inputs enter a publication transaction. |
| Render transaction → existing public media | New media could overwrite prior batches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-04 | Tampering | Renderer inputs/hashes | medium | mitigate | Exact roles, IDs, output dependencies, and source-contract tests. |
| T-25-07 | Tampering / DoS | Public media transaction | high | mitigate | Copy-preserve, temp render, full validation, atomic replace, rollback. |
</threat_model>

<verification>Run Python compilation, source-focused media tests, and `git diff --check`; do not run the long binary render in this plan.</verification>
<success_criteria>The renderer and source contract are green and ready for one isolated long publication task.</success_criteria>
<output>Create `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-10-SUMMARY.md` when done.</output>
