---
phase: 25-numerical-methods-batch-4-logistic-regression-optimization-a
plan: "04"
type: execute
wave: 4
depends_on: [25-03]
files_modified:
  - src/modules/math-lab/utils/banknoteDataset.ts
  - src/modules/math-lab/utils/banknoteLogistic.ts
  - tests/numerical-methods-batch-4.test.ts
autonomous: true
requirements: [P25-SC3, P25-SC4]
must_haves:
  truths:
    - "Browser computation independently parses the local D-02/D-03 snapshot and recomputes train-only standardization rather than reading stored statistics."
    - "The DOM-free TypeScript engine reproduces the D-05 five runs, D-06 extreme check, D-07 stop priority, D-18 objective, and D-19 traces within locked tolerances."
    - "Expected numerical failures return D-24 terminal metadata and the last finite accepted state; learner inputs never silently substitute NaN, Infinity, or out-of-range values."
    - "No browser Python, runtime UCI request, or synthetic/real scenario union is introduced per D-04 and D-25."
  artifacts:
    - path: "src/modules/math-lab/utils/banknoteDataset.ts"
      provides: "Strict base-safe Banknote CSV parser, loader, and train-statistics derivation"
      exports: ["parseBanknoteDataset", "loadBanknoteDataset", "computeBanknotePreprocessing"]
    - path: "src/modules/math-lab/utils/banknoteLogistic.ts"
      provides: "Typed stable logistic objective, Armijo, stop machine, presets, and traces"
      exports: ["stableSigmoid", "softplus", "stableBinaryCrossEntropy", "lossAndGrad", "armijoStep", "shouldStop", "trainLogistic"]
  key_links:
    - from: "src/modules/math-lab/utils/banknoteDataset.ts"
      to: "/datasets/numerical-methods/banknote-authentication.csv"
      via: "withPublicBase plus injectable fetch and AbortSignal"
      pattern: "withPublicBase"
    - from: "src/modules/math-lab/utils/banknoteLogistic.ts"
      to: "public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.json"
      via: "focused parity tests at 1e-9 scalar and 1e-8 parameter tolerances"
      pattern: "standardized-armijo"
---

<objective>
Implement the deterministic TypeScript dataset boundary and logistic-training engine that independently reproduce the Notebook contract.

Purpose: Make both existing browser labs compute the real case locally without embedding math in Vue or using Python in the browser.
Output: Two pure typed utilities and green parsing, arithmetic, state-machine, failure, and cross-runtime parity tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-RESEARCH.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-PATTERNS.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-03-SUMMARY.md
@src/utils/publicPath.ts
@src/modules/math-lab/utils/logitCalibration.ts
@src/modules/math-lab/utils/optimizers.ts

<interfaces>
Dataset rows expose stable `banknoteId`, four features, target `0 | 1`, and split `train | validation | test`.
Loader states distinguish loading, ready, aborted, HTTP error, parse error, and schema error.
Terminal reasons are exactly `gradient-norm`, `loss-and-step`, `validation-patience`, `max-iterations`, `non-finite`, and `line-search-failed`; kinds are mathematical, model-selection, or safety.
The five preset IDs and every numeric constant come from `numerical-methods-batch-4-v1`.
</interfaces>
</context>

## Artifacts this phase produces

- Files: `banknoteDataset.ts` and `banknoteLogistic.ts`.
- Types: `BanknoteRow`, split/loader/preprocessing records, `TrainingConfig`, `TrainingPreset`, `TracePoint`, `TrainingRun`, `TerminalState`, and terminal kind/reason unions.
- Symbols: strict parser/loader/statistics functions; stable sigmoid/softplus/BCE; objective/gradient; Armijo; stop evaluator; five preset records; trainer and final-selection helper.
- Fields: accepted-state metrics and `attemptedIteration` described in the Plan 03 output contract.
- CLI flags: none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the strict local dataset boundary</name>
  <read_first>
    - `src/utils/pythonDataToolsOutputs.ts` — base-safe injectable fetch, abort, and typed error-state pattern.
    - `scripts/python-data-tools/bikeSharingContract.mjs` — BOM/CRLF normalization and exact numeric CSV boundary.
    - `src/utils/publicPath.ts` — use `withPublicBase` directly.
    - `public/datasets/numerical-methods/banknote-authentication-manifest.json` — exact counts, schema, and statistics.
    - `tests/numerical-methods-batch-4.test.ts` — write failing parser/loader/statistics cases before production code.
  </read_first>
  <files>src/modules/math-lab/utils/banknoteDataset.ts, tests/numerical-methods-batch-4.test.ts</files>
  <behavior>
    - Valid LF/CRLF/BOM input yields 1,372 typed rows and exact split/class counts.
    - Header, row width, ID uniqueness/order, finite features, target, split, and counts fail with line/column-aware errors.
    - Train means/population scales equal locked anchors and are recomputed from parsed training rows.
    - Loader resolves default and Pages base paths and distinguishes abort, HTTP, parse, and schema failures.
  </behavior>
  <action>Create the strict seven-column parser and base-safe async loader per D-02/D-03/D-25. Accept injectable fetch, `AbortSignal`, and base URL for Node tests. Never contact UCI or fall back remotely. Stored means/scales are test anchors only. Keep the numeric schema narrow rather than building a general CSV framework.</action>
  <verify><automated>node --test --test-name-pattern='dataset parser|dataset loader|train-only preprocessing|public base' tests/numerical-methods-batch-4.test.ts</automated></verify>
  <acceptance_criteria>
    - All valid/boundary/error cases are typed and deterministic.
    - Every malformed or non-finite row is rejected before training.
    - Default `/` and `/ML_tutorial_Site/` fetch paths pass.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stable logistic training and exact terminal semantics</name>
  <read_first>
    - `src/modules/math-lab/utils/banknoteLogistic.ts` — create/modify this production file.
    - `src/modules/math-lab/utils/logitCalibration.ts` — stable sigmoid branch.
    - `src/modules/math-lab/utils/optimizers.ts` — pure typed utility and bounded-input conventions only.
    - `public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.json` — authoritative run and terminal anchors.
    - `tests/numerical-methods-batch-4.test.ts` — write arithmetic/state/parity tests first.
  </read_first>
  <files>src/modules/math-lab/utils/banknoteLogistic.ts, tests/numerical-methods-batch-4.test.ts</files>
  <behavior>
    - BCE is finite at ±1000, analytic gradient matches centered differences, and intercept L2 is excluded.
    - Armijo 32 rejects then 16 accepts and every accepted row satisfies sufficient decrease on the penalized training objective.
    - Six terminal reasons, same-iteration priority, accepted-state-only tracing, and last-finite failure behavior are exact.
    - Five presets match Notebook counts/reasons/anchors; final selection rejects non-mathematical transient winners.
  </behavior>
  <action>Implement D-15's function layering and all locked constants without DOM, D3, Vue, fetch, or `aiBridgeMath.ts`. Validate finite bounded configs before execution; advanced invalid input must return an understandable validation result instead of replacement values. Keep a learner-reachable upper learning-rate bound of `Number.MAX_VALUE` so the exact D-24 browser probe can use raw features, fixed step, `learningRate=Number.MAX_VALUE`, and `maxIterations=10`; this must terminate as `non-finite` at attempted iteration 1 while retaining the finite iteration-0 row. Record iteration 0 and accepted finite updates only. Keep validation checkpointing downstream of accepted training-objective steps. Expose single-variable suggestions keyed by the exact safety/selection reason for D-24.</action>
  <verify><automated>node --test --test-name-pattern='stable BCE|gradient|Armijo|stop priority|last finite|five run parity|final selection' tests/numerical-methods-batch-4.test.ts</automated></verify>
  <acceptance_criteria>
    - Displayed scalars and parameter vectors meet the declared tolerances for all five runs.
    - Failure fixtures retain finite iteration 0 and distinguish attempted iteration; the raw/fixed/`Number.MAX_VALUE`/10-iteration browser configuration deterministically returns `non-finite` at attempted iteration 1.
    - `evaluateTrainingScenario` remains untouched and no sixth preset exists.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static CSV → browser parser | Untrusted/malformed text enters typed runtime state. |
| Learner controls → iterative trainer | Editable numeric values can trigger invalid arithmetic or excessive work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering / DoS | CSV parser/loader | high | mitigate | Exact schema/count/finite checks, typed error states, no remote fallback. |
| T-25-02 | Tampering / DoS | Logistic engine | high | mitigate | Stable logit arithmetic, max 500 steps, max 30 backtracks, minimum alpha, last-finite results. |
| T-25-03 | Tampering / DoS | Numeric controls | medium | mitigate | Finite range validation and explicit error; no silent parameter substitution. |
</threat_model>

<verification>
Run `node --test tests/numerical-methods-batch-4.test.ts` with the media-only cases filtered if they remain red, plus `git diff --check`.
</verification>

<success_criteria>
Pure TypeScript independently reproduces the locked dataset statistics, five runs, state semantics, and failures without browser Python or provenance leakage.
</success_criteria>

<output>Create `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-04-SUMMARY.md` when done.</output>
