---
phase: 25-numerical-methods-batch-4-logistic-regression-optimization-a
plan: "03"
type: execute
wave: 3
depends_on:
  - 25-02
files_modified:
  - scripts/numerical-methods/generate-batch-4-notebook.py
  - public/notebooks/numerical-methods/banknote-logistic-optimization.zh-CN.ipynb
  - public/notebooks/numerical-methods/batch-4-outputs/optimization-summary.json
  - public/notebooks/numerical-methods/batch-4-outputs/training-diagnostics-summary.json
  - public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.json
  - public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.csv
  - public/notebooks/numerical-methods/batch-4-outputs/manifest.json
  - tests/numerical-methods-batch-4.test.ts
autonomous: true
requirements:
  - P25-SC1
  - P25-SC2
  - P25-SC3
must_haves:
  truths:
    - "One executed Chinese Notebook carries D-12 from local data integrity through the D-05 run matrix, D-06 extreme check, D-17 gradient check, D-16 library baseline, and D-11 diagnostic report."
    - "The manual NumPy authority is layered `stable_bce` → `loss_and_grad` → `armijo_step` → `should_stop` → `train_logistic` per D-14 and D-15."
    - "D-07 convergence, validation selection, and safety termination remain distinct; traces obey D-19 and preserve the last finite state required by D-24."
    - "Only `standardized-armijo` receives the D-20 compact test report at threshold 0.5, with probability-based ROC-AUC and honest endpoint-only comparison."
  artifacts:
    - path: "public/notebooks/numerical-methods/banknote-logistic-optimization.zh-CN.ipynb"
      provides: "Shared clean-kernel teaching authority for both chapters"
    - path: "public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.json"
      provides: "Complete accepted-state runs and terminal metadata"
      contains: "standardized-armijo"
    - path: "public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.csv"
      provides: "Normalized downloadable finite trace rows"
    - path: "public/notebooks/numerical-methods/batch-4-outputs/manifest.json"
      provides: "Hash, bytes, environment, constants, outputs, and clean-kernel audit"
  key_links:
    - from: "scripts/numerical-methods/generate-batch-4-notebook.py"
      to: "public/notebooks/numerical-methods/banknote-logistic-optimization.zh-CN.ipynb"
      via: "nbformat/nbclient deterministic construction and error-forbidden execution"
      pattern: "NotebookClient"
    - from: "public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.json"
      to: "public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.csv"
      via: "same accepted finite trace rows and exact normalized header"
      pattern: "contract_version,run_id,iteration"
---

<objective>
Generate and lock the shared executed Notebook and every canonical numerical output.

Purpose: Establish the only Python teaching authority and exact cross-runtime/media anchors before browser implementation.
Output: Deterministic Notebook, two chapter summaries, full JSON/CSV traces, manifest, and green Notebook/output tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-RESEARCH.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-PATTERNS.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-02-SUMMARY.md
@docs/curriculum-v3/numerical-methods/batch-4-contract.md

<interfaces>
`Batch4TraceFile` fields: `contractVersion`, `datasetSha256`, `constantsSha256`, fixed five-name `parameterOrder`, and `runs`.
`TrainingRun` fields: `runId`, `featureSpace`, `method`, `config`, `start`, nullable `firstBacktrack`, `bestValidation`, `terminal`, `eligibleForFinalSelection`, and `trace`.
`TracePoint` fields: `iteration`, train/validation BCE, objective, gradient/step norms, accepted step, backtracks, nullable relative objective change, best-validation flag, and five parameters.
CSV header is the exact 19-column order locked in `25-RESEARCH.md`.
</interfaces>
</context>

## Artifacts this phase produces

- Files: the shared Notebook and five `batch-4-outputs` files listed in frontmatter.
- Notebook symbols: `stable_bce`, `loss_and_grad`, `armijo_step`, `should_stop`, `train_logistic`.
- Output fields: `start`, `firstBacktrack`, `bestValidation`, `terminal.kind`, `terminal.reason`, `terminal.iteration`, optional `terminal.attemptedIteration`, `eligibleForFinalSelection`, complete `trace`, and final-report/baseline comparisons.
- Generator CLI behavior: normal publish and `--check`; `--check` regenerates, byte-compares, standalone-reruns, and writes nothing.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the deterministic Notebook and numerical state machine</name>
  <read_first>
    - `scripts/numerical-methods/generate-batch-4-notebook.py` — extend the dataset transaction created in Plan 02.
    - `scripts/numerical-methods/generate-batch-3-notebook.py` — copy deterministic cell IDs, nbclient execution, finite validation, standalone rerun, byte comparison, and rollback patterns.
    - `docs/curriculum-v3/numerical-methods/batch-4-contract.md` — authoritative constants, state priority, run anchors, and output schema.
    - `public/notebooks/numerical-methods/logit-bias-calibration.zh-CN.ipynb` — inspect the latest Chinese executed-Notebook teaching cadence.
  </read_first>
  <files>scripts/numerical-methods/generate-batch-4-notebook.py, public/notebooks/numerical-methods/banknote-logistic-optimization.zh-CN.ipynb</files>
  <behavior>
    - Extreme logits produce stable BCE 1000 or 0 while the separately contained naive comparison records non-finite behavior without entering JSON.
    - The centered-difference probe at h=1e-6 agrees with the analytic gradient within 2e-9 and confirms intercept exclusion from L2.
    - Armijo rejects 32 and accepts 16 at the first update; every accepted step satisfies sufficient decrease.
    - Terminal priority is gradient norm, conjunctive loss-and-step, validation patience, then max iterations; non-finite and line-search failure retain the last finite row.
  </behavior>
  <action>Extend the generator to construct the complete D-12 executed Chinese Notebook with stable IDs and cells ordered from data/source checks through stable BCE, gradient check, five runs, stopping/failure fixtures, final selection, scikit-learn endpoint comparison, and four-step diagnostic report. Implement the D-14/D-15 manual NumPy path exactly, use SciPy only as an independent stable-function comparison, and use `LogisticRegression(C=25/24, l1_ratio=0.0, solver='lbfgs', fit_intercept=True, tol=1e-12, max_iter=5000)` only at the endpoint per D-16. Validation never participates in Armijo/gradient updates. Keep extreme and forced-failure probes outside the five-run matrix. Strip timing metadata, forbid cell errors, and reject every non-finite published number.</action>
  <verify>
    <automated>python3 -m py_compile scripts/numerical-methods/generate-batch-4-notebook.py &amp;&amp; python3 scripts/numerical-methods/generate-batch-4-notebook.py</automated>
  </verify>
  <acceptance_criteria>
    - Notebook executes from a clean kernel with the eight exact requirements pins and no network access.
    - Five run IDs/outcomes and all constants equal research; six terminal reasons are exercised across runs/fixtures.
    - Manual versus baseline metrics equal the locked table, and the Notebook explicitly rejects iteration-by-iteration solver comparison.
    - `allow_nan=False`, recursive finite checks, temp cleanup, environment restoration, and rollback protect publication.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Publish summaries, complete traces, and the output manifest atomically</name>
  <read_first>
    - `public/notebooks/numerical-methods/banknote-logistic-optimization.zh-CN.ipynb` — authoritative executed cells from Task 1.
    - `public/notebooks/numerical-methods/batch-3-outputs/manifest.json` — hash/bytes/environment/output record pattern.
    - `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-RESEARCH.md` — exact JSON interfaces, CSV header, anchors, and tolerances.
    - `scripts/numerical-methods/generate-batch-4-notebook.py` — output transaction and validator.
  </read_first>
  <files>public/notebooks/numerical-methods/batch-4-outputs/optimization-summary.json, public/notebooks/numerical-methods/batch-4-outputs/training-diagnostics-summary.json, public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.json, public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.csv, public/notebooks/numerical-methods/batch-4-outputs/manifest.json</files>
  <action>Publish both chapter summaries, the complete JSON trace, normalized CSV, and manifest in one rollback-capable transaction. Record iteration 0 and every accepted finite update only; rejected/non-finite candidates remain terminal metadata. Make JSON and CSV accepted rows exactly agree. Optimization summary owns all five numerical comparisons; diagnostics summary owns cross-run visible/cause/change/expected-next chains and the selected-run final report per D-11/D-21. Include start, first backtrack, best validation, terminal, exact reason, dataset/generator/requirements/Notebook hashes, constants/splits, Python/package versions, and output hashes/bytes. Keep later media hashes out to avoid a cycle.</action>
  <verify>
    <automated>python3 scripts/numerical-methods/generate-batch-4-notebook.py --check</automated>
  </verify>
  <acceptance_criteria>
    - `--check` regenerates offline, byte-compares every committed Notebook/output artifact, standalone-reruns the downloaded Notebook, and writes nothing.
    - JSON/CSV row counts and finite values agree, terminal rows exist, and nullable values are JSON `null`.
    - Final report occurs only for `standardized-armijo`, threshold is 0.5, and ROC-AUC is probability-based.
    - Manifest includes every required source/output hash without including media outputs.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Turn the Notebook and output contract tests green</name>
  <read_first>
    - `tests/numerical-methods-batch-4.test.ts` — extend the Wave 0 suites created in Plan 02.
    - `public/notebooks/numerical-methods/batch-4-outputs/manifest.json` — expected output IDs and hashes.
    - `public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.json` — exact run and terminal records.
    - `public/notebooks/numerical-methods/batch-4-outputs/banknote-training-traces.csv` — accepted-state parity source.
  </read_first>
  <files>tests/numerical-methods-batch-4.test.ts</files>
  <behavior>
    - Exact split/preprocessing and five run anchors are asserted.
    - Gradient, Armijo, six terminal reasons/priority, last-finite invariant, final eligibility, and baseline mapping are asserted.
    - Artifact paths, hashes, bytes, output IDs, CSV header, JSON/CSV row parity, and finite serialization are asserted.
  </behavior>
  <action>Complete the Notebook/output portions of the focused test using research tolerances rather than cross-language byte equality. Assert that `standardized-too-large` cannot win through its transient validation minimum, the baseline uses `C=25/24`, and no sixth run, threshold tuner, PR-AUC, calibration report, or per-iteration solver alignment entered the outputs.</action>
  <verify>
    <automated>node --test --test-name-pattern='dataset|notebook|output|gradient|Armijo|terminal|baseline' tests/numerical-methods-batch-4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All Notebook/output-focused suites pass in under 10 seconds.
    - Every D-03, D-05 through D-09, D-12, and D-14 through D-21 numerical assertion is executable.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CSV → Python numerical kernel | Static numeric input enters iterative computation. |
| Notebook execution → committed JSON/CSV | Generated values become browser/media authority. |
| scikit-learn endpoint → teaching comparison | Third-party solver output is presented beside manual computation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-02 | Tampering / DoS | Notebook outputs and traces | high | mitigate | Stable logit arithmetic, finite assertions, bounded iterations/backtracking, atomic publication, byte-drift check. |
| T-25-03 | Repudiation | Terminal reason/model selection | medium | mitigate | Typed terminal kind/reason/iteration, accepted-state rows, deterministic priority, explicit eligibility record. |
| T-25-SC | Tampering | Baseline package | high | mitigate | Consume only Plan 01-approved exact pin and record package versions in manifest. |
</threat_model>

<verification>
Run `python3 scripts/numerical-methods/generate-batch-4-notebook.py --check`, the focused Node suites named above, and `git diff --check`.
</verification>

<success_criteria>
The clean-kernel Notebook and all locked outputs reproduce the exact research anchors, preserve state semantics, and provide hash-audited inputs for TypeScript, pages, illustration, and Manim.
</success_criteria>

<output>
Create `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-03-SUMMARY.md` when done.
</output>
