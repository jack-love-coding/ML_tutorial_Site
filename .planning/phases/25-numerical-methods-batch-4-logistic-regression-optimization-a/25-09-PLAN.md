---
phase: 25-numerical-methods-batch-4-logistic-regression-optimization-a
plan: "09"
type: execute
wave: 9
depends_on: [25-08]
files_modified: []
autonomous: false
requirements: [P25-SC4, P25-SC5]
must_haves:
  truths:
    - "Both existing routes pass the eight-state Chinese/English × desktop/390px browser matrix with no console errors or horizontal overflow."
    - "Five optimization presets, reset, invalid-arithmetic last-finite feedback, diagnostics comparisons/toggles, and all synthetic provenance labels work interactively."
    - "Default and Pages-base local downloads/media resolve; checkpoint submission and all Progress stores behave exactly as before."
    - "Video failure and reduced-motion fallbacks retain poster, transcript, summary, labels, and critical teaching meaning."
  artifacts: []
  key_links:
    - from: "human browser matrix"
      to: "P25-SC5 release acceptance"
      via: "explicit approval after automated gates"
      pattern: "390x844"
---

<objective>
Complete human browser acceptance for the rebuilt optimization and training-diagnostics lessons.

Purpose: Verify responsive readability, keyboard flow, fallbacks, route/progress compatibility, and teaching clarity that source/build checks cannot establish.
Output: Recorded approval of the exact browser matrix; no repository changes.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-VALIDATION.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-08-SUMMARY.md
</context>

## Artifacts this phase produces

- New files, symbols, fields, or CLI flags: none.
- Execution artifact: recorded human approval for the eight-state browser matrix and fallback/progress checks.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve bilingual desktop/mobile routes and fallbacks</name>
  <read_first>
    - `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-VALIDATION.md` — use the exact browser/media checklist.
    - `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-08-SUMMARY.md` — confirm automated gates are green before opening the checkpoint.
    - `src/modules/math-lab/labs/MathGradientLab.vue` and `src/modules/math-lab/labs/TrainingDiagnosticsLab.vue` — know the exact interactions and labels under review.
  </read_first>
  <what_built>The two existing bilingual lessons now share one reproducible Banknote case, deterministic TypeScript labs, local downloads, one illustration, and three Notebook-bound videos; automated gates passed in Plan 08.</what_built>
  <action>Serve the standard build and Pages-base build using existing commands. Review `/math-lab/modules/optimization?route=numerical-deepening-path` and `/math-lab/modules/training-diagnostics?route=numerical-deepening-path` in Chinese and English at desktop and 390×844. Exercise D-22's five presets/reset/advanced bounds and D-24's last-finite failure, then D-23's run comparisons/toggles and D-04's five separately labeled synthetic support modes. Verify keyboard use, non-color status, no overflow/console errors, checkpoint submission, unchanged progress persistence, all local downloads, and reduced-motion/video-failure poster/transcript/summary fallbacks.</action>
  <how_to_verify>
    1. Check both routes in both locales at desktop and 390×844: eight states, zero console errors, zero horizontal overflow.
    2. On optimization, run all five presets, reset, bounded advanced controls, and a failing configuration; confirm exact markers/reason/suggestion and preserved last finite state.
    3. On diagnostics, compare stable versus too-small and Armijo versus too-large, toggle curves, verify the four-step chain, then visit all five synthetic modes and their provenance labels.
    4. Open dataset, Notebook, requirements, summaries, JSON, CSV, dictionary, illustration, posters, and videos under standard and Pages bases.
    5. Submit both existing checkpoints and confirm no storage key/identity regression.
    6. Simulate reduced motion and one video failure; verify poster/transcript/summary/labels preserve the lesson.
  </how_to_verify>
  <verify><manual>Human confirms every numbered browser/fallback/progress step after Plan 08's automated gate is green.</manual></verify>
  <acceptance_criteria>
    - All eight route/locale/viewport states pass without overflow or console errors.
    - Interactions, local assets, checkpoints, progress, and fallbacks behave exactly as specified.
    - No route, backend, browser Python, progress migration, or deferred feature appears.
  </acceptance_criteria>
  <resume_signal>Type `approved` when all checks pass, or report the route, locale, viewport, interaction, and observed issue.</resume_signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built static app → human browser | Real layout, media, keyboard, route, and storage behavior crosses from source assumptions into observed UX. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Tampering / availability | Pages assets/routes | medium | mitigate | Verify both bases, every download/media path, zero console errors. |
| T-25-11 | Denial of understanding | Mobile/motion/color UX | medium | mitigate | 390px, keyboard, reduced-motion/video-failure, non-color, transcript/summary checks. |
</threat_model>

<verification>
Human approval is recorded only after the exact matrix and interactive/fallback/progress checklist passes.
</verification>

<success_criteria>
P25-SC4 and P25-SC5 are visually and interactively verified in both locales, both viewports, and both asset-base modes.
</success_criteria>

<output>Create `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-09-SUMMARY.md` when done.</output>
