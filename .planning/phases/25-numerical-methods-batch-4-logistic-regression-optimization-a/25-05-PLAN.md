---
phase: 25-numerical-methods-batch-4-logistic-regression-optimization-a
plan: "05"
type: execute
wave: 5
depends_on: [25-04]
files_modified:
  - src/modules/math-lab/data/numericalBatch4Modules.ts
  - src/modules/math-lab/data/numericalBatch4Notebook.ts
  - src/modules/math-lab/data/modules.ts
  - src/modules/math-lab/components/MathLabNotebookCompanion.vue
  - src/modules/math-lab/pages/MathLabModulePage.vue
  - src/styles/modules/math-lab.css
  - src/modules/math-lab/labs/MathGradientLab.vue
  - src/modules/math-lab/labs/TrainingDiagnosticsLab.vue
  - tests/numerical-methods-batch-4.test.ts
autonomous: true
requirements: [P25-SC4, P25-SC5]
must_haves:
  truths:
    - "The existing `optimization` chapter teaches D-10's stable BCE → scale → fixed step → Armijo → stopping/failure chain and owns D-21's five-run comparison."
    - "The existing `training-diagnostics` chapter teaches D-11's visible → cause → one-variable change → expected next run chain and owns the D-20 final-report connection without becoming a scored exercise."
    - "Both existing lab identities are upgraded in place per D-13; each appears once, routes/checkpoints/progress remain unchanged, and synthetic support modes retain explicit D-04 provenance."
    - "Preset-first explicit Run controls and bounded advanced controls implement D-22 through D-25 with last-finite feedback and no reactive retraining."
    - "One D-12 Notebook, shared dataset/requirements, chapter summaries, full JSON/CSV traces, and dictionary are base-safe local downloads."
  artifacts:
    - path: "src/modules/math-lab/data/numericalBatch4Modules.ts"
      provides: "Outermost detailed bilingual enhancer for only optimization/training-diagnostics"
      exports: ["enhanceNumericalBatch4Module"]
    - path: "src/modules/math-lab/data/numericalBatch4Notebook.ts"
      provides: "Two chapter companions sharing one Notebook and supporting downloads"
      exports: ["numericalBatch4ChapterIds", "numericalBatch4NotebookForModule"]
    - path: "src/modules/math-lab/labs/MathGradientLab.vue"
      provides: "Explicit-run Banknote optimization workbench"
    - path: "src/modules/math-lab/labs/TrainingDiagnosticsLab.vue"
      provides: "Real-run comparison plus separate synthetic support surfaces"
  key_links:
    - from: "src/modules/math-lab/data/modules.ts"
      to: "src/modules/math-lab/data/numericalBatch4Modules.ts"
      via: "outermost enhancer after Batches 1–3"
      pattern: "enhanceNumericalBatch4Module"
    - from: "src/modules/math-lab/pages/MathLabModulePage.vue"
      to: "src/modules/math-lab/data/numericalBatch4Notebook.ts"
      via: "companion resolver while existing labs remain defineAsyncComponent imports"
      pattern: "numericalBatch4NotebookForModule"
    - from: "src/modules/math-lab/labs/TrainingDiagnosticsLab.vue"
      to: "src/modules/math-lab/utils/aiBridgeMath.ts"
      via: "separate synthetic-only selector group"
      pattern: "evaluateTrainingScenario"
---

<objective>
Rebuild the two existing bilingual lessons and primary labs around the locked Banknote case while preserving every public identity and support surface.

Purpose: Complete the learner-facing teaching loop from numerical optimization to actionable curve diagnosis.
Output: Batch 4 enhancer/companions, base-safe supporting downloads, upgraded labs, page registration, responsive styles, and preservation/content tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-CONTEXT.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-PATTERNS.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-04-SUMMARY.md
@src/modules/math-lab/data/numericalBatch3Modules.ts
@src/modules/math-lab/data/numericalBatch3Notebook.ts

<interfaces>
Module IDs remain `optimization` and `training-diagnostics`; lab IDs remain `optimization-gradient-lab` and `training-diagnostics-lab`.
Canonical URLs remain `/math-lab/modules/{moduleId}?route=numerical-deepening-path`.
The existing storage key remains `ml-atlas:math-lab-progress:v1`; Progress V2 composition is untouched.
Companions add optional `supportingDownloads: readonly DownloadableCourseAsset[]` while retaining Notebook/dataset/requirements primary fields.
</interfaces>
</context>

## Artifacts this phase produces

- Files: `numericalBatch4Modules.ts` and `numericalBatch4Notebook.ts`; modified registry/page/companion/style/lab/test files listed in frontmatter.
- Symbols: `enhanceNumericalBatch4Module`, `numericalBatch4ChapterIds`, `NumericalBatch4NotebookCompanion`, `numericalBatch4NotebookForModule`.
- Field: optional `supportingDownloads` on the companion union, containing trace JSON/CSV, data dictionary, and chapter summary records.
- UI state: dataset load state, editable draft config, committed run result, preset ID, real primary/comparison IDs, and curve toggles.
- CLI flags: none.

<tasks>

<task type="auto">
  <name>Task 1: Add the detailed bilingual enhancer and shared Notebook companions</name>
  <read_first>
    - `src/modules/math-lab/data/numericalBatch3Modules.ts` — enhancer, section helpers, visual creation, and one-lab deduplication.
    - `src/modules/math-lab/data/numericalBatch3Notebook.ts` — typed companion records and resolver.
    - `src/modules/math-lab/data/optimizationModule.ts` — preserve existing checkpoint, lab, concepts, quizzes, misconceptions, and optimizer-family link.
    - `src/modules/math-lab/data/aiBridgeModules.ts` — preserve training-diagnostics identity and synthetic sections.
    - `src/modules/math-lab/data/modules.ts` — wrap the current enhancer chain outermost.
    - `tests/numerical-methods-batch-4.test.ts` — add content and preservation assertions.
  </read_first>
  <files>src/modules/math-lab/data/numericalBatch4Modules.ts, src/modules/math-lab/data/numericalBatch4Notebook.ts, src/modules/math-lab/data/modules.ts, tests/numerical-methods-batch-4.test.ts</files>
  <action>Create enhancers only for the two locked IDs. In `optimization`, layer D-10/D-15 copyable code and exact D-08 summaries around stable BCE, scale conditioning, the five runs, Armijo, terminal kinds, failures, and a link to optimizer-comparison without repeating SGD/Momentum/RMSProp/Adam. In `training-diagnostics`, use the same traces for D-11/D-21 four-step diagnoses and compact final-report connection. Preserve base content and deduplicate each lab placement. Create two localized companions sharing D-12's Notebook/dataset/requirements/trace/dictionary assets but using their chapter summary/output IDs. Apply the enhancer outermost; do not change provider identity, module order, quizzes, checkpoint IDs, or next-route behavior.</action>
  <verify><automated>node --test --test-name-pattern='module content|companion|one primary lab|route order|checkpoint|progress|synthetic provenance' tests/numerical-methods-batch-4.test.ts</automated></verify>
  <acceptance_criteria>
    - Every new learner string has both locales and forms a question/concept/visual-or-lab/numeric-code/misconception/checkpoint/next-step loop.
    - Formula variables, code names, output labels, and lab terminology agree with the Notebook.
    - Existing URLs, order, checkpoint IDs, lab IDs, storage key, and all five synthetic modes remain exact.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire base-safe supporting downloads without changing page/progress composition</name>
  <read_first>
    - `src/modules/math-lab/components/MathLabNotebookCompanion.vue` — modify its existing union and download rendering.
    - `src/modules/math-lab/pages/MathLabModulePage.vue` — append the Batch 4 resolver; retain async lab registry and progress code.
    - `src/styles/modules/math-lab.css` — extend existing notebook selectors and mobile collapse only.
    - `src/utils/publicPath.ts` — apply `withPublicBase` to every supporting path.
    - `src/modules/math-lab/data/numericalBatch4Notebook.ts` — companion interface created in Task 1.
  </read_first>
  <files>src/modules/math-lab/components/MathLabNotebookCompanion.vue, src/modules/math-lab/pages/MathLabModulePage.vue, src/styles/modules/math-lab.css, tests/numerical-methods-batch-4.test.ts</files>
  <action>Add the Batch 4 companion type to the component union and render a wrapping optional supporting-download list using `withPublicBase` for each path. Append the Batch 4 resolver after existing companion resolvers. Keep `MathGradientLab` and `TrainingDiagnosticsLab` lazy, and leave route-query, quiz/checkpoint, and Progress V1/V2 composition untouched. Extend only adjacent `.math-notebook-companion__*` styles with a readable one-column 390px collapse.</action>
  <verify><automated>node --test --test-name-pattern='supporting downloads|lazy labs|public base|progress preservation|localized content' tests/numerical-methods-batch-4.test.ts</automated></verify>
  <acceptance_criteria>
    - Old companions render unchanged when `supportingDownloads` is absent.
    - Every Batch 4 asset uses a leading-slash canonical path and resolves under both bases.
    - No new route, eager lab import, storage key, or broad global style appears.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Upgrade both primary labs around real-data computation and honest provenance</name>
  <read_first>
    - `src/modules/math-lab/labs/MathGradientLab.vue` — preserve identity, locale prop, shell, accessible controls, and readout idioms.
    - `src/modules/math-lab/labs/TrainingDiagnosticsLab.vue` — preserve D3 presentation and synthetic engine wiring.
    - `src/modules/math-lab/utils/banknoteDataset.ts` — consume typed async loader states.
    - `src/modules/math-lab/utils/banknoteLogistic.ts` — consume presets, runs, terminal metadata, and suggestions.
    - `src/modules/math-lab/utils/aiBridgeMath.ts` — preserve the exact five-ID synthetic union and generator.
    - `tests/numerical-methods-batch-4.test.ts` — write source/component behavior assertions first.
  </read_first>
  <files>src/modules/math-lab/labs/MathGradientLab.vue, src/modules/math-lab/labs/TrainingDiagnosticsLab.vue, tests/numerical-methods-batch-4.test.ts</files>
  <behavior>
    - Optimization presets load drafts; only Run commits computation; Reset returns the locked default; bounded advanced controls expose only D-22 fields.
    - Optimization displays start, first backtrack, best validation, terminal kind/reason, last finite values, and one suggested variable change.
    - Diagnostics selects real primary/comparison runs and curve visibility, presents the D-11 chain, and separately retains all five synthetic support modes.
    - Loading, abort, malformed data, invalid controls, video-independent numeric summaries, keyboard operation, and non-color status are understandable in both locales.
    - In Advanced controls, raw features + fixed method + learning rate `Number.MAX_VALUE` + maximum iterations `10` is learner-reachable without a sixth preset and deterministically shows `non-finite`, attempted iteration 1, the finite iteration-0 state, and one lower-step suggestion.
  </behavior>
  <action>Replace the reactive quadratic authority in `MathGradientLab` with a thin consumer of the local dataset and pure engine while preserving the registered component identity. Keep L2, Armijo c/rho, and patience fixed in primary UI. Make the Advanced learning-rate field finite and bounded through `Number.MAX_VALUE`; the documented raw/fixed/`Number.MAX_VALUE`/10-iteration configuration must be enterable by keyboard, must not become a sixth preset, and must render `non-finite` at attempted iteration 1 while retaining iteration 0 and suggesting a lower learning rate. In `TrainingDiagnosticsLab`, derive D3 paths only from already-computed real traces, add separate real selectors/toggles, and keep `evaluateTrainingScenario` isolated under a clearly labeled deterministic synthetic support section. Do not turn diagnosis into a quiz or dashboard builder, and never silently alter invalid learner parameters.</action>
  <verify><automated>node --test --test-name-pattern='MathGradientLab|TrainingDiagnosticsLab|explicit Run|bounded controls|last finite|synthetic support' tests/numerical-methods-batch-4.test.ts</automated></verify>
  <acceptance_criteria>
    - Five presets reproduce locked anchors and labels; custom controls remain finite and bounded.
    - The exact raw/fixed/`Number.MAX_VALUE`/10 advanced configuration exposes the D-24 last-finite path in the real UI without adding a sixth preset.
    - Training diagnostics comparisons and all five synthetic modes remain reachable and separately sourced.
    - Status includes text/shape/labels, controls have labels/current values/reset/keyboard behavior, and reduced motion loses no teaching content.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Typed local content → sanitized learner page | Bilingual Markdown/formulas and source metadata render in the DOM. |
| Learner controls → TypeScript engine | Editable values trigger bounded computation. |
| Static assets → default/Pages URLs | Local downloads must not break or fall back remotely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Tampering / DoS | Lab controls | medium | mitigate | Explicit Run, finite bounds, max work, last-finite failures, no silent substitution. |
| T-25-04 | Tampering / XSS | Lesson Markdown/media copy | medium | mitigate | Typed `LocalizedCopy`, existing MarkdownMath sanitizer, no raw HTML path. |
| T-25-05 | Tampering / availability | Public assets | medium | mitigate | Leading-slash records, `withPublicBase`, local-only paths, focused base tests. |
</threat_model>

<verification>
Run `node --test tests/numerical-methods-batch-4.test.ts`, `npm run build`, and `git diff --check`.
</verification>

<success_criteria>
Both existing routes provide complete bilingual numerical/diagnostic teaching loops, their upgraded primary labs recompute the locked case, and all routes/checkpoints/progress/synthetic support remain compatible.
</success_criteria>

<output>Create `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-05-SUMMARY.md` when done.</output>
