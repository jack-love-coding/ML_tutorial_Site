---
phase: 28.1
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [GRAD-01, GRAD-02, GRAD-03]
files_modified:
  - src/data/sharedRegressionTeachingData.ts
  - src/modules/ai-overview/data/experiments.ts
  - src/simulations/gradientDescentLesson.ts
  - scripts/gradient-descent/build-assets.py
  - scripts/manim/render_gradient_descent.py
  - scripts/manim/scenes/gradient_descent_deep_dive.py
  - public/gradient-descent/v1/
  - public/manim/gradient-descent/
  - tests/gradient-descent-engine.test.ts
  - tests/gradient-descent-assets.test.ts
---

<objective>
Create the deterministic numerical and media authority for the six-chapter gradient-descent rebuild, so every visible value can be reproduced from one shared dataset and one implementation contract.
</objective>

<tasks>

<task type="auto">
  <name>Build the shared dataset and pure TypeScript engine</name>
  <action>Extract the five-row clear-trend dataset into a readonly shared module, make AI Overview consume it, and implement predictions, residuals, MSE, analytic and finite-difference gradients, one-step updates, least squares, deterministic batching, trajectories, standardization, and explicit convergence/oscillation/divergence status.</action>
  <verify>Run the focused engine tests and verify the locked anchor and batching sequence.</verify>
</task>

<task type="auto">
  <name>Publish executed bilingual data assets</name>
  <action>Generate CSV, summary JSON, six typed scene payloads, interaction manifest, environment record, and two clean-kernel executed NumPy Notebooks. Bind every file to hashes and stable public paths.</action>
  <verify>Run the asset generator in check mode and validate Notebook outputs against TypeScript anchors.</verify>
</task>

<task type="auto">
  <name>Publish the gradient-rule Manim package</name>
  <action>Follow the Math-To-Manim pipeline to create the reverse knowledge tree, verbose prompt, language-neutral scene, 1080p30 video, SVG poster, bilingual segmented transcripts, chapter markers, hashes, and selective preview/publish/check renderer.</action>
  <verify>Use ffprobe, hash checks, marker bounds, and source-contract tests.</verify>
</task>

</tasks>

<verification>
Focused numerical and asset tests, full test suite, standard build, Pages build, and security audit pass.
</verification>

<success_criteria>
- All anchor values and finite-difference checks match the locked numerical contract.
- Mini-batch and SGD updates use actual sample subsets with deterministic replay.
- Notebooks, JSON, CSV, and animation metadata form one hash-verified package.
- The 1080p30 animation is between 75 and 90 seconds and contains no language-dependent prose on screen.
</success_criteria>
