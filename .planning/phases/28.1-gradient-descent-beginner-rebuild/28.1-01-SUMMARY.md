---
phase: 28.1
plan: 01
subsystem: gradient-descent-assets
tags: [gradient-descent, numpy, notebook, manim, deterministic-assets]
key-files:
  created:
    - src/simulations/gradientDescentLesson.ts
    - public/gradient-descent/v1/output-manifest.json
    - public/manim/gradient-descent/gradient-rule.mp4
  modified:
    - src/modules/ai-overview/data/experiments.ts
    - scripts/manim/render_ai_overview.py
metrics:
  tests: 1027
  passed: 999
  skipped: 28
  animation_seconds: 84
---

# Phase 28A Plan 01 Summary

## Delivered

- Extracted the five-row study-hours dataset into one readonly source consumed by AI Overview and the new course engine.
- Added a pure TypeScript engine for prediction, residuals, MSE, analytic and finite-difference gradients, least squares, standardization, exact updates, deterministic real-sample batching, and unclamped trajectory status.
- Published a hash-bound package containing CSV, six interaction payloads, summary/environment/manifests, and two clean-kernel executed bilingual NumPy Notebooks.
- Published an 84-second language-neutral 1920×1080 30fps Manim animation with poster, reverse knowledge tree, verbose prompt, bilingual segmented transcripts, chapter markers, probe data, and hashes.
- Updated the AI Overview Manim source contract so the extracted shared dataset remains part of its auditable fixture chain without re-encoding unchanged videos.

## Numerical anchors

- `(w,b)=(6,47)` → predictions `[53,59,65,71,77]`, residuals `[-1,0,0,1,1]`, MSE `0.6`, gradient `(-3.2,-0.4)`.
- `eta=0.02` → `(6.064,47.008)`, MSE `0.440192`.
- Least squares → `(6.5,45.7)`, MSE `0.06`.
- Seed `2801`, mini-batch size `2`, final batch size `1`; all visible batch IDs come from actual samples.

## Verification

- `npm test`: 999 passed, 28 skipped, 0 failed.
- `npm run test:ci`: 999 passed, 28 skipped, 0 failed.
- `npm run build`: passed with the existing large-chunk warning.
- `npm run build:pages`: passed with the existing large-chunk warning.
- `npm run security:audit`: 0 vulnerabilities.
- Asset builder and both Manim renderers pass read-only drift checks.

## Deviations

- The AI Overview renderer originally assumed the regression samples were literal text inside `experiments.ts`. Its contract check now accepts the dedicated shared source and refreshes source-bound JSON without re-rendering unchanged video bytes.

## Self-Check: PASSED

All planned asset, numerical, reproducibility, animation, build, and security gates passed. The paged UI and labs remain intentionally owned by Plan 02.
