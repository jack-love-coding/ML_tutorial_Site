---
phase: 27-linear-regression-rebuild
plan: "06"
type: execute
wave: 5
depends_on:
  - 27-05
files_modified:
  - tests/linear-regression-content.test.mjs
  - tests/linear-regression-layout.test.mjs
  - tests/algorithm-progress.test.ts
  - tests/curriculumProgress.test.ts
  - src/data/linearRegressionModule.ts
  - src/i18n/messages.ts
  - src/curriculum/adapters/algorithmAdapter.ts
autonomous: true
requirements:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "The eight literal chapter IDs remain reachable but are ordered and titled as the locked one-case spine per D-07 and D-08."
    - "Every chapter contains complete zh-CN/en question, formula/variables, row-then-batch calculation, sklearn counterpart, locked result/code binding, misconception feedback, selective prompt, and next step per D-09 through D-12."
    - "The narrative preserves raw cnt, leakage explanation, common preprocessing/method roles, model/original coefficient meaning, OLS versus Ridge/Lasso objective distinction, and page-visible result scope per D-01 through D-06 and D-13 through D-18."
    - "Chapter 5 teaches “normal equation / 正规方程”, the augmented-design pseudoinverse relation, intercept/weight mapping, stable `numpy.linalg.lstsq` implementation rationale, and the same-split three-method comparison per D-13."
    - "Held-out diagnosis follows optimization proof and covers hourly nonlinearity, widening spread, atemp instability, named cases, concise log1p, and combined review per D-19 through D-24."
    - "Module/route/chapter/checkpoint/progress identities, safe rendering, code-copy, bilingual parity, Pages paths, mobile/reduced-motion contract, and need-driven media remain compatible per D-25 through D-28."
  artifacts:
    - path: "tests/linear-regression-content.test.mjs"
      provides: "Wave 0 bilingual eight-chapter teaching-loop contract"
    - path: "src/data/linearRegressionModule.ts"
      provides: "Rebuilt typed Bike Sharing course definition"
      contains: "slug: 'linear-regression'"
    - path: "src/curriculum/adapters/algorithmAdapter.ts"
      provides: "Preserved IDs in the new canonical chapter order"
  key_links:
    - from: "src/data/linearRegressionModule.ts"
      to: "src/data/linearRegressionAssets.ts"
      via: "typed chapter result/code/download bindings rather than copied locked numbers"
      pattern: "linearRegressionChapterAssets"
    - from: "src/curriculum/adapters/algorithmAdapter.ts"
      to: "tests/curriculumProgress.test.ts"
      via: "reordered existing IDs preserve V1/V2 progress records"
      pattern: "fit-line|multivariate|residual-loss"
---

<objective>
Rewrite the typed bilingual eight-chapter lesson around the one Bike Sharing case while proving identity and progress compatibility.

Purpose: Deliver the complete teaching corridor before UI polish, with one notation/result contract and no lost deep links or learner progress.
Output: Bilingual module content, matching locale/adapter order, Wave 0 content/compatibility tests, and passing progress migration assertions.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md
@.planning/phases/27-linear-regression-rebuild/27-05-SUMMARY.md
@.planning/phases/26-loss-functions-rebuild/26-06-SUMMARY.md
</context>

## Artifacts this phase produces

- New test file: `tests/linear-regression-content.test.mjs`.
- New/updated content symbols in existing files: reordered `linearRegressionModule.chapters`, Bike-specific bilingual `linearRegressionManifest` lesson order, existing linear-regression locale message keys with Bike copy.
- No new route, module slug, chapter ID, checkpoint ID, progress key, storage schema, or asset path authority is created.
- Chapter order is exactly `fit-line`, `multivariate`, `residual-loss`, `training-motion`, `polynomial`, `model-limits`, `overfitting`, `regularization`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-06-01: Establish Wave 0 content, route, checkpoint, and progress contracts</name>
  <read_first>
    - `tests/linear-regression-content.test.mjs` — create this owned Wave 0 target.
    - `tests/linear-regression-layout.test.mjs` — preserve lazy route, dedicated page branch, eight IDs, sidebar/pager, preset sync, safe rendering, and fallbacks.
    - `tests/algorithm-progress.test.ts` — add a pre-rebuild linear-regression record and preserve the V1 storage key.
    - `tests/curriculumProgress.test.ts` — prove V1 source stores are not mutated and V2 merge retains reordered IDs.
    - `src/data/algorithmCheckpoints.ts` — lock existing checkpoint and revisit identities; do not modify it.
    - `src/router/index.ts` — lock route order and base redirect; do not modify it.
    - `src/data/linearRegressionModule.ts` — current content/order target.
    - `.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md` — own 27-W0-05 content/compatibility portion.
  </read_first>
  <files>tests/linear-regression-content.test.mjs, tests/linear-regression-layout.test.mjs, tests/algorithm-progress.test.ts, tests/curriculumProgress.test.ts</files>
  <behavior>
    - Exact module slug/root route/eight literal IDs/deep links/checkpoint IDs/revisit links and storage keys persist.
    - The new exact chapter order and all eight paired bilingual teaching loops are required.
    - Formulas, variable names, feature order, residual sign, method roles, locked output IDs, sklearn counterparts, limitations, and next-step bridge agree.
    - Chapter 5 content tests assert both locale terms, `X_tilde = [1, X]`, `theta = (X_tilde^T X_tilde)^+ X_tilde^T y`, `theta[0] = b`, `theta[1:] = w`, the `numpy.linalg.lstsq` mapping/rationale, and all three same-split methods.
    - Existing V1 completed lessons/checkpoint attempts survive reordering and merge into V2 without mutating raw V1 stores.
    - Safe Markdown/math, code-copy, local asset paths, no remote runtime content, plain learner terminology, mobile/reduced-motion/non-color structure remain required.
  </behavior>
  <action>Create the content test and update only test expectations first. Separate immediately passing identity/preservation scaffolds from RED Bike-content/order groups owned by Tasks 27-06-02/03 and UI groups owned by Plans 27-07/08. Assert exact eight IDs and new order without accepting renamed aliases. Add exact bilingual Chapter 5 assertions for “normal equation / 正规方程”, the augmented `X_tilde` convention, pseudoinverse formula, `theta` to `b`/`w` mapping, the reason executable code uses `numpy.linalg.lstsq` instead of explicit inversion, and NumPy batch GD/reference/sklearn comparison on the identical split/design. Seed progress fixtures with all old lesson IDs plus both checkpoint attempts; prove route and revisit targets remain resolvable after adapter reordering. Scan learner-facing content for plain `结果/观察/对照/参考输出` semantics and require any occurrence of the discouraged audit term to be explained in prose rather than used as navigation or a standalone teaching label.</action>
  <verify>
    <automated>node --test --test-name-pattern="identity scaffold|route preservation|checkpoint preservation|progress preservation" tests/linear-regression-content.test.mjs tests/linear-regression-layout.test.mjs tests/algorithm-progress.test.ts tests/curriculumProgress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four test files parse and preservation scaffolds pass against the pre-rebuild implementation.
    - Bike chapter/order/content groups fail only on production work assigned to this plan.
    - UI/release groups explicitly name Plans 27-07/08 rather than disappearing from coverage.
    - 27-W0-05 now covers content, identity, routes, checkpoint, progress, safe rendering, terminology, and responsive fallback contracts.
  </acceptance_criteria>
  <done>Every compatibility boundary is locked before the content array or adapter order changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-06-02: Rebuild all eight bilingual chapters around one row-to-batch case</name>
  <read_first>
    - `src/data/linearRegressionModule.ts` — modify this typed module in place.
    - `src/i18n/messages.ts` — preserve existing keys while replacing housing-oriented shell copy in both locales.
    - `src/types/ml.ts` — preserve `AlgorithmModuleDefinition`, `StorySection`, controls, presets, and snapshots.
    - `src/data/linearRegressionAssets.ts` — bind typed asset/output IDs, not copied full-precision numbers.
    - `src/simulations/linearRegression.ts` — use stable scenario/preset identifiers.
    - `tests/linear-regression-content.test.mjs` — make all eight chapter teaching groups GREEN.
    - `.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md` — use the Eight-Chapter Mapping and numerical terminology.
  </read_first>
  <files>src/data/linearRegressionModule.ts, src/i18n/messages.ts, tests/linear-regression-content.test.mjs</files>
  <action>Retain the typed module slug, route, checkpoints, controls, presets, simulation callback, and `withTeachingFrame` mapping. Reorder existing IDs exactly to the locked sequence and write complete paired bilingual chapters: (1) real hourly cnt question, leakage guard, representative `instant=11550`, xᵀw+b and sklearn predict; (2) fixed split, canonical vector, train-only scaling, X@w+b and sklearn transform/predict; (3) one residual/loss/gradient contribution to vector/MSE/gradients and sklearn metrics; (4) actual NumPy batch GD trace/stopping plus sklearn fit check; (5) teach “normal equation / 正规方程” with `X_tilde = [1, X]`, `theta = (X_tilde^T X_tilde)^+ X_tilde^T y`, `theta[0] = b`, and `theta[1:] = w`; explicitly explain that `numpy.linalg.lstsq` is the stable numerical implementation used instead of forming an inverse, compare it with NumPy batch GD and sklearn on the identical split/design, then give the concise same-case polynomial behavior; (6) model-space and original-unit coefficients/intercept with conditional noncausal interpretation; (7) held-out MSE/MAE/R², hourly curve, widening spread, expandable resolved cases `17213`, `15628`, `14965`, and `15604`, concise raw/log1p comparison; (8) atemp-only instability, Ridge/Lasso different objectives, combined review panel, linear boundary, and Phase 28 handoff. Each chapter must include core question, formula/variable map, numerical/code connection, typed output binding, misconception and reasoned feedback, selective non-blocking prompt, and next step. Keep sklearn continuous across chapters and use learner-facing plain terms.</action>
  <verify>
    <automated>node --test --test-name-pattern="fit-line|multivariate|residual-loss|training-motion|polynomial|model-limits|overfitting|regularization|bilingual|notation|normal equation|正规方程|augmented|lstsq|three-method|sklearn" tests/linear-regression-content.test.mjs tests/linear-regression-assets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exact chapter order and IDs match research; every old deep-link ID still exists once.
    - Both locales contain all teaching-loop elements and use the same formulas, variable/code identifiers, result IDs, code IDs, method roles, and next-step graph.
    - Chapter 5 contains the exact bilingual normal-equation relation, augmented intercept mapping, stable `lstsq` explanation, and same-split three-method comparison; content tests reject omission or explicit-inverse runtime code.
    - Chapters use only the five resolved representative/named `instant` IDs from typed outputs and preserve their role explanations.
    - Main path uses raw cnt and only the canonical five features; leakage, atemp-only, log1p, polynomial, Ridge, and Lasso scope is stated exactly.
    - Locked full-precision results come from typed assets; prose may show derived readable rounding but contains no second independent result table.
    - Checkpoints remain selective/non-blocking and no assessment bank, backend, remote asset, or second dataset appears.
  </acceptance_criteria>
  <done>The typed module now forms one complete bilingual Bike Sharing learning corridor across all eight preserved chapter IDs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-06-03: Reorder the curriculum adapter and prove progress-safe compatibility</name>
  <read_first>
    - `src/curriculum/adapters/algorithmAdapter.ts` — change only the existing linearRegressionManifest order/metadata.
    - `tests/linear-regression-layout.test.mjs` — make route/order/safe-render groups GREEN.
    - `tests/algorithm-progress.test.ts` — verify exact algorithm storage compatibility.
    - `tests/curriculumProgress.test.ts` — verify non-mutating V1→V2 merge.
    - `src/router/index.ts` — confirm no route edit is required.
    - `src/data/moduleCatalog.ts` — confirm no duplicate/reordering outside the module is required.
  </read_first>
  <files>src/curriculum/adapters/algorithmAdapter.ts, tests/linear-regression-layout.test.mjs, tests/algorithm-progress.test.ts, tests/curriculumProgress.test.ts</files>
  <action>Update only the existing `linearRegressionManifest.lessons` order/titles to match the rebuilt module while preserving slug, root route, literal IDs, curriculum role, prerequisite position, checkpoint/revisit identities, and all storage schemas/keys. Make the preservation/migration tests GREEN: a pre-rebuild record with completed old IDs and checkpoint attempts must resolve after the reorder; V1 raw stores remain byte-equivalent; V2 contains the merged record; every revisit/deep link resolves. Do not modify router, module catalog, navigation menus, checkpoint source, or progress implementations unless a failing compatibility test proves an actual existing bug; any such deviation must remain narrow and documented.</action>
  <verify>
    <automated>node --test tests/linear-regression-content.test.mjs tests/linear-regression-layout.test.mjs tests/linear-regression-assets.test.ts tests/algorithm-progress.test.ts tests/curriculumProgress.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Adapter and module chapter arrays agree exactly on the eight-ID order.
    - Both bespoke route entries remain lazy and ahead of the generic route; base redirect still targets fit-line.
    - Existing completion and checkpoint-attempt fixtures survive unchanged through V1 and V2 reads.
    - No duplicate module, lesson, route, checkpoint, or storage identity is introduced.
    - Focused content/compatibility tests and production build pass.
  </acceptance_criteria>
  <done>The rebuilt content is catalog-compatible and every existing deep link, checkpoint, and progress record remains valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Typed localized content → sanitized renderer | Formula/Markdown/code copy must not introduce unsafe HTML or locale drift. |
| Reordered lesson manifest → stored progress | Existing V1/V2 records must resolve without mutation or loss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-06 | Elevation of Privilege / Information Disclosure | bilingual Markdown/code | high | mitigate | Typed LocalizedCopy, safe renderer assertions, no raw HTML/iframe/script, and matching formula/code IDs. |
| T-27-08 | Tampering | route/checkpoint/progress identities | high | mitigate | Exact literal ID/route/storage fixtures and non-mutating V1→V2 migration tests. |
| T-27-07 | Tampering | content asset bindings | medium | mitigate | Typed local registry IDs and no literal remote/runtime paths. |
</threat_model>

<verification>
Run content, layout, asset, algorithm-progress, and curriculum-progress tests plus `npm run build` and `git diff --check`. Confirm router/checkpoint/progress implementation files are unchanged unless a test-proven narrow deviation was required.
</verification>

<success_criteria>
- Eight complete bilingual chapters implement D-01 through D-28 without deferred scope.
- Formula, code, asset, and method terminology is consistent.
- Existing IDs, routes, checkpoints, and progress records survive the reorder.
- Detailed teaching remains dominant and safe-rendered.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-06-SUMMARY.md` when done.
</output>
