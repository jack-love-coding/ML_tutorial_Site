---
phase: 27-linear-regression-rebuild
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/linear-regression-math.test.ts
  - tests/linear-regression-simulation.test.ts
  - src/simulations/linearRegressionBike.ts
  - src/simulations/linearRegression.ts
autonomous: true
requirements:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "One DOM-free authority implements the canonical Bike feature order, row/batch prediction, prediction-minus-actual residual, MSE gradients, metrics, and coefficient conversion per D-03, D-10, D-15, and D-27."
    - "The fixed first-13,903/train and final-3,476/test membership, train-only population scaling, unscaled workingday, and leakage exclusions are represented exactly per D-04, D-05, and D-14."
    - "The deterministic trace uses zero initialization, learning rate 0.1, cap 5,000, gradient-norm tolerance 1e-8, and the 772-update anchor; three-method agreement is checked at 1e-6 per D-13, D-16, D-17, and D-20."
    - "Pure diagnostic reducers expose hourly residual shape, prediction-bin spread, named cases, atemp-only instability, Ridge objective separation, and concise log1p comparison per D-02 and D-19 through D-24."
    - "The existing simulation import boundary remains stable while California/synthetic values cease to be the primary numerical authority per D-01, D-11, D-25, and D-28."
  artifacts:
    - path: "src/simulations/linearRegressionBike.ts"
      provides: "Guarded Bike regression math, trace, coefficient, and diagnostic authority"
      exports:
        - LINEAR_REGRESSION_FEATURE_ORDER
        - LINEAR_REGRESSION_SPLIT
        - predictRegressionRow
        - predictRegressionBatch
        - evaluateRegressionBatch
        - runBatchGradientDescent
        - convertRegressionCoefficients
        - deriveHeldoutDiagnostics
    - path: "tests/linear-regression-math.test.ts"
      provides: "Wave 0 LINR-01 numerical and guard contract"
    - path: "tests/linear-regression-simulation.test.ts"
      provides: "Wave 0 LINR-04 deterministic Bike simulation contract"
  key_links:
    - from: "src/simulations/linearRegression.ts"
      to: "src/simulations/linearRegressionBike.ts"
      via: "stable facade delegates calculations instead of duplicating formulas"
      pattern: "linearRegressionBike"
    - from: "tests/linear-regression-math.test.ts"
      to: ".planning/phases/27-linear-regression-rebuild/27-RESEARCH.md"
      via: "exact split, scaling, coefficient, metric, convergence, and tolerance anchors"
      pattern: "13903|772|1e-6"
---

<objective>
Create the single pure TypeScript numerical authority for the Phase 27 Bike Sharing case and lock it with failing-first tests.

Purpose: Ensure formulas, bounded browser replay, generated-output parity, and later Vue composition cannot drift.
Output: Guarded pure regression math, deterministic Bike snapshots, and Wave 0 numerical/diagnostic tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md
@.planning/phases/26-loss-functions-rebuild/26-02-SUMMARY.md
</context>

## Artifacts this phase produces

- New file: `src/simulations/linearRegressionBike.ts`.
- New test file: `tests/linear-regression-math.test.ts`.
- New exported constants/types: `LINEAR_REGRESSION_FEATURE_ORDER`, `LINEAR_REGRESSION_CONTINUOUS_FEATURES`, `LINEAR_REGRESSION_SPLIT`, `RegressionFeature`, `RegressionRow`, `RegressionPreprocessing`, `RegressionMetrics`, `RegressionGradient`, `RegressionFit`, `RegressionMethodComparison`, `HeldoutDiagnostics`.
- New exported functions: `predictRegressionRow`, `predictRegressionBatch`, `evaluateRegressionBatch`, `computeMseGradient`, `batchGradientStep`, `runBatchGradientDescent`, `convertRegressionCoefficients`, `compareRegressionMethods`, `deriveHeldoutDiagnostics`.
- Modified stable facade: `src/simulations/linearRegression.ts`; no existing public import used by components or tests is removed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-01-01: Establish Wave 0 numerical and Bike diagnostic contracts</name>
  <read_first>
    - `tests/linear-regression-math.test.ts` — create this owned Wave 0 target.
    - `tests/linear-regression-simulation.test.ts` — preserve useful deterministic scenario coverage while replacing California-specific assertions.
    - `tests/loss-functions-math.test.ts` — copy finite guards, immutable-result, close-to, and finite-difference test structure.
    - `src/simulations/linearRegression.ts` — inventory current public exports and snapshot consumers before writing tests.
    - `.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md` — use Pattern 1–5 anchors and tolerances exactly.
    - `.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md` — own 27-W0-01 and 27-W0-04.
  </read_first>
  <files>tests/linear-regression-math.test.ts, tests/linear-regression-simulation.test.ts</files>
  <behavior>
    - Feature order is exactly temp, hum, windspeed, workingday, hr; atemp is comparison-only and casual/registered are rejected as leakage.
    - Split index is 13,903 with boundary instants 13,903 and 13,904; continuous statistics match research and workingday remains binary.
    - Residual is prediction minus actual; MSE divides by n; analytical weight/intercept gradients match central finite differences on hand-checkable batches.
    - Zero-init GD is finite and deterministic, stops at update 772 for gradient norm <=1e-8, and agrees with the reference within 1e-6.
    - Diagnostic reducers retain nonlinearity, widening-spread, atemp-only coefficient movement, Ridge objective distinction, named cases, and log1p scale labels.
    - Empty/mismatched rows, wrong width/order, zero variance, NaN, Infinity, invalid learning rate/cap, and malformed diagnostics fail closed.
  </behavior>
  <action>Write the missing numerical test and revise the existing simulation test before production implementation. Use small hand-computable fixtures for prediction, residual, MSE/MAE/R², one GD update, finite differences, and coefficient conversion; use the research anchors only for the locked Bike integration cases. Name future-facing test groups so Task 27-01-02 and Task 27-01-03 can make them GREEN without one giant snapshot oracle. Preserve current public facade signatures in the compatibility assertions and cite D-01 through D-05, D-10, D-13 through D-17, D-19 through D-24, and D-27 in test descriptions.</action>
  <verify>
    <automated>node --test --test-name-pattern="hand-checkable|guard|feature order|split scaffold|facade preservation" tests/linear-regression-math.test.ts tests/linear-regression-simulation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files parse under Node 24 and expose filterable groups for formulas, guards, trace, diagnostics, and facade compatibility.
    - Hand-checkable formula/guard and existing-facade preservation groups pass immediately.
    - Missing pure Bike exports and locked snapshots fail only in groups explicitly owned by Tasks 27-01-02/03.
    - No expected repository-wide test count is hard-coded.
  </acceptance_criteria>
  <done>27-W0-01 and 27-W0-04 have executable owners; current scaffolds pass and later numerical groups are explicitly RED on absent Plan 27-01 artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-01-02: Implement guarded row-to-batch regression math and deterministic fitting</name>
  <read_first>
    - `src/simulations/linearRegressionBike.ts` — create this modified target.
    - `tests/linear-regression-math.test.ts` — implement only the contracts established by Task 27-01-01.
    - `src/simulations/lossFunctionsMath.ts` — preserve finite guards, frozen result records, and explicit TypeScript runtime imports.
    - `scripts/python-data-tools/bikeSharingContract.mjs` — preserve column order, source identity, and normalization semantics.
    - `.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md` — Pattern 1–4 is the numerical source of truth.
  </read_first>
  <files>src/simulations/linearRegressionBike.ts, tests/linear-regression-math.test.ts</files>
  <action>Implement the exported immutable types/constants/functions listed in this plan. Fix the feature tuple to `temp`, `hum`, `windspeed`, `workingday`, `hr`; standardize only `temp`, `hum`, `windspeed`, and `hr` with stored train population means/scales; retain `workingday` unchanged; keep intercept separate. Implement `prediction = Xw + b`, residual `prediction - actual`, MSE/MAE/R², `2 X^T r / n`, `2 sum(r) / n`, copied-vector central differences, one batch step, and zero-init batch GD with finite checks, learning rate 0.1, cap 5,000, stop priority non-finite → gradient norm <=1e-8 → cap. Convert standardized coefficients with beta=w/scale and the corresponding intercept shift; reject leakage columns and any malformed/non-finite state. Return readonly copies so Vue cannot mutate the authority.</action>
  <verify>
    <automated>node --test --test-name-pattern="prediction|residual|MSE|MAE|R2|gradient|finite difference|coefficient|GD|guard|split" tests/linear-regression-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All exported arrays/results are readonly copies and the module imports no Vue, DOM, fetch, filesystem, D3, or Three.js API.
    - The research means/scales, split boundaries, model coefficients/intercept, metrics, update 772, gradient norm, and 1e-6 method tolerance pass at full assertion precision.
    - `workingday` is never standardized; `atemp` cannot enter the primary feature tuple; casual/registered input is rejected.
    - Finite-difference coordinates match analytical gradients on smooth fixtures and invalid numeric states throw typed errors.
  </acceptance_criteria>
  <done>The pure TypeScript authority reproduces the locked row/batch, gradient, fitting, metric, and coefficient contracts without DOM or component math.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-01-03: Compose Bike diagnostics behind the stable simulation facade</name>
  <read_first>
    - `src/simulations/linearRegression.ts` — preserve every public import consumed by the existing lesson.
    - `src/simulations/linearRegressionBike.ts` — use the Task 27-01-02 authority, never duplicate formulas.
    - `tests/linear-regression-simulation.test.ts` — make the Bike and compatibility groups GREEN.
    - `src/types/ml.ts` — preserve `TrainingSnapshot` and experiment result contracts.
    - `.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md` — follow the stable facade assignment and Do Not Copy list.
  </read_first>
  <files>src/simulations/linearRegression.ts, tests/linear-regression-simulation.test.ts</files>
  <action>Refactor the existing facade to delegate prediction, residual, fitting replay, metrics, coefficient conversion, and diagnostic reducers to `linearRegressionBike.ts`. Preserve the current exported entry points and readonly snapshot shapes required by components. Replace California-specific primary authority and the hand-rolled linear solver path with Bike-backed states; keep polynomial, overfit, and regularization scenario IDs only as same-case diagnostic adapters. Expose staged states in order: optimization completion (loss, gradient norm, method deltas), then hourly residual curve and prediction-bin spread, then atemp-only OLS coefficient allocation and separately labeled Ridge/Lasso objective states, named held-out records, concise log1p comparison, and combined review panel.</action>
  <verify>
    <automated>node --test tests/linear-regression-math.test.ts tests/linear-regression-simulation.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Existing public facade imports compile and no route/component consumer requires a rename.
    - The primary snapshot contains only Bike Sharing anchors; no California numerical claim or second dataset is exposed.
    - Optimization-complete state precedes diagnostic interpretation and carries loss, gradient norm, and three-method deltas.
    - Hour-8/hour-17/hour-23 residual summaries, spread bins, atemp comparison, named-case roles, Ridge/Lasso objective labels, and log1p scale labels match the research contract.
    - Focused tests and the production build pass.
  </acceptance_criteria>
  <done>The stable simulation facade now presents one deterministic Bike case from fitting through limitations while preserving consumer compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Numeric arrays → pure regression authority | Malformed, non-finite, reordered, or oversized state enters calculations. |
| Locked build results → browser replay | Cross-runtime values must share feature, residual, intercept, and tolerance conventions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-01 | Tampering | feature/order/formula contract | high | mitigate | Literal feature tuple, strict widths, residual sign, hand fixtures, finite differences, and readonly outputs. |
| T-27-05 | Denial of Service | GD and diagnostic reducers | medium | mitigate | Finite guards, bounded iterations, stop priority, bounded arrays, and typed errors. |
| T-27-08 | Tampering | stable simulation facade | medium | mitigate | Public-export regression tests and no duplicated numerical authority. |
</threat_model>

<verification>
Run `node --test tests/linear-regression-math.test.ts tests/linear-regression-simulation.test.ts`, `npm run build`, and `git diff --check`. Confirm `.planning/config.json` and `docs/gpt_advice.md` are unchanged and unstaged.
</verification>

<success_criteria>
- LINR-01 has one tested notation/calculation authority.
- LINR-02/03 replay the locked GD and method-agreement contract.
- LINR-04 diagnostics remain separate from optimizer-completion proof.
- No Vue component or second dataset owns regression calculations.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-01-SUMMARY.md` when done.
</output>
