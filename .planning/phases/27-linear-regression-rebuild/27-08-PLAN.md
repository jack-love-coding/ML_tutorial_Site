---
phase: 27-linear-regression-rebuild
plan: "08"
type: execute
wave: 7
depends_on:
  - 27-04
  - 27-06
  - 27-07
files_modified:
  - tests/linear-regression-release.test.mjs
  - scripts/qa/linearRegressionBrowserMatrix.js
  - src/components/LinearRegressionPagedLesson.vue
  - src/components/LinearRegressionDownloads.vue
  - src/views/AlgorithmView.vue
  - src/styles/modules/linear-regression.css
  - src/styles/modules/linear-regression-responsive.css
autonomous: true
requirements:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "The paged lesson renders the exact eight-ID order, chapter-local results/labs/code, unchanged checkpoint, one consolidated download area, and next-step bridge per D-07 through D-12 and D-25."
    - "All public loading/download links use typed descriptors and withPublicBase; corruption or HTTP failure produces a bilingual static fallback rather than unlocked values per D-06, D-17, D-18, and D-27."
    - "Desktop and 390x844 learners in both locales can navigate every deep link, operate/reset labs, copy code, expand named cases, and access all nine downloads without overflow, overlap, dead links, or color/motion-only meaning per D-23 and D-25."
    - "Learner copy uses plain result/observation/comparison/reference-output terminology per D-26, and no old California/second-dataset or semantically inconsistent media remains per D-01 and D-28."
    - "Offline assets, focused/full tests, standard build, root browser matrix, Pages build/base checks, and security audit all block release."
    - "Final release recomputes the exact protected-file SHA/status/index baseline, enforces explicit staged/worktree allowlists, rejects generated-output residue outside the owned package, and scans executable Phase 27/Notebook code for remote-network, shell, or install paths."
  artifacts:
    - path: "src/components/LinearRegressionDownloads.vue"
      provides: "Localized base-safe download surface for all nine registered assets"
    - path: "scripts/qa/linearRegressionBrowserMatrix.js"
      provides: "Bilingual desktop/mobile root plus eight-deep-link production-preview matrix"
    - path: "tests/linear-regression-release.test.mjs"
      provides: "Final page composition, responsive, fallback, terminology, and browser-script contract"
  key_links:
    - from: "src/components/LinearRegressionPagedLesson.vue"
      to: "src/data/linearRegressionAssets.ts"
      via: "chapter bindings and strict parsed compact summary"
      pattern: "linearRegressionChapterAssets"
    - from: "src/components/LinearRegressionDownloads.vue"
      to: "src/utils/publicPath.ts"
      via: "withPublicBase resolves every registered download"
      pattern: "withPublicBase"
    - from: "scripts/qa/linearRegressionBrowserMatrix.js"
      to: "/learn/linear-regression/:chapterId"
      via: "both locales and desktop/390px production-preview cases"
      pattern: "fit-line|regularization"
---

<objective>
Complete page composition, downloads, responsive/reduced-motion styling, and the full automated release matrix.

Purpose: Ship the rebuilt lesson as an accessible, base-safe, reproducible production experience without compatibility regressions.
Output: Final paged lesson integration, download surface, scoped styles, browser matrix, and all release gates.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md
@.planning/phases/27-linear-regression-rebuild/27-04-SUMMARY.md
@.planning/phases/27-linear-regression-rebuild/27-06-SUMMARY.md
@.planning/phases/27-linear-regression-rebuild/27-07-SUMMARY.md
@.planning/phases/26-loss-functions-rebuild/26-07-SUMMARY.md
</context>

## Artifacts this phase produces

- New files: `src/components/LinearRegressionDownloads.vue`, `scripts/qa/linearRegressionBrowserMatrix.js`, `tests/linear-regression-release.test.mjs`.
- New component symbols: `downloadGroups`, `localizedAssetLabel`, `activeChapterAssetBinding`, `loadChapterSummary`, `abortActiveLoad`.
- Modified page/integration/style files are exactly those listed in frontmatter.
- No new route, ID, checkpoint, progress store, dependency, remote asset, backend, assessment system, or media package is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-08-01: Establish final page, download, responsive, and browser release contracts</name>
  <read_first>
    - `tests/linear-regression-release.test.mjs` — create this final Wave 0 owner.
    - `scripts/qa/linearRegressionBrowserMatrix.js` — create this committed Playwright-CLI probe.
    - `tests/linear-regression-layout.test.mjs` — preserve route/sidebar/pager/preset contracts.
    - `tests/linear-regression-labs.test.mjs` — preserve lab/loading/accessibility contracts from Plan 27-07.
    - `scripts/qa/lossFunctionsBrowserMatrix.js` — copy production-preview matrix structure, console/request/overflow/interaction probes.
    - `src/components/LinearRegressionPagedLesson.vue` — current page shell and old hard-coded rows/visual blocks.
    - `.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md` — complete 27-W0-05 and 27-GATE ownership.
  </read_first>
  <files>tests/linear-regression-release.test.mjs, scripts/qa/linearRegressionBrowserMatrix.js</files>
  <behavior>
    - Source tests require chapter-local typed results/labs, unchanged checkpoint, one consolidated download surface, safe renderer/code-copy, abortable loading, and no component math.
    - Styles require <=1080 stacking, <=720 single-column controls/navigation, 390px no-overflow, focus visibility, non-color cues, and reduced-motion static access.
    - Browser matrix covers zh-CN/en, 1440x1000 and 390x844, root plus all eight deep links, chapter active state, pager/sidebar, row/batch, GD step/reset, method/coefficient/diagnostic controls, named-case expander, code copy, checkpoint, nine downloads, fallback labels, and next step.
    - Matrix rejects overflow/overlap/dead fragment/empty link/console error/new warning/external course-asset request and asserts reduced-motion.
    - A source scan parses Phase 27 TypeScript/Vue/QA/generator files and only Notebook code cells, rejecting remote HTTP/network clients plus shell/install escapes; attribution prose and audited manifest metadata are checked separately and cannot become executable/runtime URLs.
  </behavior>
  <action>Create the source/release test and committed matrix before page edits. The browser script must be a Playwright-CLI `async (page) => {}` program targeting `http://127.0.0.1:4173`, use only local origin requests, set locale through the existing storage key, emulate reduced motion, iterate exact routes/viewports/locales, and return a failing summary when any case violates the contract. Add a named `no remote runtime` test that scans Phase 27 TypeScript/Vue, QA and asset-generator sources, public result URL fields, and parsed code cells from both generated Notebooks. It must fail executable remote `http(s)` URLs, remote-capable client/socket/process imports or calls, shell escapes, package-install commands, and dynamically assembled remote destinations; allow the local preview origin and registered local public paths. Attribution/source URLs are allowed only in localized prose or explicitly audited manifest metadata, never Notebook code cells or runtime URL construction. Add fixture tests for the final scope classifier covering permitted Phase 27 paths, the two exact protected exceptions, and rejection of `dist`, `.cache`, screenshots, unrelated generated public assets, or unrelated staged/worktree files. Source tests must name Task 27-08-02 as the production owner and Task 27-08-03 as the execution owner.</action>
  <verify>
    <automated>node --test --test-name-pattern="release scaffold|browser matrix source|route matrix contract" tests/linear-regression-release.test.mjs tests/linear-regression-layout.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both new files parse and the matrix/source scaffold groups pass.
    - Production page/style/download groups fail only on Task 27-08-02 changes.
    - Matrix includes all 36 locale×viewport×route cases and required interactions/failure probes.
    - The `no remote runtime` and scope-classifier fixtures fail on injected external URL/network/shell/install and out-of-scope paths while permitting only audited attribution metadata and local runtime paths.
    - No runtime source/style file is modified in this task.
  </acceptance_criteria>
  <done>The final production experience and release matrix are fully specified before page composition changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-08-02: Compose the paged lesson, downloads, fallbacks, and scoped responsive styles</name>
  <read_first>
    - `src/components/LinearRegressionPagedLesson.vue` — keep route-derived active chapter, sidebar, pager, MarkdownMathContent, lab/results composition.
    - `src/components/LinearRegressionDownloads.vue` — create from the LossFunctionsDownloads typed descriptor pattern.
    - `src/views/AlgorithmView.vue` — preserve the dedicated lazy linear-regression branch and preset synchronization guard.
    - `src/styles/modules/linear-regression.css` — modify only module-scoped desktop styles.
    - `src/styles/modules/linear-regression-responsive.css` — modify only breakpoints/reduced-motion rules.
    - `src/data/linearRegressionAssets.ts` — use registered descriptors/bindings.
    - `src/components/LossFunctionsDownloads.vue` — reuse base-safe localized download semantics.
    - `tests/linear-regression-release.test.mjs` — make all source/style/page groups GREEN.
  </read_first>
  <files>src/components/LinearRegressionPagedLesson.vue, src/components/LinearRegressionDownloads.vue, src/views/AlgorithmView.vue, src/styles/modules/linear-regression.css, src/styles/modules/linear-regression-responsive.css, tests/linear-regression-release.test.mjs</files>
  <action>Remove hard-coded fuel/residual rows, Vue-computed MSE, old California claims, and inconsistent media bindings from the paged lesson. Compose each chapter from typed module copy, safe Markdown/math, its bound lab/result/code/reference output, sidebar/pager, and unchanged checkpoint/revisit behavior. Load compact summary state through the registered asset/strict parser with abort-on-change/unmount and explicit loading/error/fallback text. Add one `LinearRegressionDownloads` after the checkpoint; group all nine descriptors by role, localize labels, and call `withPublicBase` on descriptor paths. Keep AlgorithmView's existing dedicated branch/lazy routing and preset guard; do not edit router/catalog/checkpoint/progress authorities. Add only module-scoped styles: <=1080 stacked content/result grids, <=720 single-column navigation/actions, 390px safe wrapping/min-width, visible focus, text/shape/pattern status cues, and reduced-motion rules that preserve every essential static fact.</action>
  <verify>
    <automated>node --test tests/linear-regression-content.test.mjs tests/linear-regression-layout.test.mjs tests/linear-regression-labs.test.mjs tests/linear-regression-release.test.mjs tests/linear-regression-assets.test.ts tests/algorithm-progress.test.ts tests/curriculumProgress.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Root and all deep links render the correct chapter in the exact locked order; sidebar/pager/preset/checkpoint/progress behavior remains.
    - Results/labs/code/downloads are placed beside the teaching step they support; the consolidated download area appears once.
    - Every download is a registered base-safe local path and all nine assets are available in both locales.
    - No component contains fit/preprocessing/gradient/metric/coefficient/diagnostic authority or raw unsafe rendering.
    - Desktop/mobile/reduced-motion/non-color source contracts and the production build pass.
  </acceptance_criteria>
  <done>The complete rebuilt course is composed in the existing page shell with local downloads, safe fallbacks, and responsive accessible styling.</done>
</task>

<task type="auto">
  <name>Task 27-08-03: Run the offline, repository, build, browser, Pages, and security release gates</name>
  <read_first>
    - `scripts/qa/linearRegressionBrowserMatrix.js` — run the exact committed matrix.
    - `scripts/linear-regression/build-phase-27-assets.py` — offline read-only package gate.
    - `package.json` — use existing test/build/preview/Pages/security scripts.
    - `tests/linear-regression-release.test.mjs` — final source/static base assertions.
    - `AGENTS.md` — Definition of Done and protected-worktree rules.
  </read_first>
  <files>scripts/qa/linearRegressionBrowserMatrix.js, tests/linear-regression-release.test.mjs</files>
  <action>Run offline asset verification, all focused Phase 27 tests, the complete repository suite, and standard build. Serve the root-base `dist` on 127.0.0.1:4173 and run the committed bilingual desktop/390px root+eight-link matrix before any Pages build overwrites dist; close the preview with an explicitly checked process status. Then run the Pages build and focused static asset/base tests against `/ML_tutorial_Site/`. Run the `no remote runtime` source/asset/Notebook-code scan and security audit. Recompute SHA-256 and require `.planning/config.json` to equal `a30166790b1080df599345c645cd3b38a797d2c8f9ce42bad32075f76d4e958a` and `docs/gpt_advice.md` to equal `31958b9a46fe97c6770228109d47594846ab26b3cdeed4be9bcb3b9d9b729f86`; require their complete short-status output to be exactly ` M .planning/config.json` followed by `?? docs/gpt_advice.md`, and require both paths absent from the staged index. Audit every staged name and every short-status worktree name against the explicit Phase 27 allowlist in the automated command, treating those two exact lines as the only pre-existing dirty exceptions; reject `dist`, `.cache`, `.playwright-cli`, screenshots, unrelated `public/**/generated`, and every unrelated staged/worktree path. Keep `git diff --check` as the final additional gate. No check may replace an error with a default or otherwise hide a failure.</action>
  <verify>
    <automated>
set -euo pipefail &amp;&amp;
python3 scripts/linear-regression/build-phase-27-assets.py --check --offline &amp;&amp;
node --test tests/linear-regression-*.test.* tests/algorithm-progress.test.ts tests/curriculumProgress.test.ts &amp;&amp;
npm test &amp;&amp;
npm run build &amp;&amp;
PWCLI="/Users/jackky/.codex/skills/playwright/scripts/playwright_cli.sh" &amp;&amp;
test -x "$PWCLI" &amp;&amp;
export PLAYWRIGHT_CLI_SESSION=phase-27-linear-regression &amp;&amp;
(
  npm run preview -- --host 127.0.0.1 --port 4173 &gt;/tmp/phase-27-root-preview.log 2&gt;&amp;1 &amp;
  PREVIEW_PID=$!;
  cleanup_preview() {
    if kill -0 "$PREVIEW_PID" 2&gt;/dev/null; then
      kill -TERM "$PREVIEW_PID";
    fi;
    set +e;
    wait "$PREVIEW_PID" 2&gt;/dev/null;
    PREVIEW_RC=$?;
    set -e;
    case "$PREVIEW_RC" in
      0|143) ;;
      *) return "$PREVIEW_RC" ;;
    esac;
  };
  trap cleanup_preview EXIT;
  "$PWCLI" open 'http://127.0.0.1:4173/learn/linear-regression' --headed;
  "$PWCLI" run-code --filename scripts/qa/linearRegressionBrowserMatrix.js;
  cleanup_preview;
  trap - EXIT
) &amp;&amp;
npm run build:pages &amp;&amp;
node --test --test-name-pattern="base|Pages|asset|download" tests/linear-regression-assets.test.ts tests/linear-regression-notebook-assets.test.ts tests/linear-regression-release.test.mjs &amp;&amp;
node --test --test-name-pattern="no remote runtime|scope classifier" tests/linear-regression-release.test.mjs tests/linear-regression-notebook-assets.test.ts &amp;&amp;
npm run security:audit &amp;&amp;
CONFIG_SHA="$(shasum -a 256 .planning/config.json | awk '{print $1}')" &amp;&amp;
ADVICE_SHA="$(shasum -a 256 docs/gpt_advice.md | awk '{print $1}')" &amp;&amp;
test "$CONFIG_SHA" = "a30166790b1080df599345c645cd3b38a797d2c8f9ce42bad32075f76d4e958a" &amp;&amp;
test "$ADVICE_SHA" = "31958b9a46fe97c6770228109d47594846ab26b3cdeed4be9bcb3b9d9b729f86" &amp;&amp;
EXPECTED_PROTECTED_STATUS="$(printf ' M .planning/config.json\n?? docs/gpt_advice.md')" &amp;&amp;
ACTUAL_PROTECTED_STATUS="$(git status --short --untracked-files=all -- .planning/config.json docs/gpt_advice.md)" &amp;&amp;
test "$ACTUAL_PROTECTED_STATUS" = "$EXPECTED_PROTECTED_STATUS" &amp;&amp;
PROTECTED_STAGED="$(git diff --cached --name-only -- .planning/config.json docs/gpt_advice.md)" &amp;&amp;
test -z "$PROTECTED_STAGED" &amp;&amp;
git diff --cached --name-only | while IFS= read -r path; do
  if test -z "$path"; then continue; fi;
  case "$path" in
    dist/*|.cache/*|.playwright-cli/*|*screenshot*|public/*/generated/*)
      printf 'forbidden staged Phase 27 path: %s\n' "$path" &gt;&amp;2;
      exit 1
      ;;
    .planning/phases/27-linear-regression-rebuild/*|.gitignore|scripts/linear-regression/*|scripts/qa/linearRegressionBrowserMatrix.js|tests/linear-regression-*|tests/algorithm-progress.test.ts|tests/curriculumProgress.test.ts|src/simulations/linearRegression.ts|src/simulations/linearRegressionBike.ts|src/data/linearRegressionAssets.ts|src/data/linearRegressionModule.ts|src/i18n/messages.ts|src/curriculum/adapters/algorithmAdapter.ts|src/components/LinearRegression*.vue|src/views/AlgorithmView.vue|src/styles/modules/linear-regression*.css|public/notebooks/linear-regression/*)
      ;;
    *)
      printf 'unrelated staged path: %s\n' "$path" &gt;&amp;2;
      exit 1
      ;;
  esac;
done &amp;&amp;
git status --short --untracked-files=all | while IFS= read -r line; do
  if test -z "$line"; then continue; fi;
  case "$line" in
    " M .planning/config.json"|"?? docs/gpt_advice.md")
      continue
      ;;
  esac;
  path="${line#???}";
  case "$path" in
    dist/*|.cache/*|.playwright-cli/*|*screenshot*|public/*/generated/*)
      printf 'forbidden Phase 27 worktree path: %s\n' "$path" &gt;&amp;2;
      exit 1
      ;;
    .planning/phases/27-linear-regression-rebuild/*|.gitignore|scripts/linear-regression/*|scripts/qa/linearRegressionBrowserMatrix.js|tests/linear-regression-*|tests/algorithm-progress.test.ts|tests/curriculumProgress.test.ts|src/simulations/linearRegression.ts|src/simulations/linearRegressionBike.ts|src/data/linearRegressionAssets.ts|src/data/linearRegressionModule.ts|src/i18n/messages.ts|src/curriculum/adapters/algorithmAdapter.ts|src/components/LinearRegression*.vue|src/views/AlgorithmView.vue|src/styles/modules/linear-regression*.css|public/notebooks/linear-regression/*)
      ;;
    *)
      printf 'unrelated Phase 27 worktree path: %s\n' "$path" &gt;&amp;2;
      exit 1
      ;;
  esac;
done &amp;&amp;
git diff --check
    </automated>
  </verify>
  <acceptance_criteria>
    - Offline package check independently reruns both Notebooks and leaves repository bytes/mtimes unchanged.
    - All focused/full tests, standard build, Pages build, and security audit pass.
    - Browser matrix passes all 36 cases with required interactions and zero overflow, overlap, dead links, console errors, unexpected warnings, or external course-asset requests.
    - Pages static checks prove all nine assets resolve under `/ML_tutorial_Site/`.
    - Protected hashes, exact two-line status baseline, and unstaged-index assertions pass; `.planning/config.json` and `docs/gpt_advice.md` retain their pre-existing dirty states without content drift.
    - Staged and worktree audits admit only explicit Phase 27 paths plus the two exact protected exceptions, reject generated-output residue/unrelated paths, and the no-remote scan passes executable sources and Notebook code cells.
  </acceptance_criteria>
  <done>Phase 27 passes every offline, numerical, content, compatibility, responsive browser, build, Pages, and security gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Typed public assets → page/download DOM | Local assets may still be missing, malformed, or served under a deployment base. |
| Bilingual course copy/code → renderer/clipboard | Rendering and copy must stay on safe project paths. |
| Browser controls/layout → user | Manipulated values, motion, color, or small viewports must not hide or break teaching state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-05 | Denial of Service | page loads/controls/layout | medium | mitigate | Abortable strict loads, bounded controls, responsive tests, safe fallbacks. |
| T-27-06 | Elevation of Privilege | Markdown/code rendering | high | mitigate | Existing sanitized MarkdownMathContent/code-copy path and no raw HTML/iframe/script. |
| T-27-07 | Tampering / Information Disclosure | downloads/Pages paths | medium | mitigate | Typed allowlist, withPublicBase, local-origin browser request probe, root/Pages checks. |
| T-27-08 | Tampering | route/checkpoint/progress/release | high | mitigate | Exact compatibility tests plus full automated release matrix. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One reproducible case connects matrix prediction, MSE gradients, three methods, coefficients, and held-out diagnosis | 01–08 | COVERED | Pure authority → generated outputs → content/UI/release. |
| REQ | LINR-01 | Consistent notation, residuals, gradients, coefficient interpretation | 01, 05–08 | COVERED | Tests, parser, bilingual chapters, labs. |
| REQ | LINR-02 | Locked NumPy batch GD fit | 01–04, 06–08 | COVERED | 772-update authority and two clean-kernel Notebooks. |
| REQ | LINR-03 | Three-method same-split comparison | 01–08 | COVERED | Common matrix/intercept and 1e-6 publication gate. |
| REQ | LINR-04 | Held-out residual/coefficient stability limitations | 01, 03–08 | COVERED | Staged diagnosis and interactive review. |
| RESEARCH | — | Exact Bike source/split/preprocessing/numerical anchors | 01–05 | COVERED | Source bridge, pure math, generator, manifest/parser. |
| RESEARCH | — | Shared bilingual Notebook, atomic publication, offline rerun | 02–05 | COVERED | No package/network/runtime Python change. |
| RESEARCH | — | Resolved deterministic row/case generator contract and frozen instant IDs | 02–08 | COVERED | IDs 11550/17213/15628/14965/15604 flow through manifest, parsers, content, and tests. |
| RESEARCH | — | Resolved media decision: no new Manim/Three.js critical-path asset | 07–08 | COVERED | Deterministic plots/D3/current workbench; data-neutral existing media only. |
| RESEARCH | — | Eight-chapter mapping, typed loading, responsive release | 05–08 | COVERED | Existing shell and identities preserved. |
| CONTEXT | D-01–D-06 | One Bike case/data/split/feature/source contract | 01–06 | COVERED | No second dataset or runtime network. |
| CONTEXT | D-07–D-12 | Preserved eight-ID case-driven teaching spine | 06–08 | COVERED | Detailed bilingual teaching remains dominant. |
| CONTEXT | D-13–D-18 | Method roles, normal-equation teaching, common preprocessing, coefficient spaces, parity | 01–06 | COVERED | Formula/intercept mapping, stable lstsq reference, full outputs/tolerances, and locale parity. |
| CONTEXT | D-19, D-20, D-21, D-22, D-23, D-24 | Staged limitations, named records, atemp/Ridge, log1p | 01, 03–08 | COVERED | Guided sequence plus combined review. |
| CONTEXT | D-25–D-28 | Compatibility, terminology, pure calculations, need-driven media | 01, 05–08 | COVERED | Tests/browser gate; no new media critical path. |

Deferred Phase 28 project/pipeline work, broader feature engineering, backend/cloud assessment, and exercise-bank ideas are excluded by context and are not gaps.

<verification>
Execute Task 27-08-03's multiline command exactly, then inspect `git status --short` and scoped diff. No manual-only verification remains.
</verification>

<success_criteria>
- The existing route/checkpoint/progress-compatible lesson is fully rebuilt around one reproducible Bike case.
- Both locales and all eight deep links work at desktop and 390px with reduced motion.
- All numerical, generated, runtime, and page values share one audited contract.
- Standard/Pages builds, offline package, tests, browser matrix, and security audit pass.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-08-SUMMARY.md` when done.
</output>
