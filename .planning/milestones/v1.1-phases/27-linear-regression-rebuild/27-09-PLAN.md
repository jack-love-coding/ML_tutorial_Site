---
phase: 27-linear-regression-rebuild
plan: "09"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/simulations/linearRegressionWorkbench.ts
  - src/simulations/linearRegression.ts
  - src/simulations/linearRegressionBike.ts
  - tests/linear-regression-authority.test.ts
  - tests/linear-regression-simulation.test.ts
  - tests/linear-regression-math.test.ts
autonomous: true
requirements:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "Every runtime teaching row, prediction, residual, prediction bin, named case, method result, coefficient view, and GD trace point comes from the strict published Bike package or an exact audited baseline derived from it per D-01 through D-06."
    - "The synchronous simulation facade no longer invents feature/target values or rounds held-out diagnostics independently of the published outputs per D-11, D-13 through D-17, and D-19 through D-24."
    - "Pure, bounded selectors outside Vue expose the six workbench result contracts without fitting, preprocessing, metric recomputation, DOM access, or network access per D-17 and D-27."
    - "Cross-authority tests compare the source CSV, strict summary, complete GD trace, complete coefficient table, complete held-out residual table, and simulation baseline."
  artifacts:
    - path: "src/simulations/linearRegressionWorkbench.ts"
      provides: "Validated published-package assembly, exact audited baseline, and six pure workbench selectors"
    - path: "tests/linear-regression-authority.test.ts"
      provides: "Cross-authority drift gate spanning source CSV, all four published result files, and runtime facade"
    - path: "src/simulations/linearRegression.ts"
      provides: "Existing synchronous simulation facade backed by the audited published baseline"
  key_links:
    - from: "src/simulations/linearRegressionWorkbench.ts"
      to: "src/data/linearRegressionAssets.ts"
      via: "strict parsed summary/trace/coefficient/residual contracts"
      pattern: "LinearRegressionLockedSummary|LinearRegressionGradientTraceRow|LinearRegressionCoefficientRow|LinearRegressionResidualRow"
    - from: "tests/linear-regression-authority.test.ts"
      to: "public/notebooks/linear-regression"
      via: "real published files parsed with production parsers"
      pattern: "parseLinearRegressionOutput"
    - from: "src/simulations/linearRegression.ts"
      to: "src/simulations/linearRegressionWorkbench.ts"
      via: "exact baseline and diagnostic selectors"
      pattern: "LINEAR_REGRESSION_PUBLISHED_BASELINE"
---

<objective>
Remove the drifted second Bike authority and provide exact package-backed selectors for the interactive lesson.

Purpose: Restore the Phase 27 promise that formulas, source rows, published outputs, simulation snapshots, and later UI controls all describe the same reproducible case per D-01 through D-06 and D-11 through D-24.
Output: A pure workbench data layer, an exact synchronous simulation baseline, and cross-authority drift tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VERIFICATION.md
@.planning/phases/27-linear-regression-rebuild/27-03-SUMMARY.md
@.planning/phases/27-linear-regression-rebuild/27-05-SUMMARY.md
@.planning/phases/27-linear-regression-rebuild/27-07-SUMMARY.md
</context>

## Artifacts this phase produces

- New files: `src/simulations/linearRegressionWorkbench.ts`, `tests/linear-regression-authority.test.ts`.
- New symbols: `LinearRegressionWorkbenchPackage`, `createLinearRegressionWorkbenchPackage`, `LINEAR_REGRESSION_PUBLISHED_BASELINE`, `selectRowBatchResult`, `selectGradientTracePoint`, `selectMethodResult`, `selectCoefficientResult`, `selectHeldoutCase`, `selectAtempComparison`.
- Modified facade/test files are exactly those listed in frontmatter.
- No package, remote data source, runtime fit, new dataset, route, checkpoint, progress store, media asset, or Phase 28 implementation is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-09-01: Define the strict workbench package and six pure selectors</name>
  <read_first>
    - `src/data/linearRegressionAssets.ts` — reuse strict summary, GD trace, coefficient, and residual row types/parsers; do not weaken them.
    - `public/notebooks/linear-regression/linear-regression-summary.json` — canonical compact result authority.
    - `public/notebooks/linear-regression/gradient-descent-trace.csv` — canonical 773-row update authority.
    - `public/notebooks/linear-regression/coefficients.csv` — canonical 24-row method/space authority.
    - `public/notebooks/linear-regression/heldout-residuals.csv` — canonical 3,476-row held-out authority.
    - `public/datasets/python-data-tools/bike-sharing-hour.csv` — sole source-row authority per D-01.
    - `tests/linear-regression-assets.test.ts` — follow strict parser and immutability expectations.
    - `.planning/phases/27-linear-regression-rebuild/27-VERIFICATION.md` — close the second-authority and label-only-control gaps.
  </read_first>
  <files>src/simulations/linearRegressionWorkbench.ts, tests/linear-regression-authority.test.ts</files>
  <behavior>
    - Package construction accepts only a strict `LinearRegressionLockedSummary`, 773 ordered GD rows covering updates 0 through 772, 24 coefficient rows with the published method/space/feature keys, and 3,476 ordered held-out rows.
    - Package construction rejects missing, duplicate, non-finite, out-of-range, or cross-file-inconsistent rows rather than filling values.
    - Row/batch selection returns the representative source row for `row` and published train-shape/metric information for `batch`.
    - GD selection clamps an integer step to the published trace and returns that exact row.
    - Method and coefficient selectors return exact published values for NumPy GD, normal-equation/lstsq, and sklearn plus model-space/original-unit choices.
    - Named-case selection resolves the four locked roles to exact residual-table rows and confirms summary prediction/actual/residual parity.
    - The atemp selector returns the published off/on comparison and coefficient-stability values without fitting a model in TypeScript.
  </behavior>
  <action>Create a dependency-free pure TypeScript workbench module per D-01 through D-06, D-13 through D-17, D-19 through D-24, and D-27. Define readonly discriminated result types for all six controls and a constructor that validates cross-file counts, update ordering, named-case instants, prediction-minus-actual residual identity, feature order, split sizes, and finite numbers. Store only the exact compact values needed by the synchronous facade in `LINEAR_REGRESSION_PUBLISHED_BASELINE`; generate/copy them from the current strict package at full precision and freeze them. The baseline is not permitted to introduce a value absent from the source/published files. Write the cross-authority test against the real source/public files, production parsers, and selector outputs; compare every baseline field, all four named cases, all prediction-bin summaries, selected GD anchors including 0 and 772, method metrics/deltas, and coefficient keys. Include mutation and malformed-package rejection cases. Do not import Vue, DOM APIs, fetch, sklearn/Python execution, or component code.</action>
  <verify>
    <automated>node --test tests/linear-regression-authority.test.ts tests/linear-regression-assets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The strict constructor accepts the committed four-file package and rejects count/order/key/cross-file drift.
    - All six selectors return exact, finite, frozen values with deterministic bounds.
    - Source instant `11550` and held-out instants `17213`, `15628`, `14965`, and `15604` match source/published rows at full available precision.
    - The baseline, bins, named cases, method values, coefficients, and trace anchors have a direct automated equality path to published authority.
    - The new module contains no UI, DOM, runtime network, fitting, scaling, metric, or gradient computation.
  </acceptance_criteria>
  <done>The lesson has one strict package-backed data contract and six pure result selectors that cannot silently drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-09-02: Replace invented facade records and approximate diagnostics</name>
  <read_first>
    - `src/simulations/linearRegression.ts` — remove the invented display-record table while preserving the public simulation callback shape.
    - `src/simulations/linearRegressionBike.ts` — retain pure math/validation functions and remove the approximate held-out fixture as an independent authority.
    - `src/simulations/linearRegressionWorkbench.ts` — consume only the exact audited baseline/selectors from Task 27-09-01.
    - `tests/linear-regression-simulation.test.ts` — replace rounded fixture assertions with package-backed parity assertions.
    - `tests/linear-regression-math.test.ts` — build diagnostic input from the audited baseline instead of the removed approximate constant.
    - `tests/linear-regression-authority.test.ts` — extend the gate through every simulation snapshot field that represents the Bike case.
  </read_first>
  <files>src/simulations/linearRegression.ts, src/simulations/linearRegressionBike.ts, tests/linear-regression-simulation.test.ts, tests/linear-regression-math.test.ts, tests/linear-regression-authority.test.ts</files>
  <behavior>
    - `simulateLinearRegression(config)` preserves its existing callback contract and seven-stage snapshot ordering.
    - Snapshot rows, predictions, targets, residuals, hourly summaries, prediction bins, named cases, method comparisons, coefficient stability, and atemp comparison equal the audited published baseline.
    - Existing pure gradient, OLS, metric, diagnostic, and validation functions keep their mathematical behavior and edge-case rejection.
    - Scenario/config bounds continue to reject or clamp NaN, Infinity, and invalid indices through existing safe paths.
  </behavior>
  <action>Refactor the synchronous facade to consume `LINEAR_REGRESSION_PUBLISHED_BASELINE` and the pure diagnostic builders from Task 27-09-01 per D-11, D-13 through D-17, D-19 through D-24, and D-27. Delete the invented `BIKE_DISPLAY_RECORDS` authority and replace the approximate exported held-out diagnostic constant with an exact package-derived input/builder; retain `deriveHeldoutDiagnostics` and all reusable pure math APIs. Preserve `simulateLinearRegression`, the existing module registration contract, scenario names, snapshot count, safe input handling, and prediction-minus-actual sign. Update math/simulation tests to derive expected values from parsed public outputs or the cross-checked baseline, never from a second handwritten expectation table. Extend the authority test so future edits to any facade case/bin/metric/trace anchor fail against the real package.</action>
  <verify>
    <automated>node --test tests/linear-regression-authority.test.ts tests/linear-regression-simulation.test.ts tests/linear-regression-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No invented Bike display table or approximate held-out fixture remains as an independent runtime/test authority.
    - All seven facade snapshots retain their expected roles and source every Bike-specific number from the audited baseline.
    - Simulation and math tests pass without blessing rounded copies of bins, cases, or coefficients.
    - Cross-authority tests fail if a facade prediction, residual, bin, named case, method result, or coefficient changes independently.
    - No product asset, route, chapter ID, checkpoint, progress key, dependency, or generated Notebook file changes.
  </acceptance_criteria>
  <done>The existing simulation facade is compatible with callers while numerically identical to the sole published Bike authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source CSV and published result files → runtime teaching state | Committed local files are authoritative but may be malformed, mutually inconsistent, or edited independently. |
| Control values → pure selectors | User-controlled indices/enums must not create NaN, out-of-range access, or hidden recomputation. |
| Audited baseline → synchronous simulation facade | A duplicated compact baseline could drift unless equality is enforced against the real package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-G09-01 | Tampering | published package/workbench constructor | high | mitigate | Strict production parsers, count/key/order/cross-file validation, frozen outputs, and real-file tests. |
| T-27-G09-02 | Denial of Service | selector indices and numeric inputs | medium | mitigate | Finite integer bounds, exhaustive enums, and explicit rejection of malformed packages. |
| T-27-G09-03 | Repudiation | compact facade baseline | medium | mitigate | Field-by-field equality from source/public files through tests; no unaudited literal is accepted. |
</threat_model>

<verification>
Run both task commands, then `node --test tests/linear-regression-*.test.*` and `npm run build`.
</verification>

<success_criteria>
- One audited Bike authority drives the source row, published outputs, simulation facade, and six pure selector contracts.
- The existing simulation API and pure mathematical functions remain compatible.
- Any future cross-authority numerical drift fails automated tests before UI execution.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-09-SUMMARY.md` when done.
</output>
