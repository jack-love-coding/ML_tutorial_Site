---
phase: 27-linear-regression-rebuild
plan: "05"
type: execute
wave: 4
depends_on:
  - 27-01
  - 27-04
files_modified:
  - tests/linear-regression-assets.test.ts
  - src/data/linearRegressionAssets.ts
autonomous: true
requirements:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "Runtime consumers can load only the nine registered local assets through base-safe paths and strict readonly types per D-06, D-17, D-18, D-23, and D-25."
    - "The summary parser requires the exact dataset hash, 13,903/3,476 split, feature order, unscaled workingday, residual sign, method tolerance, finite values, and bounded result shapes per D-03 through D-05 and D-14."
    - "Malformed, stale, unknown, non-finite, oversized, or cross-generation assets fail closed; no component silently repairs or invents a numerical result per D-27."
    - "Typed chapter bindings expose the representative row, method comparison, coefficients, held-out diagnostics, named cases, and downloads needed for D-10, D-15, and D-19 through D-24."
    - "The parser freezes representative instant 11550 and named-case instants 17213, 15628, 14965, and 15604 together with their deterministic role/selection contract."
    - "Root and GitHub Pages base paths work without a remote runtime fallback per D-25."
  artifacts:
    - path: "src/data/linearRegressionAssets.ts"
      provides: "Typed local asset registry, strict locked-summary parser, and chapter bindings"
      exports:
        - linearRegressionAssets
        - linearRegressionChapterAssets
        - parseLinearRegressionSummary
        - getLinearRegressionAsset
    - path: "tests/linear-regression-assets.test.ts"
      provides: "Wave 0 asset, parser, manifest, base-path, and fallback contract"
  key_links:
    - from: "src/data/linearRegressionAssets.ts"
      to: "public/notebooks/linear-regression/output-manifest.json"
      via: "exact typed registry and strict contract/version/hash validation"
      pattern: "linear-regression-summary"
    - from: "src/data/linearRegressionAssets.ts"
      to: "src/utils/publicPath.ts"
      via: "components resolve registered leading-slash paths through withPublicBase"
      pattern: "publicPath"
---

<objective>
Create the strict TypeScript boundary between published regression assets and browser/course consumers.

Purpose: Prevent stale, malformed, non-finite, or deployment-base-broken generated outputs from entering the lesson.
Output: Typed asset registry, readonly summary schema/parser, chapter bindings, and Wave 0 corruption/base-path tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md
@.planning/phases/27-linear-regression-rebuild/27-01-SUMMARY.md
@.planning/phases/27-linear-regression-rebuild/27-04-SUMMARY.md
@.planning/phases/26-loss-functions-rebuild/26-06-SUMMARY.md
</context>

## Artifacts this phase produces

- New files: `src/data/linearRegressionAssets.ts`, `tests/linear-regression-assets.test.ts`.
- New types/constants: `linearRegressionAssetIds`, `LinearRegressionAssetId`, `LinearRegressionAssetKind`, `LinearRegressionAssetDescriptor`, `LinearRegressionChapterId`, `LinearRegressionLockedSummary`, `LinearRegressionMethodResult`, `LinearRegressionNamedCase`, `LinearRegressionDiagnostics`, `linearRegressionAssets`, `linearRegressionChapterAssets`.
- New functions: `getLinearRegressionAsset`, `parseLinearRegressionSummary`, `parseLinearRegressionOutput`, `validateLinearRegressionManifestContract`.
- No runtime fetch is implemented in this plan; components consume the registry/parser in Plans 27-07/08.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-05-01: Establish Wave 0 strict asset and corruption contracts</name>
  <read_first>
    - `tests/linear-regression-assets.test.ts` — create this owned Wave 0 target.
    - `tests/loss-functions-compatibility.test.ts` — reuse strict registry/parser, identity, finite, and base-path assertions.
    - `tests/linear-regression-notebook-assets.test.ts` — consume the exact publication inventory and hash proofs; do not duplicate generator rules inconsistently.
    - `public/notebooks/linear-regression/output-manifest.json` — exact nine-member public authority.
    - `public/notebooks/linear-regression/linear-regression-summary.json` — strict runtime schema input.
    - `src/utils/publicPath.ts` — preserve root and Pages path behavior.
    - `.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md` — own 27-W0-02 parser/base portions.
  </read_first>
  <files>tests/linear-regression-assets.test.ts</files>
  <behavior>
    - Registry IDs/paths/inventory exactly cover the nine public files and contain no remote or unregistered path.
    - Parser requires contract/version/source hash, feature order, split rows/boundaries, residual sign, finite values, exact method keys, 1e-6 tolerance, bounded arrays, and known statuses.
    - Wrong/missing/extra keys, nonfinite values, malformed rows/bins/cases, wrong feature/order/split/hash/sign/tolerance, unknown method/status, and oversized arrays throw.
    - Representative/named-row role metadata must use exact IDs 11550, 17213, 15628, 14965, and 15604 and the resolved IQR/hour/residual/exclusion/lowest-instant selection contract.
    - Paired notebooks share topic/bundle and code/output parity proof.
    - Every path resolves locally for `/` and `/ML_tutorial_Site/`.
  </behavior>
  <action>Write the test before the registry/parser. Use deep-cloned corruption fixtures derived from the committed summary/manifest and one assertion per trust boundary. Assert exact asset IDs and leading-slash paths, manifest coverage in both directions, file existence, source/dataset identity, strict parser rejection, readonly output, chapter binding IDs, local-only URLs, and base resolution. Do not hard-code learner display rounding as parser precision and do not accept an unknown key by omission.</action>
  <verify>
    <automated>node --test --test-name-pattern="public inventory scaffold|base path|manifest existence" tests/linear-regression-assets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new test parses and current public inventory/existence/base scaffolds pass.
    - Parser and typed-registry groups fail only on absent `linearRegressionAssets.ts`.
    - Corruption cases cover every 27-W0-02 secure behavior listed in validation.
    - No network request or component implementation is introduced.
  </acceptance_criteria>
  <done>27-W0-02 has a complete runtime asset/parser owner before browser consumption code exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-05-02: Implement the typed registry, strict parser, and chapter bindings</name>
  <read_first>
    - `src/data/linearRegressionAssets.ts` — create this target.
    - `tests/linear-regression-assets.test.ts` — implement exactly the Task 27-05-01 contract.
    - `src/data/lossFunctionsAssets.ts` — copy literal-union registry, `satisfies`, strict helpers, readonly result, and topic pairing patterns.
    - `src/data/linearRegressionModule.ts` — use only the eight preserved chapter IDs; do not modify content yet.
    - `public/notebooks/linear-regression/linear-regression-summary.json` — exact schema and values.
    - `public/notebooks/linear-regression/output-manifest.json` — exact descriptor/inventory metadata.
  </read_first>
  <files>src/data/linearRegressionAssets.ts, tests/linear-regression-assets.test.ts</files>
  <action>Define literal asset IDs/descriptors for both Notebooks, summary, three CSVs, requirements, environment, and manifest; every path begins with `/notebooks/linear-regression/`. Pair locale Notebooks under one topic and bind only registered output/download IDs to preserved chapters. Implement fail helpers that require exact object keys and version, source SHA, row/split/boundary counts, canonical feature/continuous order, workingday policy, leakage list, residual sign, method roles, tolerance, finite recursively bounded arrays, the five resolved representative/named-case IDs and deterministic role-selection metadata, diagnostic statuses, and immutable copies. Dispatch parsing only for registered JSON output IDs and throw on unknown IDs. Keep URL resolution out of the parser; consumers must pass descriptor paths to `withPublicBase`.</action>
  <verify>
    <automated>node --test tests/linear-regression-assets.test.ts tests/linear-regression-notebook-assets.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All nine descriptors are typed, local, manifest-backed, existent, and grouped by exact semantic role.
    - The parser accepts the committed summary and rejects every mutated fixture from Task 27-05-01.
    - Mutating any resolved teaching-row ID, selection rule, role, exclusion, or tie-break causes parser rejection.
    - Returned arrays/records are readonly copies; no `any`, silent coercion, defaulted unknown field, or numerical recomputation exists.
    - Chapter bindings use only `fit-line`, `multivariate`, `residual-loss`, `training-motion`, `polynomial`, `model-limits`, `overfitting`, `regularization`.
    - Focused asset/Notebook tests and TypeScript production build pass.
  </acceptance_criteria>
  <done>Published results cross one strict typed boundary before any lesson content or Vue component consumes them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public JSON/manifest → TypeScript domain | Static bytes are untrusted until strict versioned parsing completes. |
| Typed descriptors → browser URL | Base-path and allowlist errors can load wrong or remote resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-04 | Tampering | summary/manifest contract | high | mitigate | Exact keys/version/hash/order/count/tolerance/finite validation and corruption tests. |
| T-27-05 | Denial of Service | runtime summary parser | medium | mitigate | Bounded arrays, exact shapes, finite checks, immutable results, and fail-fast typed errors. |
| T-27-07 | Tampering / Information Disclosure | asset registry/base paths | medium | mitigate | Literal allowlist, leading-slash local paths, manifest bidirectional coverage, and root/Pages tests. |
</threat_model>

<verification>
Run focused asset and Notebook tests, `npm run build`, `git diff --check`, and verify no unregistered/remote path. Confirm protected unrelated files remain untouched.
</verification>

<success_criteria>
- Every public asset is typed, local, base-safe, and manifest-backed.
- Every locked numerical value is accepted only through strict finite parsing.
- Corrupt/stale/wrong-generation output fails closed.
- Later plans can consume readonly typed chapter results without duplicating paths or math.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-05-SUMMARY.md` when done.
</output>
