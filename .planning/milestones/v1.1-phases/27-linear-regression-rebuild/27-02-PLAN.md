---
phase: 27-linear-regression-rebuild
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - scripts/linear-regression/verify-bike-source.mjs
  - scripts/linear-regression/build-phase-27-assets.py
  - scripts/linear-regression/requirements.txt
  - scripts/linear-regression/environment-contract.json
  - tests/linear-regression-notebook-assets.test.ts
autonomous: true
requirements:
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "The sole source is the committed 17,379-row Bike snapshot and its existing UCI/DOI/CC BY 4.0/hash/schema contract; ordinary generation has no network path per D-01 and D-06."
    - "One indivisible candidate inventory owns both independently executed locale Notebooks, summary, complete coefficient/GD/residual tables, requirements, environment, and manifest per D-17 and D-18."
    - "The generator freezes feature order, leakage exclusions, chronological split, train-only scaling, unscaled workingday, common intercept convention, and unregularized method roles per D-03 through D-05 and D-13 through D-16."
    - "Wave 0 requires bilingual normal-equation terminology/formula mapping and the deterministic representative/named-row role contract before candidate generation per D-10, D-13, and D-23."
    - "Staging is ignored and outside public; invalid source, environment, cells, outputs, inventory, or partial selection cleans the candidate and publishes nothing."
    - "No registry package resolution is introduced; only the already audited Phase 26 exact pins and local wheel hashes may be reused."
  artifacts:
    - path: "scripts/linear-regression/verify-bike-source.mjs"
      provides: "Thin JSON bridge to the existing Bike contract without a second CSV parser"
    - path: "scripts/linear-regression/build-phase-27-assets.py"
      provides: "Offline candidate/environment/Notebook transaction CLI"
      contains: "--prepare-candidates"
    - path: "tests/linear-regression-notebook-assets.test.ts"
      provides: "Wave 0 LINR-02/03 candidate, kernel, parity, safety, and staging contract"
  key_links:
    - from: "scripts/linear-regression/verify-bike-source.mjs"
      to: "scripts/python-data-tools/bikeSharingContract.mjs"
      via: "imports and invokes the existing source/schema/hash authority"
      pattern: "verifyBikeSharingSnapshot"
    - from: "scripts/linear-regression/build-phase-27-assets.py"
      to: "scripts/linear-regression/environment-contract.json"
      via: "exact offline wheel/Python/platform verification before candidate work"
      pattern: "no-index|offline"
---

<objective>
Establish the fail-closed offline environment, source bridge, complete candidate inventory, and bilingual Notebook execution shell.

Purpose: Make later full-data generation reproducible and prevent partial or network-dependent learner assets.
Output: Wave 0 Notebook tests, exact environment inputs, staging-only generator, and source-contract bridge.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md
@.planning/phases/26-loss-functions-rebuild/26-03-SUMMARY.md
</context>

## Artifacts this phase produces

- New files: `scripts/linear-regression/verify-bike-source.mjs`, `scripts/linear-regression/build-phase-27-assets.py`, `scripts/linear-regression/requirements.txt`, `scripts/linear-regression/environment-contract.json`, `tests/linear-regression-notebook-assets.test.ts`.
- New Python symbols: `CONTRACT_VERSION`, `FEATURE_ORDER`, `CONTINUOUS_FEATURES`, `SPLIT_INDEX`, `EXPECTED_CANDIDATE_FILES`, `NotebookJob`, `CandidateTransaction`, `verify_environment`, `verify_source_contract`, `build_notebook_blueprint`, `verify_candidate_inventory`, `prepare_candidates`, `main`.
- New CLI modes: `--verify-environment --offline`, `--prepare-candidates --staging-root .cache/linear-regression/phase-27-staging --offline`, and `--verify-candidates ... --offline`; publication/check modes are reserved for Plan 27-04.
- New ignored path: `/.cache/linear-regression/phase-27-staging`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-02-01: Establish Wave 0 candidate, environment, and Notebook contracts</name>
  <read_first>
    - `tests/linear-regression-notebook-assets.test.ts` — create this owned Wave 0 target.
    - `tests/loss-functions-notebook-assets.test.ts` — reuse candidate inventory, fresh-kernel, parity, corruption, rollback, and repository-clean patterns.
    - `scripts/loss-functions/requirements.txt` — exact inherited eight-pin source; do not change pins.
    - `scripts/loss-functions/environment-contract.json` — preserve local wheelhouse identity and isolated execution fields.
    - `.gitignore` — add only the exact Phase 27 staging root.
    - `.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md` — own 27-W0-03 and the candidate/publication portions of 27-W0-02.
  </read_first>
  <files>tests/linear-regression-notebook-assets.test.ts, scripts/linear-regression/requirements.txt, scripts/linear-regression/environment-contract.json, .gitignore</files>
  <behavior>
    - Candidate inventory is exact and indivisible: two locale Notebooks, summary JSON, GD trace CSV, coefficient CSV, held-out residual CSV, requirements, environment contract, and output manifest.
    - Ordered code cells are identical across locales; markdown is localized; each Notebook has a distinct fresh-kernel proof; normalized numerical outputs match.
    - Network, registry resolution, shell commands, install cells, raw HTML/scripts, widgets, uncontrolled iframes, public staging, and partial locale/file selectors are rejected.
    - Exact requirements, Python/platform, wheel filenames/bytes/hashes, isolated kernel settings, and cleanup are validated before candidate work.
    - Source/split/feature/order/tolerance tampering and unexpected/missing candidate files fail closed.
    - Notebook/output tests require “normal equation / 正规方程”, `X_tilde = [1, X]`, the pseudoinverse relation, `theta[0] = b` and `theta[1:] = w`, the `numpy.linalg.lstsq` stability explanation, and the three-method comparison.
    - Selection tests require ordinary `instant=11550` and held-out role instants `17213`, `15628`, `14965`, and `15604`, including the deterministic IQR/residual/hour filters and lowest-`instant` tie-breaks.
  </behavior>
  <action>Create the test file first and copy the exact existing eight-pin requirements plus audited environment identity into the Phase 27 script directory. Add only `/.cache/linear-regression/phase-27-staging` to `.gitignore`. Structure named test groups for inventory, environment, kernel, locale parity, numerical contract, normal-equation bilingual teaching/mapping, deterministic teaching-row roles/IDs, safety, staging, candidate verification, publication, rollback, offline rerun, base paths, and corruption. Assert the conceptual augmented-design/pseudoinverse formula, `theta` to `b`/`w` mapping, stable `lstsq` implementation rationale, three-method comparison, all five resolved `instant` IDs, and their exact deterministic selection/tie-break rules. Current environment/inventory scaffolds must pass; later candidate/public groups may be RED only on artifacts assigned to Plans 27-03/04.</action>
  <verify>
    <automated>node --test --test-name-pattern="inventory scaffold|environment contract|safety|staging scaffold" tests/linear-regression-notebook-assets.test.ts &amp;&amp; git check-ignore -q .cache/linear-regression/phase-27-staging</automated>
  </verify>
  <acceptance_criteria>
    - Requirements match the inherited eight-pin file byte-for-byte and no package/pin is added.
    - Environment and staging scaffold groups pass without creating a public file or contacting a registry.
    - Candidate, publication, and offline groups name Plans 27-03/04 as their owners.
    - 27-W0-03 and candidate/publication coverage for 27-W0-02 are executable and filterable.
  </acceptance_criteria>
  <done>The exact offline environment and complete-package Wave 0 contracts exist before generator implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-02-02: Implement source verification and the staging-only candidate shell</name>
  <read_first>
    - `scripts/linear-regression/verify-bike-source.mjs` — create this target as a bridge, not a parser.
    - `scripts/linear-regression/build-phase-27-assets.py` — create this target.
    - `scripts/python-data-tools/bikeSharingContract.mjs` — import its columns, SHA helper, artifact validation, and committed snapshot verification.
    - `public/datasets/python-data-tools/manifest.json` — exact dataset identity, count, bytes, schema version, and hash.
    - `public/datasets/python-data-tools/data-dictionary.json` — target/leakage/normalization semantics.
    - `public/datasets/python-data-tools/bike-sharing-hour.csv` — immutable local input; never rewrite it.
    - `scripts/loss-functions/build-phase-26-assets.py` — copy candidate transaction, environment verification, kernel isolation, code safety, and cleanup architecture.
    - `tests/linear-regression-notebook-assets.test.ts` — make the Task 27-02-01 scaffold groups GREEN.
  </read_first>
  <files>scripts/linear-regression/verify-bike-source.mjs, scripts/linear-regression/build-phase-27-assets.py, tests/linear-regression-notebook-assets.test.ts</files>
  <action>Implement `verify-bike-source.mjs` as a thin importer of the existing Bike contract that validates the committed CSV/manifest/dictionary and emits deterministic JSON containing source hash, rows, schema, row order, feature roles, target relationship, and boundary records. In Python, implement a standard-library outer process with exact constants `SPLIT_INDEX=13903`, canonical feature/continuous tuples, nine candidate paths, and one shared ordered code blueprint plus two complete markdown dictionaries. Verify the inherited environment only from the audited local wheelhouse with no index/network and unique temporary venv/kernel/Jupyter directories; reject drift before Notebook work and clean all temporary state in `finally`. `--prepare-candidates` may create only a fresh ignored non-public transaction and may not accept subset selectors; leave full calculation/execution to Plan 27-03 and all public mutation to Plan 27-04.</action>
  <verify>
    <automated>python3 -m py_compile scripts/linear-regression/build-phase-27-assets.py &amp;&amp; node scripts/linear-regression/verify-bike-source.mjs &gt; /tmp/phase-27-bike-source.json &amp;&amp; node --test --test-name-pattern="source bridge|inventory|environment|kernel|safety|staging|cleanup" tests/linear-regression-notebook-assets.test.ts &amp;&amp; python3 scripts/linear-regression/build-phase-27-assets.py --verify-environment --offline</automated>
  </verify>
  <acceptance_criteria>
    - The bridge imports `bikeSharingContract.mjs`; it does not implement a second CSV parser/hash/schema.
    - Source verification reports 17,379 rows and SHA-256 `e03de4ee4ef4dc376ac6e04bf829673c6269e8eba5c60fa121640fa2f829504f`.
    - Environment verification proves every exact pin/wheel/import and leaves no temp venv, kernelspec, or scoped Jupyter state.
    - Candidate shell rejects public roots, subsets, remote URLs, unsafe cells, stale environment/source identity, and cleans injected failures.
    - No file under `public/notebooks/linear-regression/` is created by this plan.
  </acceptance_criteria>
  <done>The immutable Bike source, exact offline environment, shared bilingual code shell, and non-public transaction boundary pass fail-closed tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Committed CSV/manifest/dictionary → generator | Static data identity and schema enter full-data computation. |
| Audited wheel cache → isolated Notebook environment | Third-party package bytes execute local generated code. |
| Generator blueprint → learner Notebook | Generated code becomes downloadable executable content. |
| Candidate staging → public tree | This plan must be technically unable to publish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-02 | Tampering | Bike source/split/feature contract | high | mitigate | Existing contract bridge, exact hashes/schema/order/counts, boundary rows, and leakage rejection. |
| T-27-03 | Elevation of Privilege / Information Disclosure | Notebook environment and cells | high | mitigate | Exact audited wheels, offline isolation, static code deny checks, fresh kernels, and cleanup. |
| T-27-04 | Tampering | candidate inventory | high | mitigate | Exact indivisible allowlist, non-public staging, hashes, and no subset selectors. |
| T-27-SC | Tampering | inherited Python packages | high | mitigate | Reuse only existing audited exact pins and local wheel hashes; no registry install, upgrade, or alternate distribution. |
</threat_model>

<verification>
Run the source bridge, Python compilation, offline environment verification, focused Notebook tests, ignore-path check, and `git diff --check`. Verify `git diff -- public` is empty and protected dirty files are unchanged.
</verification>

<success_criteria>
- One existing Bike contract remains the only data authority.
- The two locale Notebooks share one code source and require independent kernels.
- Candidate work is offline, complete-package-only, cleanup-safe, and outside public.
- No dependency pin or package source changes.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-02-SUMMARY.md` when done.
</output>
