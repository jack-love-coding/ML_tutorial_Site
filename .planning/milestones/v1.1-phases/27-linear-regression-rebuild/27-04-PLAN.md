---
phase: 27-linear-regression-rebuild
plan: "04"
type: execute
wave: 3
depends_on:
  - 27-03
files_modified:
  - scripts/linear-regression/build-phase-27-assets.py
  - tests/linear-regression-notebook-assets.test.ts
  - public/notebooks/linear-regression/bike-linear-regression.zh-CN.ipynb
  - public/notebooks/linear-regression/bike-linear-regression.en.ipynb
  - public/notebooks/linear-regression/linear-regression-summary.json
  - public/notebooks/linear-regression/gradient-descent-trace.csv
  - public/notebooks/linear-regression/coefficients.csv
  - public/notebooks/linear-regression/heldout-residuals.csv
  - public/notebooks/linear-regression/requirements.txt
  - public/notebooks/linear-regression/environment-contract.json
  - public/notebooks/linear-regression/output-manifest.json
autonomous: true
requirements:
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "All nine public members publish as one lock-scoped directory transaction only after source, numerical, locale, environment, inventory, and hash verification per D-05, D-06, D-17, and D-18."
    - "Initial publication supports an absent target with no backup, while replacement supports a seeded complete target; injected failures restore exact prior absence or exact prior bytes/modes and remove lock/temporary/staging/backup residue."
    - "Offline check copies the public package outside the repository, independently reruns both Notebooks, re-derives outputs, and proves repository bytes/sizes/mtimes unchanged."
    - "The public package preserves complete residuals and compact page values without runtime network or partial publication per D-23 and D-25."
    - "No learner-facing asset is accepted when method tolerances, hashes, feature/split/residual contracts, code/output parity, or finite-value checks fail."
  artifacts:
    - path: "public/notebooks/linear-regression/output-manifest.json"
      provides: "Atomic package inventory, integrity proofs, and offline rerun contract"
    - path: "public/notebooks/linear-regression/heldout-residuals.csv"
      provides: "Complete downloadable 3,476-row held-out residual output"
    - path: "scripts/linear-regression/build-phase-27-assets.py"
      provides: "Complete-only atomic publisher, rollback paths, and read-only offline check"
      contains: "--publish-candidates"
  key_links:
    - from: "scripts/linear-regression/build-phase-27-assets.py"
      to: "public/notebooks/linear-regression/output-manifest.json"
      via: "validate-copy-lock-swap-final-verify transaction"
      pattern: "publish-candidates|rollback"
    - from: "tests/linear-regression-notebook-assets.test.ts"
      to: "public/notebooks/linear-regression/bike-linear-regression.en.ipynb"
      via: "external fresh-kernel rerun, parity, and repository-clean proof"
      pattern: "offline|standalone|parity"
---

<objective>
Publish the complete Phase 27 asset package atomically and prove offline, read-only reproducibility.

Purpose: Ensure learners never receive mixed-generation or stale Notebook/output assets.
Output: Exact nine-member public package, rollback-safe publisher, and independent offline integrity gate.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md
@.planning/phases/27-linear-regression-rebuild/27-03-SUMMARY.md
@.planning/phases/26-loss-functions-rebuild/26-05-SUMMARY.md
</context>

## Artifacts this phase produces

- New public files: the exact nine paths listed in frontmatter under `public/notebooks/linear-regression/`.
- New generator functions: `acquire_publication_lock`, `verify_public_bundle`, `publish_candidates_atomically`, `restore_public_backup`, `rerun_public_notebook`, `verify_repository_clean`, `offline_check`.
- New CLI modes: `--publish-candidates --staging-root .cache/linear-regression/phase-27-staging` and `--check --offline`.
- No dataset is duplicated; the existing `public/datasets/python-data-tools/` package remains the referenced source authority.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-04-01: Lock complete-only publication, rollback, and corruption behavior</name>
  <read_first>
    - `tests/linear-regression-notebook-assets.test.ts` — extend the Plan 27-02/03 Wave 0 owner.
    - `scripts/linear-regression/build-phase-27-assets.py` — publication implementation target.
    - `tests/loss-functions-notebook-assets.test.ts` — copy atomic swap, rollback, standalone rerun, repository-clean, and base-path failure injections.
    - `.cache/linear-regression/phase-27-staging/public/notebooks/linear-regression/output-manifest.json` — exact validated candidate inventory from Plan 27-03.
    - `.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md` — complete 27-W0-02/03 and release-integrity ownership.
  </read_first>
  <files>tests/linear-regression-notebook-assets.test.ts, scripts/linear-regression/build-phase-27-assets.py</files>
  <behavior>
    - Publication refuses missing/extra members, subsets, source/environment/generator/hash drift, nonfinite JSON, tolerance failures, and invalid CSV shapes before public mutation.
    - Lock contention fails without changing public bytes.
    - Tests begin from both an absent target and a seeded complete existing target, then cover initial-publication success and replacement success as complete-directory atomic moves.
    - Pre-, mid-, post-swap, final-verify, and interrupted transactions restore the exact prior state: absent remains absent; existing files retain exact bytes and modes.
    - Success removes backup, private staging, and lock residue only after final verification.
    - Offline check reruns each public Notebook in a separate external temp package/kernel with external network blocked and rejects any repository byte/size/mtime change.
  </behavior>
  <action>Add the publication and offline groups first, including corruption probes for source SHA, row order, split boundary, leakage feature, workingday scaling, residual sign, method tolerance, locked metric, code/output parity, file hash, unexpected/missing file, and transaction failures. Run the complete matrix from two fixtures: no `public/notebooks/linear-regression/` target, and a seeded complete target whose files include recorded bytes and permission modes. Implement one directory-ownership transaction: verify the exact candidate outside public, acquire the exact lock, materialize and verify a complete private sibling temporary directory, then atomically move that directory into the target when the target was absent without creating a backup. For replacement, atomically move the prior complete target to backup, atomically move the complete temporary directory into place, verify final bytes/semantics, and only then delete backup/lock. Every exception or interrupt must restore the recorded prior state exactly—remove a newly installed target when the prior state was absent, or restore the seeded target with byte/mode identity—and remove lock, temporary target, candidate staging, and backup residue. Test failure injection at candidate verification, lock, temporary preparation, target/backup moves, post-move verification, cleanup, and interruption. Do not expose a locale/file/topic selector.</action>
  <verify>
    <automated>node --test --test-name-pattern="publication|absent target|seeded existing target|replacement|inventory|subset|rollback|lock|mode|residue|corruption" tests/linear-regression-notebook-assets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Failure injection covers every transaction stage and exact package corruption class.
    - Initial-publication and replacement success expose only complete nine-member directories; no subset is visible at any transaction boundary.
    - Every injected failure leaves prior absence unchanged or the seeded public directory byte-and-mode identical, with no lock/backup/private temporary/candidate-staging residue.
    - Candidate provenance remains bound to the generator identity that created it; later publisher edits do not rewrite candidate bytes.
    - The publication command has exactly one complete-package path and no partial selector.
  </acceptance_criteria>
  <done>Atomicity, rollback, lock, inventory, hash, and corruption behavior are executable before the public swap.</done>
</task>

<task type="auto">
  <name>Task 27-04-02: Publish the verified nine-member package as one transaction</name>
  <read_first>
    - `scripts/linear-regression/build-phase-27-assets.py` — use Task 27-04-01 publisher only.
    - `.cache/linear-regression/phase-27-staging/public/notebooks/linear-regression/output-manifest.json` — candidate authority.
    - `public/notebooks/linear-regression/output-manifest.json` — create/replace only through the atomic directory transaction.
    - `tests/linear-regression-notebook-assets.test.ts` — run publication and public-integrity groups.
  </read_first>
  <files>scripts/linear-regression/build-phase-27-assets.py, tests/linear-regression-notebook-assets.test.ts, public/notebooks/linear-regression/bike-linear-regression.zh-CN.ipynb, public/notebooks/linear-regression/bike-linear-regression.en.ipynb, public/notebooks/linear-regression/linear-regression-summary.json, public/notebooks/linear-regression/gradient-descent-trace.csv, public/notebooks/linear-regression/coefficients.csv, public/notebooks/linear-regression/heldout-residuals.csv, public/notebooks/linear-regression/requirements.txt, public/notebooks/linear-regression/environment-contract.json, public/notebooks/linear-regression/output-manifest.json</files>
  <action>Run the complete-only publisher once against the exact Plan 27-03 staging root from the expected first-publication state where `public/notebooks/linear-regression/` is absent. Preserve candidate bytes exactly; do not hand-edit public JSON, CSV, Notebook, requirements, environment, or manifest files. The successful path must atomically move one complete verified sibling candidate directory into the absent target without requiring or leaving a backup. Re-read and validate the final public directory after the move, including exact nine-member inventory, every file hash/byte count, source/split/feature/residual contract, finite strict JSON, CSV rows/headers, code/output parity, environment identity, method tolerance, named-case IDs, and local dataset references. Retain the seeded-existing-target replacement success test from Task 27-04-01 as the separate proof that later regeneration also swaps only complete packages.</action>
  <verify>
    <automated>python3 scripts/linear-regression/build-phase-27-assets.py --publish-candidates --staging-root .cache/linear-regression/phase-27-staging &amp;&amp; node --test --test-name-pattern="publication|public inventory|public hash|strict JSON|CSV|parity" tests/linear-regression-notebook-assets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exactly nine declared public members exist and match the candidate hashes/bytes.
    - First publication succeeds from an absent target without backup residue, and the seeded replacement fixture succeeds without partial visibility.
    - Both Notebooks, all three CSV/JSON result families, requirements, environment, and manifest belong to one generation.
    - Complete residual CSV has exactly 3,476 data rows with prediction-minus-actual residuals.
    - No transaction residue, partial package, remote runtime URL, or unregistered file exists.
  </acceptance_criteria>
  <done>The exact candidate package is learner-visible as one complete, verified public directory.</done>
</task>

<task type="auto">
  <name>Task 27-04-03: Prove standalone offline reruns and repository integrity</name>
  <read_first>
    - `scripts/linear-regression/build-phase-27-assets.py` — implement/use the read-only `--check --offline` path.
    - `tests/linear-regression-notebook-assets.test.ts` — make standalone, repository-clean, parity, and base-path groups GREEN.
    - `public/notebooks/linear-regression/output-manifest.json` — exact rerun inputs and expected proofs.
    - `src/utils/publicPath.ts` — use its root/Pages path semantics in tests; do not modify it.
  </read_first>
  <files>scripts/linear-regression/build-phase-27-assets.py, tests/linear-regression-notebook-assets.test.ts</files>
  <action>Implement `--check --offline` as a read-only gate: snapshot hash, byte size, and mtime for every git-visible repository entry; copy the public package and referenced Bike dataset contract into an external temporary root; build the exact no-index environment; rerun each locale Notebook separately with a fresh kernel and external DNS/socket access blocked except Jupyter loopback; canonicalize Notebook representation; compare code, normalized outputs, regenerated summary/CSVs, hashes, feature/split/tolerance contracts, and environment proofs; remove all external temp state; finally require the repository snapshot to be unchanged.</action>
  <verify>
    <automated>python3 scripts/linear-regression/build-phase-27-assets.py --check --offline &amp;&amp; node --test tests/linear-regression-notebook-assets.test.ts &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Both public Notebooks rerun independently from only local inputs and exact local wheels.
    - External network attempts, repository writes, locale drift, numerical drift, hash drift, and base-path drift fail.
    - Root and `/ML_tutorial_Site/` resolutions reference only local package/dataset paths.
    - The check leaves every repository byte, size, and mtime unchanged and cleans all external environments/kernels.
  </acceptance_criteria>
  <done>The published package is independently reproducible offline and the verification path is demonstrably read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Ignored candidate → public course assets | A partial or corrupted generation must never replace learner-visible bytes. |
| Public Notebooks → isolated rerun | Downloadable code must reproduce without external network or repository mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-04 | Tampering / Repudiation | public package transaction | high | mitigate | Exact allowlist/hashes, lock, private staging, final verification, full rollback, and residue tests. |
| T-27-03 | Elevation of Privilege / Information Disclosure | public Notebook rerun | high | mitigate | External temp package, exact wheels, fresh kernels, blocked external sockets, safe-cell checks. |
| T-27-07 | Tampering / Information Disclosure | public paths | medium | mitigate | Local allowlist and root/Pages base-path tests; no remote runtime URL. |
| T-27-SC | Tampering | isolated Python environment | high | mitigate | Existing audited exact no-index wheel hashes only. |
</threat_model>

<verification>
Run atomic publication tests, publish once, run `--check --offline`, run the full Notebook test, scan for exact transaction residue, and run `git diff --check`. Confirm only the declared public directory and plan-owned files changed.
</verification>

<success_criteria>
- Public assets are one indivisible generation.
- Failure cannot leave a mixed or missing package.
- Both learner Notebooks reproduce offline in independent clean kernels.
- Complete residual output is downloadable and all integrity checks are read-only.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-04-SUMMARY.md` when done.
</output>
