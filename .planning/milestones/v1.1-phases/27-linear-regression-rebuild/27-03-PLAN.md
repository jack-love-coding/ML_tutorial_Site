---
phase: 27-linear-regression-rebuild
plan: "03"
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - scripts/linear-regression/build-phase-27-assets.py
  - tests/linear-regression-notebook-assets.test.ts
autonomous: true
requirements:
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "Full-data calculation uses raw cnt, the canonical five features, atemp only in the controlled comparison, and no casual/registered inputs per D-02 through D-04."
    - "All methods use the same first-80/final-20 membership, train-only scaling, unscaled workingday, transformed matrix, and intercept convention per D-05 and D-14."
    - "The bilingual Notebook teaches the normal equation as `X_tilde = [1, X]` and `theta = (X_tilde^T X_tilde)^+ X_tilde^T y`, maps `theta[0]`/`theta[1:]` to `b`/`w`, and explains why the stable code authority is numpy.linalg.lstsq rather than explicit inversion per D-13."
    - "NumPy batch GD, the normal-equation/lstsq numerical reference, and sklearn LinearRegression fulfill their distinct roles and pass 1e-6 coefficient/prediction agreement before candidates can validate per D-13, D-16, and D-17."
    - "Both locale Notebooks derive from one code blueprint, execute independently, and produce identical normalized numerical outputs per D-09 and D-18."
    - "The staged package includes complete coefficient/trace/residual outputs and staged diagnosis in the locked order per D-19 through D-24."
    - "Deterministic selection freezes training instant 11550 and held-out instants 17213, 15628, 14965, and 15604 with role definitions and lowest-instant tie-breaks per D-10 and D-23."
  artifacts:
    - path: ".cache/linear-regression/phase-27-staging/notebooks/linear-regression/bike-linear-regression.zh-CN.ipynb"
      provides: "Independently executed Chinese learner Notebook candidate"
    - path: ".cache/linear-regression/phase-27-staging/notebooks/linear-regression/bike-linear-regression.en.ipynb"
      provides: "Independently executed English learner Notebook candidate"
    - path: ".cache/linear-regression/phase-27-staging/notebooks/linear-regression/linear-regression-summary.json"
      provides: "Versioned compact page/runtime numerical authority candidate"
    - path: ".cache/linear-regression/phase-27-staging/notebooks/linear-regression/output-manifest.json"
      provides: "Complete candidate hashes, proofs, contract, and rerun metadata"
  key_links:
    - from: "scripts/linear-regression/build-phase-27-assets.py"
      to: "src/simulations/linearRegressionBike.ts"
      via: "shared formulas, feature order, residual sign, stopping constants, and parity fixtures"
      pattern: "temp.*hum.*windspeed.*workingday.*hr"
    - from: "output-manifest.json"
      to: "bike-linear-regression.zh-CN.ipynb"
      via: "fresh-kernel proof, code hash, normalized-output hash, and file hash"
      pattern: "codeHash|outputHash|sha256"
---

<objective>
Generate and verify the complete ignored Bike Sharing regression candidate package.

Purpose: Produce the one numerical authority that later public assets and runtime parsers can consume without browser fitting or hand-copied values.
Output: Two independently executed bilingual Notebooks, compact summary, complete tables, exact environment copies, and one verified manifest in ignored staging.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VALIDATION.md
@.planning/phases/27-linear-regression-rebuild/27-01-SUMMARY.md
@.planning/phases/27-linear-regression-rebuild/27-02-SUMMARY.md
@.planning/phases/26-loss-functions-rebuild/26-04-SUMMARY.md
</context>

## Artifacts this phase produces

- Ignored candidate files: `bike-linear-regression.zh-CN.ipynb`, `bike-linear-regression.en.ipynb`, `linear-regression-summary.json`, `gradient-descent-trace.csv`, `coefficients.csv`, `heldout-residuals.csv`, `requirements.txt`, `environment.json`, `output-manifest.json`, each under `.cache/linear-regression/phase-27-staging/notebooks/linear-regression/`.
- New Python functions in the existing generator: `load_verified_bike_frame`, `make_chronological_split`, `fit_training_scaler`, `build_model_matrix`, `run_numpy_batch_gd`, `fit_lstsq_reference`, `fit_sklearn_reference`, `compute_method_deltas`, `compute_residual_diagnostics`, `compute_collinearity_comparison`, `compute_log_target_comparison`, `select_teaching_rows`, `execute_notebook_job`, `normalize_notebook_outputs`, `write_candidate_bundle`.
- No public file or runtime TypeScript/Vue file is created or modified in this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-03-01: Compute and lock the full Bike fit, method parity, and held-out diagnostics</name>
  <read_first>
    - `scripts/linear-regression/build-phase-27-assets.py` — extend the Plan 27-02 staging shell.
    - `tests/linear-regression-notebook-assets.test.ts` — make only numerical/source candidate groups GREEN.
    - `src/simulations/linearRegressionBike.ts` — mirror formula order, residual sign, and constants.
    - `public/datasets/python-data-tools/bike-sharing-hour.csv` — immutable input.
    - `public/datasets/python-data-tools/data-dictionary.json` — feature roles and bilingual meanings.
    - `.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md` — use Pattern 1–5 full-precision anchors.
  </read_first>
  <files>scripts/linear-regression/build-phase-27-assets.py, tests/linear-regression-notebook-assets.test.ts</files>
  <behavior>
    - Source contract, exact boundary rows, feature/leakage roles, train-only statistics, unscaled workingday, and residual sign are asserted before fitting.
    - GD uses zero initialization, learning rate 0.1, cap 5,000, gradient tolerance 1e-8 and stops at 772; lstsq uses an augmented ones column; sklearn uses the unaugmented matrix with fit_intercept true.
    - Bilingual method metadata and Notebook content contain “normal equation / 正规方程”, `X_tilde = [1, X]`, `theta = (X_tilde^T X_tilde)^+ X_tilde^T y`, the intercept/weight mapping, and the reason `numpy.linalg.lstsq` is used instead of forming an inverse.
    - Coefficients/intercepts/predictions pass absolute 1e-6 method assertions and retain full precision in outputs.
    - Test metrics approximate 40142.538619 MSE, 135.296640 MAE, 0.174252 R² only after agreement establishes optimizer completion.
    - Hour residual, prediction-bin spread, named held-out cases, atemp-only OLS/Ridge/Lasso comparison, and log1p count-scale comparison are deterministic and correctly qualified.
  </behavior>
  <action>Implement the full calculation model inside the generator using NumPy/pandas/sklearn only in the isolated environment. Split by row before statistics; explicitly reconstruct the canonical transformed columns; never standardize workingday or use atemp/casual/registered in the primary design. Represent the teaching relation as `X_tilde = [1, X]` and `theta = (X_tilde^T X_tilde)^+ X_tilde^T y`, map `theta[0]` to `b` and `theta[1:]` to `w`, but keep `numpy.linalg.lstsq` as the runtime reference rather than explicitly forming a matrix inverse; store rank/singular values/condition number and run actual zero-init batch GD. Select the ordinary training row as minimum absolute residual among rows with raw `cnt` in the inclusive training IQR, tie by lowest `instant`, and assert `instant=11550`. Select held-out roles deterministically: minimum raw-count prediction (`17213`); maximum positive `actual - prediction` for `hr` 7–9 (`15628`); the same for `hr` 16–19 (`14965`); then maximum absolute residual excluding those rows (`15604`), always tie by lowest `instant`. Use `actual - prediction` only for underprediction ranking while storing residual as `prediction - actual`. Compute hourly residual means, prediction-quartile residual SD/MAE, atemp correlation/condition/coefficient/prediction/error comparison, deterministic perturbation plus Ridge stability, separately labeled Lasso objective, and a concise log1p comparison with inverse-transformed count metrics. Write strict finite intermediate candidate JSON/CSV files only under the transaction root.</action>
  <verify>
    <automated>python3 scripts/linear-regression/build-phase-27-assets.py --prepare-data-candidates --staging-root .cache/linear-regression/phase-27-staging --offline &amp;&amp; node --test --test-name-pattern="source|split|feature|leakage|scaler|GD|normal equation|正规方程|augmented|lstsq|sklearn|three-method|method delta|representative row|named case|residual|atemp|Ridge|Lasso|log1p" tests/linear-regression-notebook-assets.test.ts tests/linear-regression-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Full-precision model coefficients/intercept, train/test metrics, update count, gradient norm, condition number, and method deltas match research tolerances.
    - Complete trace, coefficient table, and all 3,476 held-out residual rows are present in staging and finite.
    - The selected row/cases are stable instant IDs with timestamp, actual, prediction, residual, and bilingual explanation-role IDs.
    - Output metadata asserts the bilingual normal-equation terminology/formula, augmented intercept convention, `theta` to `b`/`w` mapping, stable `lstsq` rationale, and all three unregularized method roles without any runtime inverse implementation.
    - Selection logic and output freeze `11550`, `17213`, `15628`, `14965`, and `15604`; mutation tests fail changed filters, role order, exclusions, tie-breaks, or IDs.
    - OLS comparison changes only atemp; Ridge/Lasso carry different-objective metadata; log1p results are not compared on raw objective scale.
    - Any source/split/scaler/leakage/tolerance/nonfinite drift deletes the failed candidate and leaves public unchanged.
  </acceptance_criteria>
  <done>The complete Bike calculation and diagnostic model is locked in ignored staging and passes cross-runtime numerical contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-03-02: Execute both locale Notebooks and seal the candidate manifest</name>
  <read_first>
    - `scripts/linear-regression/build-phase-27-assets.py` — use the shared blueprint and candidate calculations from Task 27-03-01.
    - `tests/linear-regression-notebook-assets.test.ts` — make clean-kernel, parity, manifest, and candidate corruption groups GREEN.
    - `scripts/loss-functions/build-phase-26-assets.py` — reuse NotebookClient job isolation, normalization, exact inventory, and failure cleanup.
    - `scripts/linear-regression/requirements.txt` — exact execution pins.
    - `scripts/linear-regression/environment-contract.json` — execution identity.
  </read_first>
  <files>scripts/linear-regression/build-phase-27-assets.py, tests/linear-regression-notebook-assets.test.ts</files>
  <action>Build the Chinese and English learner Notebooks from separate complete markdown dictionaries around one ordered byte-identical code-cell blueprint. In both locales, teach “normal equation / 正规方程”, the augmented-design/pseudoinverse relation, intercept/weight mapping, and why executed code calls `numpy.linalg.lstsq` instead of explicitly inverting the Gram matrix; present NumPy batch GD, this non-iterative reference, and sklearn as the same-split three-method comparison. Execute each Notebook in its own fresh kernel with `allow_errors=False`, deterministic working directory, clean execution counts, no external sockets, no shell/install/raw HTML/widget cells, and normalized machine-independent metadata. Require code-source parity and normalized numerical-output parity while allowing localized markdown. Write the exact nine-member candidate inventory, byte sizes, SHA-256 hashes, semantic roles, source/split/feature/residual contract, the five resolved row/case IDs plus selection-rule version, environment identity, distinct kernel proof IDs, code/output hashes, 1e-6 method assertions, rerun command, and generator identity into `output-manifest.json`; then verify the whole candidate again from disk.</action>
  <verify>
    <automated>python3 scripts/linear-regression/build-phase-27-assets.py --prepare-candidates --staging-root .cache/linear-regression/phase-27-staging --offline &amp;&amp; python3 scripts/linear-regression/build-phase-27-assets.py --verify-candidates --staging-root .cache/linear-regression/phase-27-staging --offline &amp;&amp; node --test tests/linear-regression-notebook-assets.test.ts tests/linear-regression-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exactly nine candidate files exist at the declared staging paths and every non-manifest member hash/byte count validates.
    - Both Notebooks execute independently with distinct proof IDs; code cell IDs/sources and normalized outputs are identical; markdown locale differs.
    - Notebook-visible tables include full coefficients, GD trace/stopping, method deltas/tolerances, metrics, diagnostics, named cases, and assertions.
    - Both Notebook variants contain the required bilingual normal-equation formula/terms and stable `lstsq` mapping, and their outputs/manifest lock the five deterministic `instant` IDs.
    - Manifest verification rejects changed source hash/order/split/feature/scaler/residual sign/tolerance/output/environment/code or unexpected/missing files.
    - `git diff -- public` remains empty and the failed-candidate cleanup probe preserves no residue.
  </acceptance_criteria>
  <done>A complete nine-member ignored candidate package passes independent clean-kernel, bilingual parity, numerical, inventory, and corruption verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Verified Bike rows → model matrices | Feature leakage, split drift, and preprocessing leakage can corrupt every result. |
| Full-data calculations → strict candidate assets | Non-finite or out-of-tolerance results must never become downloadable output. |
| Shared code → two independent kernels | Locale drift or hidden environment state can falsify reproducibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-01 | Tampering | formula/method conventions | high | mitigate | Cross-runtime fixtures, exact matrix/intercept conventions, finite checks, and 1e-6 assertions. |
| T-27-02 | Tampering | split/preprocessing/features | high | mitigate | Exact boundary IDs, train-only statistics, unscaled binary feature, and leakage denylist. |
| T-27-03 | Elevation of Privilege / Repudiation | Notebook jobs | high | mitigate | Generator-owned safe cells, distinct clean kernels, offline sockets, code/output proofs. |
| T-27-04 | Tampering | candidate package | high | mitigate | Strict nine-member manifest, hashes, schema/version, corruption tests, cleanup, and no public write. |
| T-27-SC | Tampering | Python packages | high | mitigate | Exact audited no-index wheel environment only. |
</threat_model>

<verification>
Run candidate preparation and verification, focused Notebook/math tests, `git diff --check`, `git check-ignore`, and a public-tree diff. Do not stage ignored candidates or protected unrelated files.
</verification>

<success_criteria>
- The locked NumPy batch-GD fit is independently reproduced in both Notebooks.
- Three unregularized methods agree under one split/matrix/intercept contract.
- Held-out limitation diagnostics remain visible despite optimization agreement.
- Candidate publication is still impossible in this wave.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-03-SUMMARY.md` when done.
</output>
