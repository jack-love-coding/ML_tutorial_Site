---
phase: 27-linear-regression-rebuild
plan: "10"
type: execute
wave: 2
depends_on:
  - 27-09
gap_closure: true
files_modified:
  - src/components/LinearRegressionLessonLab.vue
  - src/components/LinearRegressionPagedLesson.vue
  - tests/linear-regression-labs.test.mjs
  - tests/linear-regression-release.test.mjs
  - scripts/qa/linearRegressionBrowserMatrix.js
autonomous: true
requirements:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
requirements_addressed:
  - LINR-01
  - LINR-02
  - LINR-03
  - LINR-04
must_haves:
  truths:
    - "Each of the six existing lab controls changes a corresponding visible numerical or visual result, not only a label, using the exact package-backed selectors from Plan 27-09."
    - "The lab loads and strictly parses the summary, complete GD trace, complete coefficient table, and complete held-out residual table through registered base-safe local paths per D-06, D-17, D-18, D-23, D-25, and D-27."
    - "Both locales expose the same control capabilities, exact records, method comparisons, coefficient spaces, atemp comparison, reset behavior, and understandable invalid/load fallback."
    - "The last linear-regression chapter links to the existing canonical Phase 28 housing project route with an explicit bilingual Phase 28 handoff, while all eight chapter routes/checkpoints/progress behavior remain unchanged per D-07 through D-12 and D-25."
    - "The committed 36-case browser matrix proves semantic output changes, the Phase 28 destination, responsive safety, reduced motion, local requests, and clean console/link behavior."
  artifacts:
    - path: "src/components/LinearRegressionLessonLab.vue"
      provides: "Package-backed six-control workbench with exact visible result panels"
    - path: "src/components/LinearRegressionPagedLesson.vue"
      provides: "Exact bilingual Phase 28 housing-project bridge"
    - path: "scripts/qa/linearRegressionBrowserMatrix.js"
      provides: "Semantic bilingual desktop/mobile control and bridge assertions"
  key_links:
    - from: "src/components/LinearRegressionLessonLab.vue"
      to: "src/simulations/linearRegressionWorkbench.ts"
      via: "six pure selectors over a strictly parsed package"
      pattern: "selectRowBatchResult|selectGradientTracePoint|selectMethodResult|selectCoefficientResult|selectHeldoutCase|selectAtempComparison"
    - from: "src/components/LinearRegressionLessonLab.vue"
      to: "src/data/linearRegressionAssets.ts"
      via: "registered descriptors, withPublicBase fetches, and parseLinearRegressionOutput"
      pattern: "linear-regression-gradient-descent-trace|linear-regression-coefficients|linear-regression-heldout-residuals"
    - from: "src/components/LinearRegressionPagedLesson.vue"
      to: "src/curriculum/routeManifest.ts"
      via: "canonical existing Phase 28 housing project route"
      pattern: "/learn/housing-price-project"
---

<objective>
Wire all six workbench controls to exact visible results and restore the intended Phase 28 learning-path handoff.

Purpose: Close the observable teaching-loop gaps without changing route identities, checkpoints, progress storage, lesson ordering, or the published numerical authority per D-07 through D-28.
Output: A genuinely interactive package-backed lab, an exact bilingual Phase 28 bridge, and semantic source/browser tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-linear-regression-rebuild/27-CONTEXT.md
@.planning/phases/27-linear-regression-rebuild/27-RESEARCH.md
@.planning/phases/27-linear-regression-rebuild/27-PATTERNS.md
@.planning/phases/27-linear-regression-rebuild/27-VERIFICATION.md
@.planning/phases/27-linear-regression-rebuild/27-09-SUMMARY.md
@.planning/phases/27-linear-regression-rebuild/27-07-SUMMARY.md
@.planning/phases/27-linear-regression-rebuild/27-08-SUMMARY.md
</context>

## Artifacts this phase produces

- New runtime files: none.
- Modified component/test/QA files are exactly those listed in frontmatter.
- Stable semantic hooks: `linear-output-row-batch`, `linear-output-gd-trace`, `linear-output-method`, `linear-output-coefficient-space`, `linear-output-heldout-case`, `linear-output-atemp-comparison`, and `linear-phase-28-bridge`.
- No new route, module ID, checkpoint, progress source, UI framework, package, backend, remote asset, critical 3D/Manim dependency, or Phase 28 project implementation is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-10-01: Make all six lab controls change exact teaching results</name>
  <read_first>
    - `src/components/LinearRegressionLessonLab.vue` — replace summary-only state and label-only readout while preserving controls, reset, keyboard labels, and the lesson's existing non-3D, reduced-motion-safe architecture; do not add a 3D rendering dependency.
    - `src/simulations/linearRegressionWorkbench.ts` — use the six selectors from Plan 27-09; do not reproduce their calculations in Vue.
    - `src/data/linearRegressionAssets.ts` — fetch registered local descriptors and call the overloaded strict output parser.
    - `src/utils/publicPath.ts` — retain GitHub Pages-safe path resolution.
    - `tests/linear-regression-labs.test.mjs` — replace skipped semantic checks with executable source/data contracts.
    - `tests/linear-regression-authority.test.ts` — preserve exact package/facade parity.
  </read_first>
  <files>src/components/LinearRegressionLessonLab.vue, tests/linear-regression-labs.test.mjs</files>
  <behavior>
    - Row mode shows instant `11550`, feature values, prediction, actual, and residual; batch mode shows the published 13,903-row by 5-feature train contract and its exact batch result/metrics.
    - GD step 0 versus 772 changes visible update, MSE, gradient norm, intercept, and coefficient values from the complete trace.
    - Method focus changes visible method name, held-out metrics, convergence/agreement data, and method delta from the same split/design matrix.
    - Coefficient space changes a visible intercept-plus-five-feature table between model space and original units using exact published rows.
    - Named-case selection changes visible instant, timestamp/hour, actual, prediction, residual, and role for `17213`, `15628`, `14965`, and `15604`.
    - The atemp toggle changes visible base/comparison correlations, coefficients, and stability interpretation using the published controlled comparison.
    - Reset restores all six defaults and their result panels; invalid package state preserves the exact audited compact facts, disables controls whose complete rows are unavailable, and explains that state bilingually.
  </behavior>
  <action>Replace the summary-only load state with one discriminated workbench load state that concurrently fetches the four registered result assets using `withPublicBase`, one abort controller per load, abort-on-chapter-change/unmount, response checks, and `parseLinearRegressionOutput`. Assemble the ready state with `createLinearRegressionWorkbenchPackage`; never cast arbitrary JSON/CSV structures in the component. Drive six computed results solely through the Plan 27-09 selectors per D-11 through D-24 and D-27. Render each result in an `aria-live`-compatible, high-contrast, text-labeled panel with the exact stable hooks listed under Artifacts; color/animation may supplement but cannot carry a result per D-25. Keep values bounded, resettable, keyboard-operable, bilingual, mobile-wrapping, and reduced-motion safe. On load/parse failure, render only cross-authority-tested compact baseline facts, disable controls that require unavailable complete rows, and explain the state in both locales; do not invent values or perform fit/scaling/gradient/metric math inside Vue. Convert the four skipped lab assertions into active contracts and add checks for all six input-to-output links, strict four-file loading, abort behavior, fallback provenance, no component math, and no raw unsafe rendering.</action>
  <verify>
    <automated>node --test tests/linear-regression-labs.test.mjs tests/linear-regression-authority.test.ts tests/linear-regression-assets.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Every existing control changes the content of its own stable output hook with exact package values.
    - The complete trace/table/residual files are strictly parsed and cross-linked before use.
    - Both locale branches contain equivalent result fields and reasoned teaching context without D-26's prohibited learner-facing term.
    - Loading, invalid, reset, keyboard, mobile, and reduced-motion behavior remain explicit.
    - All lab semantic tests execute; none of the six control/output contracts is skipped.
    - Vue contains no fit, scaling, gradient, metric, coefficient conversion, or held-out diagnostic authority.
  </acceptance_criteria>
  <done>The workbench controls now produce visible exact numerical evidence from the same published Bike case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-10-02: Correct the Phase 28 bridge and strengthen semantic browser coverage</name>
  <read_first>
    - `src/components/LinearRegressionPagedLesson.vue` — update only the final bridge destination/copy and add a stable hook.
    - `src/data/housingPriceProjectModule.ts` — canonical live module slug and `/learn/housing-price-project` route.
    - `src/curriculum/routeManifest.ts` — canonical curriculum route identity.
    - `src/curriculum/adapters/algorithmAdapter.ts` — existing `housing-price-project` mapping to `project-tabular-regression`.
    - `.planning/ROADMAP.md` — Phase 28 goal and canonical sequence.
    - `tests/linear-regression-release.test.mjs` — add exact source/route/copy/output contracts and allow the Plan 27-09 workbench file.
    - `scripts/qa/linearRegressionBrowserMatrix.js` — replace label-change probes with paired control/output assertions while retaining all 36 cases.
    - `tests/python-and-housing-modules.test.mjs` — preserve existing housing route contracts.
  </read_first>
  <files>src/components/LinearRegressionPagedLesson.vue, tests/linear-regression-release.test.mjs, scripts/qa/linearRegressionBrowserMatrix.js</files>
  <behavior>
    - The final bridge href is exactly `/learn/housing-price-project` and the destination resolves through the existing module/catalog/manifest.
    - Chinese bridge copy is exactly: `阶段 28`; `继续进入表格回归项目`; `把本课确认的线性模型边界带入现有房价项目：使用冻结本地数据、防泄漏流水线、诚实基线、受控改进与残差复盘。`; `进入房价预测项目`.
    - English bridge copy is exactly: `Phase 28`; `Continue to the tabular-regression project`; `Carry this lesson's linear-model boundary into the existing housing project with frozen local data, a leakage-safe pipeline, an honest baseline, controlled improvement, and residual review.`; `Open Housing Price Project`.
    - The browser matrix remains 2 locales × 2 viewports × 9 routes = 36 cases.
    - For each relevant route/locale/viewport, the matrix captures each stable output before and after its control changes and fails unless the corresponding result changes to the expected package-backed identity/value.
    - Its machine-readable return contains exactly one interaction record for each locale/viewport pair with true `semanticChecks` keys `rowBatch`, `gdTrace`, `method`, `coefficientSpace`, `heldoutCase`, and `atempComparison`, plus both `summary-failure` and `summary-corruption` injection records for each pair.
    - The matrix also asserts exact bridge href/copy, reset restoration, zero overflow/overlap, local requests, no console errors/unexpected warnings/dead links, and reduced-motion static access.
  </behavior>
  <action>Change only the last-chapter bridge target and localized copy to the exact strings above per D-07 through D-12 and D-25; do not create or implement Phase 28 content. Give the link `data-testid="linear-phase-28-bridge"` and keep existing pager/sidebar/chapter/checkpoint behavior. In release tests, assert the exact href/copy, cross-check that the housing module/manifest already owns the target, and include the Plan 27-09 workbench source in the Phase 27 scope/no-remote classifier. Strengthen the committed browser matrix per D-17, D-18, D-23, D-25, and D-27: use the six stable output hooks, record pre/post text or structured values, exercise every option/endpoint required by Task 27-10-01, and assert the matching numerical identity rather than a changed control label. Retain all 36 locale×viewport×route cases and existing checkpoint, download, code-copy, fallback, request, console, link, overlap, overflow, and reduced-motion probes. Return a stable machine-readable result contract: exactly four `interactions` entries keyed by the two locales and two viewports, each with true `semanticChecks` booleans named `rowBatch`, `gdTrace`, `method`, `coefficientSpace`, `heldoutCase`, and `atempComparison`; retain exactly eight `failureInjections` entries covering `summary-failure` and `summary-corruption` for every locale/viewport pair, including the fallback/leak/console/warning evidence already asserted by the script.</action>
  <verify>
    <automated>node --test tests/linear-regression-labs.test.mjs tests/linear-regression-release.test.mjs tests/linear-regression-layout.test.mjs tests/python-and-housing-modules.test.mjs &amp;&amp; node --check scripts/qa/linearRegressionBrowserMatrix.js</automated>
  </verify>
  <acceptance_criteria>
    - Final bridge source and tests name the exact canonical Phase 28 route and bilingual copy.
    - No earlier chapter's pager or route behavior changes.
    - Browser source contract contains 36 cases and six paired control/output semantic probes.
    - Browser results expose the exact four semantic interaction records and eight failure-injection records required by Plan 27-11's machine-readable release assertion.
    - Row/batch, GD, method, coefficient space, named case, and atemp assertions each fail if only the label changes.
    - Existing housing module identity remains `housing-price-project` mapped to `project-tabular-regression`; no deferred Phase 28 work appears.
    - Release/scope/no-remote tests admit the new pure workbench file without broadening unrelated path access.
  </acceptance_criteria>
  <done>The complete lesson hands off to Phase 28 and its automated browser matrix proves meaningful interaction in both locales and viewports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local published CSV/JSON → Vue render state | Malformed or mismatched content must not become unlocked teaching values or unsafe DOM. |
| User controls → pure selector/render output | Values can be manipulated outside normal controls and must remain bounded and deterministic. |
| Lesson bridge → router/catalog | A hardcoded but wrong route can silently break the curriculum sequence. |
| Browser matrix → release claim | Label-only assertions can report false confidence without verifying changed teaching results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-G10-01 | Tampering | four-file workbench load | high | mitigate | Registered base-safe paths, response checks, strict parsers, package constructor, and exact audited fallback. |
| T-27-G10-02 | Denial of Service | control inputs/load lifecycle | medium | mitigate | Bounded selectors, abort-on-change/unmount, finite checks, reset tests, and responsive browser coverage. |
| T-27-G10-03 | Spoofing | Phase 28 bridge | medium | mitigate | Exact href/copy plus manifest/module/source/browser assertions. |
| T-27-G10-04 | Repudiation | semantic browser result | high | mitigate | Paired input/output hooks and exact identity/value assertions across both locales and viewports. |
| T-27-G10-05 | Elevation of Privilege | rendered asset/copy content | high | mitigate | Existing safe rendering paths, text interpolation, no raw HTML/script/iframe, and no-remote source gate. |
</threat_model>

<verification>
Run both task commands. Plan 27-11 owns the production-preview execution and repository-wide release gate.
</verification>

<success_criteria>
- All six controls visibly change exact package-backed teaching results and reset correctly.
- Both locales and desktop/mobile source contracts remain equivalent and accessible.
- The last chapter links to the existing Phase 28 housing project with exact localized handoff copy.
- The 36-case browser matrix is ready to reject label-only controls and a wrong downstream route.
</success_criteria>

<output>
Create `.planning/phases/27-linear-regression-rebuild/27-10-SUMMARY.md` when done.
</output>
