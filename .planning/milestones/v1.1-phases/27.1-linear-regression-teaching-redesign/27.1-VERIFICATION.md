---
phase: 27.1-linear-regression-teaching-redesign
status: passed
verified: 2026-08-01
---

# Phase 27A Verification

## Goal verdict

Passed. The production route now presents all eight preserved linear-regression chapters as one readable vertical teaching flow driven by the same real Bike Sharing dataset and reproducible published outputs.

## Requirement coverage

| Requirement | Result | Verification |
| --- | --- | --- |
| LINR-05 | Passed | Typed block and layout tests confirm the single-column sequence and chapter-local full-width observation lab. |
| LINR-06 | Passed | Summary v2 and feature-stage CSV reproduce train-only exploration and validation comparisons. |
| LINR-07 | Passed | Numerical tests prove ordered disjoint splits, leakage exclusion, train-only selection preprocessing, and recomputable final test metrics. |
| LINR-08 | Passed | Manifest tests validate both executed Notebooks, eleven figures, local download paths, hashes, fallbacks, and final-chapter references. |

## Automated gates

- `.python-data-tools-venv/bin/python scripts/linear-regression/build-phase-27a-assets.py --check` — passed.
- `node --test tests/linear-regression-content.test.mjs tests/linear-regression-layout.test.mjs tests/linear-regression-labs.test.mjs tests/linear-regression-phase27a.test.mjs tests/linear-regression-release.test.mjs` — 65 passed.
- `npm test` — 951 passed.
- `npm run build` — passed; only the existing large-chunk warning remains.
- `npm run build:pages` — passed; only the existing large-chunk warning remains.
- `npm run security:audit` — passed, zero vulnerabilities.

## Browser gates

- All eight Chinese chapter routes passed at 1200px.
- `fit-line`, `training-motion`, and `regularization` passed in Chinese and English at 1440px, 768px, and 390px.
- The 1440px matrix showed the sticky sidebar and hid the top directory; smaller widths showed the collapsible top directory and hid the sidebar.
- Every checked viewport had zero horizontal overflow, two observation controls, intact local figures, and zero console errors.
- The chapter-eight page exposed six public reference links and nine base-safe download links.
- Code copy and mobile-directory controls were exercised successfully.

## Remaining non-blocking observations

- Vite continues to emit the repository's existing browser-externalization development warnings from the Markdown/source-map dependency chain.
- Production builds continue to report the existing large-chunk warning; Phase 27A did not add synchronous route imports.
