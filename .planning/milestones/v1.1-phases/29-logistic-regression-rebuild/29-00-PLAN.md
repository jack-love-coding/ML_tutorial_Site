---
phase: 29-logistic-regression-rebuild
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/logistic-regression-math.test.ts
  - tests/logistic-regression-assets.test.ts
  - tests/logistic-regression-parity.test.ts
  - tests/logistic-regression-calibration.test.ts
  - tests/logistic-regression-content.test.mjs
  - tests/logistic-regression-rendering.test.ts
  - tests/logistic-regression-labs.test.mjs
  - tests/logistic-regression-cockpit.test.mjs
  - tests/logistic-regression-media.test.mjs
  - tests/logistic-regression-release.test.mjs
autonomous: true
requirements: [LOGR-01, LOGR-02, LOGR-03, LOGR-04]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every Phase 29 production task begins behind an already-authored, non-vacuous test contract rather than defining its acceptance assertions while implementing the behavior."
    - "The numerical contracts pin stable BCE, all finite-difference steps, exact scratch/sklearn configurations, immutable parity tolerances, calibration boundaries, and the Phase 29/30 test-disclosure boundary."
    - "Content, rendering, route compatibility, lazy labs, media integrity, accessibility fallback, and final release behavior have executable fail-first assertions before production files change."
  artifacts:
    - path: "tests/logistic-regression-math.test.ts"
      provides: "Stable numerical and finite-difference contract"
    - path: "tests/logistic-regression-assets.test.ts"
      provides: "Manifest, package, Notebook, hash, and disclosure contract"
    - path: "tests/logistic-regression-parity.test.ts"
      provides: "Fixed scratch/sklearn alignment contract"
    - path: "tests/logistic-regression-calibration.test.ts"
      provides: "Calibration invariants and synthetic provenance contract"
    - path: "tests/logistic-regression-content.test.mjs"
      provides: "Bilingual six-chapter content contract"
    - path: "tests/logistic-regression-rendering.test.ts"
      provides: "Safe Markdown/KaTeX and responsive rendering contract"
    - path: "tests/logistic-regression-labs.test.mjs"
      provides: "Six lazy scene, control, fallback, and lifecycle contracts"
    - path: "tests/logistic-regression-cockpit.test.mjs"
      provides: "Legacy route/progress/checkpoint compatibility contract"
    - path: "tests/logistic-regression-media.test.mjs"
      provides: "Source, ffprobe, transcript, marker, hash, and player contract"
    - path: "tests/logistic-regression-release.test.mjs"
      provides: "Final phase scope and browser-matrix result contract"
  key_links:
    - from: "tests/logistic-regression-parity.test.ts"
      to: ".planning/phases/29-logistic-regression-rebuild/29-RESEARCH.md"
      via: "literal optimizer, sklearn, stopping, warning, and tolerance assertions"
      pattern: "2e-4|1e-6|max_iterations"
    - from: "tests/logistic-regression-cockpit.test.mjs"
      to: "src/components/LogisticRegressionPagedLesson.vue"
      via: "preserved six IDs, route, progress, checkpoint, and single-column layout assertions"
      pattern: "linear-score|linear-limits"
    - from: "tests/logistic-regression-release.test.mjs"
      to: ".planning/phases/29-logistic-regression-rebuild/29-VALIDATION.md"
      via: "exact task/test inventory and Phase 30 exclusion checks"
      pattern: "LOGR-01|LOGR-04"
---

<objective>
Create the complete Phase 29 fail-first verification boundary before any numerical engine, asset generator, media, lab, or course implementation changes.

Purpose: Make every later plan implement an independently authored learner and integrity contract, with fixed numerical tolerances and no same-task acceptance invention.
Output: Ten focused test files covering math, assets, parity, calibration, content, rendering, labs, compatibility, media, and final release behavior.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/29-logistic-regression-rebuild/29-CONTEXT.md
@.planning/phases/29-logistic-regression-rebuild/29-RESEARCH.md
@.planning/phases/29-logistic-regression-rebuild/29-VALIDATION.md
@package.json
@tests/logistic-regression-cockpit.test.mjs
@tests/numerical-methods-batch-4.test.ts
@tests/linear-regression-course.test.mjs
@tests/optimizer-course-media.test.mjs

<interfaces>
The tests describe the phase-local exports, static package, preserved six chapter IDs, current route/progress/checkpoint contracts, `ChapteredMediaPlayer` behavior, and Pages-safe public paths. They may dynamically import not-yet-created files and report a named assertion failure, but must not hide missing implementation behind skip/todo branches.

The fixed numerical authority is the resolved contract in `29-RESEARCH.md`: train-only `ddof=0` scaling, explicit intercept/no penalty, exact sklearn 1.9 and scratch Armijo configurations, immutable `2e-4` coefficient/intercept and `1e-6` validation-probability tolerances, and the eight-step centered-difference table with the `h=1e-6` error bound.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author fail-first numerical, asset, parity, and calibration contracts</name>
  <files>tests/logistic-regression-math.test.ts, tests/logistic-regression-assets.test.ts, tests/logistic-regression-parity.test.ts, tests/logistic-regression-calibration.test.ts</files>
  <read_first>
    - `29-RESEARCH.md` resolved numerical choices and `29-CONTEXT.md` D-01 through D-07, D-09 through D-11, D-16, and D-19 through D-22.
    - `tests/numerical-methods-batch-4.test.ts` for Banknote source/split primitives and historical math assertions, without inheriting its final-test reporting.
    - `scripts/linear-regression/build-phase-27a-assets.py` tests for atomic publication, drift, source-cell, and hash expectations.
  </read_first>
  <action>Create four non-vacuous suites before production implementation. Pin score/sigmoid/BCE extremes, batch gradient, invalid dimensions/targets/non-finite values, the exact parameter vector and all eight centered-difference steps. Pin the exact scratch Armijo and sklearn 1.9 configurations, warning capture/failure policy, terminal metadata, fixed parity tolerances, and failure rather than tolerance relaxation. Specify the complete Phase-29-only bilingual Notebook/package inventory, clean-kernel output parity, atomic rollback/drift, SHA-256/source-cell binding, frozen handoff shape, and learner-facing absence of test records. Specify positive-temperature ordering/default-label invariants, explicit bin edges, empty-bin behavior, full-precision ECE, and isolated XOR/circle provenance. Each suite must contain at least one named assertion that fails against the current pre-Phase-29 tree for the intended missing contract rather than from a syntax error or unconditional throw.</action>
  <acceptance_criteria>
    - The parity suite contains literal assertions for every scratch/sklearn parameter, stopping threshold, warning policy, terminal field, `2e-4` coefficient/intercept tolerance, and `1e-6` validation-probability tolerance.
    - The math/asset suites require the full `[1e-1,...,1e-8]` centered-difference table, no monotonic-error claim, full-precision storage, and maximum component error `<=2e-9` at `h=1e-6`.
    - The asset contract requires a new `public/logistic-regression/phase-29/` bilingual package and rejects importing learner outputs from the Batch 4 final-test Notebook.
    - All four files parse, register named tests without skips/todos, and produce an expected RED result against the current tree.
  </acceptance_criteria>
  <verify>
    <automated>sh -c 'node --check tests/logistic-regression-math.test.ts && node --check tests/logistic-regression-assets.test.ts && node --check tests/logistic-regression-parity.test.ts && node --check tests/logistic-regression-calibration.test.ts && ! node --test tests/logistic-regression-math.test.ts tests/logistic-regression-assets.test.ts tests/logistic-regression-parity.test.ts tests/logistic-regression-calibration.test.ts'</automated>
  </verify>
  <done>The complete numerical/static-authority contract exists and fails for named missing Phase 29 behavior before implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Author fail-first bilingual content, safe rendering, lazy lab, and compatibility contracts</name>
  <files>tests/logistic-regression-content.test.mjs, tests/logistic-regression-rendering.test.ts, tests/logistic-regression-labs.test.mjs, tests/logistic-regression-cockpit.test.mjs</files>
  <read_first>
    - `29-CONTEXT.md` D-08 through D-16 and D-18 through D-25 for chapter flow, lab ownership, evaluation boundary, wording, and compatibility.
    - Existing `tests/logistic-regression-cockpit.test.mjs` for legacy IDs, props/emits, route, progress, and checkpoint expectations that remain valid.
    - `tests/linear-regression-course.test.mjs` and adjacent Markdown/KaTeX tests for bilingual block structure, safe rendering, copy feedback, and Pages paths.
  </read_first>
  <action>Create the content, rendering, and labs suites and revise the existing cockpit suite without removing its compatibility assertions. Require six complete bilingual chapters in fixed order, safe `String.raw`/Markdown/KaTeX rendering, no raw HTML bypass, final-only references/downloads, plain learner wording, and no Phase 30 metrics/test disclosure. Require exactly six scene IDs, route-level lazy loading of only the current lab, 2–3 bounded control groups, last-valid restoration, keyboard/reset/user-start/reduced-motion behavior, non-color encodings, and semantic table/text fallbacks. Update the cockpit assertions to require the new single-column lesson with one dedicated lab after teaching content while preserving the old route, IDs, Progress/localStorage, checkpoint, props/emits, deep links, and compatibility component file. Include named current-tree RED assertions and no skip/todo escape.</action>
  <acceptance_criteria>
    - Every legacy ID and compatibility surface has a positive assertion, while the retired shared cockpit has a negative primary-mount assertion.
    - Both locales require the complete chapter structure and reject learner-visible `Evidence`, `Ref ID`, raw TeX delimiters, chapter citations, and reserved-test data.
    - Lab tests cover endpoints plus one outside step, NaN/Infinity restoration, abort/unmount cleanup, mobile topology, reduced motion, keyboard operation, and complete fallbacks.
    - All four files parse and produce an expected RED result for named missing Phase 29 behavior.
  </acceptance_criteria>
  <verify>
    <automated>sh -c 'node --check tests/logistic-regression-content.test.mjs && node --check tests/logistic-regression-rendering.test.ts && node --check tests/logistic-regression-labs.test.mjs && node --check tests/logistic-regression-cockpit.test.mjs && ! node --test tests/logistic-regression-content.test.mjs tests/logistic-regression-rendering.test.ts tests/logistic-regression-labs.test.mjs tests/logistic-regression-cockpit.test.mjs'</automated>
  </verify>
  <done>The learner flow, renderer, six labs, and legacy compatibility contracts exist and fail for intentional pre-implementation gaps.</done>
</task>

<task type="auto">
  <name>Task 3: Author fail-first media integrity and final release contracts</name>
  <files>tests/logistic-regression-media.test.mjs, tests/logistic-regression-release.test.mjs</files>
  <read_first>
    - `29-CONTEXT.md` D-17, D-18, and D-23 plus the four-media requirements in `29-RESEARCH.md`.
    - `tests/optimizer-course-media.test.mjs` for source/metadata/ffprobe/hash/player fallback parity.
    - `29-VALIDATION.md` for the complete task inventory, browser matrix, build, security, asset-drift, and media-drift release gate.
  </read_first>
  <action>Create media and release suites that pin four scene IDs and stems, exact source/prompt/tree/transcript/poster/MP4/metadata relationships, 1920x1080 H.264 at 30fps, bounded markers, source-manifest and per-file hashes, user-start playback, marker seeking, failure fallback, reduced motion, both Pages bases, and cleanup. Define the release suite's exact 30-case locale/viewport/route matrix, six semantic scene interactions, failure injections, route/progress/checkpoint compatibility, Phase 30 exclusion, local-only runtime requests, complete test inventory, and same-HEAD build/drift/security command expectations. Use fixtures or source inspection to make at least one assertion in each suite RED for a named missing Phase 29 artifact; do not create binary placeholders.</action>
  <acceptance_criteria>
    - The media contract covers all four complete packages and fails on any byte/path/hash/marker/transcript/source/registry mismatch.
    - The release contract enumerates every route/locale/viewport case and rejects duplicate, skipped, missing, malformed, or test-disclosing records.
    - Both suites parse, have no skipped/todo tests, and produce expected RED results against the current tree without depending on generated binary placeholders.
  </acceptance_criteria>
  <verify>
    <automated>sh -c 'node --check tests/logistic-regression-media.test.mjs && node --check tests/logistic-regression-release.test.mjs && ! node --test tests/logistic-regression-media.test.mjs tests/logistic-regression-release.test.mjs'</automated>
  </verify>
  <done>Media and final-release acceptance are independently authored and fail first before their production assets and browser evaluator exist.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/logistic-regression-math.test.ts`
- `tests/logistic-regression-assets.test.ts`
- `tests/logistic-regression-parity.test.ts`
- `tests/logistic-regression-calibration.test.ts`
- `tests/logistic-regression-content.test.mjs`
- `tests/logistic-regression-rendering.test.ts`
- `tests/logistic-regression-labs.test.mjs`
- Updated `tests/logistic-regression-cockpit.test.mjs`
- `tests/logistic-regression-media.test.mjs`
- `tests/logistic-regression-release.test.mjs`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Planning contracts → executable tests | Prose requirements become code that may incorrectly pass vacuously or omit a locked boundary. |
| Historical Banknote tests/assets → Phase 29 fixtures | Reuse could accidentally carry Batch 4 test reporting into learner-facing expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-00-01 | Tampering | Wave 0 suites | high | mitigate | Require named fail-first assertions, no skips/todos, literal fixed numerical contracts, and expected RED verification against the current tree. |
| T-29-00-02 | Information Disclosure | test fixtures | high | mitigate | Use training/validation teaching records for learner contracts and assert reserved test records are absent from every Phase 29 presentation/download surface. |
| T-29-00-03 | Repudiation | validation coverage | medium | mitigate | Map every later task to an exact test command in `29-VALIDATION.md` and keep task IDs/waves/dependencies machine-reviewable. |
| T-29-00-04 | Denial of Service | test fixtures | low | mitigate | Bound generated arrays and use source/metadata fixtures rather than creating or decoding production media in Wave 0. |
</threat_model>

<verification>
- Every test file passes `node --check`, contains named assertions without skip/todo, and the three grouped commands prove RED against missing Phase 29 behavior.
- `29-VALIDATION.md` maps all ten files to later production tasks and keeps `wave_0_complete: false` until this plan actually executes.
- No production source, generated public asset, external API, package install, or database schema is changed by Wave 0.
</verification>

<success_criteria>
- All ten test contracts exist before Plan 29-01 begins.
- Fixed numerical configurations and tolerances are literal assertions, not executor-selected values.
- The Phase 29/30 disclosure boundary and all six legacy route/progress/checkpoint contracts fail closed.
</success_criteria>

<output>
Create `.planning/phases/29-logistic-regression-rebuild/29-00-SUMMARY.md` when done.
</output>
