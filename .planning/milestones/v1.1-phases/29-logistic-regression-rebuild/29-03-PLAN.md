---
phase: 29-logistic-regression-rebuild
plan: "03"
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - src/modules/logistic-regression/data/media.ts
  - scripts/manim/scenes/logistic_regression.py
  - scripts/manim/render_logistic_regression.py
  - scripts/manim/logistic_regression/
  - docs/curriculum-v3/logistic-regression/manim/
  - tests/logistic-regression-media.test.mjs
autonomous: true
requirements: [LOGR-01, LOGR-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Four language-neutral Manim scene specifications exist, while the score/sigmoid and likelihood/BCE/gradient scenes already have complete prerequisite-first knowledge packages using numerical anchors loaded from the Phase 29 manifest."
    - "The two foundational scenes have depth-limited reverse knowledge trees, exact MathTex notation, verbose timed visual prompts, bilingual transcripts, non-color cues, and metadata-ready chapter markers."
    - "The selective renderer can preview or publish one named scene, validates sources before invoking Manim, and provides a read-only check path for the final published package."
  artifacts:
    - path: "scripts/manim/scenes/logistic_regression.py"
      provides: "Four manifest-bound Manim scene classes"
      exports: ["LinearScoreToSigmoidScene", "LikelihoodBceGradientScene", "ConfidentMistakeScene", "RegularizationConfidenceScene"]
    - path: "scripts/manim/render_logistic_regression.py"
      provides: "Selective preview/publish/check renderer and metadata validator"
    - path: "src/modules/logistic-regression/data/media.ts"
      provides: "Typed bilingual runtime media registry"
      exports: ["logisticMediaRegistry"]
    - path: "tests/logistic-regression-media.test.mjs"
      provides: "Wave 0 registry/source/metadata/media integrity contract"
  key_links:
    - from: "scripts/manim/scenes/logistic_regression.py"
      to: "public/logistic-regression/phase-29/manifest.json"
      via: "source validation and loaded numerical anchors before constructing each scene"
      pattern: "phase-29/manifest.json"
    - from: "src/modules/logistic-regression/data/media.ts"
      to: "src/components/ChapteredMediaPlayer.vue"
      via: "existing assetPath/posterPath/title/alt/transcript/chapterMarkers props"
      pattern: "chapterMarkers|transcript"
---

<objective>
Establish the mathematically rigorous four-scene source contract and fully author the two foundational Phase 29 animation packages before publishing binary media.

Purpose: Make the animation narrative, equations, prerequisite order, accessibility fallbacks, and numerical anchors auditable independently from long render jobs.
Output: Two reverse knowledge trees, two verbose prompts, four transcripts, four Manim scene classes, a selective renderer, typed registry, and source-contract tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/29-logistic-regression-rebuild/29-CONTEXT.md
@.planning/phases/29-logistic-regression-rebuild/29-RESEARCH.md
@.planning/phases/29-logistic-regression-rebuild/29-02-SUMMARY.md
@public/logistic-regression/phase-29/manifest.json
@scripts/manim/render_optimizer_comparison.py
@scripts/manim/scenes/optimizer_comparison.py
@src/components/ChapteredMediaPlayer.vue
@src/modules/optimizer-comparison/data/media.ts

<interfaces>
`ChapteredMediaPlayer` accepts `assetPath`, `posterPath`, localized `title`, localized `alt`, localized `transcript`, and `chapterMarkers: { id; startSeconds; title: LocalizedCopy }[]` and already owns base-safe URL resolution, marker seeking, reduced-motion behavior, fallback, and listener cleanup.

The Phase 29 manifest supplies canonical row terms, likelihood/BCE/gradient anchors, scratch/L2 comparison anchors, source cell IDs, and hashes. Scene source may assert expected identities but learner-visible numbers must be formatted from loaded data.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the four-scene source and runtime media contract</name>
  <files>tests/logistic-regression-media.test.mjs, src/modules/logistic-regression/data/media.ts, scripts/manim/scenes/logistic_regression.py, scripts/manim/render_logistic_regression.py</files>
  <read_first>
    - `scripts/manim/render_optimizer_comparison.py` for selective scene rendering, ffprobe validation, metadata hashing, and check-mode behavior.
    - `scripts/manim/scenes/logistic_regression.py` current classes and existing public asset names that must be rebuilt rather than orphaned.
    - `src/modules/optimizer-comparison/data/media.ts` and `tests/optimizer-course-media.test.mjs` for registry/metadata parity.
    - Math-To-Manim references `reverse-knowledge-tree.md`, `verbose-prompt-format.md`, and `manim-code-patterns.md` for prerequisite DAG, exact MathTex, palette, timing, and transition requirements.
  </read_first>
  <behavior>
    - Renderer scene IDs map one-to-one to four class names and public asset names; unknown or duplicate IDs fail before Manim runs.
    - `preview` uses a fast local render; `publish` explicitly requests 1920x1080 at 30fps; `check` is write-free and validates all published assets, sources, markers, transcripts, hashes, duration, frame size, fps, and codec.
    - Scene source loads the Phase 29 manifest/interaction anchors, rejects identity/hash/value drift, and formats learner-visible values from the loaded data.
    - Runtime registry paths, hashes, markers, titles, alt copy, and transcripts must equal canonical metadata and use the existing player contract.
  </behavior>
  <action>Implement the source and registry behavior required by the Plan 29-00 media contract. Replace the preview-only logistic renderer with explicit `--scene`, `--quality preview|publish`, and `--check` modes modeled on the optimizer renderer. Define four scene classes and one shared fixed palette/layout/anchor loader; use raw strings for every `MathTex` formula and shapes/line styles/labels in addition to color. Bind score, probability, likelihood, BCE, gradient, and regularization numbers to the published Phase 29 manifest per D-05, D-09, D-10, and D-17. Define the typed `logisticMediaRegistry` for `ChapteredMediaPlayer`; do not implement another video player. Refine only source/registry fixtures in the prior media test; binary assertions remain fixed for Plan 29-04.</action>
  <acceptance_criteria>
    - One command can target any single scene for preview or publication without rendering the other three.
    - Scene construction fails closed when the asset contract, source cell, hash, or numerical anchor drifts.
    - Registry and source tests recognize exactly four media IDs and the three preserved public stems plus the new likelihood stem.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/manim/render_logistic_regression.py --validate-sources && npm test -- tests/logistic-regression-media.test.mjs</automated>
  </verify>
  <done>The complete source and runtime contract is green before expensive media publication begins.</done>
</task>

<task type="auto">
  <name>Task 2: Author the score-to-sigmoid knowledge package</name>
  <files>scripts/manim/logistic_regression/linear-score-to-sigmoid-prompt.md, scripts/manim/logistic_regression/linear-score-to-sigmoid-tree.json, docs/curriculum-v3/logistic-regression/manim/linear-score-to-sigmoid-transcript.zh-CN.md, docs/curriculum-v3/logistic-regression/manim/linear-score-to-sigmoid-transcript.en.md</files>
  <read_first>
    - `29-CONTEXT.md` D-08, D-09, D-10, and D-17 for the assigned teaching arc.
    - The Phase 29 `linear-score.json`, `sigmoid-probability.json`, and `threshold-decisions.json` files for exact source-cell/numeric anchors.
    - Math-To-Manim verbose prompt format for timestamped opening action, equation, visual sequence, transition hook, and pacing.
  </read_first>
  <action>Build a depth-three-or-less prerequisite DAG that stops at beginner foundations and progresses from weighted sum and number line through monotone sigmoid, probability, odds/log-odds, and extreme scores. Enrich every node with exact formulas, variable definitions, the canonical real row anchor, non-color visual encoding, and timed transitions; topologically order foundations before the target. Write separate complete Chinese and English transcripts whose equations and values match, while the animation itself remains language-neutral per D-17. Include a poster-complete final frame and chapter-marker timestamps so the lesson survives without motion per D-18.</action>
  <acceptance_criteria>
    - The tree is acyclic, depth-limited, foundation-first, and covers every concept used by the prompt and scene.
    - The prompt specifies exact MathTex, positions, palette roles, timings, transitions, and source anchor IDs rather than vague visual directions.
    - The two locale transcripts have matching formula/value/marker structure and fully explain every animated state without relying on color.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/manim/render_logistic_regression.py --validate-sources --scene linear-score-to-sigmoid</automated>
  </verify>
  <done>The score-to-sigmoid animation has a complete auditable teaching package ready for rendering.</done>
</task>

<task type="auto">
  <name>Task 3: Author the likelihood-to-gradient knowledge package</name>
  <files>scripts/manim/logistic_regression/likelihood-to-bce-gradient-prompt.md, scripts/manim/logistic_regression/likelihood-to-bce-gradient-tree.json, docs/curriculum-v3/logistic-regression/manim/likelihood-to-bce-gradient-transcript.zh-CN.md, docs/curriculum-v3/logistic-regression/manim/likelihood-to-bce-gradient-transcript.en.md</files>
  <read_first>
    - `29-CONTEXT.md` D-05, D-08, D-09, D-10, D-17, and D-18.
    - Phase 29 `log-loss.json` interaction payload plus manifest row, batch, and gradient anchors.
    - Math-To-Manim verbose prompt format for likelihood, stable BCE, batch gradient, and update-direction transitions.
  </read_first>
  <action>Build a depth-three-or-less reverse knowledge tree that starts with one Bernoulli outcome and progresses through product likelihood, floating-point underflow, log-sum, negative mean BCE, the `(p-y)x` sample contribution, batch gradient, and update direction. Bind exact manifest anchors to raw MathTex, define every symbol and shape, use the fixed semantic palette together with shapes and line styles, and specify voiceover-friendly transitions and a poster-complete conclusion. Create structurally matching Chinese and English transcripts and marker sets per D-09, D-10, D-17, and D-18.</action>
  <acceptance_criteria>
    - The likelihood-to-BCE derivation explicitly shows why logs turn products into sums and why the negative mean is optimized.
    - Sample and batch gradient values match the interaction asset, and the update arrow uses the correct descent direction.
    - The package can be understood from poster and transcript alone and does not depend on autoplayed motion.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/manim/render_logistic_regression.py --validate-sources --scene likelihood-to-bce-gradient</automated>
  </verify>
  <done>The likelihood-to-gradient animation has a complete auditable teaching package ready for rendering.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/modules/logistic-regression/data/media.ts`: four-entry typed media registry.
- `scripts/manim/scenes/logistic_regression.py`: four manifest-bound Manim classes and shared primitives.
- `scripts/manim/render_logistic_regression.py`: selective validation/preview/publish/check CLI.
- `scripts/manim/logistic_regression/linear-score-to-sigmoid-tree.json` and `likelihood-to-bce-gradient-tree.json`: reverse knowledge trees.
- `scripts/manim/logistic_regression/linear-score-to-sigmoid-prompt.md` and `likelihood-to-bce-gradient-prompt.md`: verbose timed prompts.
- `docs/curriculum-v3/logistic-regression/manim/linear-score-to-sigmoid-transcript.zh-CN.md`, `linear-score-to-sigmoid-transcript.en.md`, `likelihood-to-bce-gradient-transcript.zh-CN.md`, and `likelihood-to-bce-gradient-transcript.en.md`: bilingual transcripts.
- `tests/logistic-regression-media.test.mjs`: media source/registry/package contract.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated numerical manifest → Manim source | Static numbers and hashes control visual formulas and labels. |
| Transcript/metadata → sanitized media player | Localized Markdown is rendered in the learner page. |
| Render CLI arguments → subprocess | Scene/quality selections reach the local Manim command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-03-01 | Tampering | media numerical anchors | high | mitigate | Validate manifest identity/hash/source cells and format every visible number from loaded data before scene construction. |
| T-29-03-02 | Tampering | render scene arguments | medium | mitigate | Accept only four enum scene IDs and two enum quality values; use argument arrays without shell interpolation. |
| T-29-03-03 | Information Disclosure | transcripts/source records | low | accept | Assets contain public educational data only; test-set results remain absent through the upstream manifest visibility contract. |
| T-29-03-04 | Denial of Service | media rendering | low | mitigate | Single-scene selection bounds work; source validation runs before Manim and check mode performs no render. |
| T-29-03-05 | Tampering | transcript Markdown | medium | mitigate | Runtime passes localized transcript through the existing sanitized `MarkdownMathContent` path and never permits raw HTML execution. |
</threat_model>

<verification>
- Source-contract tests and `--validate-sources` pass for all four scene IDs.
- `python3 scripts/manim/render_logistic_regression.py --scene linear-score-to-sigmoid --quality preview` proves one selective render without publishing.
- No new package install or external API is introduced; Manim/ffprobe versions are recorded from the existing environment.
</verification>

<success_criteria>
- D-05, D-09 through D-11, D-17, D-18, D-23, and D-24 are encoded in inspectable media sources and registry contracts.
- Both foundational animations follow prerequisite-first Math-To-Manim trees and exact mathematical narratives.
- Plan 29-04 can finish the two diagnostic packages and publish all binaries without changing the shared source contract.
</success_criteria>

<output>
Create `.planning/phases/29-logistic-regression-rebuild/29-03-SUMMARY.md` when done.
</output>
