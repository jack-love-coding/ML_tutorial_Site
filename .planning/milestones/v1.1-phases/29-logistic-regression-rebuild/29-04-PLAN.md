---
phase: 29-logistic-regression-rebuild
plan: "04"
type: execute
wave: 4
depends_on: [29-03]
files_modified:
  - scripts/manim/logistic_regression/
  - docs/curriculum-v3/logistic-regression/manim/
  - public/manim/logistic-regression/
  - src/modules/logistic-regression/data/media.ts
  - tests/logistic-regression-media.test.mjs
autonomous: true
requirements: [LOGR-01, LOGR-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The confident-mistake and regularization scenes have prerequisite-first knowledge trees, exact prompts, bilingual transcripts, non-color cues, and poster-complete conclusions bound to Phase 29 numerical anchors."
    - "All four Phase 29 videos are locally published as language-neutral H.264 MP4 plus SVG poster at 1920x1080 and 30fps with valid duration-bounded chapter markers."
    - "Canonical metadata binds video, poster, prompt, reverse tree, bilingual transcripts, Manim version, numerical-source hashes, and runtime registry values by SHA-256."
    - "The three existing public media stems continue to resolve, and the new likelihood media package is available through the same Pages-safe player contract."
  artifacts:
    - path: "public/manim/logistic-regression/metadata.json"
      provides: "Canonical four-package media inventory and integrity metadata"
    - path: "public/manim/logistic-regression/linear-score-to-sigmoid.mp4"
      provides: "Score/sigmoid animation"
    - path: "public/manim/logistic-regression/likelihood-to-bce-gradient.mp4"
      provides: "Likelihood/BCE/gradient animation"
    - path: "public/manim/logistic-regression/log-loss-confident-mistake.mp4"
      provides: "Confident mistake animation"
    - path: "public/manim/logistic-regression/regularization-confidence-field.mp4"
      provides: "Parity/L2 animation"
  key_links:
    - from: "public/manim/logistic-regression/metadata.json"
      to: "src/modules/logistic-regression/data/media.ts"
      via: "exact ID/path/hash/transcript/marker parity test"
      pattern: "sha256|chapterMarkers"
    - from: "public/manim/logistic-regression/*.mp4"
      to: "public/logistic-regression/phase-29/manifest.json"
      via: "metadata numerical source hash and scene pre-render assertions"
      pattern: "sourceManifestSha256"
---

<objective>
Complete the two diagnostic animation knowledge packages, then render, verify, and publish all four logistic-regression media packages as one integrity-checked local release.

Purpose: Finish the remaining prerequisite-first teaching narratives and turn all audited sources into usable learner media without breaking existing paths or allowing registry/metadata drift.
Output: Two reverse trees, two prompts, four transcripts, four MP4 files, four SVG posters, canonical metadata, final runtime registry hashes, and passing media integrity tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/29-logistic-regression-rebuild/29-CONTEXT.md
@.planning/phases/29-logistic-regression-rebuild/29-RESEARCH.md
@.planning/phases/29-logistic-regression-rebuild/29-03-SUMMARY.md
@public/logistic-regression/phase-29/manifest.json
@scripts/manim/render_logistic_regression.py
@scripts/manim/scenes/logistic_regression.py
@src/modules/logistic-regression/data/media.ts
@tests/logistic-regression-media.test.mjs

<interfaces>
The Plan 29-03 renderer owns enum scene selection, source validation, explicit `1920,1080`/30fps publish arguments, ffprobe checks, prompt/tree/transcript lookup, and metadata serialization. The typed registry consumes the resulting canonical metadata through literal local public paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the confident-mistake knowledge package</name>
  <files>scripts/manim/logistic_regression/log-loss-confident-mistake-prompt.md, scripts/manim/logistic_regression/log-loss-confident-mistake-tree.json, docs/curriculum-v3/logistic-regression/manim/log-loss-confident-mistake-transcript.zh-CN.md, docs/curriculum-v3/logistic-regression/manim/log-loss-confident-mistake-transcript.en.md</files>
  <read_first>
    - `29-CONTEXT.md` D-05, D-08, D-09, D-11, D-17, and D-18 for stable loss, derivation depth, animation, and fallback contracts.
    - `public/logistic-regression/phase-29/interactions/log-loss.json` plus the canonical manifest for the extreme wrong-prediction anchor.
    - `scripts/manim/scenes/logistic_regression.py` and the Phase 29-03 tree/prompt/transcript conventions.
  </read_first>
  <action>Build a depth-three-or-less reverse knowledge tree connecting an extreme wrong score, stable sigmoid evaluation, stable softplus BCE, residual signal, and the resulting large sample-gradient contribution without probability clipping. Bind exact manifest anchors to raw MathTex, define symbols and finite-value behavior, and use the shared palette with shapes, arrows, line styles, and labels. Author a verbose timed prompt, matching Chinese and English transcripts, ordered chapter markers, and a poster-complete conclusion that makes the confident-mistake mechanism understandable without motion per D-11, D-17, and D-18.</action>
  <acceptance_criteria>
    - The tree is acyclic, depth-limited, foundation-first, and covers every concept used by the prompt and scene.
    - Extreme-score probability, BCE, residual, and gradient contribution stay finite and equal the interaction asset at the declared precision.
    - Prompt and transcripts explain the same formulas, values, marker sequence, and non-color encodings.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/manim/render_logistic_regression.py --validate-sources --scene log-loss-confident-mistake</automated>
  </verify>
  <done>The confident-mistake animation has a complete auditable teaching package ready for rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Author the regularization knowledge package</name>
  <files>scripts/manim/logistic_regression/regularization-confidence-field-prompt.md, scripts/manim/logistic_regression/regularization-confidence-field-tree.json, docs/curriculum-v3/logistic-regression/manim/regularization-confidence-field-transcript.zh-CN.md, docs/curriculum-v3/logistic-regression/manim/regularization-confidence-field-transcript.en.md</files>
  <read_first>
    - `29-CONTEXT.md` D-04, D-08, D-11, D-15, D-17, and D-18 for objective alignment and L2 teaching boundaries.
    - `public/logistic-regression/phase-29/interactions/regularization.json` plus manifest scratch/library and L2 anchors.
    - `scripts/manim/scenes/logistic_regression.py` and the shared knowledge-package contract from Plan 29-03.
  </read_first>
  <action>Build a depth-three-or-less reverse knowledge tree that first establishes aligned unregularized scratch/library agreement, then explicitly changes to the separate L2 objective and shows coefficient shrinkage and confidence-field behavior. Do not imply that scikit-learn defaults share the scratch objective. Bind exact manifest anchors to raw MathTex, variable definitions, and the shared non-color visual grammar. Author the verbose timed prompt, structurally matching Chinese/English transcripts, ordered markers, and a poster-complete conclusion per D-11, D-17, and D-18.</action>
  <acceptance_criteria>
    - The tree and prompt make the change of objective explicit before any coefficient comparison.
    - Unregularized parity and L2 values match their separate manifest configurations at the declared tolerances.
    - Both transcripts preserve the same mathematical order, values, marker structure, and caution against causal or universal-best interpretations.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/manim/render_logistic_regression.py --validate-sources --scene regularization-confidence-field</automated>
  </verify>
  <done>The regularization animation has a complete auditable teaching package ready for rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the release and seal runtime registry parity</name>
  <files>public/manim/logistic-regression/, src/modules/logistic-regression/data/media.ts, tests/logistic-regression-media.test.mjs</files>
  <read_first>
    - All four validated tree, prompt, transcript, and scene packages from Plans 29-03 and 29-04 Tasks 1–2.
    - `scripts/manim/render_logistic_regression.py`, `public/logistic-regression/phase-29/manifest.json`, and existing public media stems.
    - `src/components/ChapteredMediaPlayer.vue` and `tests/optimizer-course-media.test.mjs` for registry parity and simulated playback/fallback states.
  </read_first>
  <behavior>
    - Typed registry and canonical metadata have exact four-entry path/hash/transcript/marker parity, and every path resolves under root and `/ML_tutorial_Site/` bases.
    - Publish output is H.264 at 1920x1080 and 30fps; marker times are ordered and bounded by finite positive durations.
    - Playback remains user-started; reduced motion leads with poster/transcript; media failure preserves poster, transcript, and marker text; unmount removes media/motion listeners.
    - An injected validation failure restores the previous public directory byte-for-byte.
  </behavior>
  <action>Render all four named scenes separately at the explicit publication profile, generate poster-complete SVG final frames, and stage the entire owned output directory. Validate codec, frame size, fps, duration, markers, posters, source packages, Manim version, numerical-manifest hash, and per-file SHA-256 before an atomic swap; exercise the rollback-injection path and avoid unrelated Manim directories. Preserve the three existing stems and add `likelihood-to-bce-gradient.{mp4,svg}` per D-17. Fill final registry hashes, durations, markers, transcripts, alt text, poster paths, and version fields from canonical metadata, then extend the focused test to cover byte drift, ffprobe, both public bases, user-start playback, reduced motion, forced failure, marker seek, and cleanup per D-18 and D-23.</action>
  <acceptance_criteria>
    - All four media packages pass ffprobe, hash, source-manifest, transcript, marker, and registry parity checks.
    - Any byte, path, hash, duration, source, or registry drift fails the focused test; rollback injection restores byte-identical prior state.
    - Poster/transcript remain complete in reduced-motion and forced-failure states, and old plus new URLs resolve under both production bases.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/manim/render_logistic_regression.py --check && npm test -- tests/logistic-regression-media.test.mjs</automated>
  </verify>
  <done>All four production packages and the runtime player contract form one atomic, hash-verified, accessible release.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/manim/logistic_regression/log-loss-confident-mistake-tree.json`, `log-loss-confident-mistake-prompt.md`, `regularization-confidence-field-tree.json`, and `regularization-confidence-field-prompt.md`.
- `docs/curriculum-v3/logistic-regression/manim/log-loss-confident-mistake-transcript.zh-CN.md`, `log-loss-confident-mistake-transcript.en.md`, `regularization-confidence-field-transcript.zh-CN.md`, and `regularization-confidence-field-transcript.en.md`.
- `public/manim/logistic-regression/linear-score-to-sigmoid.mp4`, `linear-score-to-sigmoid.svg`, `likelihood-to-bce-gradient.mp4`, `likelihood-to-bce-gradient.svg`, `log-loss-confident-mistake.mp4`, `log-loss-confident-mistake.svg`, `regularization-confidence-field.mp4`, and `regularization-confidence-field.svg`.
- `public/manim/logistic-regression/metadata.json` with source, transcript, marker, ffprobe, version, and hash metadata.
- `src/modules/logistic-regression/data/media.ts`: final hash-bound `logisticMediaRegistry`.
- `tests/logistic-regression-media.test.mjs`: source/binary/registry/player contract.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Staged render output → tracked public directory | Binary and metadata files become production learner assets. |
| Canonical metadata → runtime registry/player | Literal paths and hashes select what browsers load. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-04-01 | Tampering | public media package | high | mitigate | Stage, ffprobe, hash, source-validate, and atomically swap the whole owned directory with rollback injection. |
| T-29-04-02 | Tampering | runtime registry | high | mitigate | Exact registry-to-metadata parity tests cover all IDs, paths, hashes, markers, transcripts, and source manifest identity. |
| T-29-04-03 | Denial of Service | failed/large media | medium | mitigate | Keep learner-triggered loading, local assets, poster-first reduced motion, and complete failure fallback; verify cleanup. |
| T-29-04-04 | Information Disclosure | media contents | low | accept | Media uses public training/validation teaching anchors only; upstream tests exclude sealed test results. |
</threat_model>

<verification>
- Both new source packages validate, then renderer check and media test pass from a clean working tree.
- `npm run build:pages` includes base-safe media URLs without eager video download from unrelated chapters.
- No package install or network-hosted runtime media is added.
</verification>

<success_criteria>
- D-05, D-11, D-15, D-17, D-18, and D-23 are fully realized rather than represented only by source files.
- All media assets are reproducible, hash-bound, accessible without motion/color, and compatible with existing paths.
- Later course integration only imports a ready registry and cannot invent media metadata.
</success_criteria>

<output>
Create `.planning/phases/29-logistic-regression-rebuild/29-04-SUMMARY.md` when done.
</output>
