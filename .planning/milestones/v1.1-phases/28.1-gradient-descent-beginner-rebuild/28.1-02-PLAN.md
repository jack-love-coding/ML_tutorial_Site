---
phase: 28.1
plan: 02
type: execute
wave: 2
depends_on: [28.1-01]
autonomous: true
requirements: [GRAD-04, GRAD-05, GRAD-06]
files_modified:
  - src/data/gradientDescentModule.ts
  - src/types/gradientDescentLesson.ts
  - src/components/gradient-descent/
  - src/components/GradientChapterLab.vue
  - src/views/AlgorithmView.vue
  - src/router/index.ts
  - src/styles/modules/gradient-descent.css
  - tests/gradient-descent-course.test.ts
  - tests/gradient-descent-routing.test.ts
---

<objective>
Replace the long scrolling gradient-descent pilot with six focused bilingual chapter pages and six dedicated accessible interactions, while preserving all public identities and progress behavior.
</objective>

<tasks>

<task type="auto">
  <name>Publish typed bilingual chapter lessons and explicit routes</name>
  <action>Write the fixed beginner sequence as typed blocks, add first-chapter and chapter routes with invalid-ID recovery, keep legacy IDs and progress keys, and show references/downloads only at the end.</action>
  <verify>Content and route tests cover both locales, old deep links, direct-load fallbacks, checkpoint placement, and safe Markdown/KaTeX.</verify>
</task>

<task type="auto">
  <name>Build six lazy dedicated labs</name>
  <action>Create one interaction per chapter with at most three control groups. Use SVG for 2D scenes, an accessible read-only Canvas for the surface, pointer and keyboard interactions, finite input guards, reduced-motion behavior, and data-table fallbacks.</action>
  <verify>Component tests cover scene mapping, controls, deterministic reset, keyboard hooks, and lifecycle cleanup.</verify>
</task>

<task type="auto">
  <name>Remove duplicate gradient UI and validate the release</name>
  <action>Delete only the confirmed unused gradient rail/loss components and the old gradient-specific rendering branch. Validate 1200px Chinese pages plus bilingual key pages at 1440/768/390px.</action>
  <verify>Run focused/full tests, both builds, security audit, browser interaction checks, and Pages direct-route checks.</verify>
</task>

</tasks>

<verification>
All six chapters are independently reachable, show one lab only, and remain usable by keyboard and on mobile without horizontal overflow.
</verification>

<success_criteria>
- Each chapter renders exactly one current lesson and one matching lab.
- User-visible copy contains neither “证据” nor untranslated structural headings.
- Learning-rate divergence, sample batches, and Bike trace are presented honestly from published data.
- Existing progress/checkpoint behavior and old deep links remain compatible.
</success_criteria>
