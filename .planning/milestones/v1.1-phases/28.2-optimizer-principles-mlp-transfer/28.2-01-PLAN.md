---
phase: 28.2
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03, OPT-04, OPT-05, OPT-06]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/28.2-optimizer-principles-mlp-transfer/
  - src/simulations/optimizers/
  - src/modules/math-lab/utils/optimizers.ts
  - src/simulations/mlp.ts
  - src/simulations/mlpPlayground.ts
  - src/types/optimizer.ts
  - scripts/optimizer-comparison/
  - public/datasets/optimizer-comparison/
  - public/notebooks/optimizer-comparison/
  - tests/optimizer-engine.test.ts
  - tests/optimizer-assets.test.ts
---

<objective>
Reconcile Phase 28/28A planning state and establish one deterministic numerical authority for optimizer state, schedules, controlled MLP comparison, and Banknote transfer before any new course UI is built.
</objective>

<tasks>

<task type="auto">
  <name>Reconcile planning metadata and register Phase 28B</name>
  <action>Mark the verified Phase 28 TPRJ requirements complete, add missing GRAD traceability, correct requirement counts and roadmap progress, advance STATE through Phase 28A, insert Phase 28B with OPT-01..06 before Phase 29, and include the approved context and all three plans. Do not touch user-owned config or advice files.</action>
  <verify>Run planning consistency checks and confirm every v1.1 requirement maps to exactly one phase with correct status.</verify>
</task>

<task type="auto">
  <name>Build the shared pure optimizer engine and compatibility adapters</name>
  <action>Implement typed discriminated unions for optimizer kinds, states, step traces, schedules, decay strategies, and benchmark manifests. Match documented PyTorch SGD Momentum, RMSProp, Adam, L2, AdamW, and scheduler semantics. Re-export the engine from the old Math Lab path so Optimizer Race remains compatible.</action>
  <verify>Add locked two-step numerical tests, state-recurrence tests, Adam bias-correction and AdamW separation tests, scheduler cadence tests, invalid-input guards, and legacy Math Lab parity tests.</verify>
</task>

<task type="auto">
  <name>Inject optimizer strategies into the MLP core and publish executed assets</name>
  <action>Introduce an optimizer strategy seam without changing existing default SGD results. Generate deterministic two-dimensional traces, fixed-circle `2→4→1 tanh` benchmark trajectories, a bilingual executed NumPy Notebook, and a manifest-backed Banknote transfer package using the existing fixed split and train-only standardization.</action>
  <verify>Verify identical data/init/batch/update budgets, default MLP regression parity, Notebook/JSON/CSV cross-reproduction, frozen-test discipline, finite values, hashes, and Pages-safe paths.</verify>
</task>

</tasks>

<verification>
Focused optimizer/asset/MLP tests, full CI-low-concurrency suite, standard build, Pages build, asset drift check, and security audit pass.
</verification>

<success_criteria>
- Phase 28/28A/28B planning metadata is internally consistent and OPT-01..06 are mapped.
- Shared engine results match the declared PyTorch-compatible equations for the first two updates.
- Existing Math Lab imports and default MLP Playground SGD snapshots remain numerically unchanged.
- Synthetic and Banknote benchmark outputs are deterministic, reproducible, hash-bound, and never use the test split for selection.
</success_criteria>

