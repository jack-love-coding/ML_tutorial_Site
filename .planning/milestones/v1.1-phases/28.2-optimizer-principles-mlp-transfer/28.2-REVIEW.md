---
phase: 28.2-optimizer-principles-mlp-transfer
reviewed: 2026-08-12T14:16:00Z
resolved_at: 2026-08-12T14:16:00Z
resolution_commit: 15c5e98
files_reviewed: 13
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
supersedes: 2026-08-12T13:23:06Z
---

# Phase 28.2: Code Review Resolution

**Status:** clean
**Resolved by:** `15c5e98` — `fix(28.2): resolve optimizer benchmark and MLP review findings`

All original CR-01…CR-03 and WR-01…WR-05 findings have regression coverage and passed focused validation, full CI, standard build, Pages build, and the checked asset contract.

## Resolved Findings

### CR-01: Executed bilingual NumPy notebook

`build-assets.mjs` now creates valid code cells, resolves assets from either the asset directory or a project ancestor, imports NumPy, replays the 328 trajectory rows, and records only the output produced by a clean Jupyter kernel execution. `--check` executes the notebook again without changing the committed file; `tests/optimizer-assets.test.ts` covers this path.

### CR-02: MLP optimizer state lifecycle

`updateState()` compares normalized optimizer configurations, creates a fresh compatible state for legacy-to-optimizer and kind/config changes, and clears state on removal. Regression tests cover legacy→Adam, Adam→Momentum, and removal.

### CR-03: Explicit-optimizer regularization

The MLP now retains Playground link-only L2 and L1 behavior with explicit optimizers. Optimizer L2 and AdamW remain separate strategies; their decay is applied to links only, never bias parameters. L1 topology changes intentionally start a fresh state for the reduced parameter vector.

### WR-01: Exact update budget and provenance

The shared-runner emits update 0 plus exactly 40 calls to `stepOptimizer()` per optimizer and benchmark. The manifest declares a hash-bound `fixed-sine-v1` initialization instead of claiming the unused seed initialized parameters.

### WR-02: Separate matched and practical benchmarks

The package publishes separately labelled `first-step-norm-matched` and `predeclared-practical` trajectories. The matched set derives each learning rate from the common initial gradient and records measured first-step norms in the manifest.

### WR-03: Banknote drift validation

The manifest binds source path/hash, split counts, and train-only means/scales. `--check` recomputes and compares all of them. A mutated isolated source fixture is a required failing regression case.

### WR-04: One numerical authority

`scripts/optimizer-comparison/trajectory-runner.ts` produces every benchmark transition through `src/simulations/optimizers/index.ts`; the prior scalar recurrence was removed.

### WR-05: Enforced manifest contract

`OptimizerBenchmarkManifest` now represents the committed v2 JSON and `assertOptimizerBenchmarkManifest()` validates it at generation and test time.

## Verification

- `OPTIMIZER_NOTEBOOK_KERNEL=ml-atlas-phase27a node scripts/optimizer-comparison/build-assets.mjs --check` — passed in the isolated worktree; clean Jupyter kernel execution.
- `OPTIMIZER_NOTEBOOK_KERNEL=ml-atlas-phase27a node --test tests/optimizer-engine.test.ts tests/optimizer-assets.test.ts` — 10 passed.
- `OPTIMIZER_NOTEBOOK_KERNEL=ml-atlas-phase27a npm run test:ci` — passed.
- `npm run build` — passed (existing Vite large-chunk warning).
- `npm run build:pages` — passed (existing Vite large-chunk warning).
- `npm run security:audit` — unchanged `nanoid <3.3.17` advisory; no dependency change was in scope.

---

_Resolved: 2026-08-12T14:16:00Z_
_Fixer: gsd-code-fixer_
