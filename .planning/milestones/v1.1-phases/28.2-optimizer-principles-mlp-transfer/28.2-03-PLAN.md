---
phase: 28.2
plan: 03
type: execute
wave: 3
depends_on: [28.2-01, 28.2-02]
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03, OPT-04, OPT-05, OPT-06]
files_modified:
  - src/data/optimizerComparisonModule.ts
  - src/modules/optimizer-comparison/
  - src/views/AlgorithmView.vue
  - src/router/index.ts
  - src/modules/math-lab/components/OptimizerRaceLab.vue
  - src/modules/gradient-descent/
  - src/modules/neural-course/
  - src/styles/modules/optimizer-comparison.css
  - tests/optimizer-course.test.ts
  - tests/optimizer-layout.test.mjs
  - tests/curriculumRoutingNavigation.test.ts
---

<objective>
Replace the static optimizer workflow lesson with six complete paged beginner chapters and six dedicated lazy interaction scenes, then connect the lesson coherently between Gradient Descent, MLP, Math Lab, and CNN.
</objective>

<tasks>

<task type="auto">
  <name>Author the six typed bilingual paged lessons</name>
  <action>Rewrite all six existing chapter IDs around the locked teaching sequence and fixed teaching structure. Use safe Markdown/KaTeX, copyable NumPy code, real published output, a single prediction prompt, end-only references/downloads, one final checkpoint, and no learner-visible Ref ID or evidence terminology.</action>
  <verify>Validate bilingual structural parity, runtime Markdown/TeX rendering, output bindings, reference placement, deep links, Progress/checkpoint compatibility, and absence of forbidden visible terms.</verify>
</task>

<task type="auto">
  <name>Build six chapter-specific lazy interaction scenes</name>
  <action>Create a lightweight course shell and six deterministic SVG/Canvas scenes for the training ledger, batch noise, Momentum/RMSProp state, Adam/decay state, schedule cadence, and controlled MLP/Banknote diagnosis. Limit each to three main control groups and support play/pause/step/reset, keyboard operation, reduced motion, finite guards, mobile layouts, and text/table fallbacks.</action>
  <verify>Run numerical scene tests, lazy-loading and structure tests, keyboard/reduced-motion checks, and responsive overflow checks.</verify>
</task>

<task type="auto">
  <name>Integrate route bridges and complete release validation</name>
  <action>Route optimizer chapters through the dedicated shell while preserving existing IDs and fallback behavior. Link Gradient Descent and MLP backprop into optimizer state, link the final optimizer chapter toward CNN, and link Math Lab Optimizer Race to the complete course while retaining it as a reference surface.</action>
  <verify>Run all focused tests, full CI tests, both builds, asset drift checks, security audit, and the declared bilingual desktop/tablet/mobile browser matrix including media failure and reduced motion.</verify>
</task>

</tasks>

<verification>
All six old deep links show exactly one chapter and its matching lab; full automated and browser release gates pass.
</verification>

<success_criteria>
- Six complete bilingual chapters form the approved beginner teaching sequence without altering legacy identity or progress data.
- Each chapter has one dedicated lazy lab and accessible non-motion fallback, with at most three main controls.
- All UI numbers derive from published assets or the shared engine; the real transfer avoids test-selection leakage and universal-winner claims.
- Gradient Descent, MLP, Math Lab, Optimizer, and CNN handoffs are explicit and route-safe.
</success_criteria>

