---
phase: 26-loss-functions-rebuild
plan: "05"
type: execute
wave: 4
depends_on:
  - 26-04
files_modified:
  - scripts/loss-functions/build-phase-26-assets.py
  - public/datasets/loss-functions/**
  - public/notebooks/loss-functions/**
  - tests/loss-functions-dataset-contract.test.ts
  - tests/loss-functions-notebook-assets.test.ts
autonomous: true
requirements:
  - LOSS-01
  - LOSS-02
  - LOSS-03
must_haves:
  truths:
    - "The only successful publication is one complete inventory swap containing both datasets, both manifests, attribution, four Notebooks, requirements/environment, two summaries, two plots, and one output manifest; no topic/locale subset is publishable (D-17 through D-20)."
    - "Every published file matches its candidate hash and every manifest path/hash/size/schema/output identity is verified before old public bytes are replaced."
    - "Offline --check is network-free and repository-write-free, four copied public Notebooks rerun independently, and locale/code/output/cross-runtime parity remains exact (D-19, D-24)."
    - "Failure before, during, or after a simulated publication restores all previous public dataset/Notebook/output bytes and leaves no backup/staging residue."
    - "Root and `/ML_tutorial_Site/` asset resolution reaches only local committed files; runtime pages never need official dataset hosts."
  artifacts:
    - path: "public/datasets/loss-functions/"
      provides: "Generator-owned published LaDe/SECOM CSV, manifests, and attribution group"
    - path: "public/notebooks/loss-functions/"
      provides: "Generator-owned four published clean-kernel Notebooks plus requirements/environment"
    - path: "public/notebooks/loss-functions/outputs/"
      provides: "Generator-owned locked summaries, plots, and complete integrity manifest"
    - path: "scripts/loss-functions/build-phase-26-assets.py"
      provides: "Complete-inventory atomic publication, offline check, standalone rerun, and rollback implementation"
      contains: "--publish-candidates"
    - path: "tests/loss-functions-notebook-assets.test.ts"
      provides: "Publication, offline, hash, parity, standalone, base-path, and rollback tests"
  key_links:
    - from: ".cache/loss-functions/phase-26-staging/"
      to: "public/datasets/loss-functions/"
      via: "one validated complete-inventory atomic swap"
      pattern: "publish-candidates|atomic"
    - from: "public/notebooks/loss-functions/outputs/manifest.json"
      to: "public/notebooks/loss-functions/"
      via: "manifest locks all four Notebook and output bytes plus locale/standalone proof"
      pattern: "notebook|sha256|standalone"
    - from: "tests/loss-functions-notebook-assets.test.ts"
      to: "src/utils/publicPath.ts"
      via: "root and GitHub Pages base resolution checks target committed local files"
      pattern: "ML_tutorial_Site|withPublicBase"
---

<objective>
Publish the already validated candidate package exactly once, then prove offline determinism, independent reruns, hashes, parity, and rollback.

Purpose: Candidate validity is insufficient if publication can expose a partial locale/topic package or if committed downloads cannot reproduce offline.
Output: One complete public package and blocking integrity evidence covering every generated byte.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-loss-functions-rebuild/26-CONTEXT.md
@.planning/phases/26-loss-functions-rebuild/26-RESEARCH.md
@.planning/phases/26-loss-functions-rebuild/26-PATTERNS.md
@.planning/phases/26-loss-functions-rebuild/26-04-SUMMARY.md
@src/utils/publicPath.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically publish the one complete candidate inventory</name>
  <files>scripts/loss-functions/build-phase-26-assets.py, public/datasets/loss-functions/**, public/notebooks/loss-functions/**, tests/loss-functions-dataset-contract.test.ts, tests/loss-functions-notebook-assets.test.ts</files>
  <behavior>
    - Publication refuses missing/unexpected inventory members, any candidate/public hash mismatch, an unapproved LaDe contract, or a topic/locale subset.
    - One successful transaction publishes actual artifacts: `lade-delivery-jilin.csv`, `lade-delivery-jilin-manifest.json`, `secom-manufacturing.csv`, `secom-manufacturing-manifest.json`, `ATTRIBUTION.md`; four named locale Notebooks; requirements/environment; `regression-loss-summary.json`, `bce-gradient-summary.json`, `regression-loss-distribution.png`, `bce-gradient-verification.png`, and `outputs/manifest.json`.
    - Existing public dataset, Notebook, and output groups are backed up together, replaced together, verified together, and restored together on any injected failure.
    - Published JSON is standards-compliant and all public paths are root-relative local paths.
  </behavior>
  <action>Add complete-inventory, subset-rejection, pre-swap, mid-swap, post-swap, and rollback-preservation tests first; confirm RED, then implement the publication boundary and return them to GREEN. Accept only the exact Plan 26-04 staging root whose complete manifest and candidate hashes revalidate. Treat the two generator-owned public directory groups in `<files>` as one transaction, even though Git records individual files. This transaction includes the staged copies of requirements and environment records; publish no separate language, dataset, Notebook, environment, summary, or plot commit. Compare old/new bytes after the swap and remove backups only after final public validation; on any exception restore every destination and leave no transaction residue.</action>
  <verify>
    <automated>python3 scripts/loss-functions/build-phase-26-assets.py --publish-candidates --staging-root .cache/loss-functions/phase-26-staging && node --test --test-name-pattern="publication|inventory|subset|rollback|standards JSON" tests/loss-functions-dataset-contract.test.ts tests/loss-functions-notebook-assets.test.ts</automated>
  </verify>
  <done>The exact complete package is present under the two generator-owned public directory groups and all atomicity/subset/rollback tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove offline check, four standalone reruns, parity, hashes, and Pages paths</name>
  <files>scripts/loss-functions/build-phase-26-assets.py, public/notebooks/loss-functions/outputs/manifest.json, tests/loss-functions-dataset-contract.test.ts, tests/loss-functions-notebook-assets.test.ts</files>
  <behavior>
    - Offline --check reads committed local inputs, performs no network request, and leaves repository bytes and mtimes unchanged.
    - Each of the four public Notebooks is copied into an isolated local package and rerun in a new kernel; normalized outputs match the committed locale/topic hash.
    - Tests reject source/normalized/generator/requirements/environment/Notebook/code/output/plot/manifest hash drift, locale code/output drift, missing license/asset, remote Notebook code, and SECOM 590/591 drift.
    - Published representative rows and objectives/gradients match the TypeScript engine at locked tolerances; MAE kink and non-finite fixed-probe statuses remain honest.
    - Every typed public path resolves for `/` and `/ML_tutorial_Site/` and no runtime asset points to Hugging Face/UCI.
  </behavior>
  <action>Add read-only, standalone, parity, hash, cross-runtime, and base-path failure injections first, confirm they detect the intended corruption in isolated copies, then harden `--check` and return the suite to GREEN. The check may use temporary work outside the repository but must not modify repository bytes/mtimes or access the network. Execute all four copied Notebooks independently; do not infer locale reproducibility from paired hashes alone. Keep non-finite teaching values as typed status plus null. Finish with a byte snapshot proving rollback and --check cleanliness.</action>
  <verify>
    <automated>python3 scripts/loss-functions/build-phase-26-assets.py --check --offline && node --test --test-name-pattern="offline|standalone|parity|hash|finite|base|590|591" tests/loss-functions-dataset-contract.test.ts tests/loss-functions-notebook-assets.test.ts tests/loss-functions-math.test.ts && git diff --check</automated>
  </verify>
  <done>Offline check, four standalone reruns, hashes, locale/output/cross-runtime parity, failure injections, rollback cleanliness, and root/Pages asset resolution all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated staging → public asset groups | A partial or reordered swap would expose an inconsistent course package. |
| Public executable Notebooks → learner download | Committed files must rerun offline and match their declared code/output. |
| Public paths → root/Pages runtime | Base-path drift would make a valid package unavailable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-17 | Tampering | atomic publication | high | mitigate | Revalidate complete inventory/hashes, swap all groups together, verify after swap, and rollback every group on failure. |
| T-26-18 | Repudiation | standalone/locale proof | medium | mitigate | Rerun all four copied public Notebooks independently and record normalized proof hashes without temp identities. |
| T-26-19 | Denial of Service | public asset paths | medium | mitigate | Test root/non-root base resolution, local file existence, and reject runtime external asset URLs. |
| T-26-20 | Tampering | offline check | medium | mitigate | Snapshot repository bytes/mtimes, block network, regenerate/compare in external temp space, and fail on any drift. |
| T-26-21 | Information Disclosure | published data groups | medium | mitigate | Re-run LaDe privacy allowlist and SECOM canonical schema checks before and after publication. |
| T-26-SC | Tampering | Python environment | high | mitigate | Reuse only the verified Plan 26-03 environment/wheel hashes with no-index. |
</threat_model>

<verification>
- Complete-inventory atomic publication command.
- Focused publication/subset/rollback tests.
- Offline write-free `--check`.
- Four copied public Notebook reruns.
- Hash, parity, cross-runtime, strict-JSON, local-path, and root/Pages base checks.
</verification>

<success_criteria>
- The public package appears only as one complete verified transaction.
- No language/topic/dataset can publish independently.
- All four downloads rerun from clean kernels offline.
- Every generated/public byte is manifest-locked and rollback-safe.
- Local assets resolve under standard and GitHub Pages bases.
</success_criteria>

<output>
Create `.planning/phases/26-loss-functions-rebuild/26-05-SUMMARY.md` when done.
</output>
