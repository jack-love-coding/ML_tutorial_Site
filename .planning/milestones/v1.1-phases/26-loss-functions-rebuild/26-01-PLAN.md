---
phase: 26-loss-functions-rebuild
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/curriculum-v3/loss-functions/phase-26-data-contract.md
  - scripts/loss-functions/build-phase-26-assets.py
  - tests/loss-functions-dataset-contract.test.ts
  - .gitignore
autonomous: true
requirements:
  - LOSS-01
  - LOSS-02
  - LOSS-03
must_haves:
  truths:
    - "The 2026-07-28 `approve-lade` decision authorizes only the pinned LaDe-D Jilin revision/hash, complete attribution/license evidence, and the privacy-minimized derivative; every other version, field set, or use fails closed (D-06, D-22)."
    - "The only online operation is an explicit source bootstrap; ordinary generation and --check are offline and fail closed (D-06, D-07, D-24)."
    - "LaDe-D Jilin and UCI SECOM source bytes are pinned by official URL plus revision/DOI and SHA-256, and source/schema drift stops the build instead of producing substitute data (D-05, D-06, D-07)."
    - "LaDe publication removes courier and precise location fields, while SECOM records both the upstream 591-feature declaration and the observed 590-value raw rows (D-06, D-07, D-08)."
  artifacts:
    - path: "docs/curriculum-v3/loss-functions/phase-26-data-contract.md"
      provides: "Auditable license, privacy, schema, source, and publication decision contract"
      contains: "LaDe-D Jilin"
    - path: "scripts/loss-functions/build-phase-26-assets.py"
      provides: "One fail-closed bootstrap/generate/check entry point for all Phase 26 assets"
      contains: "--check"
    - path: "tests/loss-functions-dataset-contract.test.ts"
      provides: "Failure-injection coverage for source, license, privacy, and schema contracts"
  key_links:
    - from: "scripts/loss-functions/build-phase-26-assets.py"
      to: "docs/curriculum-v3/loss-functions/phase-26-data-contract.md"
      via: "Generator validates the recorded approval and exact source/privacy contract before publication"
      pattern: "contractVersion|license|removedFields"
    - from: "tests/loss-functions-dataset-contract.test.ts"
      to: "scripts/loss-functions/build-phase-26-assets.py"
      via: "CLI and failure-injection tests exercise bootstrap, offline check, and fail-closed validation"
      pattern: "bootstrap|check|590|591"
---

<objective>
Establish the legal, privacy, source-integrity, and schema boundary for both real Phase 26 datasets before any learner-facing asset is published.

Purpose: The hardest phase constraint is not a formula; it is proving that the two real datasets may be locally redistributed without leaking unnecessary location data or silently rewriting an upstream schema.
Output: A recorded product-owner authorization boundary, one auditable data contract, and a tested generator that can bootstrap only the two exact official sources while leaving public publication to Plan 26-05.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-loss-functions-rebuild/26-CONTEXT.md
@.planning/phases/26-loss-functions-rebuild/26-RESEARCH.md
@.planning/phases/26-loss-functions-rebuild/26-PATTERNS.md
@scripts/numerical-methods/generate-batch-4-notebook.py
@public/datasets/numerical-methods/banknote-authentication-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record and preflight the resolved LaDe-D authorization boundary</name>
  <files>docs/curriculum-v3/loss-functions/phase-26-data-contract.md</files>
  <action>Read the resolved 2026-07-28 `approve-lade` decision from `26-RESEARCH.md`, then record it in the data contract before implementing generator behavior. The contract must bind the authorization to LaDe-D Jilin revision `be2cec02775cafc8d52230303f32134382bcc50b`, SHA-256 `12e2cf4664dd5b4475d39dddee8872f5a03b3082f08f0eece7f103baee6c6e73`, complete attribution/license evidence, and a privacy-minimized local derivative that removes `courier_id`, GPS coordinates, and precise stop fields. Fail closed if the research resolution is absent or differs from `approve-lade`, or if the source identity, hash, attribution/license evidence, or privacy denylist drifts. Per D-06, do not silently synthesize data, change delivery domain, use another LaDe version, retain additional source fields, or extend this approval to another use.</action>
  <verify>
    <automated>rg -q "Open Questions \\(RESOLVED\\)" .planning/phases/26-loss-functions-rebuild/26-RESEARCH.md && rg -q "2026-07-28.*approve-lade" .planning/phases/26-loss-functions-rebuild/26-RESEARCH.md && rg -q "be2cec02775cafc8d52230303f32134382bcc50b" .planning/phases/26-loss-functions-rebuild/26-RESEARCH.md && rg -q "12e2cf4664dd5b4475d39dddee8872f5a03b3082f08f0eece7f103baee6c6e73" .planning/phases/26-loss-functions-rebuild/26-RESEARCH.md && rg -q "courier_id" .planning/phases/26-loss-functions-rebuild/26-RESEARCH.md && rg -qi "GPS" .planning/phases/26-loss-functions-rebuild/26-RESEARCH.md && rg -qi "precise stop" .planning/phases/26-loss-functions-rebuild/26-RESEARCH.md && rg -q "approve-lade" docs/curriculum-v3/loss-functions/phase-26-data-contract.md && rg -q "be2cec02775cafc8d52230303f32134382bcc50b" docs/curriculum-v3/loss-functions/phase-26-data-contract.md && rg -q "12e2cf4664dd5b4475d39dddee8872f5a03b3082f08f0eece7f103baee6c6e73" docs/curriculum-v3/loss-functions/phase-26-data-contract.md && rg -q "courier_id" docs/curriculum-v3/loss-functions/phase-26-data-contract.md && rg -qi "GPS" docs/curriculum-v3/loss-functions/phase-26-data-contract.md && rg -qi "precise stop" docs/curriculum-v3/loss-functions/phase-26-data-contract.md</automated>
  </verify>
  <done>The data contract records the exact resolved authorization and source/privacy boundary; drift in the decision, pinned source identity, evidence, or denylist blocks subsequent work without substitution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the dual-dataset contract and generator modes</name>
  <files>docs/curriculum-v3/loss-functions/phase-26-data-contract.md, scripts/loss-functions/build-phase-26-assets.py, tests/loss-functions-dataset-contract.test.ts, .gitignore</files>
  <behavior>
    - Test first: the generator exposes mutually explicit source-bootstrap, local generation, source-cache verification, and offline read-only check modes.
    - Test first: source identity is exact for LaDe-D Jilin revision be2cec02775cafc8d52230303f32134382bcc50b and SHA-256 12e2cf4664dd5b4475d39dddee8872f5a03b3082f08f0eece7f103baee6c6e73.
    - Test first: source identity is exact for UCI SECOM DOI 10.24432/C54305 and ZIP SHA-256 eea568baf3c2229096d7d294cf0b096b5502bd96d92c0b80a65b84714059be8e.
    - Test first: standards JSON uses null plus an explicit status for non-finite teaching probes and rejects bare NaN/Infinity.
    - Test first: --check opens no network connection, writes no repository file, and fails if committed bytes differ from regenerated expectations.
  </behavior>
  <action>Write the failing tests first, run the focused test to confirm RED, then implement the standard-library outer generator and rerun to GREEN. Consume the Task 1 contract containing the resolved `approve-lade` boundary, official URLs, retrieval date, revision/DOI, source hashes, licenses, attribution, expected row counts, target/label mappings, transformation version, and publication deny lists. Add only the exact ignored Phase 26 source-cache path to .gitignore. The generator may download only in the explicit bootstrap mode; it must validate source bytes before extraction, use strict JSON with allow_nan disabled, stage candidates in a temporary directory, and provide rollback-safe publication primitives for Plan 26-05. Reuse the already pinned Numerical Methods Python environment and audited wheel-cache mechanics; add no package. Per D-05 through D-10, do not reuse the earlier MSE=2.5 fixture, invent data, or attach real row IDs to the synthetic ±1000 probes.</action>
  <verify>
    <automated>python3 -m py_compile scripts/loss-functions/build-phase-26-assets.py && node --test tests/loss-functions-dataset-contract.test.ts && git check-ignore -q .cache/loss-functions/phase-26-sources</automated>
  </verify>
  <done>The contract and CLI modes are machine-auditable; focused tests prove exact source identities, strict JSON, offline/write-free --check, and rejection of unapproved or substituted inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bootstrap and validate the exact official source cache</name>
  <files>scripts/loss-functions/build-phase-26-assets.py, tests/loss-functions-dataset-contract.test.ts, docs/curriculum-v3/loss-functions/phase-26-data-contract.md</files>
  <behavior>
    - LaDe validation observes 31,415 data rows, derives finite delivery durations from accept/delivery timestamps with rollover handling, and rejects duplicate course IDs or unexpected source fields.
    - LaDe candidate normalization retains only course row ID, source row number, city, aoi_type, accept/delivery timestamps, ds, and derived duration; courier_id and precise coordinate/stop fields are publication errors.
    - SECOM validation observes 1,567 rows, 1,463 pass and 104 fail labels, exactly 590 raw measurement values per row, declaredFeatureCount 591, and observedFeatureCount 590.
    - SECOM preserves raw missing values, maps -1 to 0 and 1 to 1 explicitly, and rejects padding, truncation, imputation, or an invented 591st column in the canonical dataset.
  </behavior>
  <action>Extend the tests first with tampered-hash, row-count, license, LaDe privacy-field, invalid timestamp, SECOM label, and 590/591 discrepancy injections; confirm the new cases fail, then implement source-cache extraction/validation and return the suite to GREEN. Run the explicit online bootstrap once against only the two pinned official URLs, followed by an offline cache verification. Do not publish into public/ in this plan: keep verified raw sources in the ignored cache so Plans 26-03 and 26-04 can build and validate the complete candidate package before Plan 26-05 performs the only public atomic publication. Record observed source facts in the contract without embedding machine-specific cache paths.</action>
  <verify>
    <automated>python3 scripts/loss-functions/build-phase-26-assets.py --bootstrap-sources --source-cache .cache/loss-functions/phase-26-sources && python3 scripts/loss-functions/build-phase-26-assets.py --verify-source-cache --source-cache .cache/loss-functions/phase-26-sources --offline && node --test tests/loss-functions-dataset-contract.test.ts</automated>
  </verify>
  <done>Both exact official source archives are present only in the ignored cache and pass hash, license, row, target, privacy, missing-value, and observed-schema validation; no public dataset or synthetic substitute has been published.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Official dataset host → build generator | Remote bytes are untrusted until revision/hash/schema/license checks pass. |
| Resolved product-owner authorization → generator | The bounded LaDe publication authority must remain explicit, versioned, and auditable. |
| Raw LaDe fields → public course data | Courier and precise location traces must not cross this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-01 | Tampering | source bootstrap | high | mitigate | Pin official URL plus revision/DOI and SHA-256; reject row/schema drift before extraction or staging. |
| T-26-02 | Information Disclosure | LaDe normalization | medium | mitigate | Enforce an allowlist and fail if courier, latitude/longitude, or precise stop fields reach a publish candidate. |
| T-26-03 | Repudiation | LaDe license approval | medium | mitigate | Record the resolved 2026-07-28 `approve-lade` decision, exact evidence, source identity, and privacy boundary in the versioned data contract consumed by the generator. |
| T-26-04 | Denial of Service | malformed archives/timestamps | low | mitigate | Bound archive members, validate row counts and finite derived durations, and abort with contextual errors. |
| T-26-SC | Tampering | Python environment | low | accept | No new package is introduced; execution reuses the existing exact pins and audited local wheel-cache boundary documented in research. |
</threat_model>

<verification>
- The checkpoint is approved before any generator action continues.
- `python3 -m py_compile scripts/loss-functions/build-phase-26-assets.py`
- `node --test tests/loss-functions-dataset-contract.test.ts`
- Explicit bootstrap succeeds only for the two pinned sources.
- Offline cache verification succeeds and `git status --short` shows no cache contents.
</verification>

<success_criteria>
- LaDe licensing ambiguity is resolved explicitly, not inferred.
- Both exact real sources pass fail-closed provenance and schema checks.
- LaDe privacy removal and SECOM 590/591 discrepancy are executable assertions.
- Network and publication modes are separated; --check is offline and write-free.
- No public asset, generated delivery substitute, new package, route, checkpoint, or progress store is changed.
</success_criteria>

<output>
Create `.planning/phases/26-loss-functions-rebuild/26-01-SUMMARY.md` when done.
</output>
