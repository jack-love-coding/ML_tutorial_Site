---
phase: 26-loss-functions-rebuild
plan: "03"
type: execute
wave: 2
depends_on:
  - 26-01
  - 26-02
files_modified:
  - scripts/loss-functions/build-phase-26-assets.py
  - scripts/loss-functions/requirements.txt
  - scripts/loss-functions/environment-contract.json
  - tests/loss-functions-notebook-assets.test.ts
  - .gitignore
autonomous: true
requirements:
  - LOSS-01
  - LOSS-02
  - LOSS-03
must_haves:
  truths:
    - "One candidate pipeline owns both datasets, both Notebook topics, all four locales, locked JSON/plots, and one manifest; no subset can be selected for publication (D-17, D-18, D-19)."
    - "Each topic has one ordered shared code-cell source and separate zh-CN/en markdown dictionaries; every locale is scheduled for its own fresh-kernel execution (D-17, D-18, D-19)."
    - "The pipeline uses the existing exact audited Python pins and local wheel cache, adds no package, and rejects environment/cache drift before candidate generation."
    - "Candidate generation targets only an ignored staging root; this plan and Plan 26-04 cannot write learner-facing datasets, Notebooks, JSON, or plots under public/."
  artifacts:
    - path: "scripts/loss-functions/build-phase-26-assets.py"
      provides: "Shared-code candidate inventory, isolated environment, staging, and validation contract"
      contains: "--prepare-candidates"
    - path: "scripts/loss-functions/requirements.txt"
      provides: "Exact reused Python pins for all four Notebook executions"
    - path: "scripts/loss-functions/environment-contract.json"
      provides: "Auditable Python/platform/wheel-cache identity contract"
    - path: "tests/loss-functions-notebook-assets.test.ts"
      provides: "Wave 0 tests for shared code, four kernels, complete inventory, and no-publication boundaries"
  key_links:
    - from: "scripts/loss-functions/build-phase-26-assets.py"
      to: "scripts/loss-functions/requirements.txt"
      via: "isolated environment verifies every exact pin and wheel hash before Notebook execution"
      pattern: "requirements|wheel|no-index"
    - from: "tests/loss-functions-notebook-assets.test.ts"
      to: "scripts/loss-functions/build-phase-26-assets.py"
      via: "tests lock the complete candidate inventory and staging-only mode"
      pattern: "candidate|staging|locale|kernel"
---

<objective>
Define and test the shared Notebook/environment/candidate pipeline before performing either full-data transformation or Notebook execution.

Purpose: The pipeline contract must make four independent executions and one indivisible publication inventory unavoidable, rather than relying on executor convention.
Output: Exact environment records, one shared-code candidate builder, a staging-only boundary, and Wave 0 Notebook/inventory tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-loss-functions-rebuild/26-CONTEXT.md
@.planning/phases/26-loss-functions-rebuild/26-RESEARCH.md
@.planning/phases/26-loss-functions-rebuild/26-PATTERNS.md
@.planning/phases/26-loss-functions-rebuild/26-01-SUMMARY.md
@.planning/phases/26-loss-functions-rebuild/26-02-SUMMARY.md
@public/notebooks/numerical-methods/requirements.txt
@public/notebooks/numerical-methods/environment.json
@scripts/numerical-methods/generate-batch-4-notebook.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the complete candidate inventory and bilingual shared-code contract</name>
  <files>tests/loss-functions-notebook-assets.test.ts, scripts/loss-functions/build-phase-26-assets.py, .gitignore</files>
  <behavior>
    - Test first: exactly two dataset candidates, two dataset manifests, attribution, four Notebooks, two summaries, two plots, requirements, environment, and one output manifest belong to one inventory.
    - Test first: delivery-losses and manufacturing-bce-gradients each define one shared ordered code-cell list and two locale markdown dictionaries.
    - Test first: paired locales require exact cell-ID/code-source parity and normalized semantic-output parity, while four executions have distinct clean-kernel proof records.
    - Test first: prepare/verify candidate modes accept only the ignored `.cache/loss-functions/phase-26-staging` root and reject any public destination.
    - Test first: no candidate Notebook code contains requests, urllib, Hugging Face, UCI fetching, shell installs, uncontrolled HTML, or widget state.
  </behavior>
  <action>Write the inventory/shared-code/no-publication tests first and confirm RED, then extend the standard-library outer generator and return them to GREEN. Create explicit topic/locale/cell IDs and one complete `CandidateInventory` assembled before generation begins. Add the exact Phase 26 staging path to .gitignore. Candidate mode must refuse partial topic/locale selectors and refuse any staging root that resolves inside public/. Keep every generated candidate path relative to the staging root so Plan 26-05 can swap a complete package without rewriting manifests.</action>
  <verify>
    <automated>python3 -m py_compile scripts/loss-functions/build-phase-26-assets.py && node --test --test-name-pattern="inventory|shared code|locale|staging|network" tests/loss-functions-notebook-assets.test.ts && git check-ignore -q .cache/loss-functions/phase-26-staging</automated>
  </verify>
  <done>The generator exposes one indivisible staging inventory, shared-code locale contracts, and no-publication guards; the Wave 0 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify the isolated four-kernel environment and candidate transaction shell</name>
  <files>scripts/loss-functions/build-phase-26-assets.py, scripts/loss-functions/requirements.txt, scripts/loss-functions/environment-contract.json, tests/loss-functions-notebook-assets.test.ts</files>
  <behavior>
    - Requirements exactly reuse the research-approved NumPy, pandas, scikit-learn, nbformat, nbclient, and existing transitive pins; no package is added.
    - Environment verification installs only from the audited local wheel cache with pip no-index and aborts on pin/wheel/Python/platform drift.
    - Four NotebookClient jobs use allow_errors false, an explicit working directory/kernel, fresh execution counts, and deterministic metadata normalization.
    - The transaction shell creates a fresh ignored staging directory, writes no public asset, cleans failed candidates, and records no machine-specific temporary path or kernelspec name.
  </behavior>
  <action>Add environment/kernel/cleanup tests first, confirm RED, then implement isolated-environment verification and the candidate transaction shell. Reuse the existing audited Numerical Methods pin set and wheel-cache mechanics; the research Package Legitimacy Audit already confirms no new package. Keep the requirements and environment contract under `scripts/loss-functions/` as generator inputs so no Phase 26 public package member exists before Plan 26-05. Plan 26-04 must copy their verified identities into staging, and Plan 26-05 must publish the final `public/notebooks/loss-functions/requirements.txt` and `environment.json` inside the same complete transaction. This task prepares four execution jobs but does not transform source data or execute/publish final candidate Notebooks. On any environment or staging failure, remove the candidate root and leave repository/public bytes unchanged.</action>
  <verify>
    <automated>python3 scripts/loss-functions/build-phase-26-assets.py --verify-environment --offline && node --test --test-name-pattern="environment|kernel|transaction|cleanup" tests/loss-functions-notebook-assets.test.ts</automated>
  </verify>
  <done>The exact isolated environment, four execution jobs, and staging cleanup contract pass without producing any learner-facing dataset, Notebook, JSON, or plot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Audited wheel cache → isolated Notebook environment | Package bytes must match the existing exact approved environment. |
| Generator definitions → executable Notebook cells | Candidate cells become downloadable code and must be controlled and offline. |
| Candidate pipeline → public tree | Plans 26-03/04 must be technically unable to publish a partial package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-08 | Tampering | candidate inventory | high | mitigate | Enumerate the exact full inventory and reject topic/locale subset selectors. |
| T-26-09 | Elevation of Privilege | generated Notebook cells | medium | mitigate | Generator-owned cells only; reject network, shell installs, scripts, raw executable HTML, and widgets. |
| T-26-10 | Denial of Service | staging transaction | medium | mitigate | Require ignored non-public staging, bounded four-job execution, cleanup on failure, and no public writes. |
| T-26-11 | Repudiation | locale/kernel execution | low | mitigate | Stable IDs and deterministic proof records distinguish all four executions without temporary identities. |
| T-26-SC | Tampering | Python packages | high | mitigate | Reuse only the audited exact pins and wheel hashes with pip no-index; abort on any identity drift. |
</threat_model>

<verification>
- `python3 -m py_compile scripts/loss-functions/build-phase-26-assets.py`
- `python3 scripts/loss-functions/build-phase-26-assets.py --verify-environment --offline`
- `node --test tests/loss-functions-notebook-assets.test.ts`
- Confirm candidate modes reject public paths and partial topic/locale selectors.
</verification>

<success_criteria>
- One complete candidate inventory and one shared-code contract per topic are executable.
- Four independent clean-kernel jobs are mandatory.
- Environment/cache drift fails before candidate generation.
- No public generated asset is created or modified.
</success_criteria>

<output>
Create `.planning/phases/26-loss-functions-rebuild/26-03-SUMMARY.md` when done.
</output>
