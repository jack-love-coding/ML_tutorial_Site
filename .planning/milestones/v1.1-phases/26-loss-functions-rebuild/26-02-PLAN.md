---
phase: 26-loss-functions-rebuild
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/simulations/lossFunctionsMath.ts
  - src/simulations/lossFunctions.ts
  - tests/loss-functions-math.test.ts
autonomous: true
requirements:
  - LOSS-01
  - LOSS-02
  - LOSS-03
must_haves:
  truths:
    - "MSE, MAE, and stable logit BCE expose per-element losses, mean objectives, per-element gradients, and mean-objective gradients with the batch factor visible (D-11, D-12)."
    - "Stable BCE stays finite at logits -1000 and 1000 without clipping, while naive/clipped/stable comparisons preserve distinct status and objective semantics (D-10, D-11, D-15)."
    - "MAE at zero residual is reported as nondifferentiable with subgradient convention 0 and is never certified as a unique central-difference derivative (D-14)."
    - "Central differences and an h sweep expose analytic value, numerical value, absolute/scaled-relative error, tolerance, and pass/kink status for MSE, MAE, and BCE (D-13)."
    - "Every public numerical function rejects empty/mismatched/non-finite values, invalid labels, and non-positive steps; Vue components contain no core math (D-16, D-24)."
  artifacts:
    - path: "src/simulations/lossFunctionsMath.ts"
      provides: "DOM-free guarded loss, gradient, stability-probe, and finite-difference authority"
      exports:
        - "stableBinaryCrossEntropy"
        - "evaluateLossGradient"
        - "centralDifferenceCoordinate"
        - "evaluateStepSweep"
    - path: "src/simulations/lossFunctions.ts"
      provides: "Existing TrainingSnapshot orchestration consuming the pure authority"
      contains: "lossFunctionsMath"
    - path: "tests/loss-functions-math.test.ts"
      provides: "Deterministic numerical, guard, kink, and fixed-probe tests"
  key_links:
    - from: "src/simulations/lossFunctions.ts"
      to: "src/simulations/lossFunctionsMath.ts"
      via: "simulation imports all loss and gradient primitives rather than keeping a second formula set"
      pattern: "from './lossFunctionsMath'"
    - from: "tests/loss-functions-math.test.ts"
      to: "src/simulations/lossFunctionsMath.ts"
      via: "unit tests verify scalar/vector outputs and finite-difference tolerances"
      pattern: "stableBinaryCrossEntropy|centralDifference"
---

<objective>
Create the pure, deterministic numerical authority for Phase 26 and refactor the existing loss simulation to consume it.

Purpose: LOSS-01 through LOSS-03 require formulas, gradients, guards, stability, and finite differences to agree independently of Vue and independently of the later Notebook assets.
Output: A tested TypeScript loss/gradient engine and a backward-compatible `simulateLossFunctions()` composition layer.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-loss-functions-rebuild/26-CONTEXT.md
@.planning/phases/26-loss-functions-rebuild/26-RESEARCH.md
@.planning/phases/26-loss-functions-rebuild/26-PATTERNS.md
@src/simulations/lossFunctions.ts
@src/modules/math-lab/utils/banknoteLogistic.ts
@src/modules/math-lab/utils/finiteDifference.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement guarded MSE, MAE, and stable BCE primitives</name>
  <files>tests/loss-functions-math.test.ts, src/simulations/lossFunctionsMath.ts</files>
  <behavior>
    - MSE/MAE return immutable per-element losses plus their mean objective for nonempty equal-length finite vectors.
    - MSE exposes 2r per-element and 2r/n mean-objective gradients.
    - MAE exposes sign(r) per-element and sign(r)/n mean-objective gradients for nonzero residuals; zero residual returns gradient 0, differentiable false, and the subdifferential note.
    - Stable sigmoid is branch-safe and stable BCE uses softplus(z)-y*z; all outputs are finite for each label at logits -1000, -20, 0, 20, and 1000.
    - Probability BCE is available only for ordinary-input comparison; clipped BCE records its epsilon and objectiveChanged status rather than becoming the canonical result.
    - Empty arrays, unequal shapes, non-finite inputs, and non-binary labels throw typed errors instead of being coerced.
  </behavior>
  <action>Write the focused tests first and confirm RED, then create `lossFunctionsMath.ts` and return them to GREEN. Define explicit result types that keep per-element and mean quantities separate. Implement branch-stable sigmoid and softplus rather than exponent caps. Implement the D-15 ten-row `(logit,label)` comparison so non-finite naive results use typed status plus nullable value, clipped rows carry objective-changing metadata, and stable rows stay finite. Per D-11, use probability BCE only to explain ordinary finite agreement; the canonical calculation is logit-domain BCE. Do not derive parameter gradients, add a training loop, or expand Softmax.</action>
  <verify>
    <automated>node --test --test-name-pattern="MSE|MAE|BCE|guard|probe" tests/loss-functions-math.test.ts</automated>
  </verify>
  <done>All loss, objective, output/logit-gradient, kink, stability-probe, and invalid-input tests pass with no clipping in the canonical BCE path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add finite-difference verification and refactor the existing simulation</name>
  <files>tests/loss-functions-math.test.ts, src/simulations/lossFunctionsMath.ts, src/simulations/lossFunctions.ts</files>
  <behavior>
    - Coordinate central differences perturb a copied float64-equivalent vector and reject invalid index or step.
    - The locked h sweep covers 10^-1 through 10^-9 and records analytic, numeric, absolute error, scaled-relative error, tolerance, and status.
    - Smooth MSE, nonzero-residual MAE, and stable BCE coordinates pass the justified locked tolerance at h=1e-5.
    - Zero-residual MAE reports kink/nondifferentiable and is excluded from equality acceptance even when a symmetric difference evaluates to 0.
    - Existing simulation consumers still receive one deterministic TrainingSnapshot and current preset/config fields remain valid.
  </behavior>
  <action>Add the finite-difference and simulation-regression tests first, observe RED, then implement central coordinate differences, error metrics, and the h-sweep evaluator before refactoring `lossFunctions.ts`. Remove duplicate local MSE/MAE/BCE/gradient implementations from the orchestration file and import the new pure functions. Preserve the existing `simulateLossFunctions(config): ModuleSimulation` contract, likelihood/NLL/MLE/Softmax support calculations, controls, presets, and snapshot shape. If the old clipped probability helper remains for D-15 comparison, name and label it explicitly as clipped; it must not feed the canonical BCE snapshot. Keep all learner-reachable values finite and bounded per D-16.</action>
  <verify>
    <automated>node --test tests/loss-functions-math.test.ts && npm run build</automated>
  </verify>
  <done>The complete focused suite passes; the simulation consumes one pure loss authority, smooth-point finite differences pass, MAE kink semantics remain honest, and the TypeScript build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Learner control values → pure numerical functions | Browser values are untrusted even when the current UI uses bounded controls. |
| Pure numerical engine → Vue/snapshot consumers | Non-finite or mislabeled results would corrupt every teaching surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-05 | Tampering | loss/gradient inputs | medium | mitigate | Validate nonempty equal shapes, finite values, binary labels, coordinate bounds, and positive finite steps at every public entry point. |
| T-26-06 | Denial of Service | stability/finite-difference engine | medium | mitigate | Use stable closed forms, bounded h lists, copied finite vectors, and no recursive or unbounded iteration. |
| T-26-07 | Repudiation | MAE kink and clipped BCE statuses | low | mitigate | Return typed differentiability/objectiveChanged/status fields and lock them in deterministic tests. |
| T-26-SC | Tampering | dependencies | low | accept | This plan adds no package and uses only the existing TypeScript/Node runtime. |
</threat_model>

<verification>
- `node --test tests/loss-functions-math.test.ts`
- `npm run build`
- Source inspection confirms `lossFunctions.ts` imports the pure authority and contains no second canonical BCE implementation.
</verification>

<success_criteria>
- LOSS-01 scalar/vector values and mean aggregation are deterministic.
- LOSS-02 output/logit gradients, outlier scale, and confident-error scale can be computed without Vue.
- LOSS-03 finite-difference checks and the h sweep are available as typed results.
- Extreme logits remain finite in the canonical path; invalid values fail clearly.
- Full parameter gradients, optimization, and expanded multiclass work remain absent.
</success_criteria>

<output>
Create `.planning/phases/26-loss-functions-rebuild/26-02-SUMMARY.md` when done.
</output>
