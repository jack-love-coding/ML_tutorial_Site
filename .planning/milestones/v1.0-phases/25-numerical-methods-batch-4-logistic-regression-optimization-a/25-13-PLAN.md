---
phase: 25-numerical-methods-batch-4-logistic-regression-optimization-a
plan: "13"
type: execute
wave: 8
depends_on: [25-12]
files_modified: []
autonomous: false
requirements: [P25-SC4, P25-SC5]
must_haves:
  truths:
    - "Both routes pass the Chinese/English × desktop/390px matrix with no console errors or horizontal overflow."
    - "Five presets, reset, exact advanced non-finite probe, diagnostics comparisons/toggles, and synthetic provenance work interactively."
    - "Default/Pages local assets, checkpoints, Progress stores, reduced-motion, and video-failure fallbacks remain intact."
  artifacts: []
  key_links:
    - from: "human browser matrix"
      to: "P25-SC5 release acceptance"
      via: "explicit approval after automated gates"
      pattern: "390x844"
---

<objective>Complete human browser acceptance for the rebuilt optimization and training-diagnostics lessons.

Purpose: Verify responsive readability, keyboard flow, deterministic D-24 failure feedback, fallbacks, and compatibility.
Output: Recorded approval of the exact browser matrix; no repository changes.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-VALIDATION.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-12-SUMMARY.md
</context>

## Artifacts this phase produces

- No repository files or symbols.
- Execution artifact: human approval of the eight-state browser/fallback/progress matrix.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve bilingual desktop/mobile routes and deterministic failure feedback</name>
  <read_first>
    - `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-VALIDATION.md` — exact browser/media checklist.
    - `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-12-SUMMARY.md` — automated gates must be green.
    - `src/modules/math-lab/labs/MathGradientLab.vue` and `src/modules/math-lab/labs/TrainingDiagnosticsLab.vue` — exact interactions under review.
  </read_first>
  <files>None — this checkpoint records browser acceptance and must not modify repository files.</files>
  <what_built>The two existing bilingual lessons share one reproducible Banknote case, deterministic TypeScript labs, local downloads, one illustration, and three Notebook-bound videos; automated gates passed in Plan 12.</what_built>
  <action>Serve the standard and Pages-base builds. Review both locked routes in Chinese and English at desktop and 390×844. Exercise all five D-22 presets and reset. Then open Advanced controls without creating a sixth preset and enter exactly: raw features, fixed method, learning rate `1.7976931348623157e308` (`Number.MAX_VALUE`), gradient tolerance `1e-5`, maximum iterations `10`; press Run and require D-24 result `non-finite`, attempted iteration 1, displayed last-finite iteration 0 values, and one suggestion to lower only the learning rate. Exercise D-23 comparisons/toggles and all five separately labeled D-04 synthetic support modes. Verify keyboard use, non-color status, checkpoints/progress, local assets, and reduced-motion/video-failure fallbacks.</action>
  <how_to_verify>
    1. Check both routes in both locales at desktop and 390×844: eight states, zero console errors, zero horizontal overflow.
    2. On optimization, run the five locked presets and Reset; confirm the locked start/backtrack/best/terminal markers and reasons.
    3. In Advanced controls enter raw + fixed + `1.7976931348623157e308` learning rate + `1e-5` gradient tolerance + `10` maximum iterations, press Run, and confirm `non-finite`, attempted iteration 1, last-finite iteration 0, and a lower-learning-rate suggestion. This is a custom probe, not a sixth preset.
    4. On diagnostics, compare stable versus too-small and Armijo versus too-large, toggle curves, verify the four-step chain, then visit all five synthetic modes and provenance labels.
    5. Open dataset, Notebook, requirements, summaries, JSON, CSV, dictionary, illustration, posters, and videos under standard and Pages bases; submit both existing checkpoints and confirm storage identities remain unchanged.
    6. Simulate reduced motion and one video failure; verify poster/transcript/summary/labels preserve the lesson.
  </how_to_verify>
  <verify><manual>Human confirms every numbered browser, exact failure-probe, fallback, and progress step after Plan 12 is green.</manual></verify>
  <done>All eight states, exact advanced failure probe, interactions, local assets, checkpoints, Progress stores, and fallbacks pass with no deferred scope.</done>
  <resume_signal>Type `approved` when all checks pass, or report the route, locale, viewport, exact input, and observed issue.</resume_signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built static app → human browser | Layout, controls, media, routes, and storage cross from source assumptions into observed UX. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Tampering / availability | Pages assets/routes | medium | mitigate | Verify both bases, all downloads/media, and zero console errors. |
| T-25-11 | Denial of understanding | Mobile/motion/color/failure UX | medium | mitigate | Exact viewport, keyboard, non-color status, deterministic last-finite probe, and fallback checks. |
</threat_model>

<verification>Human approval is recorded only after the exact matrix and numbered deterministic procedure passes.</verification>
<success_criteria>P25-SC4/P25-SC5 are visually and interactively verified in both locales, both viewports, and both asset-base modes.</success_criteria>
<output>Create `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-13-SUMMARY.md` when done.</output>
