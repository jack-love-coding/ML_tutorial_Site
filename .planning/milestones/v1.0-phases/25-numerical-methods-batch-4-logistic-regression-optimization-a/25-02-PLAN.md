---
phase: 25-numerical-methods-batch-4-logistic-regression-optimization-a
plan: "02"
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - docs/curriculum-v3/numerical-methods/batch-4-contract.md
  - public/notebooks/numerical-methods/requirements.txt
  - scripts/numerical-methods/generate-batch-4-notebook.py
  - public/datasets/numerical-methods/banknote-authentication.csv
  - public/datasets/numerical-methods/banknote-authentication-manifest.json
  - public/datasets/numerical-methods/banknote-authentication-data-dictionary.json
  - tests/numerical-methods-batch-4.test.ts
  - tests/numerical-methods-batch-4-manim.test.ts
autonomous: true
requirements:
  - P25-SC1
  - P25-SC2
  - P25-SC3
  - P25-SC4
  - P25-SC5
must_haves:
  truths:
    - "The local UCI Banknote snapshot preserves DOI, CC BY 4.0, ZIP/member hashes, exact seven-column schema, source-spelled `curtosis`, and class-0/class-1 wording per D-02."
    - "Persisted assignments implement D-03 exactly: seeds 20260725/20260726, 960/206/206 rows, exact split class counts, and train-only population statistics."
    - "The contract locks the D-05 five-run matrix, D-06 extreme-logit check, D-07 terminal priority, D-09 constants, D-18 L2 convention, and D-19 trace fields before runtime work."
    - "The approved scikit-learn 1.9.0 pin is added once for the D-16 endpoint baseline."
    - "After Plan 01 approval, one explicit network bootstrap populates a hash-audited ignored wheel cache; normal generation and offline `--check` each create and remove their own isolated eight-pin venv and kernelspec without ambient Jupyter packages."
  artifacts:
    - path: "docs/curriculum-v3/numerical-methods/batch-4-contract.md"
      provides: "Authoritative Batch 4 constants, schemas, naming, and preservation boundaries"
      contains: "numerical-methods-batch-4-v1"
    - path: "public/datasets/numerical-methods/banknote-authentication.csv"
      provides: "1,372 local rows with stable IDs and persisted split assignments"
      contains: "banknote_id,variance,skewness,curtosis,entropy,class,split"
    - path: "tests/numerical-methods-batch-4.test.ts"
      provides: "Wave 0 numerical, artifact, lesson, preservation, and parity contract"
    - path: "tests/numerical-methods-batch-4-manim.test.ts"
      provides: "Wave 0 six-role media and integrity contract"
  key_links:
    - from: "scripts/numerical-methods/generate-batch-4-notebook.py"
      to: "public/datasets/numerical-methods/banknote-authentication.csv"
      via: "explicit hash-gated source refresh; ordinary generation remains offline"
      pattern: "1e2acd9a2085fadf3d8145c12d3d22af853320d52294a6590c2eaf75fdc05227"
    - from: "public/datasets/numerical-methods/banknote-authentication-manifest.json"
      to: "docs/curriculum-v3/numerical-methods/batch-4-contract.md"
      via: "shared contract version, counts, hashes, split constants, and statistics"
      pattern: "numerical-methods-batch-4-v1"
    - from: "scripts/numerical-methods/generate-batch-4-notebook.py"
      to: "public/notebooks/numerical-methods/requirements.txt"
      via: "explicit wheel-cache bootstrap followed by per-invocation `venv`, `pip --no-index`, exact-version/import checks, and a temp-prefix ipykernel"
      pattern: "--bootstrap-environment-cache"
---

<objective>
Lock the Phase 25 contract, approved Python environment, verified local dataset, and failing-first validation surface.

Purpose: Give Notebook, TypeScript, Vue, and media executors one immutable numerical and provenance boundary instead of rediscovering constants.
Output: Contract document, one approved requirements pin, isolated-runtime bootstrap/check modes, hash-audited Banknote snapshot/manifest/dictionary, dataset refresh mode, and two Wave 0 tests.
</objective>

<execution_context>
@/Users/jackky/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackky/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-CONTEXT.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-RESEARCH.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-PATTERNS.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-VALIDATION.md
@.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-01-SUMMARY.md

<interfaces>
Contract version: `numerical-methods-batch-4-v1`.
CSV header: `banknote_id,variance,skewness,curtosis,entropy,class,split`.
Canonical media IDs: `banknote-feature-scaling`, `banknote-fixed-vs-armijo`, `banknote-training-diagnostics`.
Dataset refresh CLI: `python3 scripts/numerical-methods/generate-batch-4-notebook.py --refresh-source --wheel-cache .cache/numerical-methods/batch-4-wheelhouse`.
</interfaces>
</context>

## Artifacts this phase produces

- Files: `batch-4-contract.md`, `banknote-authentication.csv`, its manifest and data dictionary, `generate-batch-4-notebook.py`, and the two Batch 4 test files listed in frontmatter.
- Contract fields: `contractVersion`, upstream URL/hash/bytes, normalized dataset hash/bytes, row/class/split counts, split seeds, train means/scales, constants, run IDs, terminal-kind/reason taxonomy, output IDs, canonical scene IDs, and preservation assertions.
- Generator CLI flags: `--bootstrap-environment-cache`, `--verify-environment`, `--wheel-cache`, and `--refresh-source`. Bootstrap is the only package-network mode; refresh may network only for hash-gated UCI source bytes, while its package installation, normal generation, and `--check` are cache-only.
- Test helpers: typed JSON reader, SHA-256 helper, public-path resolver assertion, bilingual-copy assertion, and finite-number walker modeled on Batch 3.

<tasks>

<task type="auto">
  <name>Task 1: Freeze the Batch 4 contract, pin, and isolated environment</name>
  <read_first>
    - `docs/curriculum-v3/numerical-methods/batch-3-contract.md` — preserve its locked-artifact and acceptance organization.
    - `public/notebooks/numerical-methods/requirements.txt` — modify this existing file once, after the 25-01 approval.
    - `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-RESEARCH.md` — use every value in `Exact Numerical Contract` and `Output and Manifest Schema` verbatim.
    - `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-CONTEXT.md` — enforce D-01 through D-29 and exclude deferred work.
  </read_first>
  <files>docs/curriculum-v3/numerical-methods/batch-4-contract.md, public/notebooks/numerical-methods/requirements.txt, scripts/numerical-methods/generate-batch-4-notebook.py</files>
  <action>Create the D-01/D-02 contract with every locked research value and, only after Plan 01 verifies the durable 2026-07-22 approval record in `25-RESEARCH.md`, append exactly one `scikit-learn==1.9.0` line per D-16. Fail closed before any requirements edit or installation if the exact approval text or any official identity anchor is missing. Implement the generator as a standard-library-only outer process: `--bootstrap-environment-cache --wheel-cache .cache/numerical-methods/batch-4-wheelhouse` is the sole network-enabled package operation and atomically records the requirements hash, Python/platform identity, and downloaded wheel hashes. Normal generation, `--verify-environment`, `--refresh-source`, and `--check` must fail closed with that exact bootstrap command if the cache is absent/stale; package installation must never download. Each creates a unique temporary venv, installs from the committed requirements with `pip --no-index --find-links=<wheel-cache>` and `PIP_NO_INDEX=1`, then verifies the exact versions and imports for `numpy`, `pandas`, `scipy`, `nbformat`, `nbclient`, `jupyterlab`, `ipykernel`, and `sklearn` before cells can run. Register that venv's kernel with `python -m ipykernel install --prefix <temp-prefix>`, scope `JUPYTER_PATH`, `JUPYTER_CONFIG_DIR`, `JUPYTER_RUNTIME_DIR`, and `IPYTHONDIR` to the temp root, and select the exact kernel name. Remove the venv, kernelspec, and scoped Jupyter directories in `finally` on success, failure, or interrupt. Record this bootstrap/offline contract and observed eight-version table; never rely on ambient third-party packages.</action>
  <verify>
    <automated>rg -q 'approved scikit-learn==1\.9\.0' .planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-RESEARCH.md &amp;&amp; test "$(rg -c '^scikit-learn==1\.9\.0$' public/notebooks/numerical-methods/requirements.txt)" -eq 1 &amp;&amp; rg -q 'numerical-methods-batch-4-v1' docs/curriculum-v3/numerical-methods/batch-4-contract.md &amp;&amp; python3 scripts/numerical-methods/generate-batch-4-notebook.py --bootstrap-environment-cache --wheel-cache .cache/numerical-methods/batch-4-wheelhouse &amp;&amp; python3 scripts/numerical-methods/generate-batch-4-notebook.py --verify-environment --wheel-cache .cache/numerical-methods/batch-4-wheelhouse</automated>
  </verify>
  <acceptance_criteria>
    - D-01, D-02, D-03, D-05 through D-09, D-14 through D-21, and D-24 through D-29 have explicit contract clauses.
    - Every constant and rounded anchor equals `25-RESEARCH.md`; no alternate parameter is introduced.
    - The durable approval text and official identity anchors exist before mutation; the approved package then appears once, and the cache manifest matches requirements/Python/platform/wheel hashes and remains ignored.
    - Offline verification uses `--no-index`, proves all eight versions/imports plus the temp-prefix kernel, and leaves no temp environment or kernelspec.
    - Missing/stale cache prints the exact bootstrap command and performs no implicit download.
  </acceptance_criteria>
  <done>The approved eight-pin contract is committed, one explicit bootstrap produces the audited ignored cache, and a separate offline verification proves exact imports/kernel selection and complete temporary cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the verified local Banknote snapshot</name>
  <read_first>
    - `scripts/numerical-methods/generate-batch-3-notebook.py` — reuse hash helpers, deterministic JSON, temp directories, environment restoration, and rollback.
    - `public/datasets/numerical-methods/sms-spam-data-dictionary.json` — data-dictionary shape.
    - `public/datasets/numerical-methods/logit-calibration-manifest.json` — dataset manifest and integrity pattern.
    - `docs/curriculum-v3/numerical-methods/batch-4-contract.md` — authoritative values created by Task 1.
  </read_first>
  <files>scripts/numerical-methods/generate-batch-4-notebook.py, public/datasets/numerical-methods/banknote-authentication.csv, public/datasets/numerical-methods/banknote-authentication-manifest.json, public/datasets/numerical-methods/banknote-authentication-data-dictionary.json</files>
  <action>Implement the generator's complete hash-gated dataset refresh boundary and use it once with `--refresh-source --wheel-cache .cache/numerical-methods/batch-4-wheelhouse`. The outer process may access the network only to acquire the official UCI ZIP; it must create a fresh temporary venv from the already-audited wheel cache with `pip --no-index`, verify all eight pinned imports, and run split/statistics work through that isolated environment rather than ambient Python. Require ZIP SHA-256 `1e2acd9a2085fadf3d8145c12d3d22af853320d52294a6590c2eaf75fdc05227`, member `data_banknote_authentication.txt` at 46,400 bytes with SHA-256 `d0539aaed2139ba7a587b3e34fb345ce503ff7d5d33dbf9912d8e195ce425cb9`, 1,372 finite rows, stable one-based IDs, and the exact two-stage stratified split from D-03. Validate class/split counts and four locked train-only `ddof=0` means/scales, then atomically publish CSV, manifest, and dictionary. Clean the temporary venv/kernel/Jupyter state in `finally`. Ordinary generation must consume the committed local CSV without network; any upstream drift aborts without replacing committed files.</action>
  <verify>
    <automated>python3 scripts/numerical-methods/generate-batch-4-notebook.py --refresh-source --wheel-cache .cache/numerical-methods/batch-4-wheelhouse &amp;&amp; python3 -m py_compile scripts/numerical-methods/generate-batch-4-notebook.py</automated>
  </verify>
  <acceptance_criteria>
    - CSV header/order, 1,372 unique IDs, finite values, exact class counts, exact 960/206/206 split counts, and per-split class counts match research.
    - Manifest includes contract/source/normalized hashes and bytes, seeds, algorithm calls, schema, counts, and locked train statistics.
    - Data dictionary explains all seven fields, attribution, license, and the source spelling without claiming undefined class meanings.
    - Refresh package installation is cache-only with `--no-index`, all eight exact imports run in a fresh isolated environment, and no venv/kernelspec/Jupyter state remains afterward.
    - Failed refresh restores prior assets and leaves no temp directory or environment mutation.
  </acceptance_criteria>
  <done>The committed CSV, manifest, and dictionary match the locked UCI hashes, schema, split/statistics contract, and attribution, while an injected refresh failure preserves all prior bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Establish Wave 0 Batch 4 contract tests</name>
  <read_first>
    - `tests/numerical-methods-batch-3.test.ts` — artifact, bilingual, parity, route, public-base, and sanitizer assertion patterns.
    - `tests/numerical-methods-batch-3-manim.test.ts` — metadata, package-role, route binding, media probe, and renderer check patterns.
    - `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-VALIDATION.md` — required focused assertions and sampling commands.
    - `tests/numerical-methods-batch-4.test.ts` — create/modify this target test file.
    - `tests/numerical-methods-batch-4-manim.test.ts` — create/modify this target test file.
  </read_first>
  <files>tests/numerical-methods-batch-4.test.ts, tests/numerical-methods-batch-4-manim.test.ts</files>
  <behavior>
    - Dataset tests pass now for provenance, hashes, schema, split counts, and train statistics.
    - Numerical/lesson/media tests name every future contract and fail only because their declared production artifacts do not exist yet.
    - Preservation tests lock both module IDs, route order, lab IDs, checkpoint IDs, existing progress storage key, and five synthetic scenario IDs.
  </behavior>
  <action>Create both focused test files before downstream implementation. Assert all P25-SC1 through P25-SC5 boundaries named in `25-VALIDATION.md`, including exact tolerances `1e-9` for displayed scalars and `1e-8` for parameters. Structure tests into named groups so later plans can run only the surface they make green. Do not fabricate total test counts. The tests must protect D-04/D-13 route, lab, progress, and synthetic-mode retention as well as D-06/D-07/D-17 numerical fixtures and D-26 through D-29 media completeness.</action>
  <verify>
    <automated>node --test --test-name-pattern='dataset|contract|preservation scaffold' tests/numerical-methods-batch-4.test.ts &amp;&amp; node --test --test-name-pattern='media contract scaffold' tests/numerical-methods-batch-4-manim.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files parse under Node 24 and expose named, filterable suites.
    - Dataset and contract assertions pass against Task 2 artifacts.
    - Missing Notebook, TypeScript, lesson, illustration, and media artifacts are represented by explicit tests assigned to later plans, not omitted.
  </acceptance_criteria>
  <done>Both Wave 0 tests parse, dataset/contract/preservation groups pass, and every later production surface has a named failing-first owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UCI ZIP → build generator | Remote bytes and schema enter a local publication transaction. |
| PyPI pin → Notebook environment | Approved third-party code enters the build toolchain. |
| Static CSV/JSON → later browser/runtime | Committed content becomes learner-visible computation input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering / DoS | Banknote ZIP, member, CSV | high | mitigate | Exact hashes/bytes, strict schema/count/finite validation, stable IDs, atomic rollback. |
| T-25-02 | Tampering | Contract/constants | medium | mitigate | Cross-file contract-version and exact-anchor tests before downstream work. |
| T-25-SC | Tampering | Python dependency | high | mitigate | Plan 01 approval plus exact single pin and no alternate distribution. |
</threat_model>

<verification>
Run `node --test --test-name-pattern='dataset|contract|preservation scaffold' tests/numerical-methods-batch-4.test.ts`, both Python syntax checks, and `git diff --check`. Confirm `docs/gpt_advice.md` remains unmodified and unstaged.
</verification>

<success_criteria>
The exact dataset and contract are locally reproducible and hash-audited, the approved dependency is pinned once, and every later Phase 25 behavior has an executable Wave 0 test owner.
</success_criteria>

<output>
Create `.planning/phases/25-numerical-methods-batch-4-logistic-regression-optimization-a/25-02-SUMMARY.md` when done.
</output>
